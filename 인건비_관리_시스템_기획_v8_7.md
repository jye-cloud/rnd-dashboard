# 인건비 관리 시스템 기획서 v8.7

> 작성일: 2026-06-03 (v8.7 갱신: 2026-06-07)
> 직전 버전: v8.6 (2026-06-07)
> **v8.7 핵심: 연차 기준 정렬 1·2단계(인건비 표를 연차 단위로 + 분기 보기에서 연차 밖 월 회색·잠금) + 이월금 덮어쓰기 버그픽스 + 예산 급여 정합(급여총액=연봉 / 실지급액=ceil(연봉/12)) + 자부담현금 합산·별도 "표시" 토글 + 예산→인건비 전송 순서·분류 전달 + 방향키(예산 셀·인건비 월급)·예산 총액 직접입력 + 배포 캐시 헤더.**
> **[구현 완료]**
> - **연차 정렬 1단계 — 인건비 전체보기(§4.10)** — 연차 있는 과제는 표·누계·차액·이월금을 그 연차 실제 기간(시작연도 기준) 전체로 봄. `getYearIndexForState`=시작연도 매핑(2027→1차 오인 픽스), `getActiveYearMonths`/`ymRange`/`getActiveYearObj`, `getVisibleMonths`·누계·인력필터·합계 6곳이 연차 월 순회, "이 월부터 채우기"→연차끝, 꼬리연도 안내. 데이터 0. 파일: `project-labor.js`.
> - **잠긴 셀 합계 제외** — 입사 전/퇴사 후(기간 외) 셀은 월합·누계·행합에서 제외(`isPersonActiveInYm`). 수동 잠금은 합산 유지. 예산 분배가 입사전/퇴사후 월을 채워 합계가 부풀던 문제 해결.
> - **나눔(split) 행 3건** — 우클릭 "이 월부터 채우기"(12월 고정 제거) / 분류 드롭다운 전체 노출+다른 행과 스왑 / 나눔 % 셀 "옆으로 채우기"가 `splitField` 따라 채워짐(`__cellSnapshot` undo).
> - **재입사 수동 잠금해제(B안)** — `personRoles[pid].unlockedYms`로 퇴사후/입사전 월 개별 입력 허용(우클릭 "🔓 이 월 입력 허용"). 잠금 우선순위: 연차밖 > 직접잠금 > 입력허용 > 자동기간잠금. (정식 처리=재직 구간 복수는 데이터 변경 건이라 보류)
> - **연차 정렬 2단계 — 분기 보기(§4.10)** — 분기 보기를 살리되 분기에 걸친 **연차 밖 월은 회색+잠금**(`getYearQuarters`/`isYmInActiveYear`). 분기 ◀▶는 연차의 분기들 안에서만 이동, 라벨=실제 달력 분기("2025년 2분기 (3/3)"), "이전 분기 복사"도 연차 분기 기준(연차 밖 칸엔 복사 안 함). 전체보기 토글은 1단계대로.
> - **이월금 덮어쓰기 버그픽스** — 예산 저장이 연차 문서를 통째 `set` 해서 인건비가 넣은 `carryoverCash`/`carryoverSelfCash`를 지우던 것 → `{merge:true}`. (예산 페이지엔 이월금 표시 안 함 — 인건비에서만.) 파일: `project-budget.js`.
> - **예산 급여 정합** — 급여총액=**연봉**, 실지급액=월급=`ceil(연봉/12)`(마스터와 동일, …334/…667). "연봉 불러오기/전체 적용"·급여총액 수동입력 자동계산·빈항목 채우기·인력 지정 4곳. 파일: `project-budget.js`.
> - **예산 총액 직접입력** — 총액 읽기전용→입력(`totalOverride`), 비우면 자동(실지급액×참여율×참여개월). `calcRowTotal`이 override 우선.
> - **방향키** — 예산 페이지 셀(↑↓ 같은 열, ←→ 전체선택 시 옆, Enter 아래) 신규 + 인건비 **월급 칸**(↑↓). (인건비 셀 방향키는 원래 있었고, 예산엔 아예 없던 걸 추가.)
> - **배포 캐시 헤더** — `firebase.json`에 js/css/html `no-cache` → 배포 후 옛 JS 캐싱("또 안먹힘") 재발 방지. (현재 브라우저 캐시는 1회 강력새로고침 필요.)
> - **자부담현금 합산/별도 "표시" 토글** — `hasSelfCash` 체크 = 데이터가 아니라 **표시 방식**(별도 ON / 합산 OFF). 데이터는 항상 지원금/자부담 분리 저장, **환급은 모드 불문 지원금만**. 합산이면 인건비 sticky 박스(예산총액·누계·차액)에서 현금=지원금+자부담 한 줄 + 작은 "지원금" 서브라인(환급기준 / 지원금 집행 / 환급 가능=`max(0, 지원금 잔여)`), 예산 페이지는 요약 아래 합산 줄. 예산 페이지 자부담 입력은 항상 허용(OFF 차단 해제). 파일: `project-labor.js`·`project-budget.js`.
> - **예산→인건비 전송 순서·분류** — 전송 시 `personRoles`(`newOrExisting`·`cashOrInkind`)도 저장 → **기존/신규·지원금/현물 반영**. 명단 순서 = **보낸(예산 입력) 순서 우선** + 기존 미전송 인력은 뒤. 선택 전송·분배 둘 다. 파일: `project-budget.js`.
> **[검증]** 전 항목 `node -c` + 노드 로직 시뮬(연차월·분기 회색·잠금 합계·나눔·재입사·월급 ceil·총액 override·합산 박스 행·전송 순서/role 병합) 통과. 실화면: 패널 용어·합산 박스·전송은 사용자 확인. 누적 = `개발정리_테스트가이드.md`.
> **[남음/순서]** (다음 세션) **정산 반납 용어 통일 + 사유 툴팁** → **§4.4 3단계**(예산 동기·차년도=최신 연봉) → **자금관리 연차 기준** → (선택) 인건비 표 푸터·예산 행별 셀도 합산 표기 → S2(인건비 대시보드+청년 자동) → S3·S4 → 과제 상세 페이지(맨 마지막). 보류: 재입사 정식(재직 구간 복수), project-budget 3책5공 미리보기.
>
> ---
> 작성일: 2026-06-03 (v8.6 갱신: 2026-06-07)
> 직전 버전: v8.5 (2026-06-05)
> **v8.6 핵심: 이월금 1단계 구현(다음 연차로 이월) + "연차 기준 정렬" 결정(큰 건·구현 대기) + 정산 반납 용어 통일·사유 툴팁·§4.4 3단계 기준(최신 연봉) 결정.**
> **[구현 완료]**
> - **이월금 1단계 — 다음 연차로 이월(§4.9)** — 잔액 박스의 "이월금" 칸(껍데기·항상 0) → **전년도→올해 받은 지원금/자부담 항목별 표시 + "📥 N차년도로 이월" 버튼**. 버튼→모달(지원금 이월 = `예산−실제` 자동 프리필·수정 가능 / 자부담현금 = 0·직접 입력 / **현물 이월 불가·칸 없음**). 확정 시 **다음 연차 예산 문서에 merge 저장**(`projectBudget/{id}_year{N}`의 `carryoverCash`·`carryoverSelfCash`, `{merge:true}`). `getCarryover`를 숫자→`{cash,selfCash}` 객체로, `getRemainingBreakdown`이 지원금·자부담엔 이월 더하고 현물엔 안 더함. **입금·미수(자금) 무영향**. 데이터 = 예산 문서 두 필드, 마이그레이션 0(기존 껍데기엔 값 없었음). 파일: `project-labor.html/js`. ⚠️ 자부담현금 잔액 자동값은 셀에 집행이 안 잡혀 부정확→모달에서 직접 입력.
> **[결정 — 미구현]**
> - **연차 기준 정렬(§4.10 · 큰 건)** — 시스템에 시간 축이 둘(연차 날짜범위 vs 달력 1~12월)이라, 연차가 달력연도에 걸치면 예산(연차)·누계(달력연도)가 어긋남. **결정: 집행·잔액·이월금을 연차 단위로 본다.** 보는 단위 = **과제(키워드)×연차**(예: "프라운호퍼 · 1차년도 (26.7~27.6)"), 화면은 그 연차 실제 기간만. 적용 범위 = **인건비 + 자금관리(둘 다 선택 연차 기준) / 전사 대시보드는 달력연도 유지**. **데이터 변경 0**(셀=월 단위, 예산·이월금=연차 단위 이미 그대로). 순서 = **인건비부터, 자금관리 그다음**.
> - **정산 반납 용어 통일** — '차감' 단어를 전부 '반납'으로. 자금 관리 "반납 N 반영"→**"정산 반납 N 반영"**, 과제 상세 "➖ 정산 차감(이자 반납 등)"→**"➖ 정산 반납(이자 반납 등)"**. 색(`#f97316`) 유지.
> - **사유 표시** — 항상 노출 X. 자금 관리 "정산 반납 N 반영"에 **마우스 올리면 툴팁으로 사유**. 입력은 과제 상세 그대로.
> - **§4.4 3단계 기준 = 최신 연봉** — 차년도(예: 2027) 예산은 그 해 **가장 최신 연봉**을 그 해 연봉으로 사용.
> **[검증]** 이월금 정적검증(node -c·HTML 태그/CSS 균형·로직 14단언) 통과. 연차 정렬·용어 통일·§4.4 3단계는 결정만(미구현).
> **[남음/순서]** 연차 기준 정렬(인건비→자금관리) → 정산 반납 용어 통일+사유 툴팁 → §4.4 3단계(예산 동기·최신 연봉) → S2(인건비 대시보드+청년 자동) → S3(과제 상세 묶음: 채용 의무+C4) → S4(채용 의무 판정) → 과제 상세 페이지(사양 미정·마지막). 보류: project-budget 3책5공 미리보기·비인건비 "공" 카운트.
>
> ---
> **v8.5 핵심: §4.4 2b(인건비 "📅 연봉 변경 반영" 선택 버튼) + 공용 모듈 승격(SalaryUtil·YearFilterUtil) + 과제 관리 표 재구성(당해 환급예정 컬럼 신규) + 과제 상세 정산 차감(이자 반납) + 자금 관리 미수에 차감 연결 + 대시보드 월 클릭 이동 + 비중 VAT 정합 + 헤더 툴팁.**
> **[구현 완료 — 실화면 다수 확인됨 / 정산 차감·자금 연결은 확인 진행 중]**
> - **§4.4 2b — 인건비 "📅 연봉 변경 반영" 선택 버튼(§4.4)** — `project-labor` 예상 탭 툴바 버튼 → 모달. 연중 변경(`salaryChanges`) 인력 목록(각자 "재계산 N칸·수동 M칸·이미반영 K칸") 중 **체크한 사람만** 변경월~ 예상 금액 재계산(`getMonthlySalaryAt×참여율`). **확정(`meta[ym].confirmed`)·변경 전 월·rate0 셀 제외**, 과제 오버라이드(`monthlySalaryOverride>0`) 인력은 별도 표기(재계산 제외). 수동(사람이 직접 친 값) 셀 정책 = **전체 건너뛰기 / 하나씩 결정 / 전체 덮어쓰기**(수동 결정 모달). 나눔(split)은 `cell.rates`별 현금/자부담/현물 분기. **묶음 undo 1개**(기존 `undoLastCell` 호환, Ctrl+Z 한 번). 파일: `project-labor.html/js`.
> - **공용 모듈 승격(드리프트 방지)** — `firestore-service.js`에 ① `window.SalaryUtil`(`normalizeYm`/`sanitizeSalaryChanges`/`getAnnualSalaryAt`/`getMonthlySalaryAt`) — persons-master·project-labor 중복 해석 함수를 공용화(두 페이지는 참조 + 로컬 폴백 유지). ② `window.YearFilterUtil`(`collectYears`/`populate`) — 하드코딩이라 2026 이후 데이터 연도가 안 나오던 연도 드롭다운 수정, 5개 페이지(projects·projects-history·dashboard·funding·labor-dashboard) 연동. **기본 기준연도 = "올해 자동"**(`new Date().getFullYear()`). 단, projects·projects-history의 레거시 `supportYear` 폴백은 리터럴 `2026`으로 고정(자동 롤오버와 디커플). 파일: `firestore-service.js` + 5개 페이지.
> - **§4.4 진입 UI 버그픽스(§4.4 1단계 보강)** — `persons-master` "연봉 변경 시점" 저장 안 됨/리셋 픽스: 해석 4함수 로컬 폴백(SalaryUtil 미정의 방어) + `readForm` 행 검증(부분/잘못된 월 행은 저장 차단 + 안내) + 월 입력 `YYYY-MM` 자동 포맷. 파일: `persons-master.js`.
> - **과제 관리 — 연구기간 = 전체 기간 일관화** — `getResearchPeriodDisplay`가 선택 연도에 걸치는 연차 슬라이스를 보이던 것 → **상위 startDate/endDate ∪ 연차의 최저 시작 ~ 최고 종료**(과제/용역/지원사업·연차 유무 무관 일관). 파일: `projects.js`.
> - **과제 관리 표 재구성 + "당해 환급예정" 신규 컬럼** — 기본 컬럼 순서 = **No · 유형 · 진행 여부 · 키워드 · 책임자 · 연구기간 · 기관명 · 총 지원금 · 당해 입금 · 당해 환급예정**. 제출일은 선택열("열 선택", 기본 OFF, 엑셀은 항상 포함 유지). **당해 환급예정** = 연도 선택 시만 표시(전체 선택 시 컬럼 자체 숨김), `projectLabor/{id}_planned`의 그 해 `cash` 합(= funding "환급 예정액"과 동일 정의)을 **비동기 로드(연도별 캐시·토큰가드)** 후 채움, 인건비 관리 대상 아니면 `-`. 헤더 ⓘ 툴팁(흰 배경·검은 글씨·옅은 파랑 `#93c5fd` 테두리/아이콘) — 총 지원금·당해 입금·당해 환급예정. 파일: `projects.html/js`.
> - **대시보드 월별 신규제안 그래프 월 클릭 → 그 달 제출 과제 리스트로 이동** — `dashboard` 차트 `onClick`(빈 막대도 그 열로 인식) → `projects.html?year=Y&submitMonth=M&newProposal=1`. `projects.js` `submitMonth`(1~12) 파라미터 → 그 해·그 달 제출분만(`submitDate.slice(0,7)`), 힌트에 "N월 제출 · 결과 K건". 파일: `dashboard.js`·`projects.js`.
> - **과제 상세 — 비중 VAT 정합** — 용역(VAT) 모드에서 분모(총 지원금)는 VAT 포함인데 분자(우리 분담)가 공급가(÷1.1)라 비중 왜곡(27.3%) → **분자도 VAT 포함 총액으로 통일**(90,000,000/300,000,000 = 30%). 비-VAT 모드는 무변경. 파일: `project-detail.js`.
> - **과제 상세 — 총 지원금(컨소시엄 전체) 콤마 자동** — 입력 즉시 콤마 + 로드 시 콤마(저장은 숫자). 파일: `project-detail.js`.
> - **과제 상세 — 정산 차감(이자 반납 등) [A안]** — 입금 일정에 연차별 **"➖ 정산 차감(이자 반납 등)"** 섹션(금액 콤마 + **사유 자유 입력**). **미수 = 예정 − (실수령 + 차감)**(차감으로 gap 메우면 "✓ 정산 완료, 미수 0"). 데이터 추가 `yb.deductions = [{amount, reason}]`(plannedPayments/actualPayments와 동일 방식, 마이그레이션 0, 총 지원금·비중·통계 무영향). 파일: `project-detail.js`.
> - **자금 관리 — 미수에 차감 연결 [옵션1+가]** — 과제별 **합계 미수 = max(0, 예정합 − 입금합 − 차감합)**(메우면 "✓ 완료"), **차감 귀속 연도 = 그 연차의 마지막 예정 입금 연도**(없으면 종료/시작일). 월별 미수 셀은 그대로(차감은 날짜 없음 → 합계·요약 카드 누적 미수·전체 합계 미수에만 반영). 표기 = **"반납 N 반영"**(밝은 오렌지 `#f97316`, 미수 빨강과 구분). `yb.deductions` 읽기만. 파일: `funding.js`.
> **[검증]** 전 항목 정적검증(node -c·태그/CSS 균형·ID/심볼 정합) 통과. §4.4 2b 엔진·UI, 환급예정 cash 합·연도필터·과제분리, 제출월 필터, 비중 30%, 콤마, 미수=예정−실수령−차감 4시나리오, 차감 귀속연도·미수 반영은 노드 시뮬 검증. 실화면: 연구기간·컬럼·툴팁·월클릭·비중·콤마는 사용자 확인 완료, **정산 차감 입력 + 자금 연결은 확인 진행 중**(누적 = `개발정리_테스트가이드.md`).
> **[남음]** (보류 중) §4.4 **3단계**(`project-budget` 예산 동기 "그 달 연봉") → **S2**(labor dashboard surface + 청년 분류 자동 일원화) → **S3**(project-detail 묶음: 채용 의무 정의 + C4 신규/기존 자동분류) → **S4**(채용 의무 판정/표시) → 과제 상세 페이지(사양 미정, 마지막). 보류: project-budget 3책5공 미리보기(타 과제 roster), 비인건비 "공" 카운트. **정산 차감 용어 통일 여부(과제 상세 "정산 차감" ↔ 자금 "반납 반영")·자금에 차감 사유 노출 여부는 다음 착수 전 결정.**
>
> ---
> **v8.4 핵심: C6 2·3단계 완료(예산에서 슬롯 골라 불러오기 + 엑셀 슬롯 컬럼) + §4.4 월급 연중 변경 1·2a단계(인사 마스터 "연봉 변경 시점" 입력 + 인건비 "그 달 연봉" 계산) + 대시보드 툴바 한 행 정렬.**
> **[구현 완료 — 실화면 확인 대기]**
> - **C6 2단계 — 예산에서 슬롯 골라 불러오기(§4.8 C6)** — `project-budget` 상단 "💰 연봉 불러오기" 바: 슬롯 셀렉트(실제 연봉 + 가용 슬롯 합집합) + **전체 적용** + 행별 드롭다운(급여 셀 안)으로 `salarySlots[슬롯] ?? 실제`의 월급(`ceil/12`)을 급여총액·실지급액에 채움. **폴백 정책 = 하이브리드(사용자 확정)**: 슬롯 빈 인력은 0으로 차단 + 주황 경고색 + "빈 항목 실제로 채우기 (N)" 버튼으로 실제 연봉 선택 주입. 데이터 추가 `row.salarySlot`(UI 기억용, 분배/C1/내보내기 무영향), 셀/키 변경 0. 파일: `project-budget.html/js`.
> - **C6 3단계 — 엑셀 슬롯 컬럼(§4.8 C6)** — `persons-master` 일괄 다운로드/업로드에 `연봉:슬롯명`(예: `연봉:제안서용(공개 가능)`) 컬럼 추가(`연봉(원)` 뒤·합집합·콤마 서식). 업로드 시 `연봉:` 접두 헤더 → `salarySlots` 비파괴 병합(빈 칸=변경 없음, 양수만, 콤마/원 파싱, `연봉:새이름`=새 슬롯). 데이터 변경 0. 파일: `persons-master.js`(+다운로드 tooltip).
> - **§4.4 월급 연중 변경 1단계 — 인사 마스터(§4.4)** — 인력 편집 모달에 **"연봉 변경 시점"**(연중 인상) 입력: 행마다 [시작월 `2026-07`]→[새 연봉(콤마)]+월급 미리보기+삭제. 모델 = `person.salaryChanges = [{from:'YYYY-MM', annualSalary}]`(계단식: 그 달부터 새 연봉, 그 전엔 기본 `annualSalary`). 해석 함수 `getAnnualSalaryAt/getMonthlySalaryAt`. **변경시점 없으면 기존과 100% 동일(안전)**. 데이터 추가 `salaryChanges` 하나, 마이그레이션 0. 파일: `persons-master.html/js`.
> - **§4.4 2a — 인건비 "그 달 연봉" 계산(§4.4)** — `project-labor`의 `getEffectiveMonthlySalary(person, ym)` 월 인식화(우선순위 ① 과제 오버라이드 평탄 ② 마스터 그 달 연봉 ③ `person.monthlySalary`). 사용자 동작 계산 지점(참여율·나눔 2경로·⚡일괄계산·가로채우기·undo)에 ym 전달. 연중 변경 인력에 월급칸 옆 **📅 인디케이터**(툴팁=변경 목록). 표시값은 base 평탄 유지. 변경시점 없으면 기존과 동일. 파일: `project-labor.html/js`.
> - **대시보드 툴바 한 행 정렬** — `dashboard` 회사 필터를 `.dash-toolbar`로 통합 → **기준 연도·회사(왼쪽) … PDF 저장(오른쪽)** 한 행. ID·JS 무변경. 파일: `dashboard.html`.
> - **(UI) 슬롯 행 한 줄 레이아웃** — `persons-master` 예산 전용 연봉(제안서용) 행을 스택→한 줄(이름·입력·미리보기). CSS만.
> **[검증]** 전 항목 정적검증(node -c·태그/CSS 균형·ID/심볼 정합) 통과. C6 2단계 슬롯 불러오기·폴백(19), C6 3단계 엑셀 왕복·병합(18), §4.4 연봉 해석·콤마(14), 2a 엔진(10)은 노드 시뮬 검증. 실화면 동작은 사용자 확인 진행 중(누적 기록 = `개발정리_테스트가이드.md`).
> **[남음]** §4.4 **2b**(인건비 "📅 연봉 변경 반영" 선택 버튼 — 변경 인력 골라 해당 월부터 예상 금액 재계산, 확정 실제 월 제외, 묶음 undo) → §4.4 **3단계**(`project-budget` + 선택 전송 ⓑ를 "그 달 연봉"으로 동기, 2027 예산 자동) → 6묶음 과제별 상세(사양 미정) → §4.5 엑셀 → C5 외부 전달. **해석 함수(persons-master·project-labor 중복)는 공용 위치 승격 권장.**
>
> ---
> **v8.3 핵심: C3(셀 메모 실제·비교 탭) + C1-b(예산 선택 전송 모달) + C6(연봉 프로파일 1단계) + 대시보드 지원금 현황 카드 재정비.**
> **[구현 완료 — 실화면 확인 대기]**
> - **C3 셀 메모 실제·비교 탭(§4.8 C3)** — 기존 예상 탭 전용 메모를 **예상·실제 탭 모두 편집**(실제 탭은 우클릭 시 메모 전용 메뉴), **비교 탭은 예상행→`_planned`·실제행→`_actual` 마커+우클릭, 차이행 비허용**(권장안 ⓐ). `openMemoPopover`가 mode로 `state.planned`/`state.actual` 분기·저장. 데이터 변경 0(기존 `cell.memo` 재사용). 파일: `project-labor.js`(단독, HTML/CSS 무변경).
> - **C1-b 예산 선택 전송 모달(§4.8 C1)** — 분배(`distributeToLabor`)가 전 인력·전월 덮어쓰던 문제 해소. **"📤 선택 전송"** 모달: ① 인력 체크 ② 필드(이름만/+참여율/+금액) ③ **금액 출처 ⓐ 예산 금액(`실지급액×%`) / ⓑ 인건비 탭 급여 재계산(`override ?? master × %`)** ④ 월 범위 ⑤ 빈 셀만(비파괴)/덮어쓰기(둘 다 메모 보존). 구분 버킷=예산 행 분류. `_planned.cells`+`_meta.personIds` merge, 키 변경 0. **C1-a(이름만 보내기)는 이 모달의 "이름만" 옵션으로 흡수.** 파일: `project-budget.html/js`.
> - **C6 연봉 프로파일(공유 슬롯) 1단계(§4.8 C6 신규)** — 예산 짤 때 불러올 연봉을 골라 쓰는 "예산 전용 연봉". **모델**: `persons.salarySlots = { 슬롯명: 연봉(원) }`(월급은 사용처 `ceil/12`), `실제`=기존 `annualSalary`(항상 존재), **빈 슬롯=실제 폴백**. 가용 슬롯 = 기본(`['제안서용(공개 가능)']`) ∪ 전 인력 키 **합집합**(별도 설정 문서 없음, "기타 추가"=새 이름 입력 후 저장). **입력 UX = A(인력 편집 모달 인라인)** 구현. 라벨: "실제 연봉 (원)"/"예산 전용 연봉 (원)". **실제·예산 전용 연봉 천단위 콤마**(`type=number`→`text`+`parseMoney`/`fmtComma`), 슬롯 행 스택 레이아웃(긴 이름 전체 표시). 데이터 추가 = `salarySlots` 하나, 셀/키 변경 0 → funding·결산·labor 무영향, 마이그레이션 0. 파일: `persons-master.html/js`.
> - **대시보드 지원금 현황 카드 재정비** — ① **노트북 줄바꿈 수정**: 윗 행을 4칸(전체·유형·완료율)→**2칸(`big-stats--duo`)**, `.type-mini-label` nowrap. ② 렌더 코드 없이 0이던 **"지원금 구성(당해)" 제거**. ③ **"입금 진행(당해)" 신설**: 입금 완료(초록 `seg-paid`)/남은 예정(회색 `seg-remain`) 막대 + 헤더 우측 **입금 완료율 배지**(`yearActual/yearSupport`, 초과입금 시 막대 100% 캡·완료율 실제값). ④ 순서 = **입금 진행 → 자금 구성**. kpis 키 변경 0. 파일: `dashboard.html/js`.
> **[검증]** 전 항목 정적검증(node -c·인라인 JS·태그/CSS 균형·ID·심볼 정합) 통과. C1-b 금액·빈셀/덮어쓰기, C6 슬롯 합집합·정규화, 대시보드 입금 진행 계산은 노드 시뮬 검증. 실화면 동작은 사용자 확인 진행 중(누적 기록 = `개발정리_테스트가이드.md`).
> **[남음]** C6 2단계(예산에서 슬롯 골라 불러오기 + 폴백 안전장치 ⓐ표시/ⓑ차단 결정) / C6 3단계(엑셀 슬롯 컬럼) / §4.4 월급 연중(범위 b, `monthlySalaryOverride` 객체화 → C1-b ⓑ 동기 수정) / 6묶음 과제별 상세(사양 미정) / §4.5 엑셀 / C5 외부 전달.
>
> ---
> **v8.2 핵심: C2(3책5공 관리) 구현 완료 + 테스트 피드백 반영(공고일 8자리·책임자 검색 드롭다운·책임자 경고·대시보드 3책5공 점검).**
> **[구현 완료 — 실화면 확인 대기]**
> - **C2 3책5공 관리(§4.8 C2)** — 공유 헬퍼 `three-five-rule.js`(신규: 책≤3·책+공≤5, `isManagedActive`/`bucketForPerson`/`countForPerson`/`isOverLimit`/`format`, Firestore·DOM 접근 0). **과제 수정**: opt-in `is3ch5gManaged` + 책임자 personId **검색 드롭다운**(재직/퇴직 필터·이름 검색·hidden 저장). **참여율**: 3책5공 컬럼(현재 연도만 `책/공`, 한도 초과 빨강·툴팁). **프로젝트별 인건비**: 모달 배지 `N개 과제`→`책/공`+"3책5공 대상" 칩, 메인 표 책임자 행 강조(나눔 포함). **인건비 예산**: 책임자 행 색 강조. **soft 경고**: 한도 초과 빨강+툴팁(차단 없음). 데이터 추가 = `projects.is3ch5gManaged`+`managerPersonId` 둘. 셀/키 변경 0. 파일: `three-five-rule.js`(신규)·`project-detail.html/js`·`participation-summary.html`·`project-labor.html/js`·`project-budget.html/js`.
> - **테스트 피드백 반영** — ① **공고일 8자리 자동 변환**(C4 규칙, `type=date`→텍스트+`formatDateInput`). ② **책임자 검색 드롭다운**(단순 select→검색·재직/퇴직 필터). ③ **대기 과제 책임자 지정 경고**: 책 2건 이상 인력을 책임자로 지정 시 참여형태·상태 무관 안내(단독/주관=⚠️3책, 컨소-참여=ℹ️정보). ④ **인건비 대시보드 "📚 3책5공 점검"** 섹션(책≥2·공≥4·한도 초과 인력만). 파일: `project-detail.html/js`·`labor-dashboard.html/js`.
> **[검증]** 전 항목 정적검증(node -c·인라인 JS·태그 균형·심볼 정합) 통과. ThreeFiveRule 카운트 로직 노드 시뮬레이션 검증. 실화면 동작은 사용자 확인 진행 중(누적 기록 = `개발정리_테스트가이드.md`).
> **[남음]** §4.4 월급 연중(범위 결정 필요) / C1(예상 분배 전송, 금액 출처 결정) / C3(셀 메모 실제·비교 탭, 권장안 ⓐ 확정·결정 거의 불필요) / 6묶음 과제별 상세(사양 미정) / §4.5 엑셀 / C5 외부 전달.
> **[C2 후속·보류]** ① project-budget 책/공 **+1 미리보기**(타 과제 roster 로드 필요 — 책임자 색만 구현). ② 비(非)인건비 3책5공 과제의 "공" 카운트(명단 캐시가 인건비 관리 과제만 담는 한계, 채용 의무 디커플링과 동일). ③ 청년 군대 예외 보정.
>
> ---
> **v8.1 핵심: v8 §3.5/§3.6 구현 완료 + C4(신규 인력 자동 판정) 구현 + 채용 의무 디커플링 결정 반영.**
> **[구현 완료 — 실화면 확인 대기]**
> - **§3.5 인건비 대시보드 재설계 + 채용 의무** — 카드 5개, 채용 의무 표, 할 일/점검(`confirmed` 정정), 추이·진척·명단·활용도 제거. 청년 자동 일원화(`getYouthInfo`, persons-master). 과제 수정에 `requiredNew`/`requiredYouth`/`requiredRetention` 입력(인건비 관리 대상과 분리된 독립 블록). 파일: `labor-dashboard.html/js`, `persons-master.js`, `project-detail.html/js`.
> - **§3.6 인력 대시보드 재설계** — 회사 칩 필터, 상단 3박스 한 행(인력 현황·입퇴사+월별막대·시점별), 청년/품질 제거. 순증감은 제외(사용자 요청). 시점별은 드롭다운 즉시 갱신(조회 버튼 제거). 월별 막대 hover 툴팁. 파일: `persons-dashboard.html/js`.
> - **C4 신규 인력 자동 판정(§4.8 C4 = §4.3 승격)** — 과제 수정에 기준일(과제시작일/공고일)+공고일+N개월 규칙(`newJudgeRule`,`announceDate`). 인력 추가 모달 신규/기존/미상 라벨·필터·일괄 재판정. 추가 시 `newOrExisting` 기본값=판정. 파일: `project-detail.html/js`, `project-labor.html/js`.
> - **채용 의무 디커플링 결정** — ① 비인건비 과제 모니터링 = **안 함**(인건비 관리 과제만, 필요 누적 시 추후). ② **유지 기한 기준일 = 과제 시작일**(첫 연차 `startDate`, 없으면 등록 시작월 폴백) — **§3.5 D9(등록 시작월) 정정**. 카운트 로직 무변경. 파일: `labor-dashboard.js`.
> **[검증]** 전 항목 정적검증(node -c·태그 균형·ID/함수 정합) 통과. 실화면 동작은 사용자 확인 진행 중(누적 기록 = `개발정리_테스트가이드.md`).
> **[남음]** C2(3책5공, 사양 확정·다음 후보) / C1·C3(후보, 일부 결정 필요) / §4.4 월급 연중(마스터 연봉 시간축 결정 필요) / §4.5 엑셀 / 6묶음 과제별 상세(사양 미정) / C5 외부 전달.
>
> ---
> **v8 핵심: §3.5 인건비 대시보드 전면 재설계 + 채용 의무 유지 관리(신규).**
> 페이지 역할 분리 확정 — **인건비 대시보드 = "사람·규정" 홈**(가용 인력·참여율 위험·채용 의무) / **자금 관리 인건비 환급 탭 = "돈" 홈**(예상/실제/환급률·미수·추이·과제별). 돈 지표(예상 연 환급·미확정·월별 추이·프로젝트별 진척)는 모두 funding으로 이관, 대시보드에서 제거.
> 상단 카드 5개(재직/등록/가용/100%/채용 의무), 본문은 채용 의무 표 + "할 일/점검"만(추이·인력 명단·활용도 제거). **채용 의무 = 등록월+12개월 유지·머릿수 기준·교체 허용·완료 시 경고 해제.** 청년 판정 만 34세 자동 일원화. 확정 기준 `confirmed` 통일(구 `sysReg` 정정). 신규 필드 `projects.requiredRetention` 하나, 셀/키 변경 0 → funding·결산 무영향. 상세·결정 로그·구현 순서 = §3.5·§3.6. **§3.6 인력 대시보드**: persons-dashboard를 인사 현황 홈으로(회사 칩 필터·상단 3박스 한 행·재직 중심·입퇴사 월별 막대·청년/데이터품질 제거). 직전(v7.4.4) 작업은 아래 보존.
>
> ---
> **[v7.4.4 핵심 — 보존]** **§4.2 예상 계획 스냅샷(Phase H) 구현 완료** (트랙 B 두 번째 항목). 새 컬렉션 `projectLaborSnapshots`(1 문서=1 스냅샷, auto-id, `where(projectId)` 단일 인덱스). 예상 탭 툴바 **📸 스냅샷** 버튼 → 모달(현재 요약+이름 입력 저장 / 이력 목록). 담는 범위(확정): **planned 셀 + personIds + personRoles**(actual·meta 제외). 행 액션 **내역 / 차이 / 복원 / 삭제**. **내역 = 표(table)** 형태(행=인력·구분, 열=입력월, 셀=참여율%+금액, 합계, 이름열 sticky) + **시트 복사(TSV 클립보드)** + **엑셀 다운로드**(파일명 `{키워드}_스냅샷_{이름}_{날짜}.xlsx`). **차이 = 스냅샷↔현재**(추가/변경/삭제, 인력별). **복원(확정) = 전체 덮어쓰기 + 복원 직전 자동백업 스냅샷**(kind `auto`). 나눔 판정은 **`personRoles.split`**(셀 `cell.rates` 잔재 아님 — 오판 수정). 작성자=`window.currentUser`. 모달 열림 중 Ctrl+Z 가로채기 차단. 별도 컬렉션·셀 키 변경 0 → funding·결산·labor-dashboard 무영향. 정적검증 통과. **별도 단독 페이지(인쇄용 표 화면)는 현재 불필요 — 향후 필요 시 과제로 보류.** 남은 §4: 4.4 월급 연중 / 4.3 신규 판정 / 4.5 엑셀(입출력).

---

## 0. v7의 위치 — 무엇이 바뀌나

### 0.1 배경

v6.0~v6.2에서 두 P0 페이지(`participation-summary` 신규, `funding` 재구성)를 완료하면서 실데이터 운영 사이클(연초 계획 → 월별 입력 → 정산)을 시스템 내에서 닫을 수 있게 됨. 그 뒤 실제로 쓰면서 **여러 페이지에 걸친 자잘한 정리·정합성·버그 항목이 한꺼번에 드러남**:

- 대시보드/과제관리에 같은 지표가 중복 표시됨 (수주 카드, 신규제안 그래프)
- 과제 수정 폼에 더 이상 안 쓰는 옵션·모호한 안내문구·구식 타이틀이 남아 있음
- 참여율 페이지에서 셀 클릭 시 정보가 과하고(과제 풀네임), 정작 필요한 실제 환급 금액이 없음
- 기업부설연구소 월별 인력 명부 펼침 버그
- 과제별 상세 / 인력 대시보드 / 인건비 대시보드는 레이아웃 자체를 손봐야 함

### 0.2 v7의 두 트랙

기존 기획서 §3의 후속 로드맵(셀 메모 → 스냅샷 → … → 인건비 대시보드)은 **전부 `project-labor` 한 페이지에 집중**돼 있었음. 이번 정리 백로그는 **대부분 다른 페이지(대시보드·과제관리·과제수정·참여율·lab)** 라 로드맵과 겹치지 않음. 따라서 두 트랙으로 분리:

| 트랙 | 내용 | 우선 |
|---|---|---|
| **트랙 A — 페이지 정리** | 대시보드·과제관리·과제수정·참여율·lab UI 정리 (이번 v7 본문 §2~§3) | **선행** |
| **트랙 B — project-labor 로드맵** | 셀 메모 / 스냅샷 / 신규 인력 판정 / 월급 연중 변경 / 엑셀 입출력 / 인건비 대시보드 (§4) | 후행·병행 |

**순서 원칙**: 트랙 A의 무위험·중간 항목을 먼저 끝내 다른 페이지를 정돈해두고 → `project-labor`는 점검(트랙 A의 큰 건)과 로드맵(트랙 B)을 한 트랙으로 묶어 진행. 같은 페이지를 두 번 갈아엎는 충돌을 피하기 위함.

### 0.3 이전 결정 사항은 모두 유효

v5 계열 데이터 모델(`personRoles`, `projectLabor` 컬렉션, 셀 키 `{projectId}_{ym}_{personId}`, 셀 값 `{ rate, cash, inkind, memo, colors }`)과 v6.1·v6.2 구현(`participation-summary`, `funding` 2탭 구조)은 그대로 유지. v7은 **정리·수정·추가**만 한다.

---

## 1. 페이지 매핑 + 기획서 반영 현황

사이드바(`sidebar.js` `MENU_ITEMS`) 기준 페이지명 ↔ 실제 파일 ↔ 기획서 반영 여부.

| 사이드바 페이지 | 파일 | 그룹 | v7 반영 |
|---|---|---|---|
| 수행 현황 | `projects-history.html` / `.js` | 1 | — |
| 일정 관리 | `index.html#/calendar` | 1 | — |
| 대시보드 | `dashboard.html` / `dashboard.js` | 2 | §2.1 (신규) |
| 자금 관리 | `funding.html` / `funding.js` | 2 | §3.5 (v8 — 돈 지표 이관·예측) |
| 과제 관리 | `projects.html` / `projects.js` | 2 | §2.2 (신규, 일부 v6.2 연결) |
| 과제별 상세 | `projects-summary.html` / `.js` | 2 | §3.2 (레이아웃 트랙, **맨 마지막 순서**) |
| **과제 수정** | `project-detail.html` / `project-detail.js` | (과제 관리 진입) | §2.3 + §3.5 (v8 채용 의무·유지 정의) |
| 인력 대시보드 | `persons-dashboard.html` / `.js` | 3 | **§3.6 (v8 재설계)** |
| 마스터 | `persons-master.html` / `.js` | 3 | §3.2 + §3.5 (v8 청년 자동 일원화) |
| 상세 | `persons-detail.html` / `.js` | 3 | §3.2 (레이아웃 트랙) |
| 참여율 | `participation-summary.html` | 3 | §2.4 + §3.1 (신규) |
| 기업부설연구소 | `lab.html` / `lab.js` | 3 | §2.5 (신규, 버그) |
| 자격증 | `certificates.html` / `.js` | 3 | — |
| 인건비 대시보드 | `labor-dashboard.html` / `.js` | 4 | **§3.5 (v8 전면 재설계)** |
| 프로젝트별 인건비 | `project-labor.html` / `.js` | 4 | §3.3 + §4 (로드맵 본진) |
| 인건비 예산 | `project-budget.html` / `.js` | 4 | §3.3 |

> **"과제 수정" 위치 주의**: 별도 사이드바 메뉴가 아니라 `project-detail.html`임. 신규 등록 시 타이틀 `[R&DM] 과제 등록`, 수정 진입 시 `[R&DM] 과제 수정`으로 `detail-title`이 동적 설정됨 (`project-detail.js`).

**기획서와 연결되는 항목 (신규 아님)**:
- §2.2 "당해 수주 총 지원금 카드 삭제"(과제 관리) — 같은 카드를 v6.2에서 `funding.html`에서 이미 삭제(v6.2 §2.5). 시스템 전체에서 걷어내는 일관된 방향.
- §2.4 "참여율 이름 클릭 시 실제 환급 금액" — v6.2 §2.8 "셀 클릭 → 인력별 분해 패널"과 같은 맥락. 단 `participation-summary`엔 이미 셀 클릭 분해 패널이 있으므로 **환급액 추가 + 표시 축소** 작업.

---

## 2. 트랙 A — 페이지별 정리 백로그

### 2.1 대시보드 (`dashboard.html` / `dashboard.js`)

> ✅ **2묶음 완료 (v7.2)** — 지원금 현황 미니 블록을 **`과제`/`지원사업`/`기타` 3분류**로 완성. `yearSupportSupport`(지원사업) 분리, 패널에 `hero-support-sum` 행 추가, 라벨 "과제 / 지원사업 / 기타". 용역은 별도 트랙이라 제외(기존 유지). §5.2 결론대로 **funding은 2분류 유지** → 대시보드(3)와 funding(2)이 의도적으로 다름.

1. ✅ **카드 순서 교체** — "전체"를 큰 숫자(메인)로, "과제"를 미니 블록으로 (지원금 현황 패널). `setText` ID 기반이라 마크업만 교체.
2. ✅ **"전체" 카드 강조** — `big-stat-num` 슬롯으로 승격.
3. ~~**입금 예정 분류 세분화** — 입금 예정을 `과제` / `지원사업` / `기타` **3분류**로.~~ ✅ **v7.2 완료** (2묶음 3번)
   - ⚠️ **정합성 결정 필요**: `funding.html`은 `과제` / `기타` **2분류**(`it.division1 === '과제'` vs 그 외, 용역 제외). 대시보드를 3분류로 가면 두 페이지의 분류 기준이 갈림. → 분류 기준을 통일할지(funding도 3분류로?), 대시보드만 3분류로 둘지 작업 시 확정. `division1`/`division2` 필드 값 분포 확인 선행.

**작업량**: 1·2는 사소, 3은 분류 로직 포함 → 소~중.

### 2.2 과제 관리 (`projects.html` / `projects.js`)

1. ~~**연도 필터 + 유형별 진입 버튼**~~ ✅ **v7.2 완료** (2묶음 2번)
   - **`수행` 호버 추가** — `#card-ongoing-all` 값에 hover 배경(초록).
   - **유형별 클릭 필터** — 수행 카드 안 버튼이 아니라, **검색창 아래 `filter-summary-row`의 유형별 항목(과제/지원사업/용역/기타)을 클릭 필터**로 구현. 클릭 시 목록이 **현재 카드필터 ∩ 그 분류**로 좁혀짐. 유형별 카운트는 항상 전체 분해 유지(선택만 강조), 제목 "수행 · 과제 5건". 신규 상태 `activeSummaryDivision`(기존 '총 신규 제안' 분류 pill과 독립), 기존 `projectMatchesDivision` 재사용, URL `odiv` 동기화. summary-row가 뜨는 모든 카드필터(미선정/종료/계속/신규)에서 동일 작동.
   - **레이아웃 미세조정**: 총 신규 제안 pill 2×2 그리드(과제/지원사업, 용역/기타) / 수행 계속·신규·종료를 **라벨 위·숫자 아래 2줄 스택**(구분선 숨김, 3등분) / 그룹 폭 비율 `1.3:3.2:2.6`(총 신규 제안 축소, 수행·지원금 확대) / 수행 카드 flex 2.
2. ✅ **"당해 수주 총 지원금" 요약 카드 삭제** — DOM 제거. `sujuTotal` 계산은 표 '당해 수주' 컬럼이 쓰므로 유지, 카드 populate 죽은 코드만 정리.
3. ✅ **"월별 신규제안 그래프" 삭제** — DOM·CSS·`renderMonthlyProposalChart`·Chart.js CDN 전부 제거.
   - **결정**: "총 신규 제안" 카운트 카드는 삭제하지 않고 **통계 그리드 맨 앞으로 이동해 유지**. 분류 필터 pill(과제/지원사업/용역/기타)이 그 카드 안에 fuse돼 있어 통째 삭제 시 필터가 사라지기 때문. pill 숫자(=신규 제안 분류 분해) vs 클릭 필터(=전체 목록 분류) 모집단 차이는 현 상태 유지 — 추후 일치시키려면 별도 작업.
4. ✅ **죽은 CSS 정리** — proposal-summary-row / monthly-* / mp-* 114줄 제거(사용 0건 확인 후). **앞으로 삭제 전 사용처 확인 후 정리 원칙.**
5. ✅ **요약 카드 3그룹화** — 신규 제안 / 진행 상태(대기·수행·선정기타·미선정) / 지원금(당해 입금·당해 입금 완료). 그룹 사이 간격+세로 구분선, 그룹 내부 좁은 간격(`.stat-group`). 폭 비율 `1.7:3:2`(tunable).

**작업량**: 1·2·3·4·5 **전부 완료** (1은 v7.2에서 filter-summary-row 방식으로).

> **디자인 메모**: 요약 카드는 현재 그룹 구분까지만 적용. 사용자 의견 "구분만 되면 됨". 카드 자체 스타일(색/타이포/여백) 추가 다듬기는 가능하나 현재는 보류 — 필요 시 별도 디자인 작업으로.

### 2.3 과제 수정 (`project-detail.html` / `project-detail.js`)

1. ~~**인건비 관리 방식 옵션 정리** — `project-laborMode` 라디오에서 "환급 X + 참여율만 관리" 옵션 삭제.~~ ✅ **v7.2 완료** (2묶음 1번)
   - `participation_only` 라디오 제거(2개 잔존), 로드 시 옛 값 안전 가드(`participation_only` → `refund_participation`).
   - ⚠️ **기획서 정정**: v7.1에 "`refund_participation`만 남김"이라 적었으나 실제로는 **`refund_only`(환급 O + 참여율 X)도 남아 있음** → 라디오 2개. 그리고 코드상 `refund_only`와 `refund_participation`은 저장 결과(`laborRefund=true`)가 같아 **기능 차이 없음** → 추후 중복 정리 검토거리.
2. ✅ **별칭 안내문구 수정** — `목록에서 빠르게 식별할 수 있는 짧은 별칭` → `목록에서 빠르게 식별할 수 있는 별칭(가능한 8자리 이내로 설정)`. (안내문구만, maxlength 강제 안 함.)
3. ✅ **타이틀 수정** — 등록/수정 모두 `[R&DM] 과제 등록/수정` → `📝 과제 정보 등록` / `📝 과제 정보 수정` (h1 + 브라우저 탭).

**작업량**: 1·2·3 **전부 완료** (v7.2).

### 2.4 참여율 (`participation-summary.html`)

> ✅ **2묶음 완료 (v7.3)** — 셀 클릭 패널 항목을 **`키워드` / `참여율` / `실제 환급액`** 3요소로 축소(과제 풀네임 제거). 합계줄에 `참여율% · 환급 합원` 병기. 로직은 전부 `participation-summary.html` 인라인 스크립트(별도 `.js` 없음, `ParticipationManagement.js`는 무관).
> - **A-1 결정**: 환급액은 예상/실제 토글과 무관하게 **항상 `actual` 셀의 `cash`**. `getPersonMonthly` 루프에서 rate는 현재 mode 셀, cash는 `bucket.actual[key]`로 별도 조회.
> - **B-2 결정**: funding 정합 — `{projectId}_meta`.meta[ym].confirmed === true 인 월만 환급액 인정. 미확정은 cash 값이 있어도 "미확정" 표시(`refundConfirmed=false`). confirmed는 과제별·월별이라 한 사람의 한 달 분해 안에서도 과제마다 갈릴 수 있음. **추가 Firestore 읽기 없음**(이미 로드하던 `_meta` 문서에서 `meta` 필드만 더 추출).
> - 키워드 = `projects.keywords`(없으면 `projectName` fallback). 죽은 CSS `ps-detail-item-name` 제거(사용처 0건 확인).
> - ⏳ **실화면 확인 대기**: 미확정 표시 / 키워드 빈 과제 fallback / 현물(kind) 항목 환급액 0원 표시.

1. ~~**셀 클릭 패널에 실제 환급 금액 추가**~~ ✅ **v7.3 완료**
2. ~~**패널 표시 축소** — `키워드` / `참여율` / `실제 환급 금액` 3개만.~~ ✅ **v7.3 완료**
   - 데이터 소스: 실제 환급 = `projectLabor/{projectId}_actual.cells[{ym}_{personId}].cash` (confirmed 월만, B-2). 키워드 = `projects.keywords`.

**작업량**: ✅ 완료. (소~중, 추가 읽기 비용 없이 마무리)

### 2.5 기업부설연구소 (`lab.html` / `lab.js`)

1. ✅ **월별 인력 명부 펼침 버그** — `lab.js` 426줄 펼침 규칙에서 `isUpload` 제거(`expanded = applicable && isToday`). 오늘 달만 펼침. *(부수 동작: 올해가 아닌 연도 조회 시 전부 접힘 — 의도된 동작)*

**작업량**: 버그 수정 → 소.

---

## 3. 트랙 A — 결정·설계가 필요한 큰 건

### 3.1 "결산 보고서" 페이지 (`settlement-report.html`, 신규) — ✅ 구현 완료 (v7.3)

연말 결산 과정에서 **월별 × 과제별 × 인력별로 각자 얼마의 지원금을 가져갔는지**를 회계에 넘기는 표. 회계가 실제 요청한 양식(행=인력×과제, 열=월별 금액)을 기준으로 잡음.

> **구현 메모**: §2.4 `participation-summary`의 검증된 인프라(firebase 로드·캐시·`isProjectActiveInYear`·회사칩/연도)를 복제하고, 표·집계·출력은 결산 전용 신규. §3.4 재정립 때 양쪽을 같이 손볼 것을 전제로 공유 추상화 대신 **복제** 선택.

**구현 내용 (확정)**:
- **형태**: 별도 페이지. 사이드바는 **참여율과 동일 레벨**(`group: 3`, `parent: 'nav-persons-dashboard'`), 참여율 바로 아래, 라벨 "결산 보고서". `sidebar.js`의 `MENU_ITEMS`에 한 줄(`nav-settlement-report`) 추가.
- **집계 축 / 행**: **인력 × 과제** (한 사람이 여러 과제면 여러 행).
- **컬럼**: **이름 / 생년월일 / 연구과제명(키워드) / 1~12월**. 이름·생년월일·과제명은 **중앙정렬**(td.sr-td-* specificity로 강제). (발령일자·종료일자·구분 컬럼 + **총계·소계 모두 제외** — 사용자 요청. 회계 양식 단순화.)
- **금액**: **지원금만** = `actual.cells[{ym}_{personId}].cash`, **confirmed 월만**(B-2, funding 정합). 현물 인력은 cash 0이라 자동 제외. 자부담현금/현물은 결산에서 제외(§3.4).
- **화면**: 인력별로 묶어 과제 행(이름·생년월일 rowspan) + 인력 그룹 구분선 + **미확정 경고**(잠정 cash 있는데 미확정인 항목을 상단에 "이름·과제(N월)" 나열). 회사 칩 + 연도 필터(`participation` 패턴, `isProjectActiveInYear`). actual만 로드(planned 미로드 — 읽기 절반).
- **출력**: **엑셀(SheetJS `xlsx@0.18.5`) + 클립보드 복사(TSV)** 둘 다. 회계 양식 그대로 **평면**(이름/생년월일/과제명/1~12월, 소계·총계 없음). 파일명 `결산보고서_{연도}[_회사].xlsx`. **PDF 없음.**
- 생년월일 = `persons.birthDate`. 과제명 = `projects.keywords`(없으면 projectName fallback).

> **부수 수정 (v7.3) — 사이드바 FOUC 깜빡임**: 동적 사이드바 페이지(`participation-summary`·`settlement-report`)에서 페이지 진입 시 사이드바 메뉴가 세로로 늘어났다 줄어드는 현상. 원인 = `#sidebar-mount` div를 sidebar.js가 `outerHTML`로 교체하는데, 메뉴 17개 자연 높이가 창 높이(예: 710px)를 넘어 `height:100vh` 클램프 전 한 프레임 튐. **해결**: ① mount div → 정적 빈 `<aside class="sidebar">` placeholder(inline `height:100vh`) ② `sidebar.js` 생성 aside에 inline `style="height:100vh;overflow-y:auto"` 추가(공통 — 모든 동적 사이드바 페이지 적용). 정상 페이지(정적 메뉴 하드코딩)는 원래 무관.

**파일**: `settlement-report.html`(신규), `sidebar.js`(메뉴 1줄 + aside inline), `participation-summary.html`(사이드바 placeholder).

**작업량**: ✅ 완료.

### 3.2 전체 레이아웃 조정 (별도 작업 트랙)

페이지 자체 레이아웃을 손보는 큰 건. 의존성 순서 있음:

- **과제별 상세** (`projects-summary.html`) — **[v8] 우선순위: 맨 마지막.** 다른 항목(특히 §3.5·§3.6 대시보드) 개발 후 착수. 내용·레이아웃 사양 미정 → 착수 시점에 재검토(변경 가능). 현재는 순서만 기록.
- **인력 대시보드** (`persons-dashboard.html`) — **[v8] §3.6에서 재설계 확정**(회사 칩 필터 + 상단 3박스 한 행). §3.6으로 흡수.
  - → **요약 카드 확정 후 마스터(`persons-master`)·상세(`persons-detail`)에도 일부 적용** (의존성: 대시보드 카드 → 마스터/상세 전파)
- **인건비 대시보드** (`labor-dashboard.html`) — **[v8] §3.5에서 전면 재설계 확정**(레이아웃 + 내용). 별도 레이아웃 작업 불필요, §3.5로 흡수.

**작업량**: 각 페이지별 1-2세션, 인력 대시보드 3종은 카드 확정 의존성으로 묶어서.

### 3.3 프로젝트별 인건비 / 인건비 예산 기능 점검 (✅ 기능 인벤토리 작성 — v7.4.2)

`project-labor.html` / `project-budget.html` 은 **기능을 하나하나 확인하면서 정리**해야 함. 기획서에 어디까지 반영돼 있는지 대조 필요(§4 로드맵·v5 기획서 Phase A 대조). 과거 사용자 방침: "일단 데이터 입력할 수 있게 만들고 하나하나 진행하자." → 입력 기능은 갖춰졌으니 이제 기능 단위 점검·정리 단계.

- 이 점검은 §4 트랙 B 로드맵과 **같은 페이지**를 건드림 → 트랙 B와 한 묶음으로 진행 권장.

> **✅ 기능 인벤토리 완료 (v7.4.2)** — 별도 파일 `프로젝트별인건비_기능인벤토리_v1.md`. project-labor(입력·뷰·인력·메타 baseline)·project-budget(3분류 원본·분배·전년복사·export) 현황 정리 + §4 대조.

**§4 로드맵 상태 (인벤토리 결론)**:
| 항목 | 상태 | 비고 |
|---|---|---|
| 4.1 셀 메모 UI | ✅ 완료(v7.4.3) | 옅은분홍배경+마커+툴팁+편집 팝오버. 우클릭 메뉴/마커 클릭 진입. 예상 편집·실제 읽기전용. |
| 4.2 Phase H 스냅샷 | ✅ 완료(v7.4.4) | 컬렉션 `projectLaborSnapshots`. 📸 버튼·이력 모달·내역(표)·차이·복원(+자동백업)·삭제·엑셀·시트복사. 단독 페이지는 보류. |
| 4.3 신규 인력 판단 | 🔴 미구현 | isNew 플래그만, 판정 로직·기준 UI 없음. 모델 확정 선행. 중. |
| 4.4 월급 연중 변경 | 🟡 진행(1·2a 완료) | `salaryChanges`(인사 마스터 연봉 변경 시점, 계단식)+`getMonthlySalaryAt`+인건비 그 달 연봉 계산·📅 표시. **2b**(인건비 선택 반영 버튼)·**3단계**(예산+선택전송) 남음. 기존 `monthlySalaryOverride`(과제 평탄 오버라이드)는 우선 적용으로 공존. |
| 4.5 엑셀 입출력 | 🔴 거의 미구현 | labor는 Ctrl+C/V 단일셀만(budget은 export 보유). 우선순위 낮음(마지막). |
| 4.6 모달 확장 | ✅ 완료 | v6.1. |
| 4.7 대시보드 본격 | ⬜ 별도 | §3.2/labor-dashboard. §3.4 점검 v7.4.2 완료. |

**권장 순서**: ~~4.1(메모)~~ ✅ → ~~4.2(스냅샷)~~ ✅ → **4.4(진행 중 — 1·2a ✅, 2b·3단계 남음)** → 4.3(모델 확정 선행) → 4.5(마지막).

**작업량**: 기능 인벤토리 작성 ✅(v7.4.2) + 항목별 정리(4.1 ✅ v7.4.3, 4.2 ✅ v7.4.4, 다음 4.4).

### 3.4 금액 3분류 재정립: 지원금 / 현금 / 현물 (✅ 전체 종료 — v7.4~v7.4.2)

> **발단(v7.3)**: §3.1 결산 사양 정의 중, 현재 "현금"이라 부르는 게 **사실 정부지원금**임이 확인됨. 용어·분류 체계를 바로잡아야 함.

> **핵심 발견 (v7.4)**: 코드 실태가 기획서 §3.4 설명(2분류·미구현·마이그레이션 필요)보다 앞서 있었음.
> - **project-budget.js는 이미 3분류 완성**: `cashOrInkind` = `'현금'|'자부담현금'|'현물'`, 라벨 매핑 `FUND_TYPE_LABELS`(`'현금'→"지원금"`, `'자부담현금'→"현금"`, `'현물'→"현물"`)가 정확히 목표대로. 즉 **데이터 키 `'현금'`(=지원금) 유지 + 새 키 `'자부담현금'` 추가** → **키 마이그레이션 불필요**. budget의 "예상 분배"는 이미 `cells[key]={rate,cash,selfCash,inkind,memo}`로 selfCash를 _planned 셀에 씀.
> - **project-labor만 2분류였음** → 이번에 budget과 동기화.
> - **funding은 안전**: `cell.cash`만 읽어 환급 집계 → 자부담현금(selfCash)은 cash=0이라 환급 0으로 올바름.
> - **자부담현금 실데이터 0건**(사용자 확인) → 추가가 순수 additive, 고아 데이터/마이그레이션 위험 없음.

**3분류 (확정)**:
| 분류(데이터 키) | 표시 | 의미 | 환급 | 셀 필드 |
|---|---|---|---|---|
| `'현금'` | **지원금** | 정부지원금 | ✅ | `cell.cash` |
| `'자부담현금'` | **현금** | 자부담 현금 | ❌ | `cell.selfCash` |
| `'현물'` | **현물** | 자부담 현물 | ❌ | `cell.inkind` |

**모델링 질문 — 모두 해소 (v7.4)**:
1. ✅ **단일선택**. 지원금+자부담 동시 병존 케이스 없음(드묾) → budget처럼 단일선택, 한 셀에 한 금액 필드만.
2. ✅ **selfCash 셀 단위 도입**. project-labor 자동계산을 3분기(`moneyFieldOf`)로 확장, 나머지 두 필드 0 클리어. budget과 쓰기 대상 일치.
3. ✅ **마이그레이션 불필요**. 데이터 키 `'현금'`(=지원금) 유지, 표시만 `FUND_TYPE_LABELS`로 재라벨, 자부담은 새 키. participation은 라벨만 3분기로(금액은 cash만 봐서 정확).
4. ✅ **단계적**, budget 라벨 표준 이식. 순서: **project-labor(완료) → participation-summary → labor-dashboard**.
   - **시각 구분**: 자부담현금 **금액 셀**엔 별도 배경색/배지 없음(사용자 요청 — "현금은 환급 X임을 모두 아니까 구분 불필요"). 단 **구분 드롭다운**은 색 구분(v7.4.1): 지원금=초록(`is-cash`), 현금(자부담)=주황(`is-selfcash`), 현물=앰버(`is-inkind`) — 자부담현금은 현물과 같은 따뜻한 계열로 지원금과 구분.
   - **조건부 노출 (대원칙)**: 자부담현금 컬럼/옵션은 **과제 단위 `project.hasSelfCash`(budget 체크박스)가 true일 때만** 보임. 안 쓰는 과제는 기존 [지원금|현물] 3열 그대로.

**✅ project-labor 구현 완료 (v7.4)** — 파일: `project-labor.js`, `project-labor.html`(CSS).
- 헬퍼 `FUND_FIELD`/`FUND_TYPE_LABELS`/`MONEY_FIELDS`/`moneyFieldOf`/`otherMoneyFields`/`fundTypeLabel`/`projHasSelfCash`/`normalizeCi`.
- 역할 드롭다운 3분류(자부담현금은 `hasSelfCash || 기존값`일 때만 노출).
- `hasSelfCash`면 예상·실제·**비교** 3탭 모두 월별 **[참여율 | 지원금 | 현금 | 현물]** 4열 + 우측 합계 3열(sticky-right-2=right:200px). colgroup·월헤더 colspan·서브헤더·tbody·tfoot·recalcSums 전부 조건부.
- 로직 5곳 3분기 전환: 자동계산(rate input) / 월급 재계산 / 가로채우기 / 일괄 자동계산 / undo. 분류 변경 후 옛값 잔존 방지로 나머지 두 필드 0.
- 정적검증 통과(node -c, 이진분기 0건, 컬럼수 균형, JS↔CSS 클래스 정합, HTML div/table 균형).
- ⏳ **실화면 확인 대기**: hasSelfCash 켠 과제 4열 표시 / 현금 입력→selfCash 저장 / 우측 sticky 3열 비겹침 / hasSelfCash 끈 과제 기존 3열.

**✅ participation-summary 구현 완료 (v7.4.1)** — 파일: `participation-summary.html`.
- `getPersonMonthly` 3버킷 분리: `cash`(지원금)/`self`(자부담현금)/`kind`(현물). `psNormalizeCi`/`psFundLabel` 헬퍼. breakdown의 `cashOrInkind`를 3-value로 보존.
- 행 집계 `sumSelf`/`monthsSelf`/`avgSelf` 추가. 행 렌더를 버킷 배열 기반으로 재작성(이름/3책5공 셀 자동 rowspan = 표시 버킷 수).
- 구분 필터: "현금"→**"지원금"** 라벨 정정, **"현금"(자부담) 버튼·행은 해당 연도 데이터에 자부담현금 분류가 있을 때만** 노출(`hasSelfData` 판정 + `updateSelfFilterVisibility`). 현금 필터 켠 채 자부담 없는 연도로 가면 '전체'로 자동 복귀.
- 셀 클릭 패널: type별(cash/self/kind) 분해·소제목 라벨 3분기. 환급액은 지원금만 유효(자부담현금/현물은 actual.cash=0 → 0, 기존 현물 동작과 동일).
- 정적검증 통과(인라인 JS node -c, 잔존 showCash/showKind 0건, div 균형 원본과 동일).
- ⏳ **실화면 확인 대기**: 자부담현금 있는 연도 "현금" 버튼/행 표시 / 지원금 행에 자부담 rate 미혼입 / 없는 연도·회사는 2줄 / 현금필터→자부담없는연도 전환 시 '전체' 복귀.

**✅ 나눔(분류 split) + 구분 컬럼 + 레이아웃 재구성 (v7.4.1)** — 파일: `project-labor.js`, `project-labor.html`.

> **발단**: 한 사람이 한 과제에서 분류 2개+ 병존(예: 현금 20% + 현물 30%)하는 드문 케이스. §3.4 단일선택 가정의 예외.

- **구분 컬럼 신설**: 고정 왼쪽 = **이름 | 월급 | 구분**(비교 탭 외). 헤더 "인력"→**"이름"**. 분류 드롭다운을 이름 밑→**구분 컬럼으로 이동**. 이름 밑엔 기존/신규(+신규면 세부역할)만.
- **나눔 토글**: **월급 칸 우측 작은 체크박스**(라벨 없음 — 실무자용). `personRoles[pid].split` 저장. **(v7.4.2 정정) 토글은 예상 탭에서만 노출** — split은 사람 단위라 예상/실제/비교가 공유하므로 실제 탭 체크박스는 잉여(중복 조작). 실제·비교는 예상에서 켠 결과(멀티행)만 표시.
- **나눔 OFF(평소)**: 1줄, 기존과 100% 동일.
- **나눔 ON(멀티행)**: 그 사람이 **분류별 여러 줄**로 분리. 줄 구성 = `personRoles[pid].splitCis`(데이터 키 배열, 기본 2줄=[주 분류, 다른 분류 1개]). 각 줄: **구분 드롭다운**(선택 가능, 다른 줄이 쓰는 분류는 옵션에서 제외해 중복 방지) + **참여율 입력**(그 분류) + **그 분류 금액**(읽기전용, 급여×%). 이름·월급은 **rowspan**으로 묶음.
- **데이터(키 변경 0)**: split은 **한 셀에 저장** — `cell.rates = {cash, selfCash, inkind}`(분류별 %) + `cell.cash/selfCash/inkind`(금액) + `cell.rate`(세 분류 합). "여러 줄"은 그 한 셀을 화면에만 여러 줄로 그리는 것. → funding·결산·labor-dashboard **무영향**.
- **나눔 켤 때 마이그레이션** `migrateCellRatesForSplit`: 그 사람의 모든 셀의 `cell.rates`를 **현재 단일 분류(`cashOrInkind`) 기준으로 초기화**(rate를 주 분류에 몰고 나머지 0). → 기존 단일값이 올바른 분류 줄에 표시되고, 이전 테스트로 어긋난 `cell.rates`도 이때 교정.
- **participation 정합**: `getPersonMonthly`가 `role.split && cell.rates`면 분류별로 분배(아니면 단일 분류). 멀티행 데이터와 일치.
- **레이아웃 폭**: 테이블 `width: max-content`(min-width 제거) → **분기/12개월 뷰에서 이름·월급·구분 폭 일관**(stretch 방지). col 폭: 이름 120 / 월급 120 / 구분 98 / 참여율 64 / 금액 92·88. (분기 뷰는 우측 여백 — 사용자 수용.)
- **퇴사자**: `.pl-row--exited .pl-name-text` 취소선 CSS 추가(누락이었음 — split·비-split 공통).
- 정적검증 통과(node -c, 컬럼수 일관, 토큰 정합, HTML div 균형).
- ⏳ **실화면 확인 대기**: 나눔 시 기존값이 맞는 분류 줄에 표시(desync 해결) / 구분 드롭다운 분류 변경·중복 제외 / 색 구분(지원금 초록·현금 주황·현물 앰버) / 뷰별 폭 일관 / 퇴사자 취소선.
- **알려진 한계**: ① 나눔 인력 **행 드래그 순서변경 불가**(끄고 옮긴 뒤 재설정) ② 나눔 참여율 **Ctrl+Z 미지원** ③ rate **가로채우기 나눔 줄 미적용** ④ 나눔 기본 2줄(3-way 동시 분할은 드롭다운으로 2개만 — 필요시 줄 추가 기능 후속) ⑤ 나눔 껐다 켜면 마이그레이션이 다시 돌아 주 분류로 초기화(분할분포 리셋).

**✅ labor-dashboard 점검 완료 (v7.4.2) — 기능 변경 없음** — 파일: `labor-dashboard.js`(주석만 추가).
- **selfCash 집계**: 대시보드의 모든 금액 집계(환급 추이 차트 / 프로젝트 환급 진척 / 요약 카드)는 `cell.cash`만 더함(8곳 전부, `node -c`+grep 확인). **selfCash·inkind 읽기 0건.** 환급(=지원금)만 보는 페이지라 자부담현금·현물을 합산하지 않는 게 **정확**(섞이면 환급 과대계상). → 추가 집계 불필요.
- **나눔 투명성**: 나눔 셀은 분류별 분배(`cell.rates`)가 아니라 **집계값**(`cell.cash`=지원금 금액, `cell.rate`=3분류 참여율 합)만 읽으므로 멀티행과 무관하게 정확. (이관 문구의 "한 셀이라 영향 적을 것" 확인됨.)
- **접두사 personId 파싱**: 두 패턴 모두 안전 — ① `prefix=pid+'_'+ym+'_'` 후 `key.indexOf(prefix)===0` + `substring(prefix.length)`(personId에 언더스코어 있어도 안전, 시작 고정) ② `ym=rest.substring(0,7)`(월 추출, personId 불필요). 셀 키 `{projectId}_{ym}_{personId}` 미변경이라 영향 없음.
- **v7.4 보강 효과**: project-labor가 분류 선택 시 나머지 두 금액 필드를 0 클리어 → `cell.cash`가 **항상 순수 지원금**임이 보장됨. 대시보드 cash 집계가 증명 가능하게 정확.
- **추가**: 향후 "환급에 자부담현금도 더해야 하나?" 오수정 방지용 의도 주석만 삽입(파일 상단 금액 모델 블록 + 환급 추이/진척 2곳). 정적검증 통과.

**남은 단계: 없음. §3.4 전체 종료.**
1. ~~participation-summary 라벨 3분기~~ ✅ (v7.4.1) + ~~나눔 cell.rates 분배~~ ✅ (v7.4.1).
2. ~~labor-dashboard selfCash 집계·personId 파싱 점검~~ ✅ (v7.4.2 — 변경 불필요 결론).
3. budget = 라벨 표준 원본(무변경), funding = 무변경(이미 안전) 확인됨.

**영향 범위**: `project-labor`(✅ v7.4), `participation-summary`(✅ v7.4.1), `project-budget`(원본, 무변경), `funding`(무변경), `labor-dashboard`(✅ v7.4.2 — 점검 완료, 주석만), §3.1 결산(이미 "지원금").

**작업량**: project-labor 1세션 ✅ + participation-summary ✅ + labor-dashboard 점검 ✅. → §3.4 완료.

### 3.5 [v8] 인건비 대시보드 재설계 + 채용 의무 관리

> **v8 신규 큰 건.** §4.7(인건비 대시보드 본격 구현) 사양 확정 + 신규 기능(채용 의무 유지 관리). §3.2 레이아웃 트랙의 labor-dashboard 부분을 여기로 흡수.
> **대원칙 — 페이지 역할 분리**: 인건비 대시보드 = "사람·규정" 홈 / 자금 관리 인건비 환급 탭 = "돈" 홈. 돈 지표는 대시보드에 중복 표시하지 않음(§0.1 중복 방지 원칙).

**결정 로그 (2026-06-03 확정)**

| # | 항목 | 결정 |
|---|---|---|
| D1 | 예상 연 환급·미확정 환급 카드 | funding으로(이미 6카드+미수 보유). 대시보드엔 두지 않음 |
| D2 | 월별 환급 추이 | funding 이관, 대시보드 제거 |
| D3 | 프로젝트별 환급 진척 | funding 이관, 분모 = **예상(planned)** (funding 데이터 구조와 일치) |
| D4 | 인력 참여율 현황(전체 명단) | 대시보드 제거(참여율 전용 페이지로) |
| D5 | 회사 인력 활용도 | 삭제(숫자는 상단 카드에 흡수) |
| D6 | 액션 아이템 | 삭제 X → "할 일/점검"으로 재초점(링크 + 채용의무 합류) |
| D7 | 확정 기준 플래그 | **`confirmed` 통일.** 대시보드가 쓰던 `sysReg`(구 2체크박스 잔재, v5.1에 confirmed로 통합)는 추이·진척 제거로 사라지고, 남는 참조('시스템 등록 미완료')도 `confirmed`로 정정 |
| D8 | 채용 의무 — 유지 기간 | 과제 단일 규칙, **등록 시작월 + 12개월**(기본, 과제별 조정) |
| D9 | 채용 의무 — 기준일 | **(v8.1 정정) 과제 시작일**(첫 연차 `startDate`, 없으면 등록 시작월 폴백). 개인 입사일(`hireDate`) 아님. (v8 원안=등록 시작월 → 디커플링 결정으로 과제 시작일로 변경) |
| D10 | 채용 의무 — 결합 | **머릿수 기준, 사람 교체 허용** |
| D11 | 채용 의무 — 유지 완료 | 12개월 채우면 그 자리 유지 완료 → 경고 해제(무기한 방지) |
| D12 | 청년 판정 | **자동 통일: 만 34세 이하**(`persons-summary`의 `getYouthInfo` 재사용, 수동 `isYouth` 폐기) |
| D13 | 채용 의무 관리 위치 | **C안** — 정의=과제 수정, 모니터링=대시보드 표. (정밀화 시 별도 페이지 B 추가) |
| D14 | 채용 의무 — 연간 요약 | 대시보드에 **올해 필수 N · 채용 M · 남음 K**(+청년) 표시(카드 + 표 합계줄) |

**(a) 상단 요약 카드 (5개)** — 전부 사람·규정(돈 카드 없음, 우측에 "환급 현황 → 자금 관리" 링크)

| # | 카드 | 메인 | 보조 | 산출 |
|---|---|---|---|---|
| 1 | 🏢 재직 인력 | 재직 N명 | (재직+퇴직 합산 표기 제거) | `status==='active'` |
| 2 | 📋 과제 등록 인원 | 등록 N / 재직 N | 전체 재직 중 X% | laborMap personIds ∩ 재직 |
| 3 | ⚡ 가용 인력 | 미참여 N · 여유 N | — | 미참여=rate합 0 / 여유=0<합<100 (겹침 없음) |
| 4 | 🚨 100% 도달 | N명 | 추가 참여 불가 | rate합 ≥ 100 |
| 5 | 🆕 채용 의무 | 채용 M / 필수 N | 남음 K · 청년 M′/N′ · ⚠️유지위험 X | §(c) 집계 |

- 카드 3+4 = 재직 인력을 미참여 / 여유 / 풀(100%) 세 칸으로 분할.

**(b) 본문 섹션** — 추이·진척·명단·활용도 모두 빠지고 두 블록만
- **제거/이관**: 월별 추이(D2)·프로젝트별 진척(D3) → funding / 인력 참여율 현황(D4) → 참여율 페이지 / 회사 인력 활용도(D5) → 삭제.
- **채용 의무 표** (§(c) 표시) — 과제별 행 + 연간 합계줄.
- **할 일 / 점검** (D6, 구 액션 아이템) — 한 줄 = `[심각도][분류] 설명 + 바로가기`. 카드에 있는 요약은 빼고 고칠 항목 + 링크만. 분류 긴급/주의/알림, 채용 의무 위반·위험 편입. 예:
  - 🔴 [채용의무] ○○과제 청년 필수 1명 미달 — 유지기한 D-28 → 바로가기
  - ⚠️ [확정] ○○과제 5월 미확정 — 환급 320만 묶임 → 바로가기 (판정 `confirmed`)
  - ℹ️ [입력] ○○과제 6월 인건비 미입력 → 바로가기

**(c) 채용 의무 관리 (신규 기능)**

- *데이터 모델* — 추가는 `projects.requiredRetention = { months: 12 }` 하나(기본 12, 과제별 수정). 재사용: `requiredNew`/`requiredYouth`, `personRoles.newOrExisting`(신규/기존 — §4.3/C4와 연동), `persons.birthDate`(청년 자동), `persons.status`/`exitDate`, 과제 참여 셀(등록 시작월 도출). **셀/키 변경 0** → funding·결산·project-labor 무영향. 기존 과제 미설정 시 기본 12(마이그레이션 0).
- *정의 — 과제 수정(`project-detail`)*: 필수 인원 일반 N / 청년 N(기존 필드) + **유지 기간(개월, 기본 12)**. 청년·일반 동일(D8).
- *유지 판정(D9~D11)*:
  - 등록 시작월 = 그 과제에서 그 사람 참여율이 처음 잡히는 월 → 그 달 **1일** 기준(개인 입사일 아님). **(v8.1 정정)** 채용 의무 유지기한 기준일은 **과제 시작일**(첫 연차 `startDate`)로 변경, 과제 시작일이 없을 때만 등록 시작월로 폴백.
  - 유지 기한 = 기준일(과제 시작일) + 유지기간(기본 12개월). 머릿수 기준 + 교체 허용. 12개월 충족 → 유지 완료(경고 해제). **채용 머릿수 카운트는 기준일과 무관하게 그 과제 신규 분류 재직 인력 전원을 셈(중간 합류 포함).**
  - 상태: ✅ 충족(신규 재직 ≥ 필수) / 🔴 미달(< 필수) / ⚠️ 위험(유지 기한 안 끝난 신규가 퇴사(예정)해 미달로 떨어질 때).
  - 신규 자격 = 그 과제 `newOrExisting==='신규'` & 재직, 청년 = + 만 34세 이하. **신규 분류 신뢰도는 §4.3/C4 구현 후 향상**(그 전엔 수동 newOrExisting).
- *청년 판정 일원화(선행, D12)*: 현재 `persons-summary`=만 34세 자동 / `labor-dashboard`·`persons-master`=수동 `isYouth` 불일치. **`getYouthInfo`(만 34세 이하, 기준일 시점) 자동으로 통일, 수동 플래그 폐기.** 채용 의무 청년 집계의 전제. ❓ 군대 등 예외 보정은 추후.
- *표시(D13·D14)*:
  - 채용 의무 표(대시보드): 과제 / 필수(일반·청년) / 채용(현재 머릿수) / 유지 기한 / 상태(✅·🔴·⚠️).
  - 연간 합계줄 + 카드 5: **올해 필수 N · 채용 M · 남음 K**(+청년 M′/N′). 필수 N=올해 활성 과제 requiredNew 합 / 채용 M=Σ min(과제별 신규 재직, requiredNew) / 남음 K=max(0,N−M).

**(d) funding 이관 — 돈 영역**
- 예상 연 환급·미확정 환급·월별 추이·과제별 진척 → **funding 인건비 환급 탭**(v6.2). funding엔 이미 요약 6카드(이번 달·누적 × 예정/환급/률) + 미수 + 과제별 추적이 있어 대부분 충족.
- D7: 추이·진척이 대시보드에서 빠지면서 `sysReg` 사용 경로 소멸 → funding은 `confirmed` 기준으로 이미 정확. 대시보드에 남는 confirmed 정정만.
- (선택) 진짜 연말 run-rate 예측선이 필요하면 funding에 추가("누적 환급 예정액"은 계획 합계지 예측 아님). 우선순위 낮음.

**(e) 데이터 영향** — 신규 필드 `projects.requiredRetention` 하나. 나머지 기존 재사용. 셀/키 변경 0 → funding·결산·participation-summary 무영향. 청년 일원화는 표시 로직(수동→자동), 데이터 마이그레이션 불필요.

**(f) 구현 순서**
1. 상단 카드 5개 재구성(카드 3 분리 + 채용 의무 카드 5).
2. 청년 판정 일원화(`getYouthInfo` 통일) — 채용 의무 선행.
3. 채용 의무: `project-detail` 유지 기간 정의 → 판정 헬퍼(등록월·12개월·머릿수) → 대시보드 표 + 연간 요약.
4. 할 일/점검 재초점(링크 + 채용의무 위험 합류, `confirmed` 정정).
5. 제거: 월별 추이·프로젝트별 진척·인력 참여율·활용도(추이·진척은 funding 확인 후).
6. funding: (선택) 연말 예측선.

**(g) 남은 미결정** — 청년 군대 예외 보정 / funding 예측선 추가 여부 / 정밀 채용 의무 페이지(B안) 승격 시점(보류).

**작업량**: 중~대. 카드·제거 소 / 채용 의무(정의+판정+표시) 중 / 청년 일원화 소(함수 통일).

### 3.6 [v8] 인력 대시보드 재설계 (persons-dashboard)

> v8 두 번째 대시보드. §3.2 레이아웃 트랙의 persons-dashboard 부분을 여기로 흡수.
> **성격 = 인사(HR) 현황 홈** — 팀장이 보고 본부장에게 보고하는 상위 뷰. 과제 투입·규정은 인건비 대시보드(§3.5), 돈은 funding. 로우데이터·디테일은 자식 페이지(마스터·상세·참여율·결산·연구소·자격증).
> 데이터 소스 = `persons`만(`subscribePersons` + `PersonsSummary`). **추가 소스 0.** (인력은 건강보험 자격득실에서 등록 → 핵심 필드 결측 없음.)

**결정 로그 (2026-06-03 확정)**

| # | 항목 | 결정 |
|---|---|---|
| E1 | 분해 축 | **회사별만**(전체/식스티/굿뉴스/패리티). 별도 팀 구조 없음 |
| E2 | 회사 칩 필터 | **추가** — 공통 필터(`rnd-company-filter`) 재사용, 페이지 전체 스코프. 전체=3사 분해 / 단일 회사=그 회사로 스코프(3사 분해 생략) |
| E3 | 상단 요약 레이아웃 | 전체/재직 요약을 **입퇴사·시점별과 같은 박스(`pd-section-box`) 형태로 통일**, **한 행에 3개** 배치 |
| E4 | 재직 중심 | "전체(재직+퇴직)" 대신 재직 메인, 퇴직은 입퇴사 박스로 |
| E5 | 청년 | **제거**(요약 카드·상세 섹션 모두). 채용 의무 청년은 인건비 대시보드(§3.5)에서 |
| E6 | 입·퇴사 | 입사·퇴사·**순증감** + **월별 입퇴사 막대**. 이직률 제외 |
| E7 | 시점별 재직 인원 | **유지**(오늘/임의 시점 + 회사별) |
| E8 | 데이터 품질 점검 | **불필요**(건강보험 자격득실 등록 → 결측 없음) |
| E9 | 자식 페이지 롤업(연구소·자격증) | **안 함**(매번 볼 사이즈 아님, 각 페이지에서) |

**레이아웃 — 상단 한 행, 동일 박스 3개**
1. **인력 현황** — 재직 N명(메인) + 회사별·성별 미니 분해(전체 보기). 단일 회사 선택 시 그 회사만.
2. **입·퇴사 현황** — 입사 N · 퇴사 N · 순증감 +N + 회사별 +/− + **월별 입퇴사 막대**(박스 안 컴팩트, 좁으면 행 아래 풀폭). 연/월 컨트롤 유지.
3. **시점별 재직 인원** — 오늘/임의 시점 재직 + 회사별. 시점 조회 컨트롤 유지.

- 셋 다 `pd-section-box` 통일 스타일 + 회사 칩 필터 연동.
- 청년 카드·청년 상세 섹션·기존 1단 개별 stat 카드·데이터 품질 블록 모두 제거 → 페이지가 이 한 행 중심으로 단순화.

**데이터/영향** — `persons`만. 모델 변경 0. 청년 자동 판정은 이미 `getYouthInfo`(만 34세)라 §3.5 청년 일원화와 정합. funding·결산·인건비 대시보드 무영향.

**작업량** — 소~중. 새 데이터 0, 대부분 레이아웃 재배치 + 회사 필터 연동.

---

## 4. 트랙 B — project-labor 로드맵 (v6.2 §3 이어받음)

v6.0의 P0 두 페이지가 완료된 후 진입 예정이던 작업들. 트랙 A 정리와 병행/후행.

### 4.1 Step 5 셀 메모 UI (project-labor) — ✅ 구현 완료 (v7.4.3)

v5 기획서 §4 Phase A.7. 사양 명확. 1세션 추산.
- (구사양) 셀 우상단 빨간 점(메모 있을 때) / 셀 배경 노랑 / hover 툴팁 / 클릭 시 메모 편집 팝오버.
- 데이터 모델상 셀 값에 `memo` 필드 이미 존재 → 모델 추가 없이 UI만.

**✅ 구현 내용 (v7.4.3)** — 파일: `project-labor.js`, `project-labor.html`(CSS).
- **표시**: 메모 있는 셀(rate 셀) = **옅은 분홍 배경(`pl-cell-has-memo`) + 우상단 모서리 마커(`pl-memo-marker`) + hover 툴팁(`td.title`)**. (기존 빨간 글씨 인라인 `pl-memo-text` 제거.)
- **진입**: ① 우클릭 컨텍스트 메뉴 맨 위 **"💬 메모 추가/편집"** 항목(모든 필드 공통, 예상 탭) ② 메모 있는 셀 **마커 클릭**.
- **팝오버**(`pl-memo-popover`): 헤더(인력명 · 연월) + 텍스트영역 + **저장/취소/삭제**(삭제는 기존 메모 있을 때만). **Ctrl+Enter 저장 · Esc 취소 · 바깥 클릭 닫기**. 위치는 앵커(셀/커서) 근처 + 화면 밖 보정.
- **탭별**: 편집은 **예상(planned)** 탭만. **실제(actual)** 탭은 메모 있으면 마커·툴팁 표시 + 클릭 시 **읽기전용** 팝오버. **비교(compare)** 탭은 마커 없음.
- **나눔(split)**: 메모는 셀 1개(키 동일) → **첫 줄 rate 셀에만** 마커(중복 방지). 진입·저장 동일.
- **데이터**: 기존 `cell.memo` 그대로(`setCell`로 patch). **키 변경 0, funding·결산·labor-dashboard 무영향**(금액만 읽음).
- **충돌 차단**: 팝오버 textarea에서 `keydown` `stopPropagation` → 전역 셀 단축키(Ctrl+Z 되돌리기·Ctrl+R·Ctrl+C/V·방향키)가 메모 입력을 가로채지 않음.
- 정적검증 통과(node -c, HTML div/table 균형, 심볼 정합, `pl-memo-text` 잔재 0).
- ⏳ **실화면 확인 대기**: 우클릭→메모 추가→저장→옅은분홍배경+마커 / 마커 클릭 편집·삭제 / Ctrl+Enter·Esc / 실제 탭 읽기전용 / 나눔 첫 줄만 마커 / 메모 입력 중 Ctrl+Z가 셀 undo로 안 샘.
- **알려진 한계(v1)**: 메모는 탭별 셀 독립(예상/실제 따로). @멘션·작성자/타임스탬프 없음(인증·작성자 모델 없어 제외).
- **잠금 셀도 메모 가능(v7.4.3)**: 메모는 값이 아닌 주석이라 잠금과 무관 — 수동 잠금 셀(🔒) 우클릭 메뉴에도 "💬 메모" 항목 추가(잠금해제와 함께 노출), 퇴사후·입사전 비활성 셀은 일반 메뉴라 기존부터 가능. 메모 팝오버는 잠금 여부와 무관하게 예상 탭에서 편집 가능.

### 4.2 Phase H 예상 계획 스냅샷 (project-labor) — ✅ 구현 완료 (v7.4.4)

v5 기획서 §6.9 + Phase H. "데이터 모델 v5.2 등록 완료" 주장은 **실제 코드엔 없었음**(기존 snapshot은 in-memory 복사-되돌리기/모달 고정용뿐) → 저장 모델을 새로 설계.

**설계 결정 (사용자 확정)**:
- **담는 범위**: 예상 셀(`planned`) + `personIds` + `personRoles`. (actual·meta(확정/지급)는 미포함 — "예상 계획"의 스냅샷이므로.)
- **복원 동작**: 전체 덮어쓰기 + **복원 직전 자동 백업 스냅샷**(kind `auto`).
- **저장 위치**: 새 컬렉션 `projectLaborSnapshots`, **1 문서 = 1 스냅샷**(auto-id). 단일 문서 배열 누적은 1MB 한계 위험 → 분리. 조회는 `where('projectId','==',pid)` 단일 equality(자동 인덱스, 복합 인덱스 불필요) + JS 정렬(createdAt desc).
- **권한**: 별도 등급 없음(전 로그인 사용자 저장/복원/삭제), 작성자(`window.currentUser` name/email/uid)만 기록.

**구현 내용 (v7.4.4)** — 파일: `project-labor.js`, `project-labor.html`(CSS).
- **진입**: 예상 탭 툴바 **📸 스냅샷** 버튼(실제·비교 탭 자동 숨김). 모달 = 현재 요약(N명·M셀) + 이름 입력 + "현재 계획 저장" + 이력 목록.
- **문서**: `{ projectId, label, kind('manual'|'auto'), createdAt, author, planned, personIds, personRoles, cellCount }`.
- **이력 행 액션 4개**: **내역 / 차이 / 복원 / 삭제**. 자동백업은 "자동백업" 배지.
  - **내역(view)** = **표(table)**. 행=인력(이름·구분, 나눔이면 배지), 열=입력된 월(단일 연도면 "N월", 여러 해면 `YYYY-MM`), 셀=`참여율%`+작은 `금액`, 우측 `합계`. 이름 열 sticky(가로 스크롤). 패널 상단 바: **📋 시트 복사**(TSV 클립보드, 시트 붙여넣기용) + **⬇ 엑셀**.
  - **차이(diff)** = 스냅샷↔현재 비교(추가/변경/삭제 카운트 + 인력별 월·참여율·금액 before→after, 색 구분). 메모·색 제외(계획값 시그니처만 비교).
  - 내역↔차이는 같은 행에서 패널만 전환(활성 버튼 강조), 같은 버튼 재클릭 시 닫힘.
- **복원**: confirm(변경 셀 수 안내) → 자동백업 저장 → `planned`/`personIds`/`personRoles` 덮어쓰기 → `scheduleSave`+`renderAll`.
- **삭제**: confirm → 문서 delete(현재 데이터 무영향).
- **엑셀/복사 공통 표 AoA**(`buildSnapshotAoa`): `이름 / 구분 / {월} 참여율(%) / {월} 금액 … / 합계 금액`. 엑셀 파일명 **`{키워드}_스냅샷_{이름}_{날짜}.xlsx`**(키워드 = `proj.keywords`||`keyword`||name fallback). SheetJS 재사용.
- **나눔 판정 = `personRoles[pid].split`**(사람 단위). `cell.rates`는 나눔 토글 잔재라 오판 — v1 리스트뷰의 "나눔 전부 표시" 버그 수정.
- **충돌 차단**: 스냅샷 모달 열림 중 Ctrl+Z 셀 undo 가로채기 차단(add-modal 가드와 동일 패턴).
- **무영향**: 별도 컬렉션 + 기존 `projectLabor` 셀 키 변경 0 → funding·결산·labor-dashboard 영향 없음.
- 정적검증 통과(node -c, div 균형, ID/클래스/함수 정합).
- ⏳ **실화면 확인 대기**: 저장→목록 / 내역 표(월·참여율·금액·합계, 이름 sticky) / 나눔 배지가 실제 split 인력만 / 시트 복사 붙여넣기 / 엑셀 파일명·내용 / 차이 추가·변경·삭제 / 복원 시 자동백업 생성 후 덮어쓰기 / 모달 중 Ctrl+Z 안 샘.
- **알려진 한계(v1)**: 스냅샷마다 planned 전체 복사 저장(인력·연수 많으면 문서 커짐, 1MB 한계 전까지 안전) / 자동백업도 일반 목록에 함께 쌓임(수동 삭제) / 복원은 항상 planned 전체 단위(부분 복원 없음).
- **향후(보류)**: **단독 페이지**(새 탭 인쇄용 표 화면)는 현재 불필요 — 표 보기+엑셀+시트복사로 실용 목적 충족. 필요 시 별도 페이지 신설로 진행.

### 4.3 신규 인력 판단 기준 (project-labor)

v5 기획서 §6.11. 2세션 추산. 데이터 모델 확정 필요.

### 4.4 월급 오버라이드 연중 변경 (project-labor)

v5 기획서 §6.12. 1-2세션 추산. 실제 케이스 만난 후 진입.

### 4.5 Step 6 엑셀 입출력 (project-labor)

v5 기획서 §4 Phase A.13. "페이지 사용성 좋으니 우선순위 낮음" — 트랙 B 마지막.

### 4.6 ~~인력 추가 모달 정보 확장~~ ✅ v6.1 완료

모달 인력 행에 현재 참여율·참여 과제 수 배지. (v6.2 §3.3 참조)

### 4.7 인건비 대시보드 본격 구현 (labor-dashboard)

v5 기획서 §4 Phase C. → **[v8] 본격 사양 확정: §3.5 참조.** 돈 지표는 funding으로 이관, 대시보드는 사람·규정(가용·참여율·채용 의무) 중심으로 재설계. §3.2 레이아웃 트랙의 labor-dashboard 부분도 §3.5로 흡수.

### 4.8 기능 후보 (백로그) — 타당성만 검토

> **용도**: 구현 전 "기능적으로 가능한가"만 확인한 후보를 누적하는 목록. 순서 확정 시 정식 §4 항목/작업으로 승격. **상태 = 후보(미착수)**, 타당성 판정만 기록. (별도 파일 대신 본문 한 곳에 유지 — 사용자 결정 2026-06-02.)

#### C1. 인건비 예상 분배 — 선택 전송 (budget → project-labor) · 타당성 ✅ · ✅ **구현 완료 (v8.3, C1-b)**

> **✅ 구현 완료 (v8.3)** — `project-budget` **"📤 선택 전송"** 모달. ① 인력 체크(전체/해제) ② 필드: 이름만 / +참여율 / +참여율+금액 ③ **금액 출처**: ⓐ 예산 금액(`실지급액×%`) / ⓑ 인건비 탭 급여 재계산(`personRoles[pid].monthlySalaryOverride>0 ? 그 값 : 마스터 monthlySalary` × %) ④ 월 범위(연차 ym 시작~끝) ⑤ 쓰기 방식: 빈 셀만(값 있는 셀 건너뜀·비파괴) / 덮어쓰기(둘 다 메모 보존). 구분 버킷=예산 행 분류(현금→cash/자부담현금→selfCash/현물→inkind). `_planned.cells`+`_meta.personIds` merge, 셀/키 변경 0. **C1-a(이름만 보내기)는 이 모달의 "이름만" 옵션으로 흡수**(단독 버튼 제거). ⚠️ ⓑ는 오버라이드 단일값 가정 → §4.4(오버라이드 객체화) 진행 시 동기 수정. 정적검증·노드 시뮬 통과. 파일: `project-budget.html/js`.
> (아래는 후보 시점 사양 — 참고용 보존)
- **발단**: 운영상 `project-labor` 예상 탭에 데이터를 **먼저** 입력함. 현재 분배(`distributeToLabor`)는 같은 인력·월 셀을 **전부 덮어씀** → 직접 입력값과 충돌. 일부만/이름만 보내고 싶음.
- **타당성**: ✅ 데이터 모델 무장애. 근거 = 인력 행은 `state.personIds`가 생성(셀 유무 무관), `personRoles` 비면 로드 시 기본값(`기존`/`현금`) 자동 채움. → **셀 없이 personId만 넣어도** 빈 셀 행으로 정상 표시·즉시 입력 가능.
- **변형**:
  - **C1-a 이름만 보내기** — `personIds.push`만 하고 `cells[key]=…` 쓰기 생략. 가장 단순(현 함수에서 셀 쓰기만 건너뛰면 됨).
  - **C1-b 값 선택** — 네 축: ① 인력 선택(체크박스) ② 필드(이름만 / +참여율 / +금액) ③ 월 범위 ④ 덮어쓰기 vs 빈 셀만(비파괴).
- **주의점(구현 시 결정)**:
  - **급여 출처 차이** — budget 금액 = `actualPay × 참여율%`, labor = 자체 월급(마스터/오버라이드). 참여율만 보낼 때 금액을 ⓐ budget값 그대로 ⓑ labor 급여로 재계산 — 정책 선택(운영상 labor 급여가 정답이면 ⓑ).
  - **3분류·나눔(split)** — 현 분배는 단일 분류(`현금/자부담현금/현물` 중 하나)만 처리. 자부담현금·split 인력 전송 정책 필요.
  - **작업량** — 데이터 장애 0, 대부분 **선택 모달 UI**. 쓰기 로직 분기는 소.
- **현 구조 메모(2026-06-02 확인)**: 분배 = `distributeToLabor()`(`project-budget.js` ~1013), **수동 버튼** `pb-distribute-btn`(자동 트리거 없음 — 전체 파일 확인). 저장(`saveBudgetData`)은 `projectBudget`에만 써서 분배와 **별개** → **예산총액은 저장만으로 labor 우측 패널 반영**(`getBudgetBreakdown`이 `budgetCash/budgetSelfCash/budgetInkind` 읽음). 즉 "저장만 = 직접 입력 셀 보존 + 예산총액 표시"가 코드 변경 없이 이미 됨.

#### C2. 3책5공 관리 (내부 과제 카운트) · 타당성 ✅ · **사양 확정 2026-06-02** · ✅ **구현 완료 (v8.2)**

> **✅ 구현 완료 (v8.2)** — 공유 헬퍼 `three-five-rule.js`(신규, 순수 모듈) + 5개 표시 지점. 데이터 추가 = `projects.is3ch5gManaged`(bool) + `managerPersonId`(체크 시) 둘. 셀/키 변경 0 → funding·결산·labor-dashboard 무영향. 기존 과제 기본 false → 마이그레이션 0. 상세·테스트 = `개발정리_테스트가이드.md` 묶음 7.
> - **헬퍼**: `isManagedActive`(is3ch5gManaged+수행) / `bucketForPerson`(책=책임자 AND 단독·컨소주관 / 공=그 외, 책임자는 명단 무관 인정) / `countForPerson` / `isOverLimit`(책>3 또는 책+공>5) / `format`("책/공").
> - **과제 수정**: opt-in 체크 + 책임자 **검색 드롭다운**(재직/퇴직 필터·이름 검색, hidden `managerPersonId` 저장). 참여형태/컨소역할은 "참여 형태" 섹션 값 재사용.
> - **참여율**: 3책5공 컬럼 — **현재 연도만** `책/공`(예 1/3), 과거 연도 `—`. 한도 초과 빨강+툴팁.
> - **프로젝트별 인건비**: 인력 추가 모달 배지 `N개 과제`→`책/공`(관리 과제일 때) + "3책5공 대상" 칩. 메인 표 책임자 행 강조(배경+이름 볼드·색, 나눔 행 포함, 뱃지 X).
> - **인건비 예산**: 책임자 행 색 강조. (책/공 **+1 미리보기**는 타 과제 roster 로드 필요 → 보류.)
> - **인건비 대시보드(추가)**: "📚 3책5공 점검" 섹션 — 책≥2·공≥4·한도 초과 인력만. (사용자 피드백 2.3. "인력 대시보드" 요청이었으나 데이터·설계상 인건비 대시보드에 구현 — persons-dashboard는 §3.6에서 persons 전용·모델 0.)
> - **책임자 지정 경고(과제 수정)**: 책 2건 이상 인력을 책임자로 지정 시 참여형태·상태 무관 안내(단독/주관 ⚠️ 3책 / 컨소-참여 ℹ️). (사용자 피드백 2.2.)
> - **공고일 8자리 자동 변환**: C4 신규 판정 공고일을 텍스트 입력+`formatDateInput`으로 변경. (사용자 피드백 1.1.)
> - **soft 경고**: 한도 초과 빨강+툴팁만(차단·alert 없음).
> - **알려진 한계**: 공(共) 카운트는 인건비 관리 과제 명단 기준(책임자=책은 어디서든 정확). 비인건비 3책5공 과제의 공 누락 가능(채용 의무 디커플링과 동일 보류).

(아래는 v8.1 시점 사양 — 구현 시 반영됨, 참고용 보존)

- **규칙**: 국가연구개발혁신법 시행령 §64. 운영 모델 = **책 ≤ 3, 책 + 공 ≤ 5** (책/공 **별개 버킷**, 한 과제는 **한쪽만** 계상). 참여율(%) 아님 — 과제 **"수"**. (규정 "5공에 책 포함"과 한도는 동일하나, **책/공 분리 표기**가 직관적이라 분리.)
- **범위**: **내부 과제만** 카운트. 외부 무시(전부 이 시스템 등록 전제). 동시성 = 내부 과제 기간 겹침.

**① 등록 (과제 수정 `project-detail`)**
- 과제 단위 **opt-in 플래그** `is3ch5gManaged`. **켠 과제만** 카운트 대상. (제외 방식 ❌ / 체크 방식 ⭕)
- 책임자 칸 분기: 체크 ON → **입력 필수 + 인력 마스터 전체 드롭다운(personId 고정)**; OFF → 기존 자유 텍스트.
- 참여형태(`단독`/`컨소`)·컨소역할(`주관`/`참여`) 기존 필드 재사용.

**② 책/공 판정** (대상 = `is3ch5gManaged` 켜짐 + 현재 수행 중)
- **책** = 그 사람이 책임자 **AND** (`단독` 또는 `컨소-주관`). ≤ 3.
- **공** = 그 외 참여 (일반 참여자 + **`컨소-참여`의 책임자**).
- 한 과제는 책/공 **한쪽만** +1. 한도: 책 ≤ 3, **책 + 공 ≤ 5**. (예: 책 3 → 공 2까지 / 책 1 → 공 4까지)
- 책임자는 지정 + 수행중이면 **명단 여부 무관 인정**(실무상 책임자 최소 참여율 지정으로 거의 명단에 있음).

**③ 카운트 라이프사이클**
- **켜짐**: 과제 **수행/진행 상태** + **프로젝트별 인건비 명단 등록**.
- **꺼짐**: 과제 **완료/종료** 시 자동 제외(현재 수행 중 아님).
- 상태값 명칭은 구현 시 실제 status에 매칭.

**④ 표시**
- **참여율 페이지** (`participation-summary`, `3책5공` 컬럼 현 `—`): **현재 연도 조회 시에만** 표시(과거 연도 미출력 — 3책5공은 현 시점 의미만). 형식 = `책/공`, 라벨·`/5` 생략 → 예 **`1/3`** (왼쪽 책, 오른쪽 공). 한도 차면 빨강.
- **인력 추가 모달** (`project-labor`, 기존 v6.3 통계 배지 ~5416): `N개 과제` → **`책/공`** 으로 교체(참여율% 유지). 모달에 **"이 과제 3책5공 대상" 표식(칩)**. 사람 추가 시 해당 버킷 +1.
- **인건비 예산** (`project-budget`): 인력 행에 `책/공 · 참여율%`. 단 예산 화면은 **저장값 +1**(이 제안 과제 반영된 **미리보기**)로 표시. 책임자 색 강조.
- **프로젝트별 인건비** (`project-labor`): 책임자 행 강조 — **행 배경 살짝 + 이름 볼드·색 변경**(뱃지 X). 컨소-참여로 공 잡혀도 색은 그대로(색 = 역할 표시 / 숫자 = 규정 카운트, 따로 감).

**⑤ 경고** — **soft만(메시지 표시, 막지 않음)**. 총 5 초과 또는 책 3 초과 시. (규정 예외·행정 사정 여지)

**⑥ 데이터** — `projects.is3ch5gManaged`(boolean) + 책임자 personId(체크 시) 추가. 참여형태·컨소역할·수행기간·상태·personIds 전부 재사용. **셀/키 변경 0** → funding·결산·labor-dashboard 무영향. 기존 과제 플래그 기본 false → 마이그레이션 0.

- **작업량**: 데이터 무장애. 플래그·드롭다운 UI(과제수정) + 책/공 카운트 헬퍼(기간겹침·역할·컨소) + `getModalPersonStats` 확장 + 참여율 컬럼 + budget 미리보기(저장값+1) + 책임자 색 + soft 경고. **중.**

#### C3. 셀 메모 — 실제·비교 탭에서도 입력 (현재 예상 탭 전용) · 타당성 ✅ · ✅ **구현 완료 (v8.3)**

> **✅ 구현 완료 (v8.3)** — 권장안 ⓐ 확정. **예상·실제 탭 모두 편집**(`openMemoPopover`가 mode로 `state.planned`/`state.actual` 분기·저장, 실제 탭은 우클릭 시 메모 전용 메뉴). **비교 탭**: 예상행→`_planned`·실제행→`_actual` 마커+우클릭, **차이행 비허용**. 데이터 변경 0(기존 `cell.memo` 재사용). 파일: `project-labor.js`(단독, HTML/CSS 무변경). 정적검증 통과.
> (아래는 후보 시점 사양 — 참고용 보존)
- **현재**: 셀 메모(v7.4.3 §4.1)는 데이터상 `_planned`/`_actual` 셀이 **둘 다 `memo` 필드 보유**. 그러나 컨텍스트 메뉴·마커가 **항상 `state.planned`만** 읽고 씀(하드코딩, project-labor.js ~2921·2940) → 사실상 **예상 탭 전용**.
- **요청**: 실제 탭·비교 탭에서도 메모 추가/편집.
- **타당성**: ✅ 데이터 무장애(필드 이미 존재, 마이그레이션 0).
  - **실제 탭**: 메모 타깃을 `activeTab === 'actual' ? state.actual : state.planned` 로 분기 + 저장도 `_actual` 문서로. (거의 그게 전부.) `buildMemoMarker`가 이미 `mode` 인자를 받아 마커 렌더는 대체로 연결돼 있음.
  - **비교 탭**: 한 셀 위치에 예상/실제/차이 **세 행** → 메모 대상이 행에 따라 갈림. 예상행→`_planned`, 실제행→`_actual`. ❓ **차이행 메모**는 파생값이라 저장처 없음 → ⓐ 차이행 비허용 vs ⓑ 별도 저장(신규 필드/문서). **권장 = ⓐ(비허용)**.
- **데이터**: 변경 0(기존 `memo` 필드 재사용). 셀/키 변경 없음.
- **작업량**: 실제 탭 **소**(타깃 분기 + 저장 문서) / 비교 탭 행별 타깃 매핑 추가 **소~중**.

#### C4. 신규인력 추천 (과제별 규칙 기반 신규/기존 자동 판정) · 타당성 ✅ · **사양 확정 2026-06-02**
> §4.3 "신규 인력 판단 기준"의 구체화. 기존 §4.3은 placeholder(v5 §6.11 참조, 모델 미확정)뿐이었음 — 이 C4가 그 모델 확정.
- **핵심 식**: **신규 = 입사일 > 컷오프**, **컷오프 = 기준일 − N개월**. (컷오프 **당일 제외** — 보수적, `>` 사용)
  - 예: 공고일 `2026-06-02`, N=6 → 컷오프 `2025-12-02`. 입사일 `2025-12-03`+ = 신규, `2025-12-02` 당일 = 기존.
  - 상한 없음(컷오프 이후면 전부 신규). 일 단위 비교(`hireDate`·컷오프 둘 다 `YYYY-MM-DD`).

**① 규칙 설정 (과제 수정 `project-detail`)**
- **기준일**(드롭다운): `과제 시작일` / `공고일`.
  - `과제 시작일` = **기존 첫 연차 `startDate` 자동 연동** → 추가 필드 0.
  - `공고일` = 새 date 필드 1개 추가.
  - 협약일은 **보류**(협약 도장일 자체는 중요치 않은 경우 많음 — 보통 과제 시작일 기준, 협약일은 시작일 뒤이거나 소급). 드롭다운이라 필요 시 옵션 한 줄로 추가 가능.
- **N개월** 입력. 규칙 = `{ baseDateType: 과제시작일|공고일, months: N }`. 미설정 과제는 자동 판정 안 함(수동 유지).

**② 판정 (자동, 사람별)**
- 입사일 > 컷오프 → **신규**, 이하 → **기존**, 입사일 없음 → **미상**(자동판정 X, 수동 선택 유도. 입사일 결측 가능성 낮음).
- 과제마다 규칙이 달라 같은 사람도 신규/기존 갈림 → 과제별 `_meta.personRoles.newOrExisting`와 정합.

**③ 추천 표시 (인력 추가 모달, `project-labor`)**
- 후보별 **`신규`/`기존`/`미상` 라벨** 자동 표시.
- **신규/기존 필터 버튼** — 클릭 시 해당 인력만 노출.
- 추가 시 `newOrExisting` 기본값 = 판정 결과(사용자 변경 가능, 강제 아님).

**④ 일괄 재판정**
- 규칙 변경 시 **이미 들어간 `newOrExisting`은 자동으로 안 건드림**(사용자 수정값 보호).
- **`일괄 재판정` 버튼** → 현재 규칙으로 그 과제 인력 전체 newOrExisting 재계산·갱신.

**⑤ 데이터** — `projects`에 **공고일(date)** + 규칙 `{baseDateType, months}` 추가. 재사용: 첫 연차 `startDate`(과제 시작일), `persons.hireDate`(일 단위), `personRoles.newOrExisting`. **셀/키 변경 0** → funding·결산·labor-dashboard 무영향.

- **작업량**: 규칙 UI(과제수정: 기준일 드롭다운+공고일 date+N) + 판정 헬퍼(월 차감·일 비교) + 모달 라벨·필터 + 일괄 재판정 버튼. **중.** §4.3 승격 후보.

#### C5. 외부 단방향 전달 — 과제 정보를 러버블 + 스프레드시트에 push (코드 단일 발원) · 타당성 ✅ · **방식 확정 2026-06-02**
> 회사에서 **러버블 앱**과 **구글 시트("[2026년도 R&D 관리]")** 를 둘 다 사용. 보는 관점이 달라 **양쪽 모두에 동일 과제가 기록돼야** 하고, **코드번호는 양쪽 일치 필수**. 현재 수동 이중입력 중 → 이걸 우리 시스템 1회 입력으로 대체.
- **핵심 구조 = 우리 시스템이 "발원지"**: 과제 생성 시 **프로젝트 코드를 우리가 단독 생성**하고, 그 한 벌을 **러버블·시트 양쪽으로 단방향 전달**. → **코드 일치 자동 보장**, 이중입력 제거.
  - 코드에 연도·구분별 **순번**(`...-26-01`, `-26-02`)이 들어가므로 **순번을 우리가 단독 관리**해야 수동 편집 충돌 방지.

**대상 1) 러버블 — 프리필 URL 핸드오프**
- 기존 구글 캘린더 연동과 동일 패턴(CalendarManagement.js `buildGoogleCalendarUrl` = API 아님, 값 채운 URL을 새 탭으로 열기).
- 값 조립 → 러버블 "새 프로젝트 생성" 페이지 URL에 **쿼리 파라미터**로 담아 새 탭 open → **사용자가 [생성] 클릭**. 예: `https://(앱).lovable.app/new?name=...&code=TV-RND-G-26-01&client=...`
- **전제(그쪽 작업, 편집 권한자 1회)**: 생성 페이지가 **URL 파라미터를 읽어 폼 prefill** 하도록 추가. → 우리가 받을 것: 생성 페이지 주소 + 약속한 파라미터 이름.

**대상 2) 구글 시트 — Apps Script 웹앱 (자동 행 추가)**
- 시트에 **Apps Script** 붙여 "데이터 받으면 맨 아래 행 추가"로 만들고 **웹앱 배포** → 나온 **웹앱 URL** 로 우리 시스템이 POST → 자동 한 줄 추가.
- 인증 = **비밀 웹앱 URL 1개**(원하면 토큰 추가). 서비스 계정·OAuth·Cloud Function **불필요**. (대안: ⓐ 시트복사=수동 붙여넣기 / ⓒ 서비스계정+Cloud Function(Blaze)=과함 → **Apps Script 채택**.)
- **전제(그쪽 작업, 시트 주인 1회)**: 스크립트 배포 + 구글 권한 승인. → 우리가 받을 것: 그 비밀 URL.

**필드 매핑 (우리 → 양쪽)**
- 공통 기존 값 재사용: 과제명 / **코드(우리 자동 생성)** / 발주처(=정부부처/클라이언트) / 담당조직 / 협약일(시작·종료) / 정부지원금 / 자부담현금(=기관부담금 현금) 등.
- 시트는 러버블 폼보다 칸이 많음(법인·사업구분·지원형태·형태(신규/계속)·전문기관·계속비(이월금) 등) → 일부 R&D 전용 칸은 우리 데이터 존재 여부 확인 필요.

- **데이터**: 우리 쪽 신규 저장 거의 없음(코드 생성·순번 관리 헬퍼 정도). 셀/키 변경 0 → funding·결산·labor-dashboard 무영향.
- **확인 필요(후속)**:
  - ① **프로젝트 코드 자동생성 규칙 + 순번 출처** (사용자: "규칙은 나중에 논의". 예 `TV-RND-G-26-01`: 조직접두(TV/CV)-RND-구분(G정부R&D/E지원사업)-연도-순번).
  - ② **타입(Billable)·상태 매핑** — 우리 상태(제안/선정/수행…) → 러버블(활성)/시트(미선정·협약체결·제출완료…), 러버블 `(미선정)` 접두어 규칙.
  - ③ **총 MM·계속비 등** 출처 — 참여/인건비/예산에서 산출 여부.
  - ④ **시트 R&D 전용 칸**(사업구분·지원형태·전문기관 등) 우리 데이터 매핑 가능 여부.
  - ⑤ **트리거 위치** — 과제 목록/상세에 "외부로 보내기"(러버블/시트) 버튼.
- **작업량**: 코드·순번 생성 + 러버블 URL 빌더 + 시트 POST + 트리거 버튼. **중**(매핑 확정 후). 단 양쪽 1회 세팅(러버블 prefill 수용 / 시트 Apps Script 배포)이 선결.

#### C6. 연봉 프로파일 (공유 슬롯) — 예산 전용 연봉 골라 불러오기 · 타당성 ✅ · **1단계 구현 완료 (v8.3)**

> **발단**: 예산(제안서)을 짤 때 인력의 **실제 연봉을 그대로 노출하기 곤란**한 경우가 있어, 용도별(공개용·제안서용 등) 연봉을 따로 두고 **골라서 불러오고** 싶음. (사용자 요구: ① 실제 연봉 ② 실제를 가린 임의 연봉 ③ 기타 추가 가능.)

**확정 설계 (전 단계 결정)**
- **공유 슬롯 방식**. `실제`(=기존 `annualSalary`, 항상 존재) + 용도 슬롯. 인력별 `persons.salarySlots = { 슬롯명: 연봉(원) }`. 월급은 사용처에서 `ceil(연봉/12)`. **빈 슬롯 = 실제 연봉으로 폴백.**
- **슬롯 목록은 별도 저장 안 함** — 가용 슬롯 = 기본(`['제안서용(공개 가능)']`) ∪ 전 인력 `salarySlots` 키 **합집합**. "기타 추가"=편집 중 새 이름 입력→저장되면 다른 인력에게도 빈 슬롯 노출. (v1 한계: 전역 일괄 이름변경/삭제 미지원.)
- **입력 UX = A + C**: A) 인력 편집 모달 인라인(1단계 완료) / C) 엑셀 다운·업로드 컬럼(3단계 예정).
- **예산 선택 = 전체 기본값 + 행별 변경**(2단계 예정). 행엔 숫자(월급/실지급액)만 들어가 기존 분배/C1/내보내기 무영향.

**① 1단계 — 인력 마스터 입력 (✅ 완료, v8.3)**
- 인력 편집 모달 "실제 연봉(원)" 아래 **"예산 전용 연봉(원)"** 섹션: 가용 슬롯마다 한 줄(슬롯명·연봉·월급 미리보기), **"+ 항목 추가"**. 슬롯명 검증(`실제` 예약·특수문자·중복·20자).
- `readForm()`이 `salarySlots`(빈 값 제외) 수집·저장(`updatePerson` 병합). 실제·예산 전용 연봉 **천단위 콤마**(`type=number`→`text`+`parseMoney`/`fmtComma`). 슬롯 행 **스택 레이아웃**(긴 이름 전체 표시).
- 데이터 추가 = `persons.salarySlots` 하나, 셀/키 변경 0 → funding·결산·labor 무영향, 마이그레이션 0. 파일: `persons-master.html/js`.

**② 2단계 — 예산에서 슬롯 골라 불러오기 (예정)**
- `project-budget` 상단에 **기본 슬롯 셀렉트**(전체 적용) + 🔍 인력 선택/행별 드롭다운 → `salarySlots[슬롯] ?? 실제`의 월급을 행에 채움. 가용 슬롯은 persons 합집합(1단계 `getAvailableSlotNames` 동일 로직).
- ★ **폴백 안전장치 결정 필요**: 슬롯 비운 사람은 실제 연봉으로 떨어지므로 — ⓐ 실제 연봉으로 불러오되 **"실제 연봉 사용" 표시** / ⓑ **빈칸·0으로 둬 실제값 유입 차단**(유출 방지). (사용자가 ②의 가림 목적과 폴백이 상충함을 인지.)

**③ 3단계 — 엑셀(C) (예정)**: 인력 다운로드/업로드에 슬롯 컬럼(예: `연봉:제안서용(공개 가능)`) 추가, 일괄 입력.

- **작업량**: 1단계 소(완료). 2단계 중(셀렉트·드롭다운·로드 분기+폴백 정책). 3단계 소~중. **C1-b ⓑ(인건비 급여 재계산)와는 별개 축** — C6는 "예산에 무슨 연봉을 불러올지", C1-b ⓑ는 labor 월급. 2단계에서 예산 행에 슬롯 월급을 채우는 형태로 연결.

---

### 4.9 이월금 — 다음 연차로 이월 (✅ 1단계 완료 — v8.6)

**개념**: 이월금 = 받았는데 그 해에 안 쓴 돈 → 다음 연차로 넘겨 씀. 입금·미수와 **완전 별개**(받은 돈이라 입금 예정/미수 계산 안 건드림). 잔액(차액) = (예산+이월)−누계. **항목별**: 지원금·자부담현금만 이월, **현물 불가**.

**1단계 구현 (project-labor)**:
- 잔액 박스 "이월금" 칸 → 전년도→올해 받은 **지원금/자부담 항목별 표시** + **"📥 N차년도로 이월" 버튼**(마지막 연차면 "이월 대상 없음").
- 이월 확정 모달: 지원금 이월(=`예산−실제 누계` 자동 프리필·수정 가능) / 자부담현금(0·직접 입력 — 셀에 집행 집계 없어 자동값 부정확) / 현물 칸 없음. 확정 시 다음 연차 예산 문서에 `carryoverCash`·`carryoverSelfCash` merge.
- `getCarryover` 숫자→`{cash,selfCash}`, `getRemainingBreakdown` 항목별 이월 적용(현물 제외). 데이터 = 예산 문서 두 필드, 마이그레이션 0.

**다음 단계 후보**: ① 예산 페이지(project-budget)에도 이월금 표시/입력 ② 자금 관리·과제 관리 표 "이월 N" 노출 ③ C5 외부 전달 "계속비(이월금)" 칸 매핑. **(연차 정렬 §4.10이 잔액 계산 정확도의 선행 조건 — 걸치는 연차는 정렬 후 자동값이 정확해짐.)**

### 4.10 연차 기준 정렬 (큰 건 · ✅ 인건비 1·2단계 완료 — v8.7 · 자금관리 대기)

**배경(문제)**: 시스템에 시간 축이 둘 — **연차 축**(예산 페이지·연차 메타·이월금: 시작~종료일, 달력연도에 걸칠 수 있음)과 **달력연도 축**(인건비 표·누계 = state.year의 1~12월, 자금 당해 = 1/1~12/31). 연차가 걸치면(예: 1차=2026.7\~2027.6, 2차=2027.7\~2028.6):
- 한 달력연도(2027)에 1차 꼬리+2차 머리가 섞여 "이 화면의 연차"가 성립 안 됨.
- 연도→연차 연결이 먼저 걸린 연차를 집어, 2027을 1차로 오인(2차 선택 불가).
- 예산(연차 전체)−누계(달력연도분만) → 차액·이월금 잔액 어긋남.

**결정**: **집행·잔액·이월금은 연차 단위로 본다.**
- **보는 단위 = 과제(키워드) × 연차.** 화면에서 연차를 직접 골라 그 연차 실제 기간만 봄. 라벨 = **키워드 · N차년도 (기간)** (예: "프라운호퍼 · 1차년도 (26.7~27.6)"). 키워드 = `proj.keywords` 필드(괄호 표기·스냅샷 파일명에 이미 쓰는 정식 필드). 같은 과제라도 1차/2차가 별개 단위.
- **적용 범위**: ✅ 인건비(표·누계·차액·이월금) — 선택 연차 기간 기준 / ✅ 자금관리(입금·미수) — 선택 연차 기준(별도 작업, 인건비 다음) / ❌ 전사 대시보드 — 달력연도 유지.
- **데이터 변경 0.** 셀은 이미 월(`YYYY-MM`) 단위, 예산·이월금은 이미 연차 단위. 바뀌는 건 "화면이 다루는 기간"뿐 → 마이그레이션 0. (인건비 집행을 달력연도로 집계하는 타 페이지는 ym 셀을 그대로 읽으므로 무영향.)

**인건비에서 손볼 표면**: 연도 드롭다운→**연차 드롭다운**(예산 페이지 패턴), "12월까지 채우기"→"연차 끝까지", 분기 네비(연차 기준 재정의), 엑셀 월 컬럼, 접힌 월 캐시 키(연도→연차), 스냅샷. 연도→연차 연결 로직(`getYearIndexForState`)은 직접 연차 선택으로 대체.

**순서**: 인건비 먼저 → 자금관리. **착수 전 잔여 확인**: 자금관리에서 "선택 연차 기준 입금·미수"를 구체화할 때 funding 현 구조(달력연도 당해) 영향 점검.

---

## 5. ✅ 작업 전 확인 항목 (모두 해소 — v7.2)

### 5.1 ✅ 해소 — 과제 수정 "환급 X + 참여율만 관리" 삭제 영향 (§2.3-1)

**결론**: 사용자 확인 결과 **`participation_only` 모드로 저장된 과제 없음** → 마이그레이션 없이 안전 삭제 완료. 저장 시 `laborRefund = (mode !== 'participation_only')`이라 이 모드만 `laborRefund=false`를 만드는 유일 경로였고, `participation-summary`/`funding`/`budget`/`dashboard`는 `laborManaged`만 보므로 영향 제한적. 로드 시 옛 값 안전 가드(`participation_only` → `refund_participation`) 추가.

(당시 확인 항목 기록)
- `participation-summary`는 과제를 **`proj.laborManaged === true`** 로 거름(v6.1 결정).
- **선행 확인**: ① 해당 모드 과제 존재 여부 조회(→ 없음) → ② 처리 방침(불필요) → ③ funding/participation 영향(`laborManaged`만 봄, 무관).

### 5.2 ✅ 해소 — 대시보드 3분류 vs funding 2분류 정합성 (§2.1-3)

**결론**: funding은 **2분류**(과제 / 기타=지원사업+기타+빈값, 용역 제외)로 깊게 구현됨(월/연 × 예정/완료 × task/other 표). 대시보드는 §2.1 목표대로 **3분류**(과제/지원사업/기타)로 완성. → **둘이 의도적으로 다름**(대시보드=한눈 개요 더 세분 / funding=상세 일정표 2열). 완전 통일하려면 funding 표를 3열로 확장하는 대공사 필요 → **별도 작업거리로 보류**. 데이터상 용역 수행 과제는 양쪽 모두 별도 트랙으로 제외.

---

## 6. 작업 순서 제안

블라스트 반경·위험도 기준 묶음. 같은 파일은 한 번에.

**1묶음 — 무위험 quick wins ✅ 완료 (v7.1)**
- ✅ 과제 수정: 타이틀(`📝 과제 정보 수정`) + 별칭 안내문구(8자리) — `project-detail` (§2.3-2,3)
- ✅ lab: 월별 인력 명부 펼침 버그 (오늘 달만) — `lab.js` (§2.5)
- ✅ 대시보드: 카드 순서 교체 + 전체 카드 강조 (§2.1-1,2)
- ✅ 과제 관리: 수주 카드 삭제 + 월별 신규제안 그래프 삭제, 총 신규 제안 카드 그리드로 이동(유지) (§2.2-2,3)
- ✅ (추가) 과제 관리: 죽은 CSS 정리 + 요약 카드 3그룹화 (§2.2-4,5)

**2묶음 — 중간 (로직·UI)**
1. ✅ **과제 수정: 인건비 관리 방식 옵션 삭제** (§2.3-1) — `participation_only` 라디오 제거 + 안전 가드. §5.1 해소.
2. ✅ **과제 관리: 유형별 클릭 필터 + 수행 호버 + 레이아웃 조정** (§2.2-1) — filter-summary-row 클릭 필터 방식.
3. ✅ **대시보드: 입금 예정 3분류** (§2.1-3) — 과제/지원사업/기타. §5.2 해소(funding은 2분류 유지로 분기).
4. ✅ **참여율: 셀 클릭 패널 환급액 추가 + 표시 축소** (§2.4) — **v7.3 완료.** 키워드/참여율/실제 환급액 3요소. A-1(항상 actual.cash) + B-2(confirmed 월만, funding 정합). 추가 읽기 비용 없음. §3.1(연말 결산 보고서)로 자연 연결.

> **다음 세션 시작점 (v8.4 이후)**: C6 2·3단계 + §4.4 1·2a + 대시보드 툴바 ✅ 완료. 추천 순서 = **§4.4 2b**(인건비 "📅 연봉 변경 반영" 선택 버튼 — `project-labor`: 변경 인력 골라 해당 월부터 예상 금액 재계산, 확정 실제 월 제외, 묶음 undo. ⚡일괄계산은 이미 그 달 연봉 사용하나 전체 대상이라, 선택+비파괴 버전이 2b) → **§4.4 3단계**(`project-budget` 행 월급/선택 전송 ⓑ를 "그 달 연봉"으로 동기 — 2027 예산 자동 새 연봉) → **6묶음 과제별 상세**(사양 미정 → 화면 기획 먼저) → **§4.5 엑셀**. **권장: 해석 함수(`getAnnualSalaryAt`/`getMonthlySalaryAt`/`sanitizeSalaryChanges`/`normalizeYm`)가 persons-master·project-labor에 동일 중복 → 2b/3단계 진입 시 공용 위치(예: `firestore-service.js`의 `window.SalaryUtil`)로 승격해 드리프트 방지.** C2 후속(budget 책/공 +1 미리보기·비인건비 과제 공 카운트)은 사례 누적 시.

**3묶음 — 큰 건 / 별도 트랙**
- **결산 보고서 페이지 (§3.1)** — ✅ **구현 완료 (v7.3).** `settlement-report.html` 신규. 지원금만, 인력×과제 행, 이름/생년월일/과제명/1~12월(총계·소계 제거, 중앙정렬), 엑셀+복사. 사이드바 FOUC 깜빡임도 해결.
- **금액 3분류 재정립: 지원금/현금/현물 (§3.4)** — **✅ 전체 완료 (v7.4·v7.4.1·v7.4.2).** project-labor 조건부 4열·로직 5곳 3분기(v7.4), participation 3분기 집계+나눔 분배(v7.4.1), 나눔(split) 멀티행·구분 컬럼·색 3분·퇴사자 취소선(v7.4.1), labor-dashboard 점검(v7.4.2 — 환급=cash만이라 변경 불필요, 주석만). 마이그레이션 0. budget/funding 무변경(이미 안전).
- 전체 레이아웃 조정: 과제별 상세 / 인력 대시보드 3종 / 인건비 대시보드 (§3.2)
- project-labor / project-budget 기능 점검 (§3.3) + 트랙 B 로드맵(§4)을 한 묶음으로

**4묶음 — [v8] 인건비 대시보드 재설계 + 채용 의무 관리 (§3.5) · 현재 다음 작업**
1. 상단 카드 5개 재구성(카드 3 미참여/여유 분리 + 채용 의무 카드 5) — `labor-dashboard`.
2. 청년 판정 일원화(`getYouthInfo` 만 34세 자동, 수동 `isYouth` 폐기) — `persons-master`·`labor-dashboard` 등. **채용 의무 선행.**
3. 채용 의무: `project-detail` 유지 기간(`requiredRetention`) 정의 → 판정 헬퍼(등록 시작월+12개월·머릿수·교체) → 대시보드 표 + 연간 요약(필수/채용/남음).
4. 할 일/점검 재초점(구 액션 아이템: 링크 + 채용의무 위험 합류, `sysReg`→`confirmed` 정정).
5. 제거: 월별 추이·프로젝트별 진척(→ funding 확인 후)·인력 참여율 현황·회사 인력 활용도.
6. (선택) funding 연말 run-rate 예측선.
- 신규 필드 `projects.requiredRetention` 하나, 셀/키 변경 0 → funding·결산 무영향. 청년·채용 의무가 §4.3/C4(신규 판정)와 연동되므로, C4를 먼저/병행하면 신규 분류 신뢰도↑.

**5묶음 — [v8] 인력 대시보드 재설계 (§3.6) · 4묶음과 병행 가능**
1. 회사 칩 필터 + 페이지 스코프(`rnd-company-filter` 공유).
2. 상단 3박스 한 행: 인력 현황 박스 신설 + 기존 입퇴사·시점별 박스 통일·정렬.
3. 입·퇴사 월별 막대(순증감 포함, 이직률 X).
4. 제거: 청년 카드·청년 상세·1단 개별 stat 카드.
- `persons`만 사용, 모델 변경 0. 청년 자동(`getYouthInfo`)은 §3.5 일원화와 정합.

**6묶음 — 과제별 상세 (`projects-summary.html`, §3.2) · 맨 마지막**
- 다른 항목(특히 §3.5·§3.6 대시보드) 개발 후 **맨 마지막**에 착수. 내용·레이아웃 사양 미정 → 착수 시점에 재검토(변경 가능). 현재는 순서만 기록.

**7묶음 — C2 3책5공 관리 (§4.8 C2) · ✅ 완료 (v8.2)**
- 공유 헬퍼 `three-five-rule.js` + 과제 수정(opt-in·책임자 검색 드롭다운)·참여율(3책5공 컬럼)·프로젝트별 인건비(모달 배지·책임자 행)·인건비 예산(책임자 색)·인건비 대시보드(3책5공 점검). 책임자 지정 경고·공고일 8자리·재직퇴직 필터까지 테스트 피드백 반영. 셀/키 변경 0.

**8묶음 — C3 셀 메모 실제·비교 탭 (§4.8 C3) · ✅ 완료 (v8.3)**
- `project-labor.js` 단독. 예상·실제 탭 편집(실제 메모 전용 우클릭) + 비교 탭 예상행/실제행 마커·우클릭(차이행 비허용). 데이터 변경 0.

**9묶음 — C1-b 예산 선택 전송 (§4.8 C1) · ✅ 완료 (v8.3)**
- `project-budget` "📤 선택 전송" 모달(인력·필드·금액 출처 ⓐ/ⓑ·월 범위·빈셀만/덮어쓰기). C1-a 흡수. `_planned.cells`+`_meta.personIds` merge, 키 변경 0.

**10묶음 — C6 연봉 프로파일 1단계 (§4.8 C6) · ✅ 완료 (v8.3)**
- `persons-master` 인력 편집 모달에 예산 전용 연봉(공유 슬롯) 입력. `salarySlots`, 합집합 슬롯, 콤마·(한 줄) 레이아웃. **2단계(예산 불러오기)·3단계(엑셀) = ✅ 완료(v8.4, 12묶음).**

**11묶음 — 대시보드 지원금 현황 카드 재정비 · ✅ 완료 (v8.3)**
- `dashboard` 윗행 2칸화(노트북 줄바꿈 해소)·죽은 "지원금 구성" 제거·"입금 진행(완료/남은+완료율)" 신설·순서(입금 진행→자금 구성). kpis 키 변경 0.

**12묶음 — C6 연봉 프로파일 2·3단계 · ✅ 완료 (v8.4)**
- **2단계**: `project-budget` "💰 연봉 불러오기" 바(슬롯 셀렉트+전체 적용+행별 드롭다운) → `salarySlots[슬롯] ?? 실제` 월급을 급여총액·실지급액에 채움. **폴백=하이브리드**(빈 슬롯 0 차단+주황 경고+"빈 항목 실제로 채우기"). `row.salarySlot` 추가, 키 변경 0.
- **3단계**: `persons-master` 일괄 다운/업로드 `연봉:슬롯명` 컬럼(합집합·비파괴 병합·콤마 파싱). 데이터 변경 0.

**13묶음 — §4.4 월급 연중 변경 1·2a단계 · ✅ 완료 (v8.4)**
- **1단계(인사 마스터)**: 편집 모달 "연봉 변경 시점"(`salaryChanges`=[{from,annualSalary}], 계단식)+해석 함수. 변경시점 없으면 기존과 동일.
- **2a(인건비)**: `getEffectiveMonthlySalary(person, ym)` 월 인식화 + 사용자 동작 계산 지점 ym 전달 + 📅 인디케이터. 표시값은 base 평탄.
- **남음**: 2b(선택 반영 버튼) → 3단계(예산+선택전송 그 달 연봉). 해석 함수 공용 위치 승격 권장.

**14묶음 — 대시보드 툴바 한 행 정렬 · ✅ 완료 (v8.4)**
- `dashboard` 회사 필터를 `.dash-toolbar`로 통합(기준 연도·회사 왼쪽 / PDF 저장 오른쪽). HTML만, ID·JS 무변경.

**15묶음 — 이월금 다음 연차로 이월 1단계 (§4.9) · ✅ 완료 (v8.6)**
- `project-labor` 잔액 박스 이월금 칸(항목별 표시)+"📥 N차년도로 이월" 버튼/모달(지원금 자동·자부담 수동·현물 제외). 예산 문서에 `carryoverCash`/`carryoverSelfCash` merge, 마이그레이션 0. 입금·미수 무영향.

**16묶음 — 연차 정렬 1·2단계 + 예산 급여/총액/전송 + 자부담 합산·별도 + 방향키 + 캐시 헤더 · ✅ 완료 (v8.7)**
- 연차 정렬 1단계(인건비 표·누계·차액·이월금을 연차 기간 전체로)+잠긴 셀 합계 제외+나눔 3건+재입사 수동 잠금해제, 2단계(분기 보기에서 연차 밖 월 회색·잠금). 파일: `project-labor.js`.
- 이월금 덮어쓰기 픽스(`{merge:true}`)·예산 급여 정합(급여총액=연봉/실지급액=`ceil/12`)·총액 직접입력·예산 셀 방향키·예산→인건비 전송 순서/분류(`personRoles`)·자부담 합산 표시 줄. 파일: `project-budget.js`.
- 자부담 합산/별도 "표시" 토글(인건비 sticky 박스)·인건비 월급 칸 ↑↓. 파일: `project-labor.js`. 배포 캐시 헤더 `firebase.json`.

> **다음 세션 시작점 (v8.7 이후)**: 연차 정렬 1·2단계 ✅ / 자부담 합산·별도 ✅ / 예산 급여·총액·전송·방향키 ✅. 추천 순서 = **정산 반납 용어 통일**(자금 "정산 반납 N 반영"·과제상세 "정산 반납(이자 반납 등)")+**사유 툴팁**(자금 hover) → **§4.4 3단계**(`project-budget` 예산 동기·차년도=최신 연봉, `window.SalaryUtil` 활용) → **자금관리 연차 기준**(인건비 연차 정렬 미러) → (선택) 인건비 표 푸터·예산 행별 셀도 합산(지원금+자부담) 표기 → S2(인건비 대시보드+청년 자동) → S3·S4 → 과제 상세 페이지(맨 마지막). 보류: 재입사 정식(재직 구간 복수=데이터 변경), project-budget 3책5공 미리보기.

> **다음 세션 시작점 (v8.6 이후)**: 이월금 1단계 ✅. 추천 순서 = **연차 기준 정렬(§4.10)** — 인건비(표·누계·차액·이월금)를 "과제(키워드)×연차" 단위로(연차 드롭다운·12월채우기→연차끝·분기/엑셀/접힌월/스냅샷 따라옴, 데이터 0) → **자금관리도 선택 연차 기준** → **정산 반납 용어 통일**(자금 "정산 반납 N 반영"·과제상세 "정산 반납(이자 반납 등)")+**사유 툴팁**(자금, hover) → **§4.4 3단계**(예산 동기, 차년도=최신 연봉) → S2(인건비 대시보드+청년 자동) → S3·S4 → 과제 상세 페이지(맨 마지막). **연차 정렬이 이월금 잔액 자동값 정확도의 선행 조건.**

**원칙**: 1·2묶음으로 다른 페이지를 먼저 정돈 → project-labor 계열(점검+로드맵)을 마지막에 한 트랙으로. project-labor를 건드리는 작업(§3.3, §4)이 여러 개라 미리 흩어서 손대면 충돌·재작업.

---

## 7. 완료 아카이브 (v6.1 ~ v6.2)

상세 사양은 v6.2 기획서 §1·§2 참조. 요약만 보존.

**v6.1 — P0-1 참여율 합계 페이지 (`participation-summary.html` 신규)**
- 데이터 소스 `project-labor` 단독, 회사 칩·연도·planned/actual 토글·구분 필터·이름 검색·미배정 토글, 연간 평균(B방식), 색깔 코딩 5단계(`<80` 초록 / `80~89` 노랑 / `≥90` 주황 / `=100` 분홍 / `>100` 빨강), 셀 클릭 우측 슬라이드 패널(과제별 분해).
- 3책 5공 규칙 시각화는 컬럼 자리만, 데이터 미연결(⏳ 후속).
- 부수: 인력 추가 모달 현재 참여율·과제 수 배지(`project-labor`).

**v6.2 — P0-2 자금관리 재구성 (`funding.html`)**
- 메인 탭 2개(지원금 입금 일정 / 인건비 환급), 요약 카드 6개 × 2, 과제별 추적 표 3행 페어(예정/입금/미수 · 예상/실제/미수).
- 분류: 입금 일정 = 과제/기타 2분류. 인건비 환급 = 분류 없음(표 배지로 충분).
- 단위: 입금 탭 = 미수(원), 환급 탭 = 환급률(%).
- 폐기: 수주 카드, 시기별 입금 현황(분기/월 카드), 알림 카드(이번 달/다음 입금/올해 남은 입금) — 사용자 헷갈림.

---

## 8. 다음 세션 이관 문구 (복붙용)

> 최신 복붙용 단일 파일 = `다음세션_이관메시지_기획서_8_4_연관.md`. (아래 임베드 블록은 v8.2 시절 버전 — 형식 참고용 보존. 실제 이어가기는 8_4 파일 사용.)

```
인건비 관리 시스템 — 이어서 작업 (v8.2 이후).

[참조 파일 — 프로젝트에 올려둠]
- 인건비_관리_시스템_기획_v8_2.md   (최신 기획서. §6 작업 순서 + §4.8 C1~C5 후보 사양)
- 개발정리_테스트가이드.md           (지금까지 구현 누적 + 실화면 테스트 체크리스트. 묶음 7 = C2)
- 코드: three-five-rule.js(신규 공유 헬퍼) / project-detail / participation-summary / project-labor / project-budget / labor-dashboard (.html/.js)

[작업 규칙]
- 코드 원본은 읽기 전용 → outputs 복사본에 누적 편집(재복사 금지). 프로젝트 자동반영 안 됨(내가 다운받아 같은 이름으로 덮어씀 = 교체됨).
- ★ three-five-rule.js 는 신규 파일 → 프로젝트에 새로 추가 + 사용 페이지 HTML에 <script src="three-five-rule.js"> 포함됨(participation-summary·project-labor·project-detail·labor-dashboard).
- 검증은 정적분석(node -c + 인라인 JS + grep 정합 + div/table 균형)까지. 실화면은 내가 확인.
- 큰 결정은 물어보고, 자잘한 건 바로 진행. 한 파일은 한 번에. 한국어 진행. 디자인 세부는 나중에 — 지금은 기능/숫자 위주.
- 매 작업마다 '개발정리_테스트가이드.md'에 누적 기록(무엇을 개발 + 무엇을 테스트 + 다음 구현).

[지금까지 한 것]
- v8.1: §3.5 인건비 대시보드+채용 의무 / §3.6 인력 대시보드 / C4 신규 인력 자동 판정 / 채용 의무 디커플링.
- v8.2: **C2 3책5공 관리 구현 완료.** 공유 헬퍼 three-five-rule.js(책≤3·책+공≤5). 과제 수정 opt-in is3ch5gManaged + 책임자 personId 검색 드롭다운(재직/퇴직 필터·이름 검색). 참여율 3책5공 컬럼(현재 연도만 책/공·초과 빨강). 프로젝트별 인건비 모달 배지(N개 과제→책/공)+칩·메인 표 책임자 행 강조. 인건비 예산 책임자 색. 인건비 대시보드 "📚 3책5공 점검" 섹션. 책임자 지정 경고(책 2건 이상 시). 공고일 8자리 자동 변환. 데이터 추가=is3ch5gManaged+managerPersonId, 셀/키 변경 0.

[이번 세션 할 일 — 다음 추천 순서]
1. §4.4 월급 오버라이드 연중 변경 — **범위 결정 먼저**: (a) 과제 오버라이드만 연중(A안 팝오버) vs (b) +마스터 연봉 시간축(상/하반기)+여러 인력 일괄. 내 케이스는 (b). 입력 UX=A안. 모델 monthlySalaryOverride 숫자→객체{YYYY-MM:금액, step}.
2. C3 셀 메모 실제·비교 탭 — 권장안 ⓐ(차이행 비허용) 확정. 결정 거의 불필요 → 바로 가능. (실제 탭: 메모 타깃 state.actual 분기 + _actual 문서 저장. 비교 탭: 예상행→_planned, 실제행→_actual, 차이행 비허용.)
3. C1 인건비 예상 분배 선택 전송 — 금액 출처 결정 필요(budget값 vs labor 급여 재계산), split/자부담 정책.
4. 6묶음 과제별 상세(projects-summary) — 사양 미정 → 화면 기획 먼저.
5. §4.5 엑셀 입출력 — 범위. 우선순위 낮음.

[결정 필요(착수 전 물어볼 것)]
- §4.4 범위 (a)/(b).
- C1 참여율만 보낼 때 금액 출처.
- 6묶음 레이아웃·내용 사양.

[C2 후속·보류 (사례 누적 시)]
- project-budget 책/공 +1 미리보기 — 타 과제 roster 로드 필요(현재 책임자 색만).
- 비(非)인건비 3책5공 과제의 "공" 카운트 — 명단 캐시가 인건비 관리 과제만 담는 한계(책=책임자는 정확).
- "3책5공 점검" 위치(인건비 대시보드 채택) — 인력 대시보드로 옮길지 여부.
- 청년 군대 예외 보정.

[확인 대기 — 실화면(이월 포함)]
- v8.2: 3책5공 — 과제 수정 opt-in·책임자 검색 드롭다운(재직/퇴직·이름검색)·저장복원 / 참여율 책/공(현재연도만·초과 빨강) / 모달 배지·칩·책임자 행 강조 / 예산 책임자 색 / 인건비 대시보드 3책5공 점검 / 책임자 지정 경고(책2건↑) / 공고일 8자리 자동 변환. (체크리스트 = 테스트가이드 묶음 7.)
- (이월) v8.1 인건비/인력 대시보드·채용 의무·C4 / §4.2 스냅샷 / §4.1 메모 / §3.4 3분류 실화면.
```

---

## 9. 변경 이력

| 버전 | 일자 | 핵심 변경 |
|---|---|---|
| **v8.6** | **2026-06-07** | **이월금 1단계(다음 연차로 이월) 구현 + 연차 기준 정렬 결정(큰 건) + 정산 반납 용어 통일·사유 툴팁·§4.4 3단계 기준 결정.** **이월금 1단계**: `project-labor` 잔액 박스 이월금 칸(껍데기)→전년도→올해 받은 **지원금/자부담 항목별 표시 + "📥 N차년도로 이월" 버튼/모달**(지원금=`예산−실제` 자동 프리필·수정 가능 / 자부담현금=0·직접 입력 / 현물 칸 없음·이월 불가). 확정 시 다음 연차 예산 문서(`projectBudget/{id}_year{N}`)에 `carryoverCash`·`carryoverSelfCash` merge. `getCarryover` 숫자→`{cash,selfCash}`, `getRemainingBreakdown` 항목별 이월(현물 제외). **입금·미수 무영향**, 데이터=예산 문서 두 필드, 마이그레이션 0(껍데기엔 값 없었음). `project-labor.html/js`. 정적검증·로직 14단언 통과. ⚠️ 자부담현금 자동 잔액은 셀에 집행 집계 없어 부정확→직접 입력. **연차 기준 정렬(결정·미구현)**: 집행·잔액·이월금을 **과제(키워드)×연차** 단위로(화면=그 연차 실제 기간만, 라벨 "키워드·N차년도(기간)"). 적용=인건비+자금관리(전사 대시보드 제외), 데이터 변경 0(셀=월·예산/이월금=연차 이미 그대로), 순서=인건비→자금관리. 인건비 손볼 표면=연차 드롭다운·"연차 끝까지 채우기"·분기/엑셀/접힌월/스냅샷·연도→연차 연결 대체. **정산 반납 용어 통일**(차감→반납: 자금 "정산 반납 N 반영"·과제상세 "정산 반납(이자 반납 등)")·**사유 툴팁**(자금 hover, 항상 노출 X)·**§4.4 3단계=최신 연봉** 결정(미구현). |
| **v8.5** | **2026-06-05** | **§4.4 2b(인건비 "📅 연봉 변경 반영" 선택 버튼) + 공용 모듈 승격(SalaryUtil·YearFilterUtil) + 과제 관리 표 재구성(당해 환급예정 신규) + 과제 상세 정산 차감(이자 반납) + 자금 미수 차감 연결 + 대시보드 월 클릭 이동 + 비중 VAT 정합 + 헤더 툴팁.** **§4.4 2b**: 예상 탭 모달 — 연중 변경(`salaryChanges`) 인력 골라 변경월~ 예상 재계산(`confirmed`·rate0 제외, 오버라이드 별도 표기), 수동 셀 정책(건너뛰기/하나씩/덮어쓰기), 묶음 undo 1개. **공용 승격**: `firestore-service.js`에 `window.SalaryUtil`·`window.YearFilterUtil`(연도 드롭다운 하드코딩 수정·기본 올해 자동, 5개 페이지 연동; projects류 `supportYear` 폴백만 2026 고정). **과제 관리 표**: No·유형·진행·키워드·책임자·연구기간·기관명·총지원금·당해입금·**당해환급예정**(연도 선택 시만, `_planned` 그 해 cash 합·비동기 캐시)+헤더 ⓘ 툴팁, 제출일 선택열. **연구기간=전체 기간 일관화**. **과제 상세**: 정산 차감(`yb.deductions=[{amount,reason}]`, 미수=예정−실수령−차감)·비중 VAT 정합(30%)·총지원금 콤마. **자금**: 합계 미수=max(0,예정−입금−차감)·차감 귀속=마지막 예정 입금 연도·표기 "반납 N 반영"(`#f97316`). **대시보드** 월 클릭→그 달 제출 과제 리스트. 데이터 추가=`deductions`만, 마이그레이션 0. 정적검증·노드 시뮬 통과. 정산 차감 입력·자금 연결은 실화면 확인 진행 중. |
| **v8.4** | **2026-06-04** | **C6 2·3단계 + §4.4 1·2a단계 + 대시보드 툴바 정렬.** **C6 2단계**(예산 슬롯 불러오기): `project-budget` "💰 연봉 불러오기" 바 — 슬롯 셀렉트(실제+합집합)+전체 적용+행별 드롭다운 → `salarySlots[슬롯]??실제` 월급(`ceil/12`)을 급여총액·실지급액에. **폴백=하이브리드(사용자 확정)**: 빈 슬롯 0 차단+주황 경고+"빈 항목 실제로 채우기(N)". `row.salarySlot` 추가, 키 변경 0. `project-budget.html/js`. **C6 3단계**(엑셀 슬롯): `persons-master` 일괄 다운/업로드 `연봉:슬롯명` 컬럼(`연봉(원)` 뒤·합집합·콤마 서식), 업로드 `연봉:` 접두→`salarySlots` 비파괴 병합(빈칸=무변경·양수만·콤마/원 파싱·`연봉:새이름`=새 슬롯). 데이터 변경 0. `persons-master.js`. **§4.4 1단계**(인사 마스터): 편집 모달 "연봉 변경 시점" 입력(`salaryChanges=[{from:'YYYY-MM',annualSalary}]`, 계단식·콤마·월급 미리보기), 해석 `getAnnualSalaryAt/getMonthlySalaryAt/sanitizeSalaryChanges/normalizeYm`. **변경시점 없으면 기존 100% 동일**. 데이터 추가 `salaryChanges` 하나, 마이그레이션 0. `persons-master.html/js`. **§4.4 2a**(인건비): `getEffectiveMonthlySalary(person, ym)` 월 인식화(① 과제 오버라이드 평탄 ② 마스터 그 달 연봉 ③ `person.monthlySalary`), 사용자 동작 계산 지점(참여율·나눔2·⚡일괄·가로채우기·undo) ym 전달, 📅 인디케이터(툴팁=변경목록), 표시값 base 평탄. `project-labor.html/js`. **대시보드 툴바**: 회사 필터를 `.dash-toolbar` 통합(연도·회사 왼쪽/PDF 오른쪽), HTML만. **(+UI)** 슬롯 행 한 줄 레이아웃(CSS). 전 항목 정적검증·노드 시뮬(19/18/14/10) 통과. **남음**: §4.4 2b(선택 반영 버튼)·3단계(예산 동기). 해석 함수 공용 승격 권장. | **C3**(셀 메모 실제·비교 탭): 예상·실제 탭 편집(실제 메모 전용 우클릭) + 비교 탭 예상행→`_planned`·실제행→`_actual` 마커·우클릭(차이행 비허용, 권장안 ⓐ). `openMemoPopover` mode 분기. 데이터 변경 0(`cell.memo` 재사용). `project-labor.js`. **C1-b**(예산 선택 전송): `project-budget` "📤 선택 전송" 모달 — 인력 체크·필드(이름만/+참여율/+금액)·금액 출처 ⓐ예산(`실지급액×%`)/ⓑ인건비 급여(`override ?? master ×%`)·월 범위·빈셀만(비파괴)/덮어쓰기(메모 보존). C1-a(이름만) 흡수. `_planned.cells`+`_meta.personIds` merge, 키 변경 0. ⚠️ ⓑ 오버라이드 단일값 가정→§4.4 객체화 시 동기 수정. `project-budget.html/js`. **C6 1단계**(연봉 프로파일·공유 슬롯): `persons.salarySlots={슬롯명:연봉}`, 실제=`annualSalary`, 빈 슬롯=실제 폴백, 가용 슬롯=기본`['제안서용(공개 가능)']`∪전인력 키 합집합(설정문서 없음). 인력 편집 모달 인라인 입력(A). 라벨 "실제 연봉/예산 전용 연봉", 실제·예산전용 콤마, 슬롯 행 스택. 데이터 추가 `salarySlots` 하나, 키 변경 0, 마이그레이션 0. `persons-master.html/js`. 2단계(예산 불러오기·폴백 안전장치)·3단계(엑셀 컬럼) 예정. **대시보드**: 지원금 현황 카드 윗행 2칸화(`big-stats--duo`, 노트북 라벨 줄바꿈 해소)·죽은 "지원금 구성(당해)" 제거·"입금 진행(당해)" 신설(완료/남은 막대 + 완료율 배지, `seg-paid`/`seg-remain`, `yearActual`/`yearSupport`, 초과입금 100% 캡)·순서 입금 진행→자금 구성. kpis 키 변경 0. `dashboard.html/js`. 전 항목 정적검증·노드 시뮬 통과. |
| **v8.2** | **2026-06-03** | **C2 3책5공 관리 구현 완료 + 테스트 피드백 반영.** **C2**: 공유 헬퍼 `three-five-rule.js`(신규, 순수 모듈 — 책≤3·책+공≤5, `isManagedActive`/`bucketForPerson`/`countForPerson`/`isOverLimit`/`format`). 과제 수정 opt-in `is3ch5gManaged` + 책임자 personId **검색 드롭다운**(재직/퇴직 필터·이름 검색). 참여율 3책5공 컬럼(현재 연도만 `책/공`·한도 초과 빨강). 프로젝트별 인건비 모달 배지(`N개 과제`→`책/공`)+"3책5공 대상" 칩·메인 표 책임자 행 강조. 인건비 예산 책임자 행 색. soft 경고(빨강+툴팁). **피드백 반영**: ① 공고일 8자리 자동 변환(텍스트+`formatDateInput`) ② 책임자 검색 드롭다운(재직/퇴직) ③ 책 2건 이상 책임자 지정 경고(참여형태·상태 무관, 단독/주관 ⚠️3책·컨소참여 ℹ️) ④ 인건비 대시보드 "📚 3책5공 점검" 섹션(책≥2·공≥4·초과만). 데이터 추가 = `projects.is3ch5gManaged`+`managerPersonId` 둘, 셀/키 변경 0 → funding·결산·labor-dashboard·participation 무영향. 전 항목 정적검증·노드 시뮬 통과. 파일: `three-five-rule.js`(신규)·`project-detail.html/js`·`participation-summary.html`·`project-labor.html/js`·`project-budget.html/js`·`labor-dashboard.html/js`. **후속·보류**: budget 책/공 +1 미리보기(roster 로드 필요)·비인건비 과제 공 카운트·청년 군대 예외. |
| **v8.1** | **2026-06-03** | **v8 §3.5/§3.6 구현 완료 + C4 신규 판정 구현 + 채용 의무 디커플링 결정 반영.** **§3.5**: 인건비 대시보드 카드 5개·채용 의무 표·할 일/점검(`sysReg`→`confirmed`)·추이/진척/명단/활용도 제거, 청년 자동 일원화(`getYouthInfo`), 과제 수정 채용 의무 입력(인건비 관리와 분리된 독립 블록). **§3.6**: 인력 대시보드 회사 칩 필터·상단 3박스 한 행·청년/품질 제거·월별 입퇴사 막대(순증감 제외, hover 툴팁)·시점별 드롭다운 즉시 갱신(조회 버튼 제거). **C4**(§4.3 승격): 과제 수정 기준일(과제시작일/공고일)+공고일+N개월 규칙(`newJudgeRule`,`announceDate`), 인력 추가 모달 신규/기존/미상 라벨·필터·일괄 재판정, 추가 시 `newOrExisting` 기본값=판정. **채용 의무 디커플링**: 비인건비 과제 모니터링 안 함(인건비 관리 과제만), **유지 기한 기준일 = 과제 시작일로 정정(D9 변경)**, 카운트 무변경. 데이터 추가 = `requiredNew/requiredYouth/requiredRetention`(§3.5) + `announceDate/newJudgeRule`(C4), 셀/키 변경 0. 전 항목 정적검증 통과·실화면 확인 진행 중. 파일: `labor-dashboard.html/js`·`persons-dashboard.html/js`·`persons-master.js`·`project-detail.html/js`·`project-labor.html/js`. 누적 테스트 가이드 = `개발정리_테스트가이드.md`. | **§3.5 인건비 대시보드 전면 재설계 + 채용 의무 유지 관리(신규).** 페이지 역할 분리 — 대시보드=사람·규정 / funding=돈. **돈 지표(예상 연 환급·미확정·월별 추이·프로젝트별 진척) 전부 funding 이관, 대시보드 제거**(D1~D3). 상단 카드 5개(재직/등록/가용[미참여·여유 분리]/100%/채용 의무). 본문=채용 의무 표 + 할 일/점검만(인력 명단·활용도 제거, D4·D5). 액션 아이템→할 일/점검 재초점(D6). **확정 기준 `confirmed` 통일**(구 `sysReg` 정정, D7). **채용 의무**: 신규 필드 `projects.requiredRetention`(기본 12개월), 기준일=**과제 등록 시작월(그 달 1일)**·머릿수 기준·교체 허용·12개월 충족 시 경고 해제(D8~D11), 정의=`project-detail`·모니터링=대시보드 표(C안, D13), 연간 요약 필수/채용/남음(D14). **청년 판정 만 34세 자동 일원화**(`getYouthInfo`, 수동 `isYouth` 폐기, D12). 셀/키 변경 0 → funding·결산·participation 무영향. §4.7·§3.2(labor-dashboard 부분)·§1 매핑이 §3.5로 흡수/연결. 구현 순서 = §3.5-(f) / §6 4묶음. **(+§3.6 인력 대시보드 재설계)** persons-dashboard를 인사 현황 홈으로: 회사 칩 필터(전체/3사) 추가, 상단 요약을 입퇴사·시점별과 같은 박스로 통일해 한 행 배치, 재직 중심, 입퇴사 월별 막대(순증감, 이직률 X), 시점별 재직 유지, 청년 카드·상세·데이터품질 블록 제거, 연구소·자격증 롤업 안 함. `persons`만, 모델 변경 0. 구현 = §6 5묶음. **(+과제별 상세(`projects-summary`)는 §3.2 레이아웃 트랙 중 맨 마지막 순서로 기록 — §6 6묶음, 사양 미정·변경 가능.)** | **§4.2 예상 계획 스냅샷(Phase H) 구현 완료** (트랙 B 두 번째). 새 컬렉션 `projectLaborSnapshots`(1문서=1스냅샷, auto-id, `where(projectId)` 단일 인덱스+JS정렬). 예상 탭 툴바 **📸 스냅샷** 버튼→모달(현재 요약+이름 저장 / 이력). 담는 범위 **planned+personIds+personRoles**(actual·meta 제외). 행 액션 **내역/차이/복원/삭제**. **내역=표**(인력×입력월, 참여율%+금액, 합계, 이름 sticky)+**시트 복사(TSV)**+**엑셀**(`{키워드}_스냅샷_{이름}_{날짜}.xlsx`, `buildSnapshotAoa` 공유, SheetJS). **차이=스냅샷↔현재**(추가/변경/삭제 인력별). **복원=전체 덮어쓰기+복원 직전 자동백업**(kind `auto`). **나눔 판정 `personRoles.split`**(셀 `cell.rates` 잔재 오판 수정). 작성자 `window.currentUser`. 모달 중 Ctrl+Z 가드. 별도 컬렉션·셀 키 변경 0 → funding·결산·labor-dashboard 무영향. 단독 페이지는 보류(향후 필요 시). 정적검증 통과. 파일 `project-labor.js`·`project-labor.html`. (+ 인벤토리 4.2 행 ✅ 갱신.) **(+ UX) 지급일 prompt에 `normalizeDateInput` — 숫자 8자리(20260601) 입력 시 자동으로 `2026-06-01` 변환(미지급 입력·수정 두 경로, 구분자 섞여도 숫자만 추려 8자리면 변환).** |
| **v7.4.3** | **2026-06-01** | **§4.1 셀 메모 UI 구현 완료** (트랙 B 첫 항목). 기존 빨간 글씨 인라인 표시(입력 UI 없음) → **옅은 분홍 배경 + 모서리 마커 + hover 툴팁 + 편집 팝오버**. 진입 ① 우클릭 메뉴 "💬 메모" ② 마커 클릭. 팝오버(인력·연월 헤더 + 텍스트영역 + 저장/취소/삭제, Ctrl+Enter·Esc·바깥클릭). 편집=예상 탭, 실제=읽기전용, 비교=마커 없음. 나눔은 첫 줄에만 마커(셀 1개). 데이터 `cell.memo` 그대로(키 변경 0, funding·결산·labor-dashboard 무영향). textarea `stopPropagation`으로 전역 셀 단축키(Ctrl+Z 등) 가로채기 차단. `buildMemoMarker`/`openMemoPopover`/`closeMemoPopover`/`onMemoOutside`/`onMemoMarkerClick` 신규, 컨텍스트 메뉴 memo 항목+디스패치, `pl-memo-text` 제거. 파일: `project-labor.js`·`project-labor.html`. 정적검증 통과(node -c, div/table 균형). **(+ §3.3 기능 인벤토리)** project-labor/budget 기능 인벤토리 작성(`프로젝트별인건비_기능인벤토리_v1.md`) + §4 로드맵 대조. |
| **v7.4.2** | **2026-06-01** | **§3.4 금액 3분류 전체 종료** — 마지막 단계 labor-dashboard 점검 완료. **기능 변경 불필요 결론.** (1) **selfCash 집계**: 대시보드 금액 집계 8곳 전부 `cell.cash`(=지원금)만 사용, `cell.selfCash`/`cell.inkind` 읽기 0건(`node -c`+grep 확인). 환급(=지원금) 관점이라 자부담현금·현물 미합산이 **정확**(섞이면 환급 과대계상). (2) **나눔 투명성**: 나눔 셀도 분배(`cell.rates`)가 아닌 집계값(`cell.cash` 금액 / `cell.rate` 3분류 합)만 읽어 멀티행과 무관하게 정확. (3) **personId 파싱 안전**: prefix 시작 고정(`key.indexOf(prefix)===0`)+`substring`이라 personId 언더스코어에도 안전, 셀 키 미변경. (4) v7.4의 "분류 선택 시 나머지 금액 0 클리어" 덕에 `cell.cash`가 항상 순수 지원금임 보장. (5) 오수정 방지 의도 주석만 추가(`labor-dashboard.js` 상단 금액모델 + 환급 추이/진척 2곳). 정적검증 통과. **§3.4 = project-labor·participation·나눔·labor-dashboard 모두 완료. budget/funding 무변경.** **(+ UI 정정)** project-labor 나눔 토글 체크박스를 **예상 탭에서만** 노출(기존 예상+실제 → 예상만). split은 `personRoles` 사람 단위라 탭 공유 → 실제 체크박스는 잉여·중복 조작이라 제거(`buildDataRow`/`buildSplitPersonRows`의 토글 렌더를 `mode === 'planned'` 가드로). 실제·비교는 결과(멀티행)만 표시. JS 렌더 가드만 변경(CSS 무변경), node -c 통과. |
| **v7.4.1** | **2026-06-01** | **§3.4 마무리 진행.** **(1) participation-summary**: `getPersonMonthly` 3버킷(지원금/현금/현물) + 나눔 `cell.rates` 분배(`role.split`일 때만), 구분 필터 "현금"→"지원금" 정정 + 자부담현금 버튼은 해당 연도 데이터 있을 때만, 셀 패널 3분기. **(2) project-labor 레이아웃 재구성**: 고정 컬럼 **이름 \| 월급 \| 구분** 신설("인력"→"이름", 분류 드롭다운을 구분 컬럼으로 이동), 콜그룹·헤더·tfoot·totalCols 정합. **(3) 나눔(분류 split) 멀티행**: 월급 우측 작은 체크박스(`split`) → 켜면 `splitCis`(분류 키 배열) 기준 분류별 여러 줄, 각 줄 **구분 드롭다운**(중복 제외)+참여율 입력(`cell.rates[field]`)+그 분류 금액(읽기전용, 급여×%), 이름·월급 rowspan. **데이터는 셀 1개**(`cell.rates`+금액+합) — 키 변경 0, funding·결산 무영향. `migrateCellRatesForSplit`(켤 때 현재 분류로 초기화 → desync 교정). `buildSplitPersonRows`/`buildClassifyCellHtml`/`buildNeSelectHtml`/`buildSplitToggleHtml`/`defaultSplitCis` 신규. **(4) 구분 색 3분**: 지원금 초록·현금(자부담) 주황(`is-selfcash`)·현물 앰버. **(5) 뷰 폭 일관**: 테이블 `max-content`로 분기/12개월 뷰 고정컬럼 폭 동일. **(6) 퇴사자 취소선** CSS 복구. 정적검증 통과. 남은 단계: labor-dashboard 점검 + 실화면 확인. |
| **v7.4** | **2026-06-01** | **§3.4 금액 3분류(지원금/현금/현물) — project-labor 단계 구현 완료.** 핵심 발견: budget이 이미 3분류(`'현금'|'자부담현금'|'현물'` + `FUND_TYPE_LABELS`)를 마이그레이션 없이 구현해뒀고 selfCash를 _planned 셀에 쓰고 있었음 → labor를 budget과 동기화. `project-labor.js`/`project-labor.html`: 헬퍼(`moneyFieldOf`/`otherMoneyFields`/`fundTypeLabel`/`projHasSelfCash` 등), 역할 드롭다운 3분류(자부담현금은 과제 `hasSelfCash`거나 기존값일 때만), 예상·실제·**비교** 3탭 조건부 4열(참여율/지원금/현금/현물)+우측 합계 3열(`pl-sticky-right-2`=right:200px, `col-selfcash`/`col-total-selfcash`), 로직 5곳 3분기(자동계산/월급재계산/가로채우기/일괄계산/undo — 지원금→cash·현금→selfCash·현물→inkind, 나머지 0 클리어). 자부담현금 셀 색/배지 구분 없음. 마이그레이션 0(실데이터 0건). funding 무변경(cash만 읽어 안전). 정적검증 통과. **모델링 질문 4개 모두 해소.** 남은 단계: participation-summary 라벨 3분기 + labor-dashboard 점검. |
| **v7.3** | **2026-05-30** | **트랙 A 2묶음 4 완료 → 2묶음 전부 완료.** 참여율(`participation-summary.html`) 셀 클릭 패널: 항목을 키워드/참여율/지원금 3요소로 축소(과제 풀네임 제거), 합계줄 `참여율% · 지원금 합원` 병기. **A-1**(지원금=항상 actual.cash, 토글 무관) + **B-2**(confirmed 월만 인정, 미확정 표시, funding 정합). `_meta`에서 `meta` 추가 추출(추가 읽기 없음), `getPersonMonthly` breakdown에 keyword/refundCash/refundConfirmed. 죽은 CSS `ps-detail-item-name` 제거. 정적검증 통과. **+ §3.1 결산 보고서 구현 완료**(`settlement-report.html` 신규 — 지원금만/confirmed, 인력×과제 행, 이름·생년월일·과제명·1~12월 중앙정렬, 총계·소계 제거, 엑셀+클립보드 복사, 미확정 경고, 회사·연도 필터). **+ 사이드바 FOUC 깜빡임 수정**(`sidebar.js` 생성 aside에 inline height/overflow + 동적 페이지 정적 aside placeholder). **+ §3.4 금액 3분류 재정립(지원금/현금/현물) 신규 큰 건 기록** — 현재 '현금'이 실은 정부지원금, 자부담현금(selfCash) 셀 도입·분류 재정의 향후 과제. | **트랙 A 2묶음 1·2·3 완료.** 과제 수정: `participation_only` 라디오 삭제 + 안전 가드(§5.1 해소). 과제 관리: filter-summary-row 유형별 클릭 필터(수행 ∩ 분류, `activeSummaryDivision`) + 수행 호버 + 총 신규 제안 pill 2×2 + 수행 계속/신규/종료 2줄 스택 + 그룹 폭 `1.3:3.2:2.6`. 대시보드: 입금 예정 3분류(과제/지원사업/기타, `yearSupportSupport` 분리, `hero-support-sum`)(§5.2 해소 — funding은 2분류 유지로 의도적 분기). 정정: 과제수정 라디오는 `refund_only`+`refund_participation` 2개 잔존(기능 동일, 중복 정리 검토거리). |
| **v7.1** | **2026-05-29** | **트랙 A 1묶음 완료.** lab 월별 명부 펼침 버그 수정(오늘 달만). 과제 수정 타이틀(`📝 과제 정보 수정`)·별칭 안내문구(8자리). 대시보드 지원금 현황 카드 순서 교체(전체 메인). 과제 관리: 당해 수주 카드 삭제, 월별 신규제안 그래프·Chart.js 제거, **총 신규 제안 카드는 통계 그리드로 이동해 유지(분류 필터 pill fuse돼 있어 통째 삭제 불가)**, 죽은 CSS 114줄 정리, 요약 카드 3그룹화(신규제안/진행상태/지원금). 디자인은 그룹 구분까지만(추가 다듬기 보류). |
| v7 | 2026-05-29 | **초점 전환 — 페이지 전반 정리.** 실사용 백로그 적재(대시보드/과제관리/과제수정/참여율/lab). 레이아웃 트랙 + project-labor 점검 분리. 트랙 A(페이지 정리)/트랙 B(로드맵) 2트랙. ⚠️ 확인 필요: 환급X 옵션 삭제 영향, 대시보드 3분류 vs funding 2분류 정합성. |
| v6.2 | 2026-05-28 | P0-2 완료 — `funding.html` 전면 재구성(메인 탭 2개, 요약 카드 6개 × 2, 과제별 추적 표 3행 페어). 사이클 중 폐기: 수주 카드, 시기별 입금 현황, 알림 카드. |
| v6.1 | 2026-05-28 | P0-1 완료 — `participation-summary.html` 신규(회사 칩·연도·planned/actual 토글·구분 필터·검색·미배정 토글·연간 평균·셀 클릭 분해). 부수: 인력 추가 모달 통계 배지. |
| v6.0 | 2026-05-27 | 우선순위 재배치 — 두 신규 페이지(참여율 합계 / 환급액 비교)를 P0로 승격. project-labor 후속은 v6.x로 이연. |
| v5.3 | 2026-05-27 | 월급 오버라이드, 행 드래그 순서, sticky 컬럼, 엑셀 틀 고정, 셀 잠금, 글자색 등 다수 패치 |
| v5.2 | 2026-05-26 | 지급 완료 메타, 스냅샷 데이터 모델, 모달 점프 방지 등 |
| v5.1 | 이전 | 인력 분류 인라인 드롭다운, 셀 input 포맷팅, 확정 단일 액션 |
| v5.0 | 이전 | project-labor 페이지 본격 사양 (12개월 펼치기, 인력 분류, 우측 sticky 박스, 메모) |
