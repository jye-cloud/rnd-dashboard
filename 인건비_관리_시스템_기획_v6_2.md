# 인건비 관리 시스템 기획서 v6.2

> 작성일: 2026-05-28
> 직전 버전: v6.1 (2026-05-28)
> v6.2 핵심: **P0-2 (자금관리 페이지 — 지원금 입금 일정 + 인건비 환급 추적) 완료**

---

## 0. v6.0의 위치 — 무엇이 바뀌었나

### 0.1 배경

v5.3까지 `project-labor`(월별 인건비) 페이지의 입력·표시 기능을 집중 작업해왔음. 이제 실데이터를 어느 정도 넣어보니 **입력 페이지 단독으로는 한계**가 드러남:

- 인력 한 사람이 여러 과제에 참여할 때 **전체 참여율 합**을 한눈에 볼 페이지 없음 → 100% 초과 위험 사전 감지 불가
- 매월 환급 예정액과 실제 환급액을 **과제 단위로 비교**할 페이지 없음 → 자금 흐름 예측·정산 어려움

이 두 페이지가 만들어져야 사용자가 실제 운영 사이클(연초 계획 → 월별 입력 → 정산)을 시스템 내에서 닫을 수 있음.

### 0.2 v6.0에서 우선순위 재배치 / v6.2 진행 상황

기획서 §4 Phase A 작업 단계(Step 5: 셀 메모 UI, Step 6: 엑셀 업로드)는 **잠시 보류**하고, 다음 두 페이지를 먼저 만든다:

| 우선순위 | 작업 | 상태 |
|---|---|---|
| **P0** | **인력별 참여율 합계 페이지 신규** (`participation-summary.html`) | ✅ **v6.1 완료** |
| **P0** | **자금관리 페이지 재구성** (`funding.html` — 지원금 입금 일정 + 인건비 환급 추적) | ✅ **v6.2 완료** |
| (부수) | **v6.3 — 인력 추가 모달 정보 확장** (project-labor) | ✅ **v6.1 완료** (P0-1 합산 로직 재사용) |

v6.x 이후 후속 (project-labor 마저 완성):
- v6.3+: Step 5 셀 메모 UI
- v6.4+: Phase H 예상 스냅샷
- v6.5+: §6.11 신규 인력 자동 판정 / §6.12 월급 연중 변경
- v6.6+: Step 6 엑셀 입출력
- v7: Phase C 인건비 대시보드

### 0.3 v5.3에서 결정·구현된 사항은 모두 유효

`personRoles` 데이터 모델, 12개월 펼치기, 인력 분류 드롭다운, 확정/지급 메타, 가로 채우기, 월급 오버라이드 등 모든 v5 계열 결정은 그대로 유지. v6은 **추가**만 한다.

---

## 1. P0 작업 1 — 인력별 참여율 합계 페이지

### 1.1 페이지 개요

**페이지명**: 인력 대시보드 내 신규 섹션 또는 별도 페이지(작업 시 결정)
**임시 ID**: `participation-summary` (또는 인력 대시보드 `persons-dashboard`의 새 섹션)

**기존 페이지 참조**: 현재 사용 중인 `index.html#/participation`의 **인력별 연간 요약** 뷰가 가장 가까운 구조. 이 페이지를 참고해 신규 페이지를 만들고, 기존 페이지는 추후 폐기.

### 1.2 핵심 기능

1. **개별 과제 참여율을 인력별로 합산**
   - 한 인력이 N개 과제에 참여할 때 N개 합산값을 월별로 표시
   - 데이터 소스: `projectLabor/{}_planned.cells` (예상값 기준) — 모든 프로젝트를 순회하며 인력 ID로 묶음
   - 색깔 코딩: `<80%` 초록 / `80~89%` 노랑 / `≥90%` 주황 / `=100%` 진주황 / `>100%` 빨강

2. **3책 5공 같은 "참여율 관리" 옵션 반영**
   - 과제 등록 시 `projects.{participationRule}` 같은 필드로 관리 (필드명 작업 시 확정)
   - 예: 어떤 과제는 합산 대상에서 제외, 어떤 과제는 별도 합산 그룹으로
   - 기존 `참여율 관리` 페이지의 3책 5공 컬럼이 이 규칙을 시각화함 — 그대로 차용

3. **인력별 연간 요약 페이지 구조 차용**
   - 현재 `#participation-view-annual` 의 핵심 컬럼: 이름 / 3책5공 합산 / 구분(현금·현물) / 1~12월 월별 / 연간 합
   - 색 규칙·필터·검색 UI도 그대로 차용
   - 단, 데이터 소스를 신·구 양쪽 다 지원할지(또는 `project-labor`만 단독 사용할지)는 작업 시 결정

### 1.3 데이터 모델

**기존 활용**:
- `projectLabor/{projectId}_planned.cells[{ym}_{personId}] = { rate, cash, inkind, memo, colors }`
- `projectLabor/{projectId}_meta.personRoles[personId] = { newOrExisting, cashOrInkind, subRole, monthlySalaryOverride, lockedYms }`
- `projects.{participationRule}` (신규 필드 후보 — 아래 §1.5)

**계산 로직**:
```
한 인력 P의 월 M 총 참여율 =
  Σ ( projects.filter(과제 P 참여).map(과제 → projectLabor[과제]_planned.cells[{M}_{P.id}].rate) )
```

### 1.4 UI 구조 안

```
┌──────────────────────────────────────────────────────────────┐
│ [회사 칩] [연도 셀렉터] [구분 필터: 전체/현금/현물]            │
│ [이름 검색] [3책5공 규칙 보기]                                │
├──────────────────────────────────────────────────────────────┤
│ 이름   │ 3책5공 │ 구분 │ 1월 │ 2월 │ ... │ 12월 │ 연간합     │
├──────────────────────────────────────────────────────────────┤
│ 김종규 │ 80%   │ 현금 │ 30% │ 30% │ ... │ 30%  │ 360%        │
│        │       │ 현물 │ 10% │ 10% │ ... │ 10%  │ 120%        │
│ 이도형 │ 100%  │ 현금 │100% │100% │ ... │100%  │ 1200% ⚠️    │
└──────────────────────────────────────────────────────────────┘
```

**중요 정책 (작업 시 확정 항목)**:
- 셀 클릭 시: 그 인력 × 그 월에 기여한 **과제별 분해**를 모달 또는 우측 패널에 표시 (예: "30%는 과제 A에서 20% + 과제 B에서 10%")
- 합산에서 제외할 과제 규칙(`participationRule`)이 어디까지인지 (예: 자부담만 제외? 특정 사업 분류 제외?)
- 색깔 코딩 임계값 — 위의 5단계 기준을 v6.0 시작값으로 쓰고 사용자 피드백으로 조정

### 1.5 미결정 항목 → v6.1 결정 사항

- ~~**데이터 위치**: `projects.{participationRule}`인지 별도 컬렉션인지~~ → 결정 보류 (3책 5공 데이터는 placeholder만 두고 미연결)
- ~~**3책 5공 규칙 데이터**: 기존 `#participation-view-annual`이 어떻게 저장하는지~~ → **컬럼 자리만 두고 UI 미리 (v6.1)**. 실 데이터 연동은 후속 작업
- **회사 필터 적용 범위** → **인력 기준 (v6.1 확정)**. 인력 마스터의 `p.company` 로 필터
- **기준 데이터** → **planned / actual 토글 (v6.1 확정)**. 상단 컨트롤 바 첫 번째 토글로 구현
- **신규 페이지 vs 인력 대시보드 섹션** → **별도 신규 페이지 `participation-summary.html` (v6.1 확정)**
- **사이드바 메뉴 위치** → **그룹 3 맨 끝, "인건비 대시보드" 다음 (v6.1 확정)**. 아이콘 🧮, 라벨 "참여율 합계"

**v6.1에서 추가로 결정된 항목 (코드 사이클 중)**:
- **데이터 소스** = `project-labor` 단독 (`projectLabor` Firestore 컬렉션). 기존 `participation` 페이지는 무시 (추후 폐기)
- **인력 셀 키 패턴 확인** = `{projectId}_{ym}_{personId}` (project-labor.js의 `getLaborKey` 함수와 동일)
- **셀 값 모델 재확인** = `{ rate, cash, inkind, memo, colors? }` — `rate`만 합산 대상(%), `cash`/`inkind`는 금액(원)이라 합산 금지
- **현금/현물 분기 방법** = `{projectId}_meta.personRoles[personId].cashOrInkind` 로 분기. 같은 사람이 과제마다 다를 수 있음
- **활성 인력 필터** = `p.status === 'active'` (또는 status 없는 경우 통과)
- **활성 과제 필터** = `proj.laborManaged === true && proj.status.includes('수행') && yearBudgets[].start/end 또는 researchStart/researchEnd 가 state.year 범위`
- **연간 합 → 연간 평균 (B방식)** = 합 / 참여한 월 수. 100% 초과 시 빨강
- **색깔 코딩 5단계** = `<80` 초록 / `80~89` 노랑 / `≥90` 주황 / `=100` 분홍(톤다운 빨강) / `>100` 빨강
- **현금/현물 행 표시** = 구분 필터가 '전체'면 무조건 두 행 다 표시 (한쪽이 0이어도 `—`로 표시)
- **미배정 인력 토글** = 카운터 옆 `👻 미배정만 보기` 버튼. 참여율 0인 사람만 표시
- **셀 클릭 상세 패널** = 우측 슬라이드 (`<aside class="ps-detail-panel">`). `body` 직속 위치 (`fixed` 정상 동작 보장)

### 1.6 작업 단계 — v6.1 완료

1. ✅ **Step 1**: 데이터 소스 결정 — `project-labor` 단독 사용 (사용자 확인 완료)
2. ✅ **Step 2**: 계산 로직 함수 — `getPersonMonthly(personId, ym, mode)` 구현
3. ✅ **Step 3**: 페이지 HTML/CSS 뼈대 — 기존 디자인 시스템(style.css/sidebar.css) 차용
4. ✅ **Step 4**: 표 렌더링 + 색깔 코딩 (5단계, 100% 분홍 톤다운)
5. ✅ **Step 5**: 셀 클릭 → 우측 슬라이드 패널에서 과제별 분해 (cashOrInkind로 필터)
6. ⏳ **Step 6**: 3책 5공 규칙 시각화 — 컬럼 자리만, 데이터 미연결 (후속)
7. ✅ **Step 7**: 회사 칩·구분 필터·이름 검색·연도 셀렉터·planned/actual 토글
8. ✅ **Step 8**: 사용자 검수 (5/27~5/28 사이클)

**추가 작업 (사용자 피드백 반영)**:
- ✅ 대상 인력 수 카운터 (`대상 X/Y명 (Z%)`) — 구분 옆 인라인
- ✅ 미배정 인력 토글 버튼 + 배지 카운트
- ✅ 툴바 한 행 강제 (flex-wrap: nowrap)
- ✅ 연간 합 → 연간 평균 (B방식)
- ✅ 현금/현물 항상 두 행 표시
- ✅ 모드 라벨 단축 (예상/실제)
- ✅ 우측 패널을 dashboard 밖으로 이동 (본문 흐름에 그려지던 버그 수정)

**산출물**:
- `participation-summary.html` (신규)
- `sidebar.js` (메뉴 1개 추가)

**작업량 추산**: ~~2-3세션~~ → **1세션 + 후속 디테일 사이클**로 종료

---


## 2. P0 작업 2 — 자금관리 페이지 재구성 (`funding.html`)

### 2.1 페이지 개요

**대상 페이지**: 기존 `funding.html` (자금 관리)
**최종 형태**: 메인 탭 2개로 재구성 — 새 페이지를 만들지 않고 기존 페이지를 갈아엎음

```
[자금 관리]
 ├─ 💸 지원금 입금 일정    ← 기존 자금관리 + 신규 요약 카드 6개
 └─ 💰 인건비 환급         ← 신규 — 월별 환급액 비교 표 + 요약 카드 6개
```

**공통 컨트롤 (두 탭 공유)**:
- 연도 셀렉터 (`_year`)
- 회사 칩 필터 (`_companyFilter`: 전체/식스티/굿뉴스/패리티)
- 분기별/월별 토글 (`currentView`: month 기본)

### 2.2 탭 1: 지원금 입금 일정

#### 2.2.1 요약 카드 (6개, 인건비 환급 탭과 동일 디자인 패턴)

| 위치 | 카드 | 색상 | 데이터 |
|---|---|---|---|
| 1 | `{Y}년 {M}월 입금 예정액` | 회색 | 그 달의 `plannedPayments` 합 |
| 2 | `{Y}년 {M}월 입금액` | 초록 | 그 달의 `actualPayments` 합 |
| 3 | `{Y}년 {M}월 미수` | 빨강 | max(0, 예정 - 입금) |
| 4 | `{Y}년 누적 입금 예정액` | 회색 | 그 해 `plannedPayments` 합 |
| 5 | `{Y}년 누적 입금액` | 초록 | 그 해 `actualPayments` 합 |
| 6 | `{Y}년 누적 미수` | 빨강 | max(0, 예정 - 입금) |

**카드 안 분리 행**: 6개 카드 모두 하단에 `과제` / `기타` 행으로 분리
- `과제` = `it.division1 === '과제'`
- `기타` = 그 외 (지원사업, 기타, 빈 값)
- 용역은 호출자에서 이미 제외

**"이번 달" 결정 로직**:
- `state.year === todayYear` → 오늘이 속한 월
- 과거/미래 연도 → 12월 (보수적, 0으로 비어 보이지 않게)

**미수 정책**:
- 음수(예정 초과 입금)는 0으로 표시 — 표의 미수 컬럼과 일관
- 의사 결정 사이클: 입금률(%) 제안 → 미수금(원)으로 채택 (도메인 적합성 우선)

#### 2.2.2 과제별 지원금 입금 추적 표

데이터 모델: `yearBudgets[].plannedPayments` / `actualPayments` (yb 안의 명시적 입금 일정)

**3행 페어 구조** (한 과제 = 3행):
- 예정 행: 그 달의 plannedPayments 합
- 입금 행: 그 달의 actualPayments 합
- 미수 행: `max(0, 누적 예정 - 누적 입금)` — 그 달까지의 누적 차이, 양수만 표시

**정렬**: 분류(과제 먼저) → 시작일 → 차수
**과제명 옆 배지**: `과제` (파랑) / `기타` (회색) — `.f-division-badge`
**합계 셀**: 누적 미수 ≤ 0 && 입금 있음 → `✓ 완료` 배지
**컬럼 너비**: f-th-kind 76px, f-cell 세로 padding 0.4rem (분기/월 통일)

#### 2.2.3 데이터 정합성

`getProjectYearSupport(it, year)` 헬퍼는 정의만 두고 호출은 안 함. dashboard와 funding의 "당해 입금 예정" 차이 원인을 추적할 때 디버깅용으로 남겨둠.

- **dashboard.js**: `yb.support`를 `calendarBreakdown`/일별비례로 분배 + `supportYear` 폴백
- **funding.js 카드**: `plannedPayments` (사용자가 직접 입력한 입금 일정)
- 두 값이 다를 수 있음 — 의도된 차이. 표가 plannedPayments 기반이므로 카드도 동일.

### 2.3 탭 2: 인건비 환급

#### 2.3.1 요약 카드 (6개)

| 위치 | 카드 | 색상 | 데이터 |
|---|---|---|---|
| 1 | `{Y}년 {M}월 환급 예정액` | 회색 | 그 달의 planned cash 합 (paid 조건 없음) |
| 2 | `{Y}년 {M}월 환급액` | 파랑 | 그 달의 actual cash 합 (`meta.confirmed=true`만) |
| 3 | `{Y}년 {M}월 환급률` | 보라 | 환급 / 환급 예정 × 100 |
| 4 | `{Y}년 누적 환급 예정액` | 회색 | 1~12월 planned cash 합 |
| 5 | `{Y}년 누적 환급액` | 파랑 | confirmed 월의 actual cash 합 |
| 6 | `{Y}년 누적 환급률` | 보라 | 누적 환급 / 누적 예정 × 100 |

**과제/기타 분리 없음**: 사용자 결정 (인건비 데이터에서는 분리가 유의미한지 명확치 않음, 분리는 표의 배지로 충분)

**의사 결정 사이클 — "환급 예정액"의 정의**:
- 처음 안: paid=true인 월의 planned 합 → paid 메타가 비어있는 데이터에서 0이 됨
- 최종: project-labor의 예상(planned) 탭 cash 합 (paid 조건 무관) → 표의 "예상" 행과 동일

**도메인 적합성**: 입금 탭은 미수(원 단위 액수), 인건비 탭은 환급률(%) — 환급은 정부 정산 후 비율이 핵심 지표라 입금률(%)이 맞음.

#### 2.3.2 과제별 인건비 환급 추적 표

데이터 모델: `projectLabor` 컬렉션
- `projectLabor/{projectId}_planned.cells[{ym}_{personId}].cash`
- `projectLabor/{projectId}_actual.cells[{ym}_{personId}].cash`
- `projectLabor/{projectId}_meta.{ym}.confirmed`

**3행 페어 구조**:
- 예상 행: 그 달의 planned cash 합 (모든 인력)
- 실제 행: 그 달의 actual cash 합 (`meta.confirmed=true`인 월만, 그 외 회색 dash)
- 미수 행: `max(0, 누적 예상 - 누적 실제)`, 첫 미확정 월 이후 회색 dash

**정렬**: 분류(과제 먼저) → 과제 시작일 → 이름
**탭 진입 시 lazy 로드**: `currentTab === 'refund'`일 때만 `loadLaborForYear()` 호출

### 2.4 데이터 헬퍼

- `_laborCache = { [projectId]: { planned, actual, meta } }` + `_laborCacheYear`
- 셀 키: `${projectId}_${ym}_${personId}` (ym = 'YYYY-MM')
- 셀 값: `{ rate, cash, inkind, memo }`
- 환급 대상: `cash` 필드만 (현물 `inkind`는 환급 X)
- 사용자가 v6.1에서 만든 `_laborCache` 패턴 그대로 차용

### 2.5 폐기·삭제된 컴포넌트 (작업 사이클 중)

작업 진행 중 사용자가 명시적으로 제거 결정:

| 제거 항목 | 이유 |
|---|---|
| "당해 수주 총 지원금" 카드 | 회사가 1년 단위로 보는 지표 아님, 노이즈 |
| "시기별 입금 현황" 섹션 (분기/월별 카드 4·12개) | 아래 표 컬럼별 합계가 같은 정보를 더 풍부하게 표시 — 중복 |
| 알림식 카드 (이번 달/다음 입금/올해 남은 입금) | 사용자/타부서가 헷갈림: "미수랑 올해 남은 입금이 왜 달라?" — 같은 정보 다른 정의 충돌 |
| "미수 발생 과제" 알림 카드 | 위 알림 카드 일부, 단순화 |
| "과제" 카드 그룹 타이틀 (h2) | 카드 자체가 의미 명확, 라벨 중복 |

### 2.6 산출물

- `funding.html` (449 → ~960줄, 카드 영역 + 인건비 환급 탭 신규)
- `funding.js` (487 → ~1164줄, `renderFundingSummary`·`renderLaborRefundSummary`·`renderLaborRefundTable` 함수 신규)

### 2.7 작업 단계 — v6.2 완료

1. ✅ **Step 1**: 기존 `funding.html` 구조 파악 + 메인 탭 2개로 재구성
2. ✅ **Step 2**: 인건비 환급 데이터 헬퍼 (`_laborCache`, `loadLaborForYear`, `getRefundTargetProjects`)
3. ✅ **Step 3**: 인건비 환급 탭 표 렌더링 (3행 페어, 누적 미수, 회색 미확정)
4. ✅ **Step 4**: 입금 일정 탭 카드 재구성 (수주 카드 제거 → 과제/기타 분리 → 6개로 확장)
5. ✅ **Step 5**: 시기별 입금 현황 → 알림식 카드 → 통째로 제거 (사이클 거쳐 단순화)
6. ✅ **Step 6**: 인건비 환급 요약 카드 6개 추가 (지급 → 환급 예정으로 의미 정정)
7. ✅ **Step 7**: 입금 일정 요약 카드도 6개로 통일 + 미수금 채택 (입금률 vs 미수 결정)
8. ✅ **Step 8**: 사용자 검수 (5/28 사이클, 다수 라벨/색상/정렬 미세 조정)

**작업량 추산**: ~~1-2세션~~ → **1세션 + 빈번한 디테일 사이클**로 종료

### 2.8 후속 검토 사항 (사용 후 결정)

- 셀 클릭 → 인력별 분해 패널 (인건비 환급 표)
- 회사 필터 칩 카운트 배지 (예: `식스티 5`)
- 인건비 환급 카드에 과제/기타 분리 추가 여부 (현재 없음, 데이터 늘면 검토)

---

## 3. v6 이후 후속 작업 (v6.3 ~ v7) — project-labor 마저 완성

v6.0의 P0 두 페이지가 v6.1·v6.2로 완료된 후 진입할 작업들. v5.3 기획서의 미진행 항목을 모두 이어받음.

### 3.1 v6.3+ — Step 5 셀 메모 UI (project-labor)

v5 기획서 §4 Phase A.7. 사양 명확. 1세션 추산.

- 셀 우상단 빨간 점 (메모 있을 때)
- 셀 배경 노랑 (메모 있을 때)
- hover 시 툴팁 표시
- 셀 클릭 시 메모 편집 팝오버

### 3.2 v6.4+ — Phase H 예상 계획 스냅샷 (project-labor)

v5 기획서 §6.9 + Phase H. 데이터 모델은 v5.2에 등록 완료. 1-2세션 추산.

- `📸 스냅샷 저장` 버튼 (예상 탭에서만)
- 스냅샷 이력 모달 (좌측 과거 / 우측 현재)
- 미결정 항목들 (§6.9): 차이 하이라이트 / 복원 버튼 / 권한 — 작업 시 확정

### 3.3 v6.3 — 인력 추가 모달 정보 확장 (project-labor) ✅ **v6.1에서 완료**

v5 기획서 §6.10. 1세션 추산. **v6.0의 P0 작업 1번이 끝나면 계산 로직 재사용 가능**(합산 참여율 계산이 같은 패턴이라). → 실제로 P0-1과 같은 세션에서 완료됨.

- ✅ 모달 인력 행에 "현재 참여율" + "참여 과제 수" 인라인 배지 표시
- ✅ 색깔 코딩 (엄격: <80% 기본 / ≥80% 노랑 / ≥90% 주황 / ≥100% 빨강 톤다운)
- ✅ 캐시 정책 — 모달 세션마다 재로드. 외부 변경 즉시 반영. 현재 편집 과제는 메모리 재사용으로 Firestore 호출 1쌍 절약
- ⏳ 캐시 TTL/장기 캐시 정책 — 사용 패턴 관찰 후 결정

**구현 요약**:
- "현재 월" = 오늘 날짜 기준 `currentYm()` (state.year와 무관, 항상 오늘)
- "현재 탭" = `state.activeTab` (planned/actual)
- 배지 형태: `[N개 과제 · X%]` 둥근 인라인 배지. 마우스 hover 시 툴팁으로 과제별 분해
- 통계 0이면 배지 숨김 (노이즈 방지)
- 모달 열림 → 첫 렌더는 통계 없이 즉시(UX) → 비동기 로드 완료 후 자동 재렌더

**산출물**:
- `project-labor.js` (4181 → 4382줄, 함수 5개·캐시 변수 2개 추가)
- `project-labor.html` (CSS 30줄 추가 — `.pl-modal-person-stats` 베이스 + 3색 변형)

### 3.4 v6.5+ — 프로젝트별 신규 인력 판단 기준 (project-labor)

v5 기획서 §6.11. 2세션 추산. 데이터 모델 확정 필요.

### 3.5 v6.6+ — 월급 오버라이드 연중 변경 (project-labor)

v5 기획서 §6.12. 1-2세션 추산. 사용자가 실제 케이스 만난 후 진입.

### 3.6 v6.7+ — Step 6 엑셀 입출력 (project-labor)

v5 기획서 §4 Phase A.13의 Step 6. 사용자 결정: "페이지 사용성 좋으니 우선순위 낮음". v6.x 마지막에.

### 3.7 v7 — Phase C 인건비 대시보드 본격 구현

v5 기획서 §4 Phase C. v6.x가 끝나야 의미 있는 통합 분석이 가능. v6.0의 P0 작업 2번이 이 대시보드의 "월별 환급 추이" 섹션의 사전 작업 격이라 자연스러운 흐름.

---

## 4. v5.3에서 그대로 유지되는 사항

다음 사항들은 v6.0에서 **변경하지 않음**:

- §1 시스템 구조 (페이지 맵, 사이드바 메뉴, 회사 구분, 권한 정책)
- §2 환급액 계산 로직
- §3 완료된 작업 목록 (project-labor v5.3까지의 모든 기능 — 12개월 펼치기, 인력 분류, 확정·지급 메타, 가로 채우기, 월급 오버라이드, 행 드래그 순서, sticky 컬럼, 엑셀 틀 고정, 셀 잠금, 글자색, 등)
- §6 미해결 이슈 (§6.1 selfCash, §6.4 salaryHistory, §6.10~6.12 등) — v6.x로 이연됨을 명시
- §8 작업 순서 가이드 (Phase A~H) — v6.0이 그 안에서의 순서 재배치임을 명시

---

## 5. v6.3 작업 시작 멘트 (다음 세션 복붙용)

```
인건비 v6.3 작업 시작.

지난 세션 종료 시점:
- v6.1 완료: P0-1 (참여율 합계 페이지) + v6.3 부수 (모달 통계 배지)
- v6.2 완료: P0-2 (자금관리 페이지 재구성 — 지원금 입금 일정 + 인건비 환급)
  · funding.html: 메인 탭 2개 + 요약 카드 6개 × 2 + 과제별 추적 표 × 2
  · 폐기: 수주 카드, 시기별 입금 현황, 알림 카드 (사용자 결정 — 헷갈림)

이번 세션 작업: [§3.1 셀 메모 UI / §3.2 스냅샷 / 다른 작업]

기획서 v6.2 §3 후속 작업 목록 보고 작업 계획 알려줘.
```

---

## 6. 변경 이력

| 버전 | 일자 | 핵심 변경 |
|---|---|---|
| v6.2 | 2026-05-28 | **P0-2 완료** — `funding.html` 전면 재구성. 메인 탭 2개(지원금 입금 일정 / 인건비 환급), 요약 카드 6개 × 2(이번 달/누적 × 입금예정·입금·미수 또는 환급예정·환급·환급률), 카드 안 과제/기타 분리 행, 과제별 추적 표 (3행 페어 — 예정/입금/미수). 사이클 중 폐기: 수주 카드, 시기별 입금 현황(분기/월 카드), 알림 카드(이번 달/다음 입금/올해 남은 입금) — 사용자 헷갈림. 표 분류 배지(과제/기타) + 정렬 우선순위(분류 우선). dashboard와의 단위 차이는 의도된 차이로 인정(`getProjectYearSupport` 헬퍼는 디버깅용으로만 정의 유지). |
| v6.1 | 2026-05-28 | **P0-1 완료** — `participation-summary.html` 신규 페이지 (Firestore 직접 연동, 회사 칩·연도·planned/actual 토글·구분 필터·이름 검색·미배정 토글, 연간 평균 표시, 셀 클릭 시 우측 패널 분해). **v6.3 부수 완료** — 인력 추가 모달에 현재 월 참여율·과제 수 배지(`project-labor.js`/`project-labor.html` 패치). 사이드바 메뉴 1개 추가. |
| v6.0 | 2026-05-27 | **우선순위 재배치** — 실데이터 연동 테스트를 위해 두 신규 페이지(참여율 합계 / 환급액 비교)를 P0로 승격. project-labor 후속 작업은 v6.x로 이연. |
| v5.3 | 2026-05-27 | Step 4.7 월급 오버라이드, 행 드래그 순서, sticky 컬럼, 엑셀 틀 고정, 셀 잠금, 글자색 등 다수 패치 |
| v5.2 | 2026-05-26 | 지급 완료 메타, 스냅샷 데이터 모델, 모달 점프 방지 등 |
| v5.1 | 이전 | 인력 분류 인라인 드롭다운, 셀 input 포맷팅, 확정 단일 액션 |
| v5.0 | 이전 | project-labor 페이지 본격 사양 (12개월 펼치기, 인력 분류, 우측 sticky 박스, 메모) |
