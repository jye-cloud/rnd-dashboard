# 다음 세션 이관 요약 — Phase A Step 4.5 진입

> 작성일: 2026-05-27
> 직전 세션: v5.1 Phase A Step 4 (인력 분류 드롭다운) + 후속 패치 (포맷팅 / 색 분리 / 차이만보기 제거)
> 다음 세션 작업: **Step 4.5 — 지급 완료 메타 (v5.2)**

---

## 1. 현재 상태 (Phase A 진척)

| Step | 내용 | 상태 |
|---|---|---|
| 1 | 데이터 모델 보강 (personRoles, meta 확장, carryover) | ✅ |
| 2 | 12개월 펼치기 + 우측 sticky 4박스 | ✅ |
| 3 | 월 접기/펼치기 인터랙션 | ✅ |
| 3.5 | 확정 버튼(검수 메타 통합) + 비교 탭 개편 | ✅ |
| 4 | 인력 분류 드롭다운 (personRoles 인라인 UI) | ✅ |
| 4 후속 | 색 분리 / 셀 input 포맷팅 / 탭 동기화 버그 / 차이만 제거 | ✅ |
| **4.5** | **지급 완료 메타 (v5.2 신규)** | ⏳ 다음 세션 |
| 5 | 셀 메모 UI | 미진행 |
| 6 | 엑셀 입출력 | 미진행 |
| 7 | 실데이터 입력 테스트 | 미진행 |
| Phase H | 예상 계획 스냅샷 (v5.2 신규, 별도 진입) | 사양만 등록 |

## 2. 직전 세션 산출물 위치

- `/mnt/project/project-labor.js` — 2647줄 (Step 4 후속 패치까지 반영)
- `/mnt/project/project-labor.html` — 1132줄
- `/mnt/project/인건비_관리_시스템_기획_v5.2.md` — 신 버전 기획서

## 3. Step 4.5 작업 사양 (지급 완료 메타)

### 데이터 모델 변경

`projectLabor/{projectId}_meta.meta[ym]` 스키마에 두 필드 추가:

```js
meta[ym] = {
  // 기존 (v5.1)
  confirmed:   boolean,       // 월 확정
  confirmedAt: ISO date,

  // v5.2 신규
  paid:        boolean,       // 지급 완료
  paidAt:      'YYYY-MM-DD',  // 사용자 입력 (기본 오늘, 수정 가능)
}
```

마이그레이션: 기존 데이터는 `paid: false` 기본. 별도 변환 불필요.

### UI 변경

| 탭 | 월 헤더 표시 |
|---|---|
| 예상 | (변경 없음) `[확정]` 버튼 또는 `✓ 확정` 뱃지 |
| **실제** | `✓ 확정 / [지급 완료]` 버튼 또는 `✓ 확정 💸 지급` 뱃지 |
| 비교 | (변경 없음) 표시 없음 |

### 동작

1. **실제 탭**의 월 헤더에 `지급 완료` 버튼 (확정 뱃지 옆)
2. 클릭 시 prompt — 지급일 입력 (기본 = 오늘 ISO date, 수정 가능)
3. 입력 → `meta[ym].paid = true`, `meta[ym].paidAt = 'YYYY-MM-DD'` → save → renderAll
4. 이미 지급 완료된 월 클릭 시: `[지급일 수정 / 지급 취소 / 취소]` 3택 confirm
5. 확정 안 된 월에 지급 완료 시도: 경고 후 자동으로 확정도 같이 (또는 사용자 선택)
6. 확정 취소 시 지급 완료도 같이 풀림 (정합성)

### 시각 표식

- 확정만: 🟦 `✓ 확정` (기존 청록 뱃지)
- 지급 완료: 🟩 `💸 지급` (신규 초록 뱃지, 날짜 툴팁)
- 둘 다: 두 뱃지 나란히

### 코드 진입점

- `onConfirmMonthClick` 옆에 `onPaidMonthClick` 신설
- 월 헤더 렌더링 부분 (`buildTable`의 thead 처리) — 실제 탭일 때 paid 뱃지/버튼 추가
- 새 CSS: `.pl-month-paid-btn`, `.pl-month-paid-badge`, `is-paid` modifier

### 참고 파일

- 기획서 §4 Phase A.8.5 (전체 사양)
- 기획서 §1.4.3 (meta 스키마)
- 기존 onConfirmMonthClick 코드 패턴 그대로 차용 가능

## 4. v5.2에 추가된 새 데이터 모델 (참고)

```js
// projectLabor/{projectId}_meta
{
  personIds: [...],
  personRoles: { /* v5 */ },

  meta: {
    [ym]: {
      // v5.1
      confirmed: bool, confirmedAt: ISO,
      // v5.2 ← Step 4.5 구현 대상
      paid: bool, paidAt: 'YYYY-MM-DD',
      // legacy (마이그레이션 후 보존만)
      sysReg, sysRegAt, amtConf, amtConfAt
    }
  },

  // v5.2 신규, 구현은 Phase H로 분리
  snapshots: [
    { id, label, createdAt, cells, personRoles }
  ]
}
```

## 5. v5.2에서 결정만 하고 미루어둔 항목

### Phase H. 예상 계획 스냅샷 이력

별도 진입 (Phase A 완료 후 우선순위 따라).

- 핵심: 명시적 "스냅샷 저장" 버튼 + 별도 모달 UI
- 사용자 원안(표 쌓기)은 UI 부담 커서 폐기
- 미결정: 셀별 차이 하이라이트 / 복원 버튼 / 권한
- 데이터 모델은 v5.2에 등록 완료
- 상세 사양은 기획서 §6.9 참조

## 6. 다음 세션 시작할 때

```
"인건비 v5.2 Step 4.5 시작.
지난 세션에서 Step 4 + 후속 패치 완료, v5.2 기획서 작성됨.
이번엔 지급 완료 메타 구현 — 실제 탭 월 헤더에 [지급 완료] 버튼 +
사용자 입력 지급일 + ✓ 확정 / 💸 지급 뱃지 분리 표시.
기획서 §4 Phase A.8.5 참조.
project-labor.js / project-labor.html 받아서 현재 상태 검토 후 작업 계획 알려줘."
```

## 7. 검증 필요 (Step 4 + 후속 패치)

다음 세션 시작 전, 사용자가 Step 4 + 후속 패치 결과물을 좀 더 써보고 발견되는 이슈 있으면 같이 가져오는 게 좋음. 특히:

- 셀렉트 색 분리 — 가독성 OK?
- 콤마/% 포맷팅 — focus/blur 전환이 자연스러운지
- 비교 탭 3컬럼(% / 현금 / 현물) — 환급 있을 때만 잘 나오는지
- subRole 탭 간 동기화 — 예상/실제/비교 다 일치하는지
- 자동계산 분기 — `현물` 선택 시 inkind 필드로 잘 가는지

이슈 발견되면 Step 4.5 작업 전 또는 함께 묶어서 처리.
