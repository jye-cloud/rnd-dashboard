# 다음 세션 이관 메시지 (v8.5 이후) — 복붙용

인건비 관리 시스템 — 이어서 작업 (v8.5 이후).

## [참조 파일 — 프로젝트에 올려둠]
- `인건비_관리_시스템_기획_v8_5.md`  (최신 기획서. §6 작업 순서 + §4.8 C1~C6 후보 사양 + 변경이력. 상단 v8.5 블록 = 이번 세션 요약)
- `개발정리_테스트가이드.md`          (지금까지 구현 누적 + 실화면 테스트 체크리스트)
- 코드(.html/.js): persons-master / project-budget / project-labor / project-detail / projects / dashboard / funding / firestore-service / 그 외 페이지

## [작업 규칙]
- 코드 원본은 읽기 전용 → **outputs 복사본에 누적 편집(재복사 금지)**. 프로젝트 자동반영 안 됨 = 내가 다운받아 같은 이름으로 덮어씀(교체). **적용 후 화면이 안 바뀌면 강력 새로고침(Ctrl+Shift+R) / 배포 확인.** (이번 세션에도 "JS만 줘도 되냐 / 화면에 안 보인다" = 옛 파일 배포·캐시 사례 있었음. 동적 렌더 섹션은 JS만으로도 그려짐.)
- 검증은 정적분석(node -c + 인라인 JS + grep 정합 + div/table·CSS 균형 + 핵심 로직 노드 시뮬)까지. 실화면은 내가 확인.
- 큰 결정은 물어보고, 자잘한 건 바로 진행. **한 파일은 한 번에. 큰 파일은 단계로 쪼개 화면 확인받으며.** 한국어 진행. 디자인 세부는 나중에 — 기능/숫자 위주. **잘 모르겠다 싶으면 김연구원·500만→600만 같은 구체 예시로 풀어 설명.**
- 매 작업마다 `개발정리_테스트가이드.md`에 누적 기록(무엇을 개발 + 무엇을 테스트 + 다음 구현).

## [지금까지 한 것]
- v8.1: §3.5 인건비 대시보드+채용 의무 / §3.6 인력 대시보드 / C4 신규 인력 자동 판정 / 채용 의무 디커플링.
- v8.2: C2 3책5공 관리(공유 헬퍼 `three-five-rule.js`).
- v8.3: C3 셀 메모(실제·비교 탭) / C1-b 예산 선택 전송 모달 / C6 1단계(예산 전용 연봉=공유 슬롯 `salarySlots`) / 대시보드 지원금 현황 카드.
- v8.4: C6 2·3단계(예산에서 슬롯 불러오기·엑셀 슬롯 컬럼) / §4.4 1·2a단계(인사 마스터 "연봉 변경 시점" + 인건비 "그 달 연봉" 계산) / 대시보드 툴바 한 행.
- **v8.5 (이번 세션):**
  - **§4.4 2b — 인건비 "📅 연봉 변경 반영" 선택 버튼** (`project-labor`): 예상 탭 툴바 버튼 → 모달(연중 변경 인력 목록·"재계산 N칸/수동 M칸/이미반영 K칸"). 체크한 사람만 변경월~ 예상 재계산. 확정·변경 전·rate0 제외, 오버라이드 인력 별도. 수동 셀 정책(건너뛰기/하나씩/덮어쓰기). **묶음 undo 1개.**
  - **공용 모듈 승격** (`firestore-service.js`): `window.SalaryUtil`(연봉 해석 4함수 — persons-master·project-labor 참조+로컬폴백), `window.YearFilterUtil`(연도 드롭다운 — 5개 페이지, 기본 "올해 자동"). 2026 이후 데이터 연도 안 나오던 버그 해결.
  - **§4.4 진입 UI 버그픽스** (`persons-master`): 연봉 변경 시점 저장 안 됨/리셋 픽스(로컬폴백 + readForm 검증 + `YYYY-MM` 자동 포맷).
  - **과제 관리** (`projects`): ① 연구기간 = **전체 기간**(최저 시작~최고 종료) 일관화. ② 기본 컬럼 재구성(No/유형/진행여부/키워드/책임자/연구기간/기관명/총지원금/당해입금/**당해 환급예정**), 제출일→선택열. ③ **"당해 환급예정" 신규 컬럼** = 연도 선택 시만(전체면 숨김), `projectLabor/{id}_planned` 그 해 cash 합(=funding 환급 예정액) 비동기 로드(연도별 캐시·토큰가드). ④ 헤더 ⓘ 툴팁(흰 배경·검은 글씨·옅은 파랑 `#93c5fd`) — 총지원금·당해입금·당해환급예정.
  - **대시보드 월 클릭 이동** (`dashboard`+`projects`): 월별 신규제안 차트 월 클릭 → `projects.html?year=Y&submitMonth=M&newProposal=1` → 그 해·그 달 제출분만.
  - **과제 상세** (`project-detail`): ① 비중 VAT 정합(용역 모드 분자도 VAT 포함 총액 → 30%). ② 총 지원금 콤마 자동(입력/로드). ③ **정산 차감(이자 반납 등) [A안]** — 입금 일정 연차별 "➖ 정산 차감" 섹션(금액+사유 자유입력). **미수 = 예정 − (실수령 + 차감).** 데이터 `yb.deductions=[{amount,reason}]`.
  - **자금 관리** (`funding`): 차감 연결 [옵션1+가] — 합계 미수 = max(0, 예정−입금−차감), 차감 귀속연도=마지막 예정 입금 연도. 표기 "**반납 N 반영**"(밝은 오렌지 `#f97316`). 요약 카드 누적 미수·전체 합계 미수도 반영, 월별 셀은 그대로.

## [다음 세션 할 일 — 추천 순서] (= 보류했던 §6.1 로드맵)
1. **§4.4 3단계 — 예산 동기** (`project-budget`): 예산 행 월급/선택 전송 ⓑ(`override ?? master ×%`)를 **"그 달 연봉"**으로. 예산은 연 단위라 "예산 연도 기준 연봉"으로 해석(2027 예산 자동 새 연봉). ⓑ가 단일 숫자 가정이던 것 동기. **착수 전 결정: 예산 연도의 어느 달 기준(연초 1월/최신값)?**
2. **S2 — labor dashboard surface + 청년 분류 일원화**: 인건비 대시보드 노출 정리 + 청년 자동(만 나이 기반)으로 통일, 수동 플래그 폐기.
3. **S3 — project-detail 묶음**: 채용 의무 정의 + C4 신규/기존 자동분류.
4. **S4 — 채용 의무 판정/표시.**
5. **과제 상세 페이지** (사양 미정, 마지막) → 화면 기획부터 물어보고 진행.

## [결정 완료 (이번 세션)]
- **연구기간 = 과제 전체 기간** 표시(연차 슬라이스 아님).
- **당해 환급예정 정의** = 예상 탭 지원금(`cash`) 합(= funding "환급 예정액"과 동일). **연도 "전체" 선택 시 컬럼 숨김.** 인건비 관리 대상만 값, 아니면 `-`.
- **비중 = VAT 포함끼리 비교**(용역 모드 분자도 VAT 포함 총액).
- **정산 차감 = A안**(연차별 금액 + **사유 자유입력**). 미수 = 예정 − 실수령 − 차감.
- **자금 차감 연결 = 옵션1**(합계·요약 카드만 반영, 월별 셀 유지) **+ 가**(차감 귀속 = 마지막 예정 입금 연도).
- 차감 표기 문구 = "**반납**", 색 = 밝은 오렌지 `#f97316`(미수 빨강과 구분).

## [결정 필요 (다음 착수 전 물어볼 것)]
- 정산 차감 **용어 통일**: 과제 상세는 "정산 차감(이자 반납 등)", 자금 관리는 "반납 N 반영" — 한쪽으로 맞출지(예: 양쪽 "반납"). 현재는 의도적으로 둠.
- 자금 관리에 **차감 사유까지 노출**할지(현재는 "반납 N 반영" 금액만, 사유는 과제 상세에서 확인).
- §4.4 3단계: 예산 연도 기준 연봉(1월/최신).
- 과제 상세 페이지(6묶음 마지막) 레이아웃·내용 사양.

## [확인 대기 — 실화면 (이월 포함)]
- v8.5: **정산 차감 입력**(과제 상세 입금 일정) / **자금 관리 미수 차감 반영**("✓ 완료" + "반납 N 반영") / §4.4 2b(연봉 변경 반영 버튼·묶음 undo) / 연도 드롭다운(YearFilterUtil). (연구기간·컬럼·당해환급예정·툴팁·월클릭·비중·콤마는 사용자 확인 완료.)
- (이월) v8.4 §4.4 1·2a·C6 2·3단계 / v8.3 C3·C1-b·C6 1단계·대시보드 입금 진행 / v8.2 3책5공 / v8.1 대시보드·채용 의무·C4 / §4.2 스냅샷.

## [보류 (사례 누적 시)]
- project-budget 책/공 +1 미리보기(타 과제 roster 로드 필요) / 비인건비 3책5공 과제 "공" 카운트 / 청년 군대 예외 보정 / 비인건비 과제 공공입금 gap.

## [데이터 모델 메모 — 이번 세션 추가분]
- `persons.salaryChanges = [{from:'YYYY-MM', annualSalary}]` (v8.4, 계단식 연봉) — 해석은 `window.SalaryUtil`.
- `yearBudget.deductions = [{amount, reason}]` (v8.5, 정산 차감/반납). plannedPayments/actualPayments와 동일 위치, 마이그레이션 0, 총 지원금·비중 무영향.
- 위 둘 다 **없으면 기존 동작 100% 동일**(비파괴).
