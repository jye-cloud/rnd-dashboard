# 다음 세션 이관 메시지 (복붙용) — v8.2 연관

```
인건비 관리 시스템 — 이어서 작업 (v8.2 이후).

[참조 파일 — 프로젝트에 올려둠]
- 인건비_관리_시스템_기획_v8_2.md   (최신 기획서. §6 작업 순서 + §4.8 C1~C5 후보 사양)
- 개발정리_테스트가이드.md           (지금까지 구현 누적 + 실화면 테스트 체크리스트. 묶음 7 = C2)
- 코드: three-five-rule.js(신규 공유 헬퍼) / project-detail / participation-summary / project-labor / project-budget / labor-dashboard (.html/.js)

[작업 규칙]
- 코드 원본은 읽기 전용 → outputs 복사본에 누적 편집(재복사 금지). 프로젝트 자동반영 안 됨(내가 다운받아 같은 이름으로 덮어씀 = 교체됨).
- ★ three-five-rule.js 는 신규 파일 → 프로젝트에 새로 추가 + 사용 페이지 HTML에 <script src="three-five-rule.js"> 포함됨(participation-summary·project-labor·project-detail·labor-dashboard).
- 검증은 정적분석(node -c + 인라인 JS + grep 정합 + div/table 균형)까지. 실화면은 내가 확인.
- 큰 결정은 물어보고, 자잘한 건 바로 진행. 한 파일은 한 번에. 한국어 진행. 디자인 세부는 나중에 — 지금은 기능/숫자 위주.
- 매 작업마다 '개발정리_테스트가이드.md'에 누적 기록(무엇을 개발 + 무엇을 테스트 + 다음 구현).

[지금까지 한 것]
- v8.1: §3.5 인건비 대시보드+채용 의무 / §3.6 인력 대시보드 / C4 신규 인력 자동 판정 / 채용 의무 디커플링.
- v8.2: **C2 3책5공 관리 구현 완료.** 공유 헬퍼 three-five-rule.js(책≤3·책+공≤5). 과제 수정 opt-in is3ch5gManaged + 책임자 personId 검색 드롭다운(재직/퇴직 필터·이름 검색). 참여율 3책5공 컬럼(현재 연도만 책/공·초과 빨강). 프로젝트별 인건비 모달 배지(N개 과제→책/공)+칩·메인 표 책임자 행 강조. 인건비 예산 책임자 색. 인건비 대시보드 "📚 3책5공 점검" 섹션. 책임자 지정 경고(책 2건 이상 시). 공고일 8자리 자동 변환. 데이터 추가=is3ch5gManaged+managerPersonId, 셀/키 변경 0.

[이번 세션 할 일 — 다음 추천 순서]
1. §4.4 월급 오버라이드 연중 변경 — **범위 결정 먼저**: (a) 과제 오버라이드만 연중(A안 팝오버) vs (b) +마스터 연봉 시간축(상/하반기)+여러 인력 일괄. 내 케이스는 (b). 입력 UX=A안. 모델 monthlySalaryOverride 숫자→객체{YYYY-MM:금액, step}.
2. C3 셀 메모 실제·비교 탭 — 권장안 ⓐ(차이행 비허용) 확정. 결정 거의 불필요 → 바로 가능. (실제 탭: 메모 타깃 state.actual 분기 + _actual 문서 저장. 비교 탭: 예상행→_planned, 실제행→_actual, 차이행 비허용.)
3. C1 인건비 예상 분배 선택 전송 — 금액 출처 결정 필요(budget값 vs labor 급여 재계산), split/자부담 정책.
4. 6묶음 과제별 상세(projects-summary) — 사양 미정 → 화면 기획 먼저.
5. §4.5 엑셀 입출력 — 범위. 우선순위 낮음.

[결정 필요(착수 전 물어볼 것)]
- §4.4 범위 (a)/(b).
- C1 참여율만 보낼 때 금액 출처.
- 6묶음 레이아웃·내용 사양.

[C2 후속·보류 (사례 누적 시)]
- project-budget 책/공 +1 미리보기 — 타 과제 roster 로드 필요(현재 책임자 색만).
- 비(非)인건비 3책5공 과제의 "공" 카운트 — 명단 캐시가 인건비 관리 과제만 담는 한계(책=책임자는 정확).
- "3책5공 점검" 위치(인건비 대시보드 채택) — 인력 대시보드로 옮길지 여부.
- 청년 군대 예외 보정.

[확인 대기 — 실화면(이월 포함)]
- v8.2: 3책5공 — 과제 수정 opt-in·책임자 검색 드롭다운(재직/퇴직·이름검색)·저장복원 / 참여율 책/공(현재연도만·초과 빨강) / 모달 배지·칩·책임자 행 강조 / 예산 책임자 색 / 인건비 대시보드 3책5공 점검 / 책임자 지정 경고(책2건↑) / 공고일 8자리 자동 변환. (체크리스트 = 테스트가이드 묶음 7.)
- (이월) v8.1 인건비/인력 대시보드·채용 의무·C4 / §4.2 스냅샷 / §4.1 메모 / §3.4 3분류 실화면.
```
