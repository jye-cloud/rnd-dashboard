# 다음 세션 이관 요약 — v5.3 작업 종료 시점

> 작성일: 2026-05-27
> 직전 세션: v5.2 Step 4.5(지급 완료) + Step 4.6(가로 채우기, 모달 UX) + Step 4.7(월급 오버라이드) 모두 구현 완료
> 다음 세션 작업: **미정 — 사용자가 실데이터 입력 후 떠오르는 요구 따라 결정**

---

## 1. 현재 상태 (Phase A 진척)

| Step | 내용 | 상태 |
|---|---|---|
| 1 | 데이터 모델 보강 (personRoles, meta 확장, carryover) | ✅ |
| 2 | 12개월 펼치기 + 우측 sticky 4박스 | ✅ |
| 3 | 월 접기/펼치기 인터랙션 | ✅ |
| 3.5 | 확정 버튼(검수 메타 통합) + 비교 탭 개편 | ✅ |
| 4 | 인력 분류 드롭다운 (personRoles 인라인 UI) | ✅ |
| 4 후속 | 색 분리 / 셀 input 포맷팅 / 탭 동기화 / 차이만 제거 | ✅ |
| **4.5** | **지급 완료 메타 (v5.2)** | ✅ |
| **4.6** | **가로 채우기 (Ctrl+R), 확정 취소 동작, 모달 UX 개선** | ✅ |
| **4.7** | **월급 오버라이드 (예상 탭 인라인)** | ✅ |
| 5 | 셀 메모 UI | 미진행 (후보) |
| 6 | 엑셀 업로드 | 미진행 (우선순위 낮음 — 사용자 결정) |
| 7 | 실데이터 입력 테스트 | 진행 중 (현재 사이클) |
| Phase H | 예상 계획 스냅샷 | 사양만 등록 |
| §6.10 | 모달 인력 정보 확장 (현재 참여율·과제 수) | 미진행 |
| §6.11 | 프로젝트별 신규 인력 판단 기준 | 미진행 |
| §6.12 | 월급 오버라이드 연중 변경 | 미진행 |

## 2. 직전 세션 산출물 위치

- `/mnt/project/project-labor.js` — **3479줄** (Step 4.7까지 반영)
- `/mnt/project/project-labor.html` — **1502줄**
- `/mnt/project/인건비_관리_시스템_기획_v5_3.md` — v5.3 기획서 (신규)

## 3. v5.3에서 구현된 것 요약

### 동작 변경 (사용자가 인지해야 할 것)

1. **확정 취소** — 확정된 월의 [확정됨] 버튼 다시 클릭하면 이제 "재확정"이 아니라 "확정 취소"로 동작. 실제 탭의 그 월 셀이 전부 삭제됨. paid도 같이 풀림.
2. **인력 추가 모달이 완전히 새로 짜임**:
   - 상단 "이미 추가된 인력"은 **기본 접힘** (카운트 칩만)
   - 각 행에 **체크박스** — 행 클릭하면 체크 토글
   - 하단 sticky 바의 **[+ N명 추가]** 한 번으로 일괄 등록
   - 모달 열린 동안 화면 점프 없음 (상단 섹션 스냅샷 고정)
   - "퇴사자도 표시" 토글 (과거 데이터 입력용)
3. **월급 셀이 input으로 (예상 탭만)** — 클릭해서 수정 가능
   - 비우고 Enter = 인력 마스터 값 복원
   - 오버라이드 적용 셀은 **파란색 강조**
   - 자동 재계산 + 한 번의 Ctrl+Z로 복원

### 데이터 모델 변경

```js
projectLabor/{projectId}_meta.personRoles[pid] = {
  newOrExisting: '기존' | '신규',
  cashOrInkind:  '현금' | '현물',
  subRole:       '',
  monthlySalaryOverride: null | number   // v5.3 신규
}
```

마이그레이션 불필요 — `monthlySalaryOverride: undefined`도 falsy로 처리됨.

### 핵심 헬퍼

```js
// 모든 월급 사용처는 이 헬퍼로 통일됨 (5군데)
function getEffectiveMonthlySalary(person, _project) {
  var roles = state.personRoles && state.personRoles[person.id];
  if (roles && typeof roles.monthlySalaryOverride === 'number' && roles.monthlySalaryOverride > 0) {
    return roles.monthlySalaryOverride;
  }
  return person.monthlySalary || 0;
}
```

## 4. 다음 세션 후보 (사용자 결정)

### 우선순위 높음 (실데이터 사이클이 막혀있다면)

- 사용자가 실데이터 입력하다가 발견한 새 이슈 — 가장 먼저 처리

### 자연스러운 다음 단계

| 후보 | 작업량 | 비고 |
|---|---|---|
| **Step 5 — 셀 메모 UI** | 1세션 | 빨간 점·노란 배경·hover 툴팁·클릭 팝오버. 사양 명확 (기획서 §4 Phase A.7) |
| **Phase H — 예상 계획 스냅샷** | 1-2세션 | 모달 UI가 무거움. 데이터 모델은 이미 v5.2에 등록. 미결정 항목 몇 개 있음(§6.9) |
| **§6.10 모달 정보 확장** | 1세션 | 현재 참여율 + 참여 과제 수. Phase B(합산 참여율) 로직 일부 차용 |
| **§6.11 프로젝트별 신규 판단** | 2세션 | 프로젝트 페이지 + 모달 양쪽. 데이터 모델 결정 필요 |
| **§6.12 월급 오버라이드 연중 변경** | 1-2세션 | 단일 값 한계 확장. 자동 재계산 정책 재검토 필요 |

### 우선순위 낮음 (사용자 결정)

- **Step 6 엑셀 업로드** — 사용자가 "페이지 사용성 좋아서 우선순위 낮음" 명시
- **Phase B 합산 참여율** — Phase A 완료 후 진행 예정

## 5. 검증 필요 (v5.3 구현물)

다음 세션 시작 전, 사용자가 실데이터 입력하면서 다음을 자연스럽게 확인:

- **확정 취소** — 정말 셀이 다 지워지는지, paid도 같이 풀리는지
- **월급 오버라이드** — 자동 재계산이 정확한지, 묶음 undo 한 번에 복원되는지
- **일괄 추가** — 검색 바꿔도 선택 유지되는지, 큰 회사(인력 50+) 케이스에서 성능 OK인지
- **상단 섹션 접기** — 카운트 갱신이 추가/제거 직후 즉시 반영되는지

이슈 있으면 다음 세션 첫 작업으로 처리.

## 6. 다음 세션 시작 멘트 (복붙용)

```
인건비 v5.3 이후 작업 시작.

지난 세션 산출물:
- project-labor.js (3479줄), project-labor.html (1502줄)
- 인건비_관리_시스템_기획_v5_3.md (신규)

지난 세션 종료 시점:
- Phase A Step 4.5(지급 완료) / 4.6(가로 채우기 + 모달 UX) / 4.7(월급 오버라이드) 완료
- v5.3 기획서 작성됨
- 다음 후보: Step 5(셀 메모) / Phase H(스냅샷) / §6.10~6.12 미구현 항목들

이번 세션엔 [작업 내용 사용자가 입력] 진행하자.

먼저 프로젝트 파일과 v5.3 기획서 보고 현재 상태 검토 후 작업 계획 알려줘.
```

## 7. 알려진 한계/주의사항

- **월급 오버라이드는 1년 단일 값** — 4월부터 월급이 바뀌는 케이스는 미지원 (§6.12)
- **확정 취소는 되돌릴 수 없음** — Ctrl+Z 대상이 아님. 실수하면 다시 확정해야 함 (예상값 그대로 복사되니 거의 동일하지만, 사용자가 실제 탭에서 수동 수정한 값이 있었다면 그건 손실)
- **모달 일괄 추가 시 점프 방지 트레이드오프** — 모달이 열린 동안 새로 추가한 인력은 상단 섹션에 즉시 안 보임 ([추가됨] 비활성 표시로만 확인). 모달 닫고 다시 열면 정상 표시. 사용자 피드백 따라 이 동작 재검토 가능.
- **이전 세션과 비교해 줄 수 증가량 큼** — js 약 832줄 추가 (2647 → 3479). 다음 세션 시작 시 코드 파악에 약간 시간 필요.
- **personRoles 기본값이 5군데 흩어져 있음** — `{ newOrExisting: '기존', cashOrInkind: '현금', subRole: '', monthlySalaryOverride: null }`. 향후 필드 추가 시 일괄 변경 필요. (헬퍼 함수로 추출하는 리팩토링 후보)
