# 다음 세션 이관 메시지 (v9.0 이후) — 복붙용

> 시스템: 한국 정부 R&D 인건비 관리 웹(바닐라 JS/HTML/CSS + Firebase/Firestore). 3개 자회사. 단일 진실 소스 = 기획서.

## [참조 파일 — 프로젝트에 올려둠]
- `인건비_관리_시스템_기획_v9_0.md`   (기획서 = 단일 진실 소스. 상단에 버전별 변경이력 + 🚨 운영 원칙, 본문에 스펙)
- `개발정리_테스트가이드.md`          (구현 누적 + 실화면 테스트 체크리스트 — 매 작업 후 갱신)
- 코드: `project-labor.js/html`, `project-budget.js/html`, `projects.js/html`, `persons-master.js/html`, `labor-dashboard.js/html`, `firestore-service.js`(공용 `window.SalaryUtil`), `three-five-rule.js`

## [작업 규칙]
- 코드 원본(`/mnt/project`)은 읽기 전용 → **outputs 복사본에 누적 편집(이미 있는 파일은 재복사 금지).** 자동반영 안 됨 = 사용자가 다운받아 같은 이름으로 덮어씀.
- **배포 ≠ 새로고침.** outputs 파일을 실제로 다시 받아 덮어쓴 뒤 **강력 새로고침(Ctrl+Shift+R)** 해야 적용됨. (브라우저 캐시가 반복적으로 헷갈리게 함 — "기능이 사라졌다/포맷이 안 먹는다"는 대개 stale 캐시.)
- 큰 결정은 물어보고 자잘한 건 바로. **한 파일씩.** 한국어. 기능·숫자 우선, 디자인 세부는 나중. 이미 내린 결정은 다시 묻지 말고 실행.
- 매 작업마다 `개발정리_테스트가이드.md` 누적 기록(무엇을 개발 + 무엇을 테스트 + 다음).

## [🚨 데이터 보존 · 사전 점검 의무 — 가장 중요]
**배경:** 이번 세션, 신규 기능이 런타임 예외(`yearClosed is not defined` — 함수 분리에 따른 스코프 누락)를 냈고 `loadLaborData`의 `.catch`가 이를 '로드 실패'로 오인해 **화면 전체가 0명**으로 보였다. 데이터는 Firestore에 안전했으나 **원인 파악·복구에 매우 오랜 시간**이 들었다. **지난 세션엔 실제로 데이터를 통째로 날린 적도 있다.** 이 부류는 비용이 매우 크다.

**매 딜리버리(파일 전달) 전 반드시 — 여러 단계 사전 점검을 반복:**
1. `node -c` 문법 검사. **(이걸로 끝내지 말 것 — 런타임 ReferenceError를 못 잡는다.)**
2. **하네스 시뮬레이션**(`/tmp/repro_harness.js` 재사용): `vm`으로 DOM/Firestore/`window.SalaryUtil` 스텁 + **실제 데이터 구조 모사**(연차 범위·personIds·셀·meta) → `loadLaborData → renderAll` 경로를 실제 실행해 **런타임 예외 0** 확인.
3. **심볼/스코프 grep 전수 점검**: 새 식별자의 **선언 위치 ↔ 사용 위치를 함수 경계까지** 대조. "쓰는 모든 함수에 선언이 있는가."
4. **데이터를 비우는 모든 `.catch`/초기화 경로**는 **렌더 예외와 로드 실패를 분리**(렌더 실패가 로드 데이터를 못 지우게). 가능하면 행/구획 단위 격리.
5. 위험 변경은 **사용자 실화면 확인 전까지 빈 상태 자동저장 금지**(`saveLaborData`가 빈 state를 Firestore에 덮어쓰지 않게).

> 요약: **문법 → 하네스 → 심볼 grep → 경로 분리**까지 다단계 사전 점검을 매번. "한 번 봤으니 됐다"는 금지.

## [지금까지 한 것 — 요약]
- v8.5~v8.9: §4.4 월급 연중 변경(1·2a·2b·연차별 오버라이드), 공용 SalaryUtil 승격, 이월금 1단계, 연차 정렬(전체/분기), 예산→인건비 전송 안전화, 가로 채우기/잠그기, 연차별 인력 순서 등.
- **v9.0(이번 세션):**
  - **연차 마감** — 연차 셀 전체 읽기전용 잠금. 순서 강제(이월금 처리 → 마감), 마감 취소(`canUncloseYear` placeholder). 상태 = `projectBudget/{id}_year{N}`의 `closed/closedAt/carryoverDone/carryoverDoneAt`(merge). 우측 차액 박스 **빨강 마감 배지** + 버튼(실제 탭만).
  - **월 지급 잠금** — '지급 완료' 시 그 월 셀 하드 잠금, 배지에 **빨간 ✓**. 지급 취소 = **사유 입력 필수** → `meta[ym].paidCancelLog`에 `{reason, at, prevPaidAt}` 이력 누적(추후 마스터 권한 추적). 확정 취소발 자동 지급취소도 이력. `isMonthPaidLocked`/`paidCancelTip` 신규.
  - **🚨 데이터 소실 방지 방어막** — `loadLaborData`: `renderAll()`을 자체 try/catch로 분리(렌더 예외가 로드 데이터 못 지움), `.catch`는 get 실패만 초기화. `buildTable` 인력 행 **행 단위 try/catch 격리**.
  - **버그픽스** — `buildSplitPersonRows`에 `yearClosed` 선언 누락(분할 등록 인력 행 안 뜨던 것) 수정.
  - **연도 선택 드롭다운화** — 숫자 스피너 → `<select>`(옵션=과제 연차 범위∪올해). `populateYearSelect`/`syncYearSelect`. 폭 130px.
  - **마감/지급 셀 가독성** — `inactive`(흐림)↔`locked`(읽기전용) 분리. 데이터 있는 잠금=또렷(`#1e293b`), 데이터 없는 비활성=흐림(`#94a3b8`).

## [확인 대기 — 실화면 (이번 세션 분)]
- 지급 완료 → 그 월 셀 잠기는지 + 배지 빨간 ✓ / 클릭 시 수정·취소(사유) 흐름 / 취소 후 잠금 해제·이력 기록.
- 연차 마감 배지 빨강 가시성 / 연도 드롭다운 동작·폭 / 마감·지급 셀 글자 가독성.

## [다음 세션 할 일 — 추천 순서]
1. **당해 예상 환급액 = 지원금 예상 + 이월금** (§4.9-3, v8.8 결정): 과제별 = Σ 예상(planned) 지원금 + 그 연차 `carryoverCash`. 월/분기 칸 현행 유지, 이월금은 **연 합계 칸에만** → 요약 카드=Σ 과제 합계로 자동 포함, 환급률 분모 증가. ⚠ `funding.js`가 `projectBudget` 미참조 → **`carryoverCash` 로드 경로 신규 추가** 필요.
2. **§4.4 3단계** — `project-budget` 예산 동기(차년도=그 해 최신 연봉). (project-budget "보내기" 금액 출처=인건비 탭이 아직 단일값 참조 → 연차별 반영 필요.)
3. **자금관리 연차 기준** — 집행·잔액·이월금을 연차 단위로(인건비와 동일 기준).
4. **S2**(인건비 대시보드 + 청년 자동 일원화) → **S3·S4**(채용 의무 묶음).
5. **C5 외부 전달** — Lovable prefill URL 패턴 + Google Sheets Apps Script 웹앱(일방향 push).
6. 과제 상세 페이지(사양 미정·마지막) → **엑셀 I/O**.

## [주의 / 함정]
- **데이터 비우는 경로 = 최우선 경계.** 위 🚨 사전 점검 의무를 매 딜리버리 반복. 특히 새 변수/헬퍼는 쓰는 모든 함수에 선언 확인.
- **연차 = 시작연도 기준** 매핑(`getYearIndexForState`, 달력연도 아님). `getActiveYearMonths`=그 연차 실제 기간 월.
- **데이터 모델 변경 0 원칙**: 이번 추가는 모두 비파괴(`closed/carryoverDone`, `meta[ym].paidCancelLog`) — 기존 funding/결산/labor 무영향.
- **`saveLaborData`는 `_planned/_actual/_meta`를 merge 없이 덮어씀** → 빈 state로 저장되면 전 연차가 날아갈 수 있음. 위험 변경 시 빈 상태 저장 금지.
- **자부담현금** = `cell.selfCash`, 현금/지원금 = `cell.cash`, 현물 = `cell.inkind`. 자부담현금 이월은 셀에 집행이 안 잡혀 자동 부정확 → 모달 직접 입력.
- **이월금 = 이미 받은 돈**(입금/미수와 무관), **현물은 이월 불가.**
- **배포 ≠ 새로고침.** 파일 다시 받아 덮어쓴 뒤 Ctrl+Shift+R.
