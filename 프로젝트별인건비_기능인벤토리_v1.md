# 프로젝트별 인건비 / 인건비 예산 — 기능 인벤토리 v1

> 작성: 2026-06-01 · 기획서 §3.3 산출물(기능 인벤토리 1세션)
> 목적: `project-labor` / `project-budget`가 **지금 실제로 하는 일**을 정리하고 §4 로드맵·v5 Phase A와 대조해 완료/부분/미구현을 가른다. 이게 있어야 §4 항목을 충돌 없이 손댄다.
> 기준 코드: `project-labor.js`(4866줄) / `project-labor.html`, `project-budget.js`(1439줄) / `project-budget-export.js`(1245줄) / `project-budget.html`.

---

## 1. project-labor — 현재 보유 기능 (baseline)

### 입력·셀
- 참여율 / 금액 셀 입력, 자동계산(참여율→금액=급여×%), 표시·raw 분리 포맷팅(`fmtCellRateDisplay`/`parseCellNumber`).
- 금액 3분류(§3.4): 지원금(cash)/현금(selfCash, 조건부)/현물(inkind). 분류 드롭다운(구분 컬럼).
- 나눔(split): 한 사람 분류별 멀티행, `cell.rates` 한 셀 저장(v7.4.1). **토글은 예상 탭만(v7.4.2).**
- 셀 우클릭 컨텍스트 메뉴: 12월까지 채우기(Ctrl+R)·이 월 잠그기 / 금액 버림(이 셀·아래로 복사, 10~10만원 단위) / 글자색(기본·빨강·파랑·초록).
- 가로채우기(`fillRateToYearEnd`), 일괄 자동 계산(⚡), 이전 분기 복사(📋, 되돌리기 토스트), 버림(`roundDownCell`/`roundDownBelow`).
- 셀 수동 잠금(`isYmManuallyLocked`/`setYmLocked`), 글자색(`cell.colors`).

### 뷰·네비
- 탭 3개: 예상(planned, 편집) / 실제(actual, read-only 월급) / 비교(compare, 차이만 토글).
- 뷰 모드: 전체 12개월 / 분기(분기 네비 ◀▶). 월 접기/펼치기(명시 접힘 캐시, "모두 펼치기").
- 우측 sticky 박스 4개(예산총액·누계·차액 등 `getBudgetBreakdown`/`getCumulativeBreakdown`/`getRemainingBreakdown`).
- 회사 칩 필터(localStorage 공유), 연도 입력, 프로젝트 선택, 채용 배너(`renderHireBanner`).

### 인력·역할
- 인력 추가 모달: 검색·퇴사자 포함 토글·현재 참여율/과제수 배지(`getModalPersonStats`, §4.6 ✅)·일괄 추가·이미 추가된 인력 스냅샷 고정(점프 방지).
- 역할: 기존/신규(+세부역할 subRole), 분류(구분). 행 드래그 순서변경(`onRowDrag*`, 자동 스크롤). 퇴사자 취소선/발령·종료 월 처리(`getExitYm`/`getHireYm`).
- 월급 오버라이드: `monthlySalaryOverride`(과제당 **단일값**).

### 메타·정합
- 월 확정(`confirmMonth`/`unconfirmMonth`) → funding 정합(confirmed), 지급 완료(`paidMonth`), 시스템 등록(sysReg) 메타. `migrateMetaToConfirmed`(구 sysReg/amtConf → confirmed 마이그레이션).
- 키보드: 방향키 셀 이동, Ctrl+R 채우기, Ctrl+Z 되돌리기(`undoLastCell`), Ctrl+C/V 단일 셀 클립보드.
- 저장: 디바운스 자동 저장(`scheduleSave`/`saveLaborData`), 저장 인디케이터.

---

## 2. project-budget — 현재 보유 기능 (baseline)

- 예산 행 입력: 분류별(현금=지원금/자부담현금/현물) 행 추가·삭제, 행별 합계·소계·총계(`calcRow*`/`calcTotals`).
- **3분류 라벨 표준 원본**: `cashOrInkind` = `'현금'|'자부담현금'|'현물'` + `FUND_TYPE_LABELS`. (§3.4 라벨이 여기서 유래.)
- 연도 탭(`renderYearTabs`), 예산 요약(`renderBudgetSummary`), 기간 계산(`monthsBetween`/`getYmListInRange`).
- **전년 복사**(`copyPreviousYear`), **예상 분배**(`distributeToLabor` — 예산을 project-labor의 _planned 셀로 분배, selfCash 포함).
- 행에 인력 배정 모달(`openPersonModal`/`selectPerson`).
- **내보내기**: `project-budget-export.js`(1245줄) + `pb-export-section`(미리보기·행 선택·엑셀). → budget은 export 이미 보유.

---

## 3. §4 로드맵 대조 — 완료/부분/미구현

| 항목 | 상태 | 코드 근거 | 남은 일 | 작업량 |
|---|---|---|---|---|
| **4.1 셀 메모 UI** | 🟡 **부분(표시만)** | `cell.memo` 모델 있음, 복사 시 전파(3174), `pl-memo-text` 표시 span(1749). **입력/편집 UI·빨간점·노란배경·툴팁·팝오버 전무.** 컨텍스트 메뉴에도 메모 항목 없음. | 메모 **입력/편집 진입**(클릭 팝오버 or 컨텍스트 메뉴) + 있음 표시(점/배경) + hover 툴팁 | **소(1세션)**, 사양 명확 |
| **4.2 Phase H 스냅샷** | ✅ **완료(v7.4.4)** | 컬렉션 `projectLaborSnapshots`(1문서=1스냅샷). 📸 버튼·이력 모달·내역(표)·차이·복원(+자동백업)·삭제·엑셀·시트복사. 나눔판정 `personRoles.split`. | 단독 페이지(보류) | 완료 |
| **4.3 신규 인력 판단 기준** | 🔴 **미구현** | `persons.isNew` 플래그·채용 배너는 있으나 **판정 기준/로직/설정 UI 없음**. | 데이터모델 확정 + 판정 로직 | 중(2세션), 모델 확정 선행 |
| **4.4 월급 오버라이드 연중 변경** | 🟡 **부분(단일값)** | `monthlySalaryOverride` 과제당 **하나** → 연중 월별 다른 월급 불가. | 월별(또는 구간별) 오버라이드 모델·UI | 중(1-2세션), 실제 케이스 후 |
| **4.5 Step 6 엑셀 입출력** | 🔴 **거의 미구현** | project-labor엔 Ctrl+C/V **단일 셀**만. 풀 엑셀 입/출력 없음. (budget은 별도 export 보유 — labor엔 없음.) | labor 표 엑셀 출력(+입력) | 중, **우선순위 낮음(트랙 B 마지막)** |
| **4.6 모달 정보 확장** | ✅ **완료** | `getModalPersonStats` 배지(참여율·과제수). | — | v6.1 완료 |
| **4.7 인건비 대시보드 본격** | ⬜ **별도 트랙** | `labor-dashboard.js`(§3.2/§4.7) — project-labor 밖. §3.4 점검은 v7.4.2 완료. | 통합 분석(트랙 B 후) | §3.2와 묶음 |

---

## 4. 권장 진행 순서 (제안)

§4.7은 §3.2 레이아웃과 묶이고(별도), 4.5는 자체 방침상 마지막. 나머지 중:

1. **4.1 셀 메모 UI** ← **다음 후보 1순위.** 사양 명확·소규모·데이터모델 이미 존재(`cell.memo`)·표시 절반 완성. 위험 낮고 즉시 체감 효과. 진입 방식만 결정(클릭 팝오버 vs 컨텍스트 메뉴 항목)하면 됨.
2. **4.2 스냅샷** — 중간 규모. 예상 계획 보존 니즈 있으면 다음. 이력 모달 UX(차이 하이라이트·복원 권한) 결정 필요.
3. **4.4 월급 연중 변경** — "실제 케이스 만난 후" 방침 → 당장은 보류 가능. 단일 오버라이드로 현재 운영 가능.
4. **4.3 신규 인력 판단** — 데이터모델 확정이 선행이라 가장 무겁고 불확실. 기준을 사용자와 먼저 정의해야 함.
5. **4.5 엑셀** — 트랙 B 마지막.

> **결정 필요(4.1 진행 시)**: 메모 입력 진입점 — ⓐ 셀 클릭 시 팝오버 ⓑ 우클릭 컨텍스트 메뉴 "메모" 항목 ⓒ 둘 다. + 표시 방식(현재 빨간 글씨 span 유지 vs 점/배경 추가).

---

## 5. 미해결·확인거리

- v5 기획서(Phase A 원문)는 현재 프로젝트 파일에 없음 → §4 로드맵(현 기획서)·코드만으로 대조함. 세부 사양 필요 시 v5 문서 확보 권장.
- 4.2 "데이터모델 v5.2 등록 완료" 주장 vs 실제 스냅샷 저장 코드 부재 → 모델 흔적만 있고 미사용인지 작업 시 확인.
- budget `distributeToLabor`와 labor 입력의 충돌/덮어쓰기 정책은 별도 점검거리(분배가 기존 입력을 덮는지).
