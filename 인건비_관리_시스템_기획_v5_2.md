# 인건비·인력 관리 시스템 기획 v5.2

> 60hz R&D Dashboard 인력·인건비 관리 기능 기획서 (완료 작업 + 남은 작업 로드맵)
> 작성일: 2026-05-27 (v5.2)
> 작성자: 정지혜 + Claude
> 이전 버전: `인건비_관리_시스템_기획_v5.md` (2026-05-27, v5.1 후속 패치까지 포함)

---

## 0. 작성 의도 (v5 / v5.1 / v5.2)

### v5 (초기 작성, 2026-05-27)

v4 작성 후, **Phase A (월별 인건비 페이지) 구현 직전에 기존 운영 엑셀 양식을 검토하면서 발견된 사용자 워크플로우 요구사항**을 반영하기 위해 v5를 작성. 코드 구현 전에 결정사항을 문서로 박아두는 것이 목적.

#### v5에서 일어난 일

1. **기존 엑셀 양식(`2024_V2G_에기평_2차년도.xlsx`)을 분석** — 실제 운영 데이터가 어떤 형태로 정리되어 있는지 확인.
2. **현재 `project-labor` 화면 설계와 사용자 워크플로우의 간극 4가지 발견**:
   - 분기 단위 이동 vs 1년 전체 모니터링 욕구 (누계/예산총액/이월금/차액 항상 확인)
   - 인력 분류 (기존/신규 × 현금/현물 × 세부 라벨) — 현재 데이터모델로 표현 불가
   - 셀 내 메모 (퇴사일·참여율 조정 사유 등) — 별도 memo 필드는 있으나 UI 미설계
   - 검수 흐름 (A 입력 → B 확인) — 현재 `meta.sysReg/amtConf`로 일부 커버되지만 날짜 메타가 없음
3. **결정**: 화면 구조를 사용자 엑셀 양식에 맞춰 개편하기로 함. 기존 양식을 디지털화하면서 굳이 다르게 만들 이유가 없다는 판단.
4. **결정**: 데이터 모델에 `projectLabor` 의 `personRoles` 추가, `projects.yearBudgets` 에 `carryover` 추가. (carryover는 v5에서는 껍데기만 — 입력 페이지·연동 결정은 추후)
5. **결정**: v4의 보류 이슈 §6.1 (selfCash), §6.2 (셀 메모) 일부 해결.

#### v5에서의 변경 위치

- **§1.4 데이터 모델** — `projectLabor` 구조 갱신 (`personRoles`, `meta` 확장), `projects.yearBudgets`에 `carryover` 추가
- **§4 Phase A** — 월별 인건비 페이지 사양 전면 개정 (12개월 펼치기 + 우측 sticky + 라벨 입력 + 셀 메모 UI)
- **§4 Phase A의 세부 Step 1~7** — 작업 순서 명시 (§8과 연계)
- **§6.1, §6.2** — 결정사항 정리, §6.7로 일부 이관
- **§6.7** — v5 결정사항 추가
- **§6.X (신규) — §6.8 기존 엑셀 양식 분석 결과** — 분석된 양식 구조와 매핑 규칙을 보존
- **§7** — 핵심 결정 사항 갱신
- **§8 작업 순서** — Phase A 세부 단계 반영
- **§9 변경 이력** — v5 줄 추가

### v5.1 (Step 3.5 + Step 4 후속 패치, 2026-05-27)

Phase A의 Step 1~3 구현 후, 사용자가 직접 사용해보고 발견한 피드백을 반영. 별도 마이너 버전을 만든 게 아니라 **v5의 §4 Phase A.6~A.8 사양 일부를 갱신**하는 형식.

#### v5.1에서 일어난 일

- **검수 메타 재구성**: 체크박스 2개(sysReg/amtConf) → "확정" 버튼 1개. 클릭 시 예상 → 실제 일괄 복사 + `meta[ym] = {confirmed, confirmedAt}`. 기존 `sysReg/amtConf` 필드는 자동 마이그레이션 후 보존.
- **비교 탭 컬럼 축소**: 한 월 5컬럼(예상%/예상현금/실제%/실제현금/차이) → 환급 있으면 3컬럼(%/현금/현물), 없으면 1컬럼(%). 각 행에 [예상]/[실제]/[차이] 라벨 표시. 두 번째 고정 컬럼은 "월급" → "구분".
- **비교 탭 금액 표시**: fmtMoney (84만) → fmtMoneyFull (840,000).
- **Step 4 본 작업**: 인력 컬럼에 personRoles 인라인 드롭다운 ([기존/신규][현금/현물]) + 신규 시 subRole 자유 텍스트. 컬럼 폭 재조정 (월급 잘림 해소). 자동계산 분기 — `cashOrInkind`에 따라 cash 또는 inkind 필드에 자동 채움.
- **Step 4 후속 패치**: 셀 input을 `type=number` → `type=text + inputMode=numeric` + dataset.raw 기반 포맷팅. 참여율 `10%` 표시 / 현금·현물 `837,533` 콤마 + 우측 정렬. focus/blur로 raw ↔ 표시값 전환.
- **드롭다운 색 분리**: 기존(슬레이트)/신규(인디고), 현금(그린)/현물(앰버) 색 modifier.
- **버그 수정**: switchTab이 buildTable을 재호출하지 않아 personRoles 변경이 한 탭에만 반영되던 문제 → onRoleChange에서 renderAll() 호출. subRole은 포커스 유지 필요해 다른 탭의 같은 input만 값 동기화.
- **"차이만 보기" 토글 제거**: 비교 탭 자체가 차이 보는 곳이고 행 라벨도 있어서 불필요. 인원수 100명 단위 과제가 아니라 5-10명 규모라 필터 가치 낮음.

### v5.2 (이번 세션, 2026-05-27)

Phase A Step 1~4 구현 완료 후, 운영 워크플로우에서 추가로 필요한 두 가지가 떠올라서 정리. **구현은 다음 세션 이후**, v5.2는 사양 결정과 위치 등록만 함.

#### v5.2에서 일어난 일

1. **§4 Phase A에 새 단계 추가**:
   - **A.9 — 지급 완료 메타 (실제 탭)**. "확정"과 별개로 "지급 완료" 버튼 + 지급일 입력. `meta[ym].paid`, `meta[ym].paidAt` 추가.
2. **§4 Phase A 이후 단계로 새 Phase H 명시 (Phase E는 이미 v2 후속이라 점유됨)**:
   - **예상(계획) 스냅샷 이력**. 계획 변동이 잦은 정부 R&D 특성상 "당시엔 어떻게 계획했었는지" 추적이 필요. **사용자 제안(표 여러 개 쌓기)이 아닌, 별도 모달 형태 추천** — 평상시 UI 보존. 다만 무게 있는 작업이라 다음 단계로 분리.
3. **§1.4 데이터 모델**: `meta[ym]` 스키마에 `paid` / `paidAt` 추가. `snapshots[]` 배열 추가 (예상 탭 한정).
4. **§6 미해결 이슈**: 6.9 신설 — 스냅샷 UI 결정사항 (모달 vs 인라인 vs 패널).
5. **§7·§8** 갱신.

#### v5.2 후속 (Step 4.5/4.6 구현 + 추가 요구사항)

Step 4.5(지급 완료) 구현 완료 후, 운영 사용 중 떠오른 작은 보강·신규 아이디어들. 미구현 항목은 §6에 등록만 함.

**구현 완료**:
- Step 4.5 — 지급 완료 메타 (실제 탭)
- Step 4.6 — 가로 채우기 (참여율 1월→12월): 우클릭 메뉴 + Ctrl+R (사용자 피드백: 매년 1월 입력 후 12월까지 복붙하는 패턴이 일반적)
- 확정 취소 동작 변경: 확정된 월 재클릭 = 재확정(덮어쓰기) → 확정 취소(실제 탭 셀 삭제 + paid 풀림)
- 인력 행 ✕ 삭제 버튼 (테스트 편의)
- 인력 추가 모달 — 퇴사자 포함 토글 (과거 데이터 입력용)
- 확정 뱃지 색 분리 (초록 → 청록, 지급 뱃지 에메랄드 신설)

**§6에 미구현으로 등록**:
- 6.10 — 인력 추가 모달 정보 확장 (현재 참여율 + 참여 과제 수 표시)
- 6.11 — 프로젝트별 신규 인력 판단 기준 (공고일/협약일 기반 자동 판정)

### v3·v4의 결정은 그대로 유효

v3 (인력 영역 정리), v4 (인력 대시보드·권한 정책·청년 판정 단순화)의 결정은 모두 보존. v5는 인건비 영역의 첫 페이지(`project-labor`)에 집중.

---

## 0.5 공통 원칙 / 이 프로젝트의 기본값

페이지를 새로 만들거나 기존 페이지를 수정할 때 반드시 따라야 하는 공통 룰. 인건비 모듈에 한정되지 않고 **전체 R&D 대시보드 어디서든 적용**됨. 향후 다른 공통 룰도 이 섹션에 추가.

### 0.5.1 레이아웃: 좁은 화면 대응의 통일된 방식

**3가지 원칙으로 어떤 모니터·노트북에서 봐도 일관된 레이아웃을 보장.**

#### 원칙 1. 본문은 모니터 폭에 관계없이 꽉 찬다

사이드바를 뺀 본문 영역(`.main-area`)을 모니터 크기와 무관하게 꽉 채운다. 14인치 노트북에서든 27인치 모니터에서든 동일한 비율로 콘텐츠가 채워져야 함.

#### 원칙 2. 좁아져도 콘텐츠는 압축하지 않고 가로 스크롤로 도망간다

`.container { min-width: 1200px }`로 본문이 1200px 미만으로 줄어드는 걸 막고, 그 이하 화면에선 `.main-area`에 가로 스크롤이 생긴다. **콘텐츠 자체는 그대로**, 좁아져도 카드·표가 깨지지 않음.

#### 원칙 3. 1400px 미만이면 사이드바 자동 collapse

`sidebar.js`의 `AUTO_COLLAPSE_BREAKPOINT = 1400`. 1400px 미만 창에선 사이드바가 자동으로 접혀서 본문이 충분한 공간을 확보. **단, 사용자가 수동으로 토글한 상태는 존중**(localStorage `rnd-sidebar-collapsed`).

#### breakpoint 산식 (왜 1200·1400인가)

- 사이드바 펼침 폭: 220px
- 사이드바 접힘 폭: 60px
- 본문 최소 폭: 1200px

→ 스크롤 없이 들어가는 창폭:
- 펼침 상태: 220 + 1200 = **1420px**
- 접힘 상태: 60 + 1200 = **1260px**

→ 1400px 미만 = 사이드바를 접어야 본문이 가로 스크롤 없이 들어감
→ 1260px 미만 = 사이드바를 접어도 본문에 가로 스크롤 발생 (콘텐츠는 1200px 폭으로 유지)

#### 구현 (`style.css` 전역 `.container` 룰)

```css
.container {
  width: 100% !important;
  max-width: none !important;
  min-width: 1200px;
  margin: 0 !important;
  padding: 20px 30px 2rem 80px;
  box-sizing: border-box;
}
```

`.main-area`는 이미 `overflow: auto`라 가로 스크롤이 자동 동작함.

#### 페이지에서 절대 하지 말 것 (Anti-patterns)

```css
/* ❌ 금지 1: 인라인 .container 재정의 — 모니터에서 좁아 보이게 만듦 */
.container { width: 1400px; margin: 0 auto; }

/* ❌ 금지 2: 1100~1400px 구간에서 그리드 무너뜨리기 */
@media (max-width: 1280px) {
  .my-grid { grid-template-columns: 1fr; }
}

/* ❌ 금지 3: 페이지 메인 컨테이너에 min-width: 0
   (단, flex/grid 자식 셀에 박는 min-width: 0은 OK — 텍스트 오버플로우 방지용) */
```

#### 허용되는 미디어 쿼리

- `@media print` — 인쇄/PDF 출력 시 레이아웃 조정
- `@media (max-width: 700px)` 이하 — 사실상 발동 안 함, 미래 모바일 대응 여지로 보존
- 모달·드롭다운 같은 작은 영역의 자체 반응형

### 0.5.2 컨테이너 구조 (HTML 뼈대)

```html
<div class="dashboard">
  <div id="sidebar-mount"></div>
  <main class="main-area">
    <div class="container">
      <header class="header">
        <h1 class="header-title">...</h1>
      </header>
      <!-- 페이지 콘텐츠 -->
    </div>
  </main>
</div>
```

### 0.5.3 사이드바

- `sidebar.js`가 자동 렌더링. 메뉴 수정은 `sidebar.js`의 `MENU_ITEMS` 한 곳.
- 창 너비 < 1400px이면 자동 collapse. 사용자 수동 토글 상태는 localStorage(`rnd-sidebar-collapsed`)로 보존.

### 0.5.4 회사 필터

- 회사 구분이 있는 페이지(인력·프로젝트·인건비 관련)에는 상단에 `.company-chips` 배치
- localStorage 키 `rnd-company-filter`로 전 페이지 공유
- 칩 형식: `.company-chip[data-company="식스티|굿뉴스|패리티|all"]`

### 0.5.5 향후 추가될 공통 룰 / 점검 항목

**룰 후보**: 토스트/알림 표시 룰, 모달 z-index 규칙, 빈 상태(empty state) 표현 일관성, 로딩 인디케이터 패턴

**언젠가 점검할 것 (지금은 안 건드림)**:
- **`min-width: 0` 사용처 점검** — 새 페이지 만들거나 수정할 때 케이스별로 결정.
  - ✅ flex/grid 자식 셀에서 텍스트 오버플로우 방지 (보존)
  - ❌ 페이지 메인 컨테이너나 카드에서 "마음껏 줄어들기" (제거 대상)
- ~~**공통 유틸 함수 추출**: `computeAge()`가 persons-master.js / persons-detail.js에 동일하게 중복 정의됨~~ → **v4에서 해결됨**: `persons-summary.js` 모듈로 추출. 자세한 내용은 §3.7 참조.
- **`.moonlight-badge` CSS 중복**: persons-master.html / persons-detail.html 두 페이지에 같은 정의가 인라인으로 박혀있음. 한 곳 바꿀 때 다른 곳도 같이. (v4 미해결)
- **`persons-mini-summary` CSS 중복** (v4 신규 점검 후보): persons-master.html / persons-detail.html에 동일 정의가 인라인. 향후 style.css로 추출 가능.

---

## 1. 시스템 구조

### 1.1 전체 페이지 맵

| 영역 | 페이지 | 상태 |
|---|---|---|
| **인력 관리** | **인력 대시보드 (`persons-dashboard`)** | ✅ **v4 신규 완료** |
| | 인력 마스터 (`persons-master`) | ✅ 완료 (v4에서 카드→한 줄 요약 정리) |
| | 인력 상세 (`persons-detail`) | ✅ 완료 (v4에서 카드→한 줄 요약 정리) |
| | 자격증 관리 (`certificates`) | ✅ 완료 |
| | 기업부설연구소 (`lab`) | ✅ 완료 (기존) |
| **인건비 관리** | 연차별 인건비 예산 (`project-budget`) | ✅ 본체 완료, 참여연구원 표 RTF 마무리 중 |
| | 월별 인건비 (`project-labor`) | 🔨 미구현 ← **다음 핵심 작업** |
| | 인건비 대시보드 (`labor-dashboard`) | 🔨 미구현 |
| **Legacy** | 인력 정보 관리 (`index.html`) | 🗑️ 폐기 예정 (기능은 인력 대시보드로 이관됨) |

### 1.2 사이드바 메뉴 구조

```
👥 인력 관리
   ├ 📈 인력 대시보드      ← 인원/회사별/청년/입퇴사/시점 조회 통합 (v4 신규)
   ├ 👥 인력 마스터        ← 명부 관리 (이름·생일·입퇴사·연봉)
   ├ 📑 인력 상세          ← 직급·학력·연구자번호·자격증·연락처
   ├ 🗃️ 자격증 관리        ← 자격증 전체 현황 (사람별 한 행, 가로 나열)
   └ 🔬 기업부설연구소

💰 인건비 관리
   ├ 💵 연차별 인건비 예산   ← project-budget
   ├ 💰 월별 인건비          ← project-labor (메인 입력)
   └ 📈 인건비 대시보드      ← labor-dashboard
```

**메뉴 순서 정책**: 인력 대시보드를 그룹 맨 위에 둠. 이유:
- 권한 범위가 가장 넓음 (B/C 권한도 접근 가능 — §1.8)
- "들어왔을 때 먼저 보이는 페이지" 역할
- 대시보드 → 상세 → 마스터 순서가 정보 흐름(요약→상세→편집)과 일치

### 1.3 회사 구분 시스템

식스티(모회사) / 굿뉴스 / 패리티(자회사) 3사 구분이 인력·프로젝트 양쪽에 적용.

- **인력**: `persons.company` 필드
- **프로젝트**: `projects.company` 필드
- **자회사가 본업**, 식스티는 모회사로서 추가 등록되는 형태 (겸직 정책 §1.5 참조)
- **색상 토큰** (style.css 전역):
  - 식스티: `#e0e7ff` / `#1e3a8a`
  - 굿뉴스: `#ffedd5` / `#9a3412`
  - 패리티: `#d1fae5` / `#065f46`
- **UI 컴포넌트**:
  - `.company-badge` + 회사별 modifier (`--sixty`/`--goodnews`/`--parity`/`--unset`)
  - `.company-chip[data-company="..."]` — 회사 필터 칩
- **필터 상태**: localStorage 키 `rnd-company-filter`로 전 페이지 공유
- **제약 룰**: 프로젝트 인력 추가 모달은 `proj.company === person.company`인 인력만 노출

### 1.4 데이터 모델 (실제 구조)

#### 1.4.1 `persons` 컬렉션

```js
{
  id, name,
  birthDate, hireDate, exitDate,
  isYouth,              // 청년 인력
  isNew,                // 신규/기존
  company,              // '식스티'|'굿뉴스'|'패리티'
  gender, ageBucket,
  annualSalary,         // 연봉
  monthlySalary,        // Math.ceil(annualSalary / 12)
  status,               // 'active'|'exited'
  memo,                 // 인력 마스터 메모 (이동/퇴사 정보)
  hrMemo,               // 인력 상세 메모

  // 인력 상세에서 관리:
  department,           // 소속
  position,             // 직급 (대표/책임/선임/연구원 등)
  finalDegree,          // '박사'|'박사수료'|'석사'|'석사수료'|'학사'|'학사재학'|'전문학사'|'기타'
                        // ※ 저장값은 붙임 형태('박사수료'), 화면 표시는 '박사(수료)'로 변환
  school,
  major,                // 학과/전공
  degreeNumber,         // 학위번호
  degreeDate,           // 학위 수여일
  researcherId,         // 연구자번호
  phone, email, address,
  certificates: [       // 자격증 배열
    { id, name, url, memo, createdAt }
  ]
}
```

#### 1.4.2 `projects` 컬렉션 (인건비 관련 필드)

```js
{
  id, projectName, keywords, status,
  company,                  // 식스티/굿뉴스/패리티
  laborManaged,             // 인건비 관리 대상 여부
  laborRefund,              // 인건비 환급 대상 여부
  requiredNew, requiredYouth,
  division1, yearBudgets,
  // ...
}
```

#### 1.4.3 `projectLabor` 컬렉션 — 월별 인건비

프로젝트당 3개 문서로 분리:

```js
// 1. 예상 계획
projectLabor/{projectId}_planned = {
  cells: {
    '{pid}_{ym}_{personId}': { rate, cash, inkind, memo }
  }
}

// 2. 실제 지급
projectLabor/{projectId}_actual = { cells: { /* 동일 */ } }

// 3. 메타  ← v5에서 확장
projectLabor/{projectId}_meta = {
  personIds: [...],

  // v5 신규: 인력의 과제 내 역할 (엑셀의 '기존/신규 × 현금/현물 × 세부라벨' 분류)
  personRoles: {
    '{personId}': {
      newOrExisting: '기존' | '신규',
      cashOrInkind:  '현금' | '현물',
      subRole:       '' | '청년 필수 1' | '청년 추가 2' | '기타 1' | ...  // 자유 입력
    }
  },

  // 월별 메타 (검수 흐름)
  //   v5 (초안): sysReg/amtConf 2개 boolean + 날짜 (sysRegAt/amtConfAt)
  //   v5.1 (개정): "확정" 단일 액션으로 통합 — confirmed/confirmedAt
  //     · 사용 워크플로우상 별도 단계로 나눌 필요 없다는 사용자 피드백 반영.
  //     · 기존 sysReg/amtConf/sysRegAt/amtConfAt 필드는 마이그레이션 후 보존만 (읽기 X).
  //   v5.2 (추가): 지급 완료 — paid/paidAt
  //     · 확정(예상→실제 복사)과 분리. 실제 송금이 일어난 시점을 별도로 기록.
  //     · 지급일은 사용자 입력 (오늘 자동 채움 + 수정 가능). 실제 송금이 지연되는 경우 대응.
  meta: {
    [ym]: {
      // v5.1 — 월 확정 (예상 → 실제 일괄 복사)
      confirmed:   boolean,
      confirmedAt: ISO date,

      // v5.2 — 지급 완료 (실제 탭의 별도 액션)
      paid:        boolean,
      paidAt:      ISO date,    // 사용자 입력 (기본값 오늘, 수정 가능)

      // (legacy v5 초안 — 마이그레이션 후 보존만)
      sysReg:    boolean,
      sysRegAt:  ISO date,
      amtConf:   boolean,
      amtConfAt: ISO date,
    }
  },

  // v5.2 신규: 예상 계획 스냅샷 이력 (예상 탭 한정)
  //   계획 변동이 잦은 정부 R&D 특성상 "당시엔 어떻게 계획했었는지" 추적이 필요.
  //   사용자가 명시적으로 "스냅샷 저장" 버튼을 누를 때만 생성 — 자동 저장 X.
  //   상세 정책은 §6.9 참조. 다음 세션 이후 구현 예정.
  snapshots: [
    {
      id:        'snap_xxxxx',
      label:     '2026 신청 시점' | '1차 변경신청 후' | ...,   // 사용자 입력 자유 텍스트
      createdAt: ISO date,
      cells:     { /* 스냅샷 당시 planned.cells 의 deep copy */ },
      personRoles: { /* 스냅샷 당시 personRoles 의 deep copy */ }
    }
  ]
}
```

**필드 명명 결정**: `newOrExisting`, `cashOrInkind`는 `projectBudget`의 동일 필드명을 그대로 따름. 두 컬렉션의 일관성을 위해 신규 명명 회피.

**`subRole` 정책**:
- 자유 텍스트 입력 (정부 규정에 따라 라벨이 바뀔 수 있어 enum 회피)
- `newOrExisting === '신규'`일 때만 표시·편집 가능
- 빈 문자열 허용 (신규지만 세부 라벨 안 쓰는 경우)

#### 1.4.4 `projectBudget` 컬렉션 — 연차별 예산

```js
projectBudget/{projectId}_year{N} = {
  rows: [
    {
      personId, personName,
      position, actualPay, monthlySalary,
      rate, participMonths,
      cashOrInkind, role, newOrExisting
    }
  ],
  period, budgetCash, budgetInkind
}
```

**v5 변경**: `projects.yearBudgets[year]`에 `carryover` 필드 추가 (이월금). v5에서는 **껍데기만** — 표시되되 입력 페이지와 다른 페이지 연동은 추후 결정. §6.X 참조.

```js
// projects 컬렉션의 yearBudgets에 추가
projects/{projectId}.yearBudgets = {
  year2: {
    total: 80000000,      // 기존: 예산 총액
    carryover: 0,         // v5 신규: 이월금. 기본 0. 입력 페이지 미정.
    // ...
  }
}
```

#### 1.4.5 `certificateSettings` 컬렉션 — 자격증 즐겨찾기 (신규)

자격증 관리 페이지의 칩 즐겨찾기 (전체 사용자 공유).

```js
certificateSettings/data = {
  favorites: ['정보처리기사', 'ADsP', ...],
  updatedAt
}
```

### 1.5 겸직 정책

같은 사람(이름+생년월일 동일)이 식스티 + 자회사 양쪽에 등록될 수 있음. 자회사가 본업, 식스티는 모회사로서 부가 등록.

**테이블 표시 규칙 (식스티 줄 기준, persons-master / persons-detail 공통)**:

| 케이스 | 상태 컬럼 | 이름 옆 | 행 색 |
|---|---|---|---|
| 식스티 재직 + 자회사 재직/퇴사 | 재직 (파랑) | `[겸직]` 뱃지 | 정상 |
| 식스티 퇴사 + 자회사 재직 | **겸직** (노란글자, 배경X) | 없음 | 정상 (회색 X) |
| 식스티 퇴사 + 자회사 퇴사 | 퇴직 (회색) | `[겸직]` 뱃지 | 회색 |
| 메모에 '이동' | 일반 처리 | 없음 | 일반 |
| 자회사끼리만 | 일반 | 없음 | 일반 (사실상 없는 케이스) |

**판별 키**: 이름 + 생년월일 (ssnTail 제거)
**행정 이관 판별**: 메모에 '이동' 문자열 포함 여부

**상세 패널 (persons-detail 한정 추가)**:
- 자회사 줄 클릭 시에도 `[겸직: 식스티]` 표시 (어디서도 등록되어 있나 알기 위함)
- 함수 분리:
  - `getMoonlightInfo()` — master 정책 그대로, **테이블용** (식스티 줄만)
  - `getCrossCompanyInfo()` — **상세 패널용** (자회사 줄에도 표시)

### 1.6 디자인 토큰

#### 색상
- 초록 `#3B6D11` — 안전/완료
- 주황 `#BA7517` — 주의 (90%↑)
- 빨강 `#A32D2D` — 위험 (100%↑)
- 파랑 `#185FA5` — 정보
- 노랑 `#854F0B` — 비교/차이
- 회색 `#5F5E5A` — 퇴사/미참여

#### 뱃지
- 청년 = 보라 `#3C3489`
- 기존 = 회색 `#5F5E5A`
- 신규·청년 = 파랑 `#0C447C`
- 활성 = 초록 `#3B6D11`
- 퇴사 = 빨강 `#791F1F`
- 겸직(`.moonlight-badge`) = 노란 그라데이션 `#fef3c7→#fde68a` / `#92400e`

#### 카드 배경
- 환급/긍정 = 연한 초록 `#EAF3DE`
- 인원/정보 = 연한 파랑 `#E6F1FB`
- 주의 = 연한 노랑 `#FAEEDA`
- 위험 = 연한 빨강 `#FCEBEB`

### 1.7 스크립트 로드 순서

**기본 패턴 (모든 페이지 공통)**:
```
firebase-* → firebase-config → firebase → auth-guard
  → firestore-service → sidebar → {page}.js → floating-buttons
```

**인력 관련 페이지 (persons-dashboard / persons-master / persons-detail)**:
```
firebase-* → firebase-config → firebase → auth-guard
  → firestore-service → sidebar
  → persons-summary.js                ← v4 신규: 공통 유틸 모듈
  → {persons-page}.js
  → floating-buttons
```

`persons-summary.js`는 `window.PersonsSummary` 글로벌 객체를 노출하며, 각 페이지 JS의 최상단에서 별칭으로 가져다 씀:
```js
var PS = window.PersonsSummary;
var computeAge   = PS.computeAge;
var getCompany   = PS.getCompany;
// ... etc
```

로드 누락 시 `throw new Error('window.PersonsSummary 가 로드되지 않았습니다 ...')`로 빠르게 실패하도록 가드 적용. 자세한 모듈 API는 §3.7 참조.

### 1.8 권한 정책 (v4 신설)

향후 도입할 페이지 권한 분리 정책. 현재(v4 시점)는 모든 사용자가 모든 페이지에 접근 가능하지만, 페이지 설계는 이 정책을 전제로 진행됨.

**3단계 권한 구분**:

| 권한 | 설명 | 예시 |
|---|---|---|
| **A** | 모든 페이지 접근 가능 (주민번호·급여 등 민감 정보 포함) | 대표·재무 |
| **B** | 일부 페이지 접근 가능 (민감 정보 페이지는 차단) | 일반 운영자 |
| **C** | 아주 소수 페이지만 접근 가능 | 외부 협력자 등 |

**페이지별 권한 매트릭스**:

| 페이지 | A | B | C | 민감도 | 비고 |
|---|:-:|:-:|:-:|---|---|
| 인력 대시보드 (`persons-dashboard`) | ✅ | ✅ | ✅ | 집계만 | 개인 식별 정보 없음 |
| 인력 상세 (`persons-detail`) | ✅ | ✅ | ❌ | 중 | 학력/연구자번호/자격증/연락처 |
| 인력 마스터 (`persons-master`) | ✅ | ❌ | ❌ | 고 | 주민번호·연봉 포함 |
| 자격증 관리 (`certificates`) | ✅ | ✅ | ❌ | 중 | 자격증 명부 |
| 기업부설연구소 (`lab`) | ✅ | ✅ | ❌ | 중 | 등록 명부 |
| 연차별 예산 (`project-budget`) | ✅ | ❌ | ❌ | 고 | 인건비 정보 |
| 월별 인건비 (`project-labor`) | ✅ | ❌ | ❌ | 고 | 인건비 정보 |
| 인건비 대시보드 (`labor-dashboard`) | ✅ | ❌ | ❌ | 고 | 인건비 정보 |

**룰**: "민감도 ↑일수록 접근 권한 ↑". 페이지 단위로 일관되게 적용 가능.

**설계 영향**:
- 요약 정보(인원 현황·청년·입퇴사 등)는 민감하지 않으므로 **인력 대시보드**에 모아둠. B/C 권한도 접근 가능.
- 주민번호·연봉이 포함된 마스터는 가장 엄격하게 차단(A만).
- 두 페이지(마스터/상세)에 같은 요약 정보를 중복 노출하지 않음 — 대시보드로 일원화.

---

## 2. 환급액 계산 로직

### 2.1 환급 대상 구분

| 구분 | 환급 | 비고 |
|---|---|---|
| 현금 (정부지원금) | ✅ | 가장 흔한 케이스, `cells.cash` |
| 현금 (자부담) | ❌ | 드물게 발생, `selfCash` (미정) |
| 현물 (회사부담) | ❌ | `cells.inkind` |

### 2.2 계산 공식

```
[월별·인력별]
환급 예정액 = planned.cells.{key}.cash
환급 확정액 = actual.cells.{key}.cash  (단, meta.{ym}.sysReg=true 일 때)

[월별·프로젝트별]
월 환급 예정 = Σ 모든 셀의 planned.cash (해당 ym)
월 환급 확정 = Σ 모든 셀의 actual.cash  (해당 ym, sysReg=true)

[연간·전 프로젝트]
연 환급 예정 = Σ 모든 프로젝트의 월별 환급 예정
연 환급 확정 = Σ 모든 프로젝트의 월별 환급 확정
```

### 2.3 자동 계산

```
참여율 입력 → 현금 자동 계산
예: monthlySalary 4,500,000 × rate 30% = 1,350,000
```

역계산은 미구현. 추후 검토.

---

## 3. 완료된 작업

### 3.1 공통 인프라

- ✅ §0.5 공통 원칙 (좁은 화면 대응 / 사이드바 / 회사 필터)
- ✅ 회사 구분 시스템 (식스티/굿뉴스/패리티)
- ✅ `firestore-service.js` 데이터 레이어 (persons / projects / labRegistrations / payroll 등 컬렉션)
- ✅ 사이드바 자동 렌더링 (`sidebar.js` MENU_ITEMS 한 곳에서 관리)

### 3.2 인력 마스터 (`persons-master`)

- ✅ 명부 CRUD (자유 입력 모달)
- ✅ 엑셀 일괄 다운로드/업데이트 (id 매칭, 빈 셀 유지)
- ✅ 월급 계산 `Math.ceil(annualSalary / 12)` 통일 + 자동 마이그레이션 (`_monthlySalaryMigrationDone`)
- ✅ 표 컬럼 정렬 (만나이/입사일/퇴사일/월급 헤더 클릭, 3단계 토글)
- ✅ 표 컬럼 너비 고정 (`table-layout: fixed`)
- ✅ 겸직 로직 (이름+생일 매칭, 메모 '이동' 키워드로 행정이관 처리)
- ✅ 메모 입력 힌트 ("회사 이동인 경우 메모에 '이동' 포함")
- ✅ **v4: 카드 영역 → 한 줄 요약 변경** — 기존 4개 카드(전체/재직/청년/퇴직)를 한 줄짜리 `.persons-mini-summary`로 압축. 우측에 작고 조용한 `📈 더 자세한 통계 →` 링크로 인력 대시보드 연결.
  - 카운트 기준: `_persons.length` 기반 (표 행 수). 겸직 unique 카운트는 대시보드에서 확인.
  - 호환성을 위해 `stat-youth`, `stat-total-sub` ID는 hidden input으로 보존.
- ✅ **v4: `persons-summary.js` 의존성 추가** — `computeAge`, `getCompany`, `getMoonlightInfo` 등을 모듈에서 가져옴. §3.7 참조.

### 3.3 인력 상세 (`persons-detail`)

- ✅ 좌측 테이블(리스트) + 우측 상세 패널 레이아웃 (`grid-template-columns: minmax(0, 2fr) minmax(460px, 1fr)`)
- ✅ 상세 패널 닫기 ✕ 버튼
- ✅ 상세 패널 "기본 정보" 섹션 (생년월일+만나이 / 입사일 / 퇴사일 — 마스터에서 관리되는 읽기 전용)
- ~~통계 카드 3개 (전체 인력 / 정보 입력 완료 / 연구소 등록)~~ → **v4에서 한 줄 요약으로 변경**
- ✅ 필터: 상태 / 회사 / 최종학위 / 연구소 등록
- ✅ 테이블 컬럼: `No | 회사 | 상태 | 이름 | 직급 | 최종학위 | 연구자번호 | 연구소 | 수정`
- ✅ 모달 수정 폼 (직급/소속/학력/연구자번호/연락처/자격증)
- ✅ 자격증 CRUD (이름/URL/메모, 자격증 1개당 1행)
- ✅ 기업부설연구소 자동 연동 (lab 등록부 최신 월 기준 ✓ 표시)
- ✅ 겸직 표시 (master 정책 + 자회사 줄에도 `[겸직: 식스티]` 추가 표시)
- ✅ 최종학위 처리:
  - 저장값: 붙임 형태 (`박사수료`, `석사수료`, `학사재학`)
  - 화면 표시: 괄호 형태 (`박사(수료)`)
  - 필터 묶음: 박사 선택 → 박사+박사수료, 석사 동일, 학사 동일
  - 필터 적용 시에만 (수료)/(재학)이 표 아래로 정렬
  - 자동 마이그레이션 (`_degreeNormalizationDone_v1`)
- ✅ 엑셀 일괄 다운로드/업데이트 (20개 컬럼, id 매칭, 자격증 콤마 분리)
- ✅ **v4: 카드 영역 → 한 줄 요약 변경** — master와 동일한 `.persons-mini-summary` 적용. "정보 입력 완료율"·"연구소 등록 수" 카드는 제거(전자는 불필요로 결정, 후자는 기업부설연구소 페이지에서 확인). 기존 ID들(`stat-hr-complete`, `stat-lab` 등)은 hidden input으로 보존.
- ✅ **v4: `persons-summary.js` 의존성 추가** — 단, `getCrossCompanyInfo`(자회사 줄 [겸직: 식스티] 표시용)는 detail 고유 함수이므로 그대로 보존.

### 3.4 자격증 관리 (`certificates`)

- ✅ 한 사람 = 한 행, 자격증 7개 가로 나열 (`table-layout: fixed`, 회사/이름 = 115px 같은 폭, 자격증 7개 균등분배)
- ✅ 모든 컬럼 중앙정렬, 긴 자격증명은 ellipsis + 툴팁
- ✅ 자격증별 보유자 수 칩 (전체 데이터 기준 집계, 같은 사람의 중복은 1명 카운트)
- ✅ 칩 즐겨찾기 ⭐ 토글 (Firestore `certificateSettings/data`에 저장, 전체 사용자 공유)
- ✅ 모드 토글 "⭐ 즐겨찾기만" / "전체"
- ✅ 회사 필터 / 검색 (이름 또는 자격증명)

### 3.5 연차별 인건비 예산 (`project-budget`)

- ✅ 본체 (인력 추가, 셀 편집, 예산 합계)
- ✅ 참여연구원 표 내보내기 모듈 (`project-budget-export.js`)
  - 표1 (정부 R&D 표준, 15컬럼, 2단 헤더, 그룹/세로 병합)
  - 표2 (인건비 계상 합계, 11컬럼)
  - 다운로드: 🌟 한글용 RTF / 📊 엑셀(.xlsx) / 📋 표 복사 (HTML+plain)
- ✅ 의존성: xlsx@0.18.5 CDN

### 3.6 인력 대시보드 (`persons-dashboard`) — **v4 신규**

기존 `index.html`(legacy)의 인력 정보 관리 화면에 있던 요약 정보를 흡수하고, master/detail 두 페이지에 중복되어 있던 카드 영역을 일원화한 통합 분석 페이지.

**구성**: 3단 레이아웃

**1단 — 인원 현황** (2줄):
- 상단 한 줄: `전체` · `재직` · `퇴직` (unique 기준, 한 사람 = 1명)
- 하단 한 줄: `식스티` · `굿뉴스` · `패리티` · `청년 인력` (회사별은 등록 레코드 기준, 단 식스티는 자회사 겸직자 제외)
- 회사별 카드는 좌측 보더로 회사 색상 적용 (`#4f46e5` / `#ea580c` / `#059669`)
- footnote: "* 건강보험 가입자명부 기준 · 식스티(모회사) 카운트에서 자회사 겸직자는 제외됨"

**2단 — 입·퇴사 + 시점 조회** (2분할):
- 좌측: 입·퇴사 현황 (올해 누적이 기본, 연도/월 선택 가능)
  - 큰 숫자 2개 (입사·퇴사) + 회사별 칩 (`+N / -N` 형식)
- 우측: 시점별 재직 인원 (오늘 기본 + 시점 조정)
  - 메인 큰 숫자 + 회사별 3개 분해
  - "다른 시점 조회" 컨트롤 + `[📌 오늘로]` 버튼
  - **겸직 포함 정책** (시점 조회만 별도 규칙 — 회사별 카드는 각 등록 레코드를 따로 셈)

**3단 — 청년 인력** (2분할):
- 좌측: 전체 청년 수 + 성별 분해
- 우측: 회사별 분포 (수평 막대 그래프, 회사 색상)

**카운팅 정책 (중요)**:

| 카드 | 기준 | 비고 |
|---|---|---|
| 전체/재직/퇴직 (1단 상단) | **unique** (이름+생일 동일 = 1명) | "어딘가 재직이면 재직" |
| 회사별 3사 (1단 하단) | **등록 레코드** | 식스티만 겸직 제외 적용 |
| 청년 인력 (3단) | unique (재직 인력 중) | 만 34세 이하 자동 판정 |
| 입사/퇴사 (2단 좌) | hireDate / exitDate 기반 | 회사별 분해 가능 |
| 시점별 재직 (2단 우) | **겸직 포함** (각 회사 레코드 따로 셈) | 사용자 요구 — 4대보험 가입자명부 관점 |

→ 1단 식스티 카드 수치와 2단 시점 조회 식스티 수치가 **다를 수 있음** (의도된 차이). 각 카드의 캡션에 기준 명시.

**상호작용**:
- 헤더 우측에 `[👥 인력 마스터]` `[📑 인력 상세]` 링크 (관련 페이지로 빠른 이동)
- 청년 카드 클릭 → master 청년 필터링 (계획, 미구현)

**페이지 권한**: A, B, C 모두 접근 가능 (§1.8 참조).

### 3.7 공통 유틸 모듈 (`persons-summary.js`) — **v4 신규**

인력 관련 페이지에서 공유하는 공통 유틸. `window.PersonsSummary` 글로벌로 노출.

**노출 API**:

| 카테고리 | 함수/상수 | 용도 |
|---|---|---|
| **날짜/나이** | `computeAge(birthDateIso, refDate?)` | 만나이 계산 |
| | `ageToBucket(age)` | 연령대 구간 (`'10'`~`'50'`) |
| | `parseDateSafe(str)` | 다양한 포맷 안전 파싱 |
| | `lastDayOfMonth(y, m)` | 월 말일 Date |
| | `formatDateIso(date)` | `'YYYY-MM-DD'` 형식 |
| **회사** | `COMPANIES`, `COMPANY_FULL_NAMES`, `COMPANY_BADGE_CLASS`, `COMPANY_COLORS` | 상수 |
| | `getCompany(person)` | 유효 회사 반환 (없으면 null) |
| | `renderCompanyBadge(company)` | 뱃지 HTML |
| **청년** | `getYouthInfo(person, refDate?)` | `{ youth: boolean }` |
| **겸직** | `getMoonlightKey(person)` | "이름\|생일" 키 |
| | `computeMoonlightMap(persons)` | 키 별 매핑 (`{persons, companies, isAdminTransfer}`) |
| | `getMoonlightInfo(person, map)` | 식스티 줄 겸직 정보 |
| **카운트** | `getEffectiveCounts(persons, opts?)` | 요약카드 통합 계산 |
| | `getJoinLeaveCounts(persons, year, month?)` | 입·퇴사 카운트 |
| | `getCountsAtDate(persons, date)` | 시점 재직 카운트 |
| **헬퍼** | `isActiveAnywhere(p, map)` | 겸직 포함 재직 판정 |
| | `getUniquePersons(persons, map)` | unique 인력 추출 |

**정책 메모**:
- 청년 판정은 **만 34세 자동** 만 사용 (`person.isYouth` 수동 체크는 v4부터 무시).
- 시점 조회의 퇴사일 처리: **퇴사일 당일까지는 재직** (`exit < refDate`일 때만 제외).
- `hireDate`가 없는 인력의 시점 조회: 보수적으로 "exitDate 없으면 모든 과거 시점에 재직 간주".
- `getMoonlightInfo`는 식스티 줄에서만 `isMoonlight=true` 반환 (자회사가 본업).

**의존하는 페이지**:
- `persons-dashboard.js` — 전체 사용
- `persons-master.js` — `computeAge`, `ageToBucket`, `getYouthInfo`, `getMoonlightInfo` 등 별칭으로 사용
- `persons-detail.js` — 동일 (단, `getCrossCompanyInfo`는 detail 고유라 페이지 내 보존)
- (향후) `labor-dashboard.js` — 인력 카운트 활용 가능
- (향후) `project-labor.js` — 겸직 처리 활용 가능

---

## 4. 남은 작업 로드맵

### Phase A. 월별 인건비 페이지 (`project-labor`) ← **다음 작업** (v5에서 사양 전면 개정)

**목적**: 사용자의 기존 운영 엑셀(`2024_V2G_에기평_2차년도.xlsx` 양식)을 디지털화한 핵심 입력 페이지. 엑셀 양식의 워크플로우를 가능한 그대로 흡수하는 것이 최우선.

**v4 대비 v5 변경점**:
- 분기 단위 이동 → **12개월 펼치기 + 분기 모드 토글** (하이브리드 뷰)
- 우측에 **sticky 박스 4개** 신규: 예산총액 / 이월금 / 누계 / 차액
- 인력 행에 **`[기존▼][현금▼] + subRole 자유텍스트`** 분류 입력
- 지나간 월은 **컬럼 폭 축소(접기)**, 클릭으로 펼치기
- 검수 메타에 날짜 기록 추가 (`sysRegAt`, `amtConfAt`)
- 셀 메모 UI 명시 (퇴사 사유, 참여율 조정 등) — v4 §6.2 보류 해소

#### A.1 상단 컨트롤바

- 회사 필터 칩 (`.company-chips`, `rnd-company-filter` 공유)
- 프로젝트 선택 드롭다운 (회사 필터 적용된 목록)
- 연도 선택
- **뷰 모드 토글**: `[전체 12개월] / [분기]` — v5 신규. 기본값 `전체 12개월`.
- 분기 모드일 때만 노출: `[◀ N분기 ▶]` 네비
- 이전 분기 복사 / 되돌리기
- 신규 채용 진척 표시

#### A.2 탭 (3개): 📝 예상 / ✅ 실제 / ⚖️ 비교

기존 그대로. 사용자 워크플로우상:
1. **연초 → 예상 탭**: 1년치 예상 입력 (12개월 펼치기 권장)
2. **수시 → 예상 탭 수정**: 입·퇴사·타과제 참여율 변경 시 예상 조정 (누계·차액 모니터링)
3. **월말 → 실제 탭**: 급여 지급 후 입력
4. **수시 → 비교 탭**: 예상 vs 실제 차이 확인

#### A.3 메인 영역: 본 테이블 + 우측 sticky 박스 (2단 레이아웃)

```
┌────────────────────────────────────────────────────────┬────────────────┐
│ [전체 12개월 / 분기] 토글                              │ 예산총액       │
├────────────────────────────────────────────────────────┤ 947,500,000원  │
│ 성명 | 구분 | 형태 | 월급 | 1 | 2 | 3 | ... | 12 | ... │ (↗예산 페이지)│
│                       (접힘)             (펼쳐짐)      │                │
│ 김종규 [기존▼][현물▼] 8.4M  · · · · ·40%·40%·30%·· ··  │ 이월금 (껍데기)│
│ 이도형 [신규▼][현금▼]                                  │ 0원            │
│         청년필수1   5.7M  ·  ·  · 25.02.20 퇴사  ··    │ ─────────────  │
│ ────────────────────────────────────────────────       │ 누계 (자동)    │
│ 소계                ·  ·  ·   45.5M  36.0M  54.8M  ·   │ 776,055,193원  │
└────────────────────────────────────────────────────────┤ 12개월 cash합  │
                                                          │ ─────────────  │
                                                          │ 차액 ★         │
                                                          │ 171,444,807원  │
                                                          │ (양수=초록)    │
                                                          └────────────────┘
```

**테이블 컬럼 구조**:

| 컬럼 | 내용 | 비고 |
|---|---|---|
| 성명 | 인력 이름 | 클릭 시 인력 상세로 |
| 구분 | `[기존▼][신규▼]` 드롭다운 | v5 신규 |
| 형태 | `[현금▼][현물▼]` 드롭다운 | v5 신규 |
| (subRole) | 신규일 때만 자유 텍스트 (예: "청년 필수 1") | 구분 아래 줄 또는 별도 |
| 월급 | 인력 마스터에서 자동 (수정 불가) | |
| 1월~12월 | 각 월별 `참여% / 현금 / 현물` 3종 | 접힌 월은 좁은 회색 컬럼 |
| (소계행) | 월별 합계 | 마지막 행 |

#### A.4 우측 sticky 박스 (4개)

| 박스 | 내용 | 계산식 | 색상 |
|---|---|---|---|
| 예산총액 | `projects.yearBudgets[year].total` | (읽기. ↗ 예산 페이지 링크) | 중립 |
| 이월금 | `projects.yearBudgets[year].carryover` | (v5 껍데기 — 0 고정 표시) | 중립 |
| 누계 (자동) | Σ planned.cells.cash (12개월 전체) | 자동 계산 | 중립 |
| **차액 ★** | 예산총액 + 이월금 − 누계 | 자동 계산 | **양수=초록(여유)/음수=빨강(초과)/0근처=중립** |

**sticky 동작**: 본 테이블을 가로 스크롤해도, 세로 스크롤해도 화면 우측에 항상 따라옴 (`position: sticky; top: 0;`).

**차액 색상 정책**:
- 0 이상이지만 적당히 큰 양수: 초록 (`#E1F5EE` 배경, `#085041` 텍스트)
- 0 미만(예산 초과): 빨강 (`#FCEBEB` 배경, `#791F1F` 텍스트)
- 0 근접(절댓값 ≤ 100,000): 중립 회색

#### A.5 12개월 펼치기와 월 접기

**기본 동작 (전체 12개월 모드)**:
- 1~12월 12개 컬럼 모두 표시
- 사용자가 클릭하지 않은 월은 기본적으로 **모두 펼쳐짐**
- 너비: 한 월당 약 100~120px (참여%/현금/현물 3 세로 한 칸)

**월 접기 UX**:
- 각 월 헤더에 작은 `−` 아이콘 → 클릭 시 해당 월 컬럼이 **좁은 회색 컬럼**(폭 ~22px)으로 축소
- 헤더에는 세로쓰기로 월 번호만 (`writing-mode: vertical-rl`)
- 접힌 컬럼 클릭 시 다시 펼침
- **상태 저장**: localStorage `rnd-pl-collapsed-months-{projectId}-{year}` (프로젝트·연도별 분리)
- **기본 정책**: 작업 빈도가 낮은 월(과거 월·미래 월)은 자동 접지 않음 — 사용자가 직접 토글. (자동 접기는 v5에서 미구현, 추후 검토)

**분기 모드 토글 시**: 현재 분기 3개월만 펼침, 나머지 9개월은 자동 접힘. `[◀ N분기 ▶]` 네비로 이동.

#### A.6 인력 분류 입력 (`personRoles`)

- 인력 추가 시: `newOrExisting` 기본 `기존`, `cashOrInkind` 기본 `현금`, `subRole` 빈 문자열.
- 두 드롭다운은 인라인 편집(클릭 시 즉시 셀렉트박스 활성화).
- `newOrExisting === '신규'`일 때만 `subRole` 입력란이 셀 아래에 나타남 (작은 폰트, placeholder: "청년 필수 1 / 청년 추가 2 / 기타 1 등").
- 같은 사람이 다른 과제에서는 다른 분류일 수 있음 — projectLabor 단위로 저장 (인력 마스터에는 저장하지 않음).
- 저장 시점: 드롭다운 변경 즉시 (디바운스 300ms).

#### A.7 셀 메모 UI (v4 §6.2 보류 해소)

엑셀의 셀 내 직접 메모(`25.06.02 퇴사`, `참여율 조정 필요 30%`, `육아휴직`, `과제 제외`, `250901~`)를 그대로 흡수.

**UI 방식**:
- 셀 우측 상단에 작은 빨간 점(`•`) → 메모 있을 때만 표시
- 셀 hover 시 우측에 메모 내용 툴팁으로 표시
- 셀 클릭 시 입력란 우측에 작은 `📝` 아이콘 → 클릭하면 메모 입력 팝오버
- 메모 있는 셀은 셀 배경이 연한 노랑(`#FAEEDA`)
- 빈 셀이지만 메모만 있는 경우 (예: "퇴사", "과제 제외") → 입력 못 하게 잠금 + 메모 텍스트 표시

**메모 형태 권장 (자유 텍스트지만)**:
- 날짜+상태: `25.06.02 퇴사`, `25.05.01 이동`
- 참여율 변경: `참여율 조정 70%`, `참여율 조정 필요 30%`
- 휴직/제외: `육아휴직`, `과제 제외`, `과제 제외(휴직)`
- 입사 시점: `250901~`

(자유 텍스트 형식. 추후 사용 빈도가 정착되면 enum화 검토.)

#### A.8 월 확정 (예상 → 실제 일괄 복사)

**v5 초안에서 v5.1로 개정**. 초안의 sysReg/amtConf 2개 체크박스 모델은 사용해보니 의미가 모호하다는 사용자 피드백 → "확정" 단일 버튼으로 통합.

```
[1월]    [확정]     ← 예상 탭의 월 헤더 우측
[1월]    [✓ 확정]  ← 실제 탭의 월 헤더 우측 (확정된 월)
[1월]              ← 비교 탭은 표시 없음
```

**동작**:
- 예상 탭에서만 "확정" 버튼 노출. 클릭 시 confirm 다이얼로그 → 예상값을 실제 탭으로 일괄 복사 → `meta[ym] = { confirmed: true, confirmedAt: ISO }`.
- 실제 탭은 `✓ 확정` 정적 뱃지로만 표시. 비교 탭은 표시 없음.
- 이미 확정된 월에 재클릭 시 경고(실제값 덮어쓰기) 후 다시 복사.
- hover 시 확정 일자 툴팁 표시.

**데이터 모델 호환성**: 기존 `sysReg/amtConf` 필드는 마이그레이션 후 보존만(읽기 안 함). 신규 데이터는 `confirmed/confirmedAt`만 사용.

#### A.8.5 지급 완료 (실제 탭) — **v5.2 신규**

월 확정과 분리된 별도 단계. 확정은 "계획이 실제로 통과되어 실제 탭에 반영된 상태"이고, 지급 완료는 "실제 돈이 회계상 송금된 상태"임. 정부 R&D 운영상 두 시점이 분리되는 경우가 많아 별개로 관리.

```
[1월]    [✓ 확정] [💸 지급]     ← 실제 탭 (확정 + 지급 완료)
[1월]    [✓ 확정] [지급 완료]    ← 실제 탭 (확정 후, 지급 전)
```

**동작**:
- **실제 탭에서만** "지급 완료" 버튼 노출. 예상 탭·비교 탭에는 노출 안 함.
- 클릭 시 지급일 입력 prompt (기본값 = 오늘, 사용자가 수정 가능) → `meta[ym] = { ..., paid: true, paidAt: '2026-04-15' }`.
- 지급일을 사용자 입력으로 둔 이유: 실제 송금이 며칠 늦어지거나 미리 입력하는 경우 둘 다 대응. "오늘" 그대로 두면 입력 시점이 그냥 박힘.
- 한 번 지급 완료된 월에 재클릭 시: "지급일 수정" 또는 "지급 취소" 선택 가능.
- 확정 안 된 월에 지급 완료 시도하면 경고(확정부터 하라는 안내). 또는 자동으로 확정 + 지급 묶음 처리.

**시각 표식**:
- 확정만 → `✓ 확정` 청록 뱃지
- 지급 완료 → `💸 지급` 초록 뱃지 (날짜 툴팁)
- 둘 다 → 두 뱃지 같이 표시
- hover 시 `확정 2026-03-30 / 지급 2026-04-15` 식 툴팁

**상태 머신**:
```
빈 상태 → 확정 → 지급 완료
         ↓
       (지급일 입력)
```

지급 완료를 취소하면 확정 상태로 돌아감. 확정을 취소하면 지급 완료도 같이 풀림(데이터 정합성).

#### A.9 액션 버튼

- `+ 인력 추가` — 회사 일치 인력만 노출 (기존 인력 추가 모달 유지)
- `📋 이전 분기 복사` — 기존 동작 유지
- `↩️ 되돌리기` — 토스트 표시
- `⚡ 일괄 자동 계산` — 모든 셀 cash = monthlySalary × rate 재계산
- `📥 엑셀 업로드` — v5 신규. 사용자 운영 엑셀 직접 import. 별도 매핑 모달.
- `📤 엑셀 다운로드` — v5 신규. 현재 화면을 엑셀로.
- `📸 스냅샷 저장` — **v5.2 신규**. 예상 탭에서만 노출. 현재 예상 계획 + personRoles를 스냅샷으로 저장. 라벨 입력 prompt. 상세는 §6.9 + Phase H.

#### A.10 키보드 조작

기존 그대로 유지 (Tab/화살표/Ctrl+C/V/Z/Esc).

#### A.11 100% 초과 차단

기존 그대로 유지. 다른 프로젝트 참여율 합산 확인 → 빨간 셀 + 저장 차단.

#### A.12 0건일 때 안내

기존 그대로 유지. "이 과제는 ○○ 명의라 ○○ 소속만 추가할 수 있습니다"

#### A.13 작업 단계 (Step 1~7)

§8 작업 순서 가이드에서 세분화. 요약:

1. **Step 1** — 데이터 모델 보강 (Firestore 마이그레이션 함수만, UI 변경 없음)
2. **Step 2** — 12개월 펼치기 + 우측 sticky 4박스 (뷰 모드 토글 포함)
3. **Step 3** — 월 접기/펼치기 인터랙션
4. **Step 4** — 인력 분류 드롭다운 + subRole 자유 텍스트
5. **Step 5** — 셀 메모 UI
6. **Step 6** — 엑셀 업로드/다운로드 매핑
7. **Step 7** — 사용자 운영 엑셀 1개 실제 입력 테스트

각 Step 완료 후 사용자 검수.

### Phase B. 합산 참여율 (월 기준) — 인력 대시보드 확장

**전제**: Phase A 완료 후 진행 (월별 데이터 필요)

**v4 변경**: v3에서는 "인력 마스터/상세 합산 참여율" 이었으나, v4에서 권한 정책 정리 후 **인력 대시보드의 새 섹션**으로 자리 잡는 게 자연스러워짐. 권한 B/C도 접근해야 의미가 있고, master는 입력 페이지 성격이므로 분석 정보를 또 넣지 않음.

**기능**:
- 인력 대시보드에 "기준 월" 셀렉터 추가
- 새 섹션 4단: "월별 참여율 분포"
  - 인력 목록 + 해당 월의 모든 프로젝트 참여율 합
  - 색깔 코딩: 초록(<80%) / 주황(≥90%) / 빨강(=100%)
- 요약 카드: "참여율 ≥ 90%", "참여율 = 100%", "참여 0%" 자동 집계
- master 표에는 컬럼 추가하지 않음 (월별 데이터는 빠르게 변하므로 명부 페이지에는 부적합)

### Phase C. 인건비 대시보드 (`labor-dashboard`)

**상단 요약 카드 (4개)**
1. 회사 전체 인력: `N명 (활성 X · 퇴사 Y)`
2. 과제 등록 인원: `X / N (P%)`
3. 여유 가용 참여율: `XXX%` ← 핵심
4. 100% 참여 인원: `N명`

**섹션 (기본 6개 + 7번째 후보)**
1. **월별 환급 추이** — 연한 파랑=예상 / 진한 파랑=실제
2. **프로젝트별 환급 진척** — 막대 (받은 금액 / 목표 환급액)
3. **인력 참여율 (현재 월)** — 정렬+색깔 코딩 + 회사 뱃지 인라인
4. **신규 채용 필수 인력 진척** — 표 + 회사 칩 카운트 뱃지
5. **회사 인력 활용도** — 좌: 숫자 / 우: 월별 추이
6. **액션 아이템** — 긴급/주의/알림 4단계

**7번째 섹션 후보 (작업 시점에 결정)**:
- A. 회사별 분해 (식스티/굿뉴스/패리티 비교)
- B. 연차별 예산 vs 실제 (`projectBudget` 활용)
- C. 참여연구원 표 일괄 출력 (`project-budget-export.js` 확장)

### Phase D. 인력 상세 보완 작업

- 자격증 모달 콤마 분리 입력 (현재는 엑셀 업로드에서만 분리 지원)
- 모달 학위 select 라벨이 `박사(수료)` 형태로 노출되는지 재확인
- 인력별 월별 급여 이력 그래프 (`salaryHistory` 채택 시)

### Phase E. v2 로드맵 후속 작업 (확정)

1. **타 사업 인건비계상률(B) 자동화** — 통합 대시보드 기반. 인력별 모든 프로젝트 참여율 합산해서 표 출력 시 자동 주입.
2. **참여기간 인력별 오버라이드 UI** — `rnd-pb-export-period` localStorage 키는 준비됨. UI 추가.
3. **회사 칩 카운트 뱃지** — `.company-chip-count` CSS 준비됨. 칩 옆에 해당 회사 인력/프로젝트 수 표시.
4. **대시보드 참여율 진척표 회사 뱃지 인라인** — Phase C 섹션 3·4에 통합.

### Phase F. v1에서 이월된 보완 작업

1. **채우기/반복** (드래그, Ctrl+D, 우클릭 "이전 달과 같음")
2. **엑셀 입출력** (project-labor에서. project-budget은 이미 있음)
3. **PDF 출력**
4. **기존 데이터 마이그레이션** (구글 스프레드시트 → 새 시스템)

### Phase G. legacy `index.html` 폐기 (v4 신규)

v4에서 인력 대시보드가 인력 정보 관리 기능을 흡수했으므로, `index.html`은 단계적으로 폐기.

**현재 상태**:
- `index.html`은 인력 정보 관리 / 인건비 관리 / 참여율 관리 / 캘린더 4개 페이지를 SPA 라우팅으로 묶고 있음
- 인력 정보 관리(`#/hr`) 기능은 인력 대시보드로 이관 완료
- 나머지 3개(`#/payroll`, `#/participation`, `#/calendar`)는 아직 사용 중

**작업 단계**:
1. 기능 동등성 검증: 인력 대시보드가 index의 인력 정보 관리 기능을 모두 커버하는지 사용자 확인
2. 사이드바에서 "인력 정보 관리" 메뉴 제거 (`index.html#/hr` 링크)
3. 나머지 기능(payroll/participation/calendar)을 별도 페이지로 분리하거나 신규 페이지 도입
4. index.html 자체 폐기

**주의**: 이 작업은 사용자 검증 후 별도 세션에서 진행 (이번 v4 세션 범위 밖).

### Phase H. 예상 계획 스냅샷 이력 (v5.2 신규)

**전제**: Phase A 완료 후. `meta.snapshots[]` 데이터 모델은 v5.2에서 등록 완료.

**범위**:
- 예상 탭에 `📸 스냅샷 저장` 버튼 추가 (§4 Phase A.9)
- 우측 sticky 또는 상단에 `📚 이력 N개 [열어보기]` 표시
- 스냅샷 비교 모달 — 좌측 과거 / 우측 현재 나란히
- 스냅샷 삭제 (수정은 불가)
- (옵션) 셀별 차이 하이라이트

**상세 사양**: §6.9 참조. 작업 진입 시 미결정 항목들(차이 하이라이트, 복원 버튼, 권한 등) 확정 필요.

**작업량 추산**: 1-2 세션. 모달 UI가 가장 무거운 부분.

---

## 5. 알려진 한계 (참여연구원 표 출력 시 임시 처리)

- 국적 → "한국" 고정
- 타 사업 인건비계상률(B) → 0 고정
- 내·외부 구분 → "내부" 고정
- 참여기간 인력별 오버라이드: localStorage 키 `rnd-pb-export-period` 준비만 됨, UI 미구현

---

## 6. 미해결 이슈 / 결정 보류

### 6.1 셀 스키마 — 자부담 현금 (selfCash) **상태: 여전히 미정**

- v1 기획: `plannedSelfCash` / `actualSelfCash` 필드 + 자부담 토글 UI
- 현재 cells 맵: `{rate, cash, inkind, memo}` — selfCash 없음
- **v5 시점 결정**: project-labor 구현 시점이 임박했음에도, 사용자 운영 엑셀(`2024_V2G_에기평_2차년도.xlsx`)에서 자부담 케이스가 관찰되지 않음 → **v5 Step 1~6에서는 미반영**. Step 7(실 데이터 입력 테스트) 결과에 따라 필요시 추가.
- 추가 시 영향 범위: project-labor UI(자부담 토글), 환급액 계산 공식, 환급 대상 구분 표

### 6.2 셀 메모 vs 인력 이동 메모 **상태: v5에서 해소**

- v1 기획: "인력 이동 메모(빨간 메모)" — 예: "굿뉴스 이동", "25.04.30 퇴사"
- v4까지: 셀의 `memo` 필드 하나 — UI 미설계
- **v5 결정**: §4 Phase A의 A.7에서 메모 UI 명시. 빨간 점 표시 + hover 툴팁 + 셀 배경 노랑.

### 6.3 인건비 대시보드 7번째 섹션 **상태: 후보 명시 + 작업 시 결정**

§4 Phase C 참조. 후보 A/B/C 중 작업 시 선택.

### 6.4 `salaryHistory` (연봉 변경 이력) **상태: 보류**

- **지혜님 의도**: "변경사항 입력할 때까지는 기존 정보 유지, 변경되어도 과거 데이터는 안 바뀌어야 함"
- **현재 상태**: 방안 3 (단순 덮어쓰기, 보류) 선택. 모든 인력에 첫 연봉을 채워넣은 후 다시 결정
- **채택 시 데이터 모델**:
  ```js
  persons.salaryHistory = [
    { effectiveFrom: '2025-01-01', annualSalary: 50000000, monthlySalary: 4166667 },
    { effectiveFrom: '2026-03-01', annualSalary: 54000000, monthlySalary: 4500000 }
  ]
  ```

### 6.5 v1 §10에서 이월된 결정 사항

- [x] **권한 관리: 지혜님만 입력? 다른 사람도?** → v4에서 정책 결정 (§1.8 A/B/C 3단계). 실제 권한 분리 시스템 도입은 별도 작업.
- [ ] 모바일 대응 범위
- [ ] 데이터 마이그레이션 방식
- [ ] "회사 인력" 정의: 인력 마스터 등록자 = 회사 인력? 사무직/대표는?
- [ ] 인건비 환급 신청 일정 → 일정 관리 페이지 연동 여부
- [ ] 수행 현황 페이지에 "인건비" 컬럼/탭 추가 여부
- [ ] 프로젝트별 인건비 예산 한도 설정 / 초과 시 처리

### 6.6 v1에서 언급된 확장 가능성 (장기)

- 예측 시뮬레이션 (다음 분기 환급액 예측)
- 자동 알림 (이메일/슬랙: 100% 도달, 미입력 등)
- 청년 인력 비율 자동 검증 (정부 규정)
- 국적 필드 추가 (현재 "한국" 고정)
- isExternal 필드 추가 (현재 "내부" 고정)

### 6.7 v4·v5에서 해결된 사항 (참고용)

이전 버전에서 결정 보류였던 항목 중 v4·v5에서 정리된 내용. 다른 작업에 참고용으로 기록.

| 항목 | 이전 상태 | 결정 | 버전 |
|---|---|---|---|
| `computeAge` 등 공통 유틸 중복 | 점검 후보 (§0.5.5) | `persons-summary.js`로 추출 완료 | v4 |
| 청년 판정 정책 | 자동(만34) + 수동(isYouth) OR | **자동만**. 수동 isYouth는 미사용 (재추가 가능). | v4 |
| 페이지 권한 정책 | 결정 보류 (§6.5) | A/B/C 3단계 정의 (§1.8). 실제 구현은 별도 작업. | v4 |
| 회사 미지정 필터 | 옵션 존재 (master/detail) | **제거 예정** — 운영상 미지정 케이스 없음. 마스터 모달에서 회사 입력 필수. | v4 |
| 카운트 통일 정책 | 페이지마다 제각각 | **명문화**: unique 기준(전체/재직/퇴직) vs 등록 레코드 기준(회사별, 식스티는 겸직제외) vs 겸직 포함(시점 조회). §3.6 참조. | v4 |
| index.html 폐기 | 미정 | **계획 수립**: Phase G로 등록. 검증 후 별도 세션. | v4 |
| 인력 페이지에 요약 카드를 둘지 | 미정 (v3는 master 4개·detail 3개) | **카드 제거 + 한 줄 요약 + 대시보드 링크**. 자세한 분석은 인력 대시보드로 일원화. | v4 |
| project-labor 월별 표시 범위 | 분기 단위만 (v3·v4) | **하이브리드 뷰**: 12개월 펼치기(기본) + 분기 모드 토글 + 월 접기 (§4 Phase A.5) | **v5** |
| project-labor 우측 sticky 박스 | 미정 | **신규**: 예산총액 / 이월금(껍데기) / 누계(자동) / 차액★(색상코딩) (§4 Phase A.4) | **v5** |
| 인력 분류 (기존/신규 × 현금/현물 × 세부) | 데이터모델 없음 | `projectLabor/_meta.personRoles` 신설, projectBudget의 필드명(`newOrExisting`, `cashOrInkind`)과 통일. subRole은 자유 텍스트 (§1.4.3, §4 Phase A.6) | **v5** |
| 셀 메모 UI (§6.2) | 보류 | **해소**: 빨간 점 + hover 툴팁 + 셀 배경 노랑 (§4 Phase A.7) | **v5** |
| 검수 메타 날짜 기록 | 미정 (sysReg/amtConf boolean만) | **`sysRegAt`/`amtConfAt` 자동 기록**, hover로 일자 표시 (§4 Phase A.8) | **v5** |
| 이월금 (carryover) 필드 | 없음 | `projects.yearBudgets[year].carryover` 추가 — **v5에서는 껍데기만**. 입력 페이지·다른 페이지 연동은 추후 결정. (§1.4.4) | **v5** |
| 검수 메타 모델 (v5.1 개정) | v5에서 sysReg/amtConf 2단계 | **"확정" 단일 액션으로 통합**. `meta[ym] = { confirmed, confirmedAt }`. 실사용 시 2단계가 의미 모호하다는 사용자 피드백 반영. legacy 필드 마이그레이션 후 보존. (§4 Phase A.8) | **v5.1** |
| 비교 탭 컬럼 구성 (v5.1) | 한 월 5컬럼 | **환급 있으면 3컬럼(%/현금/현물), 없으면 1컬럼**. 각 행에 [예상]/[실제]/[차이] 라벨. 두번째 고정 컬럼 "월급"→"구분". | **v5.1** |
| 인력 분류 UI 위치 (v5.1) | 별도 컬럼 가정 | **인력 컬럼 내 인라인 드롭다운**. [기존/신규][현금/현물], 신규 시 ↳ subRole 자유 텍스트. 색 분리(기존=슬레이트/신규=인디고/현금=그린/현물=앰버). 자동계산 분기는 cashOrInkind 따름. | **v5.1** |
| 셀 input 포맷팅 (v5.1) | type=number, 콤마 없음 | **type=text + inputMode=numeric + dataset.raw**. 참여율 `10%`, 금액 `837,533`. focus/blur로 raw ↔ 표시 전환. | **v5.1** |
| 지급 완료 메타 (v5.2) | 없음 | **신규**: 실제 탭에 "지급 완료" 버튼. 지급일은 사용자 입력(기본 오늘). `meta[ym].paid`, `meta[ym].paidAt`. 확정과 별개. (§4 Phase A.8.5) | **v5.2** |
| 예상 계획 스냅샷 (v5.2) | 없음 | **신규**: 명시적 "스냅샷 저장" 버튼. `snapshots[]` 배열에 시점 저장. 평상시 UI 보존(별도 모달에서 비교). 데이터 모델만 v5.2에 등록, 구현은 Phase H 분리. (§6.9) | **v5.2** |

### 6.8 기존 운영 엑셀 양식 분석 (v5에서 추가)

**대상 파일**: `2024_V2G_에기평_2차년도.xlsx` — 식스티의 2024년도 V2G 에기평 과제(2차년도) 인건비 운영 양식.

**구조 요약**:

- 한 파일 = 한 시트 = 한 과제 (`'03. 인건비'`)
- 시트 내 **두 블록**: 상단(실제 사용) + 하단(예상 계획) 구조
- 각 블록 헤더:
  - 컬럼 그룹: `구분(직접비/인건비) | 기존/신규 | 현금/현물 | 성명 | 급여총액 | 실지급액 | 참여율 | 참여개월 | 1월~12월 (현금/현물 각 1열씩) | 누계(현금/현물) | 예산총액 | 이월금 | 차액`
  - 메타 헤더(실제 블록만): `집행 완료 일자 | 시스템 등록 일자 | 금액 확인 완료 일자` (월별로 입력)
- 데이터 행:
  - 인력별 1행
  - "신규" 행은 `청년 필수 1/2/3`, `청년 추가 1~5`, `기타 1~3` 같은 세부 라벨이 컬럼에 박혀있음
  - 셀 안에 직접 메모: `25.06.02 퇴사`, `참여율 조정 필요 30%`, `육아휴직`, `과제 제외`, `250901~`, `12.15(퇴사) 반영`
- 소계·합계 행: 시트 하단 (각 월의 현금/현물 합계, 누계 등)

**시스템과의 매핑**:

| 엑셀 양식 | 시스템 필드 |
|---|---|
| `기존/신규` 컬럼 | `personRoles[personId].newOrExisting` (v5 신규) |
| `현금/현물` 컬럼 | `personRoles[personId].cashOrInkind` (v5 신규) |
| `청년 필수 1`, `기타 2` 등 세부 라벨 | `personRoles[personId].subRole` (v5 신규, 자유 텍스트) |
| 성명 | 인력 마스터의 `name`으로 매칭 |
| 급여총액(연봉) | 인력 마스터의 `annualSalary` |
| 실지급액(월급) | 인력 마스터의 `monthlySalary` |
| 참여율 | `cells[key].rate` |
| 참여개월 | 자동 계산 (cells에서 rate>0 인 월 수) 또는 명시 입력 |
| 월별 현금/현물 | `cells[key].cash` / `cells[key].inkind` |
| 셀 내 메모 (`25.06.02 퇴사` 등) | `cells[key].memo` (v5 UI 신설) |
| 누계 (현금+현물) | 자동 계산 (Σ cells.cash) |
| 예산총액 | `projects.yearBudgets[year].total` |
| 이월금 | `projects.yearBudgets[year].carryover` (v5 껍데기) |
| 차액 | 자동 계산 (예산총액 + 이월금 − 누계) |
| 집행 완료 일자 (월별) | `meta[ym].sysRegAt` 또는 별도 필드 — 추후 결정 |
| 시스템 등록 일자 (월별) | `meta[ym].sysRegAt` (v5 신규) |
| 금액 확인 완료 일자 (월별) | `meta[ym].amtConfAt` (v5 신규) |

**무시 결정**:
- 시트 하단 합계 행 R47/R48의 외로운 숫자 두 개 (12월 현금/현물 위치에 `57,129,093` / `57,128,493`) — 사용자 확인 결과 "무시해도 되는 값". 시스템에 반영 안 함.

**워크플로우 요약** (사용자 설명):
1. 연초에 예상 내역 입력 → 입·퇴사·타과제 시작 시 참여율 조정 (예상 탭에서 수시 수정, 누계·차액 항상 확인)
2. 월말 급여 지급 후 실제 사용 내역 등록
3. 예상 환급액 vs 실제 환급액 비교 (비교 탭)
4. 기존엔 두 사람이 관리(A 입력 / B 확인)했으나 v5 시점부터 한 사람이 전체 관리 — v5.1에서 "확정" 버튼 1개로 통합.

### 6.9 예상 계획 스냅샷 (v5.2 신규, 구현은 Phase H)

**배경**: 정부 R&D 과제는 신청 시점 계획 → 1차 변경 신청 → 2차 변경 신청 식으로 계획이 자주 바뀜. 변경 후 정산·감사 시 "당시엔 어떻게 계획했었는지" 추적이 필요. 현재 시스템은 최신 계획만 보관하므로 과거 계획이 덮어쓰여짐.

**사용자 원래 제안**: 기존 계획을 그대로 복사해서 화면 아래에 또 하나의 표로 두고(접기 가능), 위쪽 표만 수정. 화면에 여러 버전이 쌓이는 구조.

**검토 결과**:
- 사용자 본인도 "이게 정말 좋은지 잘 모르겠다"고 명시. 평상시 화면에 여러 표가 쌓이는 부담을 우려.
- 표가 쌓이면 스크롤/인지 부하 증가. 인력 5-10명 × 12개월 표가 두세 개면 화면이 매우 무거워짐.
- 또한 "현재 유효한 버전이 어느 것인지" 매번 다시 확인해야 함.

**결정 (v5.2)**: 별도 모달 + 명시적 스냅샷 모델로 채택.

```
┌─ 예상 탭 ─────────────────────────────────────────┐
│ [+ 인력 추가] [📋 이전 분기 복사] [📸 스냅샷 저장]  │   ← v5.2 신규 액션
│ ─────────────────────────────────────────────── │
│                                                  │
│   [현재 예상 계획 표 — 평소처럼 깨끗하게 한 개]   │
│                                                  │
│ ─────────────────────────────────────────────── │
│ 📚 이력 3개  [열어보기]                           │   ← 우측 sticky 또는 상단
└──────────────────────────────────────────────────┘

[열어보기 클릭 → 별도 모달]
┌─ 스냅샷 이력 ────────────────────────────────────┐
│ ◯ 2026-01-15  "신청 시점"        [현재와 비교]   │
│ ◯ 2026-03-22  "1차 변경 후"      [현재와 비교]   │
│ ◯ 2026-05-10  "전월 마감 시점"   [현재와 비교]   │
│                                                  │
│ [선택한 스냅샷 표 좌측, 현재 표 우측 — 나란히]   │
└──────────────────────────────────────────────────┘
```

**핵심 정책**:
- **명시적 저장만**: "스냅샷 저장" 버튼 클릭 시에만 생성. 자동 저장 X. 사용자가 의식적으로 변동 시점을 표시할 때만.
- **라벨 자유 입력**: 저장 시 라벨 prompt (예: "1차 변경 후", "이도형 합류 전"). 빈 라벨도 허용(타임스탬프만).
- **편집 불가**: 스냅샷은 읽기 전용. 잘못 저장했으면 "삭제"만 가능, 수정 X. 수정 가능하면 "당시 계획"의 신뢰성 깨짐.
- **personRoles 같이 저장**: 인력 분류도 변동 가능하므로 스냅샷에 같이 deep copy.
- **실제 탭은 스냅샷 X**: 실제값은 한 번 입력되면 사실(fact)이고, 변경 시 그냥 수정. 스냅샷 개념 부적합.
- **저장 위치**: `projectLabor/{projectId}_meta.snapshots[]` (§1.4.3 참조).
- **용량 관리**: 한 스냅샷이 보통 인력 10명 × 12개월 × 4필드 = 480셀 정도. 텍스트로 10-30KB. 한 과제에 누적 10-20개면 부담 적음. 단, 100개 넘어가면 경고 + 오래된 것 정리 안내.

**구현 단계**:
- v5.2: 데이터 모델만 등록 (§1.4.3). 코드 작업 X.
- 다음 Phase: Phase H ("예상 스냅샷 이력") — UI/UX 별도 단계로 분리.
- 실제 작업 진입 시 추가로 결정할 항목:
  - 스냅샷 비교 모달 — 셀별 차이 하이라이트할지? 표 단순 나란히만?
  - 스냅샷에서 현재로 "복원" 버튼 둘지? (취소 흐름)
  - 다운로드 시 어느 스냅샷을 내릴지 선택?
  - 권한: 누구나 저장 vs 관리자만?

**대안 검토(폐기됨)**:
- (가) 셀 단위 변경 이력 — 너무 무거움, Notion 스타일. 폐기.
- (나) 자동 일 단위 스냅샷 — 의미 없는 데이터 누적. 폐기.
- (다) 사용자 원안(표 쌓기) — UI 부담 큼. 위 결정안으로 대체.

### 6.10 인력 추가 모달 — 정보 표시 확장 (v5.2 신규, 미구현)

**배경**: 현재 `project-labor`의 인력 추가 모달은 인력 행에 `이름 + 월급` 만 표시. 인력을 선택할 때 "이 사람이 다른 과제에 얼마나 참여 중인지" 알 수 없어, 추가 후 100% 초과 차단에 걸려서야 알게 됨.

**개선안**: 모달 인력 행에 다음 정보 추가
- **현재 참여율**: 타과제 포함 전체 합산 참여율 (현재 월 또는 사용자가 선택한 기준 월)
- **참여 과제 수**: 해당 인력이 현재 참여 중인 과제 개수
- 색깔 코딩 (`Phase B`의 색 규칙 차용): `<80%` 초록 / `≥90%` 주황 / `=100%` 빨강

**예시 UI**:
```
김종규  4,500,000/월  현재 75% (2개 과제)  [+ 추가]
이도형  3,800,000/월  현재 100% (3개 과제) ⚠️ [+ 추가]
박소연  4,200,000/월  현재 30% (1개 과제)  [+ 추가]
```

**기술적 고려사항**:
- 합산 참여율 계산은 `Phase B`의 "월별 참여율 분포" 로직 차용 (모든 프로젝트의 `projectLabor/{}_planned.cells` 를 인력 ID로 합산)
- 모달 열 때마다 전 프로젝트의 cells를 훑어야 하므로 비용 있음. 캐시 고려 (계산 결과를 `state._participationCache[ym]`에 저장, 셀 변경 시 무효화)
- 기준 월: 일단 "오늘이 속한 월"로 시작. 추후 모달에 월 선택 셀렉터 추가 검토.

**전제**: Phase B(합산 참여율) 작업과 같이 진행하거나, Phase B 후에 진입. Phase B 없이 단독 진행도 가능 (계산 로직만 재사용).

**작업량**: 1세션. 캐시 정책이 가장 무거운 부분.

### 6.11 프로젝트별 신규 인력 판단 기준 (v5.2 신규, 미구현)

**배경**: 정부 R&D 과제마다 "신규 채용" 인정 기준이 다름.

**관찰된 케이스**:
- 케이스 A: **공고일 기준 6개월 전 채용**부터 신규로 인정
- 케이스 B: **공고일 기준 3개월 전 채용**부터 신규
- 케이스 C: **협약일 이후 채용**된 인력만 신규 (이전 채용은 기존)
- 케이스 D: 정해진 기준 없음 (지금 시스템 동작)

**현재 시스템 한계**:
- `persons.isNew` boolean 1개로만 관리
- 인력 마스터에 등록할 때 수동으로 체크
- **프로젝트별로 다른 기준이 안 통함** — 같은 인력이 과제 A에선 신규, 과제 B에선 기존일 수 있는데 일괄 적용
- 결과: 사용자가 매번 머릿속으로 계산해서 `personRoles.newOrExisting`을 수동 설정

**개선안**: 프로젝트 단위로 신규 판단 기준 등록 → 인력 추가 시 자동 판정

**데이터 모델 안 (확정 전)**:
```js
// projects 컬렉션에 추가
projects/{projectId} = {
  // ...
  newPersonCriteria: {
    type:       'announcement' | 'agreement' | 'none',    // 공고일 기준 / 협약일 이후 / 기준 없음
    referenceDate: 'YYYY-MM-DD',                          // 공고일 또는 협약일
    monthsBeforeRef: 6 | 3 | 0,                           // 공고일의 N개월 전부터 인정 (type='announcement'일 때만)
    // type='agreement' → referenceDate 이후 hireDate만 신규
    // type='announcement' → referenceDate 의 monthsBeforeRef 개월 전 ~ 이후 hireDate가 신규
    // type='none' → 자동 판정 안 함 (사용자 수동 설정)
  }
}
```

**자동 판정 로직 (예시)**:
```js
function judgeIsNewForProject(person, project) {
  var c = project.newPersonCriteria;
  if (!c || c.type === 'none') return null;       // 자동 판정 X (수동)
  if (!person.hireDate) return null;
  var hire = new Date(person.hireDate);
  var ref  = new Date(c.referenceDate);
  if (c.type === 'agreement') {
    return hire >= ref;                            // 협약일 이후 채용 = 신규
  }
  if (c.type === 'announcement') {
    var cutoff = new Date(ref);
    cutoff.setMonth(cutoff.getMonth() - c.monthsBeforeRef);
    return hire >= cutoff;                         // 공고일의 N개월 전부터 = 신규
  }
  return null;
}
```

**UI**:
- 프로젝트 등록/수정 페이지에 "신규 인력 판단 기준" 섹션 추가
  - 라디오 3택: 공고일 기준 / 협약일 이후 / 기준 없음
  - 공고일 기준 선택 시: 공고일 date + "N개월 전부터" 숫자 입력
  - 협약일 선택 시: 협약일 date
- `project-labor` 인력 추가 모달의 인력 행에 자동 판정 결과 표시
  - `자동: 신규` 인디고 뱃지 / `자동: 기존` 슬레이트 뱃지 / `판정 불가 (hireDate 없음 또는 기준 미설정)`
- 인력 추가 시 `personRoles.newOrExisting` 에 자동 판정값 채움 (사용자가 드롭다운에서 덮어쓸 수 있음)

**미결정 항목**:
- 인력 마스터의 `persons.isNew`와의 관계: 자동 판정이 있으면 마스터의 `isNew`는 의미가 약해짐. 둘 다 보존할지, 마스터 필드 deprecation할지.
- 공고일/협약일이 `projects` 컬렉션 어디에 들어가야 하는지 (현재 `projects` 스키마에 그런 필드 없음 — 신설 필요)
- 협약일 이후 입사인데 공고일 기준으로는 신규 아닌 경우 — 사용자 의도가 어느 쪽인지

**전제**: 사용자가 실제 운영하는 과제들의 기준 사례를 더 모은 후 데이터 모델 확정. 우선순위 낮음(현재는 수동 드롭다운으로 커버 가능).

**작업량**: 2세션. 프로젝트 페이지 + project-labor 모달 양쪽 수정 + 자동 판정 로직.

---

## 7. v1에서 살아남은 핵심 결정 사항 (v5 갱신)

| 항목 | 결정 | 적용처 |
|---|---|---|
| 인력 관리 방식 | 자유 입력 + 자동 매칭 | persons-master ✅ |
| 예상/실제 화면 | 탭 구분 (예상/실제/비교) | project-labor (예정) |
| 월별 레이아웃 | **v5 변경**: 12개월 펼치기(기본) + 분기 모드 토글 + 월 접기. 기존 "분기 단위 이동만"에서 확장. | project-labor (예정) |
| **우측 sticky 박스** (v5 신규) | 예산총액 / 이월금 / 누계 / 차액 4박스 — 항상 보이도록 | project-labor (예정) |
| **인력 분류** (v5 신규) | 기존/신규 + 현금/현물 + subRole 자유 텍스트 (`personRoles`) | project-labor (예정) |
| 환급액 계산 공식 | 월급 × 참여율 | 유효 |
| 참여율 100% 초과 | 입력 차단 | project-labor (예정) |
| 환급 구분 | 현금(정부)=환급 / 현금(자부담)=환급X / 현물=환급X | selfCash 자체가 미정이므로 보류 |
| 회사 전체 인원 | 인력 마스터에서 자동 집계 | persons-master ✅ |
| 퇴사 처리 | 흔적 남김. 지급된 달은 회색 처리 안 함. **v5 추가**: 셀 메모로 사유 기록 (예: `25.06.02 퇴사`) | project-labor (예정) |
| 시스템 등록/금액 확인 | 월별 컬럼 헤더 체크박스. **v5 추가**: `sysRegAt`/`amtConfAt` 자동 일자 기록 | meta.{ym}.sysReg/amtConf로 데이터 모델 준비됨 |
| **엑셀 입출력** (v5 신규) | persons-master 패턴 차용 (id 매칭, 빈 셀 유지). 양방향(다운로드 템플릿 + 업로드) | project-labor (예정, Step 6) |

---

## 8. 작업 순서 가이드 (다음 세션 진입 시) — v5 갱신

### Phase A 세부 단계 (v5에서 7단계로 명문화)

```
Phase A. 월별 인건비 페이지 (project-labor) ← 최우선

Step 1. 데이터 모델 보강 (UI 변경 없음)  ✅ 완료 (v5)
   - projectLabor/{projectId}_meta 스키마에 personRoles 추가
   - meta.{ym}.sysRegAt / amtConfAt 추가
   - projects.yearBudgets[year].carryover 필드 (껍데기, 0 기본값)
   - 기존 데이터 마이그레이션 함수 (있는 경우, 안전한 기본값 채움)

Step 2. 12개월 펼치기 모드 + 우측 sticky 4박스  ✅ 완료 (v5)
   - 상단 [전체 12개월 / 분기] 토글
   - 우측 sticky 박스: 예산총액 / 이월금 / 누계(자동) / 차액★(색상)
   - 분기 모드는 기존 동작 보존 (호환성)
   - 누계·차액 자동 계산 로직

Step 3. 월 접기 / 펼치기 인터랙션  ✅ 완료 (v5)
   - 각 월 헤더에 − 아이콘 → 클릭 시 좁은 회색 컬럼으로 축소
   - localStorage: rnd-pl-collapsed-months-{projectId}-{year}
   - 분기 모드 토글 시 자동 접힘 (현재 분기 외 9개월)

Step 3.5. 사용자 피드백 1차 반영  ✅ 완료 (v5.1)
   - 검수 메타 재구성: 체크박스 2개 → "확정" 버튼 1개
   - 비교 탭 컬럼 축소 (5→2, v5.2에서 다시 3컬럼 복구) + 행 라벨 [예상]/[실제]/[차이]
   - 비교 탭 금액 표시 fmtMoneyFull로

Step 4. 인력 분류 드롭다운 (personRoles)  ✅ 완료 (v5.1)
   - 인력 셀 내 인라인 드롭다운 [기존/신규][현금/현물]
   - 신규 선택 시 ↳ subRole 자유 텍스트
   - 자동계산 분기: cashOrInkind에 따라 cash/inkind 중 적절한 필드로
   - 컬럼 폭 재조정 (월급 잘림 해소)

Step 4 후속. 사용자 피드백 2차 반영  ✅ 완료 (v5.1)
   - 비교 탭 차이행 현물 컬럼 복구 (환급 있을 때 3컬럼)
   - 참여율 % / 금액 콤마 + 우측 정렬 (type=text + dataset.raw)
   - personRoles 색 분리 (기존/신규, 현금/현물)
   - 탭 간 personRoles 동기화 버그 수정
   - "차이만 보기" 토글 제거

Step 4.5. 지급 완료 메타 (v5.2 신규)  ← **다음 세션 진입점**
   - 실제 탭 월 헤더에 "지급 완료" 버튼
   - 클릭 시 지급일 prompt (기본 오늘, 수정 가능)
   - meta[ym].paid, meta[ym].paidAt 저장
   - 시각: ✓ 확정 / 💸 지급 뱃지 분리 표시
   - 확정 ↔ 지급 완료 상태 머신 (§4 Phase A.8.5 참조)

Step 5. 셀 메모 UI (§4 Phase A.7)
   - 메모 있는 셀: 빨간 점 + 셀 배경 노랑
   - hover 툴팁 / 클릭 시 입력 팝오버
   - 빈 셀이지만 메모만 있는 경우 (예: "퇴사") 입력 잠금

Step 6. 엑셀 업로드 / 다운로드 매핑
   - persons-master 패턴 차용 (xlsx@0.18.5 CDN)
   - 다운로드: 현재 화면 → 엑셀 (사용자 양식 매핑)
   - 업로드: id 매칭(이름+생일) → cells / personRoles / meta 일괄 갱신
   - 빈 셀 유지 (부분 업데이트)
   - 미매칭 인력은 모달로 사용자 확인

Step 7. 사용자 운영 엑셀 1개 실제 입력 테스트
   - 2024_V2G_에기평_2차년도.xlsx 또는 유사 엑셀
   - 누계 / 예산총액 / 차액이 엑셀 합계와 일치하는지 검증
   - 확정/지급 완료 흐름 운영 가능성 확인
   - 필요 시 selfCash, 그 외 추가 결정 사항 도출

→ 여기까지 Phase A.
→ 그 다음 Phase B (합산 참여율 월 기준 — v4부터 인력 대시보드 섹션으로 위치 변경).
→ 그 다음 Phase C (인건비 대시보드).
→ Phase H (예상 스냅샷 이력)는 별도 진입 — Phase A 완료 후 사용자 우선순위 따라.
```

### v5 작업 후 다음 세션 권장 진입점

v4·v5 직후 우선순위:

1. **Step 1 데이터 모델 보강** — 코드 변경 작아 안전. Step 2 이후 작업의 전제.
2. **Step 2 12개월 펼치기 + 우측 sticky** — 사용자 워크플로우 임팩트가 가장 큰 변화.
3. **Step 3~5 순차 진행** — 사용자 검토 후 다음 Step.
4. **Step 6 엑셀 입출력** — 실 데이터 입력의 핵심.
5. **Step 7 실 데이터 테스트** — 1개 과제로 끝까지 입력 후 결정 보류 항목(§6.1 selfCash 등) 재검토.

다른 작업(인력 대시보드 보강, Phase G index.html 폐기 등)은 Phase A 완료 후 별도 세션.

---


## 9. 변경 이력

| 일자 | 내용 | 작성자 |
|---|---|---|
| 2026-05-25 | v1 초안 (기획 세션) | 정지혜 + Claude |
| 2026-05-26 | v2 작성. 페이지 4→5, 회사 구분 시스템, 참여연구원 표 모듈, projectLabor 구조 변경, projectBudget 신규 반영. 미해결 이슈 명시. | 정지혜 + Claude |
| 2026-05-26 | §0.5 공통 원칙 신설. 11개 페이지에 박혀있던 `.container` 인라인 일괄 제거. 좁은 화면 대응 정책 3원칙 확립. | 정지혜 + Claude |
| 2026-05-26 | persons-master 작업: 엑셀 일괄 다운로드/업데이트, 월급 ceil, 표 정렬, 겸직 로직 재설계. | 정지혜 + Claude |
| 2026-05-26 | persons-detail 작업: 닫기 버튼, 기본 정보 표시, 통계카드/필터에서 연구자번호 제거, 컬럼 정리, 겸직 표시(자회사 포함), 최종학위 정규화/필터 묶음, 엑셀 다운로드/업데이트, 패널 폭 비율화. | 정지혜 + Claude |
| 2026-05-26 | certificates 작업: 한 사람=한 행 가로 나열(7컬럼 고정 균등분배), 칩 즐겨찾기 ⭐ Firestore 저장(`certificateSettings/data`), 모드 토글. | 정지혜 + Claude |
| 2026-05-26 | **v3 작성**. 완료/남은 작업 명확 분리(§3 vs §4). 인력 관리 영역 통합(§1.1 페이지 맵 확장). 결정 보류 사항에 salaryHistory 추가(§6.4). 모듈 책임 분리 명시(`getMoonlightInfo` vs `getCrossCompanyInfo`). | 정지혜 + Claude |
| 2026-05-27 | **v4 작성**. 인력 대시보드(`persons-dashboard`) 신규 도입 — 인원 현황·회사별·청년·입퇴사·시점 조회 통합. master/detail의 카드 영역을 한 줄 요약으로 정리(§3.2·§3.3). 공통 유틸 모듈 `persons-summary.js` 추출(§3.7). 청년 판정 정책 단순화(자동만). 권한 정책 §1.8 신설(A/B/C 3단계). index.html 폐기 계획 Phase G 추가. 카운트 로직 통일(§3.6에 명문화). | 정지혜 + Claude |
| 2026-05-27 | **v5 작성**. Phase A 구현 직전 사용자 운영 엑셀(`2024_V2G_에기평_2차년도.xlsx`) 분석 후 사양 전면 개정. 화면 구조 변경: 12개월 펼치기(기본) + 분기 모드 토글 + 월 접기 인터랙션, 우측 sticky 4박스(예산총액·이월금·누계·차액★). 데이터 모델 보강: `personRoles`(기존/신규×현금/현물×subRole), `meta.{ym}.sysRegAt/amtConfAt`, `projects.yearBudgets[year].carryover`(껍데기). 셀 메모 UI 명시(§4 Phase A.7) — v4 §6.2 보류 해소. Phase A 작업을 Step 1~7로 명문화(§8). 엑셀 입출력을 §7 핵심 결정에 추가. §6.8에 운영 엑셀 양식 분석 결과 보존. | 정지혜 + Claude |
| 2026-05-27 | **v5.1**. Phase A Step 1~4 구현. 사용자 피드백 반영하여 검수 메타 모델 개정 (sysReg/amtConf 2단계 → "확정" 단일 액션, `meta[ym]={confirmed,confirmedAt}`). 비교 탭 컬럼 축소(5→2→다시 3)와 행 라벨([예상]/[실제]/[차이]). 인력 분류 UI 인라인 드롭다운 + subRole, 색 분리(기존/신규, 현금/현물). 셀 input `type=text + dataset.raw` 기반 포맷팅(참여율 `10%`, 금액 `837,533`). 자동계산 분기는 `personRoles.cashOrInkind` 따름. "차이만 보기" 토글 제거. 컬럼 폭 재조정. | 정지혜 + Claude |
| 2026-05-27 | **v5.2 작성**. Phase A Step 4 완료 후 운영 워크플로우 추가 요구사항 2건 정리. (1) 실제 탭에 "지급 완료" 버튼 + 지급일 입력 — `meta[ym].paid/paidAt` 추가. 확정과 별개 단계로 분리 (§4 Phase A.8.5). (2) 예상 계획 스냅샷 — `snapshots[]` 배열 데이터 모델 등록, 명시적 저장 + 별도 모달 UI 정책 결정(§6.9). 구현은 Phase H로 분리. v5.2에서는 사양만 확정, 코드 작업 X. | 정지혜 + Claude |
| 2026-05-27 | **v5.2 후속 (Step 4.5/4.6 구현 + 추가 사양)**. 구현 완료: Step 4.5 지급 완료 메타, Step 4.6 가로 채우기(우클릭+Ctrl+R, 참여율 1월→12월), 확정 취소 동작 변경(재확정 → 확정취소+실제셀삭제), 인력 행 ✕ 삭제 버튼, 인력 추가 모달 퇴사자 토글, 확정/지급 뱃지 색 분리(청록/에메랄드). 미구현 등록: §6.10 인력 추가 모달 정보 확장(현재 참여율·참여 과제 수 표시), §6.11 프로젝트별 신규 인력 판단 기준(공고일/협약일 기반 자동 판정). | 정지혜 + Claude |

---

*문서 끝*
