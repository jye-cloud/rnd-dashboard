# 개발 정리 · 테스트 가이드 (인건비 시스템 v8)

> 이 문서는 작업하면서 **누적 기록**합니다. 최종 테스트는 이 문서 기준으로 진행.
> 표기: ✅ 구현 완료 / 🧪 테스트 필요 / ⏳ 다음에 구현(보류) / ⚠️ 알아둘 점
> 검증 단계: 정적검증(문법·태그균형·ID정합)까지는 끝, **실화면 동작은 사용자 확인**.

---

## 묶음 4 — 인건비 대시보드 재설계 + 채용 의무 (§3.5)

**건드린 파일:** `labor-dashboard.html` / `labor-dashboard.js` / `project-detail.html` / `project-detail.js` / `persons-master.js`

### 무엇을 개발했나

1. **상단 카드 5개 재구성** (`labor-dashboard`)
   - 재직 인력 / 과제 등록 인원(등록N·재직N·%) / 가용 인력(미참여·여유 분리) / 100% 도달 / 채용 의무(채용M·필수N + 남음·청년·유지위험)
   - 돈 카드 없음. 우측에 "환급 현황 → 자금 관리" 링크.

2. **채용 의무 표** (신규 — `labor-dashboard`)
   - 과제별 행: 과제(키워드) / 필수(일반·청년) / 채용(현재 머릿수·청년) / 유지 기한(D-day) / 상태
   - 상태: ✅충족 · 🔴미달 · ⚠️위험(유지기한 전 신규 퇴사 예정으로 미달 위험) · ✅유지완료(기한 충족 → 경고 해제)
   - 연간 합계줄: 올해 필수N · 채용M · 남음K · 청년 M'/N' · 유지위험
   - 판정 근거: 신규 = 과제별 `personRoles.newOrExisting==='신규'` & 재직 / 청년 = 만 34세 자동(`getYouthInfo`) / 등록 시작월 = 그 과제 첫 참여율(>0) 월 1일 / 유지기한 = 시작월 + 유지기간(기본 12개월)

3. **할 일 / 점검** (구 액션 아이템 재초점 — `labor-dashboard`)
   - `[분류] 설명 + 바로가기` 형태. 분류: 채용의무 / 확정 / 입력
   - 채용의무 미달·위험, 미확정(환급 묶임, **confirmed 기준** — 구 sysReg 정정), 인건비 미입력
   - 100% 도달은 카드에 있으므로 할 일에서 제외

4. **청년 판정 일원화** (`persons-master.js`)
   - `getYouthInfo` = 만 34세 이하 **자동만** (수동 isYouth OR 조합 폐기). persons-summary.js와 통일.

5. **채용 의무 정의 입력** (`project-detail`)
   - 필수 일반 / 필수 청년 / 유지 기간(개월, 기본 12) — 저장: `requiredNew`, `requiredYouth`, `requiredRetention:{months}`
   - **인건비 관리 대상 체크와 무관하게 항상 표시되는 독립 블록** (4열 그리드 아래 전체폭)

6. **제거/이관**: 월별 환급 추이·프로젝트별 진척·인력 참여율 명단·회사 인력 활용도 (돈 지표는 자금 관리로). Chart.js 로드 제거.

### 🧪 테스트 체크리스트 (묶음 4)

**A. 인건비 대시보드 — 상단 카드**
- [ ] 카드가 5개 뜬다 (재직 / 등록 / 가용 / 100% / 채용 의무)
- [ ] 가용 카드에 "미참여 N · 여유 N"이 두 숫자로 나뉘어 보인다
- [ ] 등록 인원 = "등록N / 재직N", 보조에 "전체 재직 중 X%"
- [ ] 기준 월(상단 셀렉트) 바꾸면 가용/100% 숫자가 그 달 기준으로 바뀐다
- [ ] 회사 칩(전체/식스티/굿뉴스/패리티) 바꾸면 인원·과제가 그 회사로 좁혀진다

**B. 채용 의무 표**
- [ ] 과제명 자리에 **키워드**가 뜬다 (← 방금 수정, "-" 아님)
- [ ] 필수/채용/청년 숫자가 과제 수정에서 넣은 값과 맞는다
- [ ] 채용(현재) = 그 과제 "신규" 분류 재직자 수 (프로젝트별 인건비에서 신규/기존 분류와 대조)
- [ ] 유지 기한 = 첫 참여월 + 유지기간, D-day 표시
- [ ] 상태 배지가 충족/미달/위험/유지완료 중 맞게 뜬다
- [ ] 합계줄: 올해 필수·채용·남음·청년 (채용·청년은 min(현재,필수)로 캡)

**C. 할 일 / 점검**
- [ ] 미확정 월(실제 입력 있는데 확정 안 함) → "[확정] … 환급 N 묶임" + 바로가기
- [ ] 인건비 미입력 과제 → "[입력] …" + 바로가기
- [ ] 채용 의무 미달/위험 과제 → "[채용의무] …" + 바로가기
- [ ] 바로가기 클릭 시 해당 과제 프로젝트별 인건비로 이동
- [ ] 점검할 항목 없으면 "✅ 점검이 필요한 항목이 없습니다"

**D. 과제 수정 — 채용 의무 입력**
- [ ] "인건비 관리 대상" 체크 안 해도 채용 의무 블록이 보인다
- [ ] 값 입력→저장→재진입 시 값 복원
- [ ] 유지 기간 비우고 저장 → 재진입 시 12로 채워짐
- [ ] 필수 비우고 저장 → 빈칸 복원 (0 강제 안 함)
- [ ] 채용 의무와 무관하게 기존 과제 저장/수정 정상

**E. 청년 판정**
- [ ] 인력 마스터에서 청년 필터/카운트가 만 34세 기준으로 동작 (수동 체크와 무관)

### ⚠️ 알아둘 점 (묶음 4)
- **합계 "채용 0명"인데 행은 "10/0"**: 필수가 0이면 채울 게 없어 합계 기여 0. 정상.
- **청년 판정 수동 체크박스**: persons-master 폼/엑셀의 `isYouth`는 데이터 호환 위해 남겨뒀으나 **판정에 영향 없는 비활성 입력**. (제거 여부는 추후 결정)
- **화면 세부 디자인은 다음에 잡기로 함** — 지금은 기능/숫자 위주로만 테스트.


### 보강 (묶음 4 후속)
- ✅ 과제명 자리 = **키워드** 우선 표시 (과제명 너무 길어서). "-" 버그 수정.
- ✅ 필수/채용 표시 = **일반·청년 동등** 노출 (`일반 0 · 청년 3`, `일반 10/0 · 청년 3/3`). 청년이 회색 괄호에 묻히던 것 수정.
- ⚠️ 일반/청년 필수는 **독립 버킷**으로 카운트(일반 0 + 청년 3 = 청년만 3명 의무). 충족 판정도 각각 비교. (청년이 일반의 부분집합인지 여부는 필요 시 추후 확정)
- ℹ️ **할 일/점검 바로가기** = 각 줄 우측 "바로가기 →" → 그 과제 `project-labor.html`(프로젝트별 인건비)로 이동. 표의 키워드도 동일 링크. 점검 항목 0개면 "✅ … 없습니다"만 표시(정상).

- ✅ **버그수정**: `renderAll`이 `renderTodoChecks()`를 부르는데 함수명이 `renderActionItems`로 남아 호출 실패 → 할 일/점검이 "분석 중…"에서 멈추던 문제. 함수명 통일로 해결. (정적검증으로 못 잡는 실행시점 에러 — 실화면 확인이 잡음)

### ⏳ 다음에 구현 (이번엔 안 한 것)
- **채용 의무 — 비(非)인건비 과제 모니터링**: 정의 입력은 모든 과제 가능해졌으나, 대시보드 표/판정은 아직 인건비 관리(`laborManaged`) + 수행 과제만 대상. 인건비 관리 안 하는 과제는 저장만 되고 표엔 안 뜸.
- **채용 의무 — 기준일 소스**: 현재 "등록 시작월(첫 참여율 월)" 고정. **공고일 / 협약일 / 과제 시작일** 등 다른 기준은 미구현. 비인건비 과제는 참여율 셀이 없어 별도 기준일(수동 입력)이 필요 → 위 모니터링 확장과 묶어서 결정 예정.
- **신규 판정 자동화 (§4.3 / C4)**: 현재 "신규/기존"은 수동(`personRoles.newOrExisting`). 규칙 기반 자동 판정은 모델 확정 후. → 자동화되면 채용 의무 "채용" 숫자 신뢰도 ↑.
- **funding 연말 run-rate 예측선** (§3.5 step6, 선택·우선순위 낮음).

---

## 묶음 5 — 인력 대시보드 재설계 (§3.6, persons-dashboard)

**건드린 파일:** `persons-dashboard.html` / `persons-dashboard.js`

### 무엇을 개발했나
1. **회사 칩 필터** 추가 (전체/식스티/굿뉴스/패리티) — `rnd-company-filter` 공유(인건비 페이지들과 동일), 페이지 전체 스코프.
2. **상단 한 행에 동일 박스 3개**:
   - 인력 현황 — 재직 N명(메인) + 성별 + 회사별(전체 보기에서만). 재직 중심.
   - 입·퇴사 현황 — 입사 · 퇴사 + 회사별 +/− + **월별 입퇴사 막대**(선택 연도 12개월, 의존성 없는 div 막대, hover 시 커스텀 툴팁으로 입/퇴 표시).
   - 시점별 재직 인원 — 오늘/임의 시점, 회사별(전체 보기에서만).
3. **제거**: 청년 카드·청년 상세 섹션·1단 전체/퇴직 개별 카드·데이터 품질 블록. (청년은 인건비 대시보드로)
4. 단일 회사 선택 시 그 회사로 스코프 + 3사 분해 생략.
- `persons`만 사용, 모델 변경 0. 청년 자동 판정과 정합.

### 🧪 테스트 체크리스트 (묶음 5)
- [ ] 상단에 박스 3개가 한 행으로 뜬다 (인력 현황 / 입퇴사 / 시점별)
- [ ] 회사 칩(전체/3사) 바꾸면 세 박스 숫자가 그 회사로 스코프된다
- [ ] 단일 회사 선택 시 회사별 분해(3사 줄)는 사라지고 그 회사 숫자만
- [ ] 입퇴사: 연/월 바꾸면 입사·퇴사 갱신
- [ ] 월별 막대: 선택 연도 12개월, 초록=입사·빨강=퇴사, 막대에 마우스 올리면 즉시 툴팁(입/퇴)
- [ ] 시점별: [조회]로 과거 시점 재직, [📌 오늘로] 복귀
- [ ] 청년 카드·청년 섹션이 더 이상 없다
- [ ] 회사 칩 선택이 인건비 대시보드 등 다른 페이지와 공유되는지(localStorage)

### ⚠️ 알아둘 점 (묶음 5)
- 화면 세부 디자인(박스 정렬·막대 크기 등)은 다음에. 지금은 숫자·동작 위주.
- 월별 막대는 Chart.js 없이 div로 그림(의존성 0). 막대 높이는 그 해 최대 월값 기준 상대 스케일.

### ⏳ 다음에 구현
- (해당 없음 — §3.6은 모델 변경 0으로 이번에 마무리)

## 묶음 6 — 과제별 상세 (§3.2) — (예정, 사양 미정)
## C4 — 신규 인력 자동 판정 (§4.8 C4)

**건드린 파일:** `project-detail.html` / `project-detail.js` / `project-labor.html` / `project-labor.js`

### 무엇을 개발했나
1. **규칙 설정 (과제 수정)** — 채용 의무 블록 아래 "신규 인력 자동 판정 규칙": 기준일 드롭다운(과제 시작일 / 공고일) + (공고일 선택 시) 공고일 date 입력 + N개월. 저장: `announceDate`, `newJudgeRule:{baseDateType, months}`. 개월 비우면 자동판정 안 함.
2. **판정 헬퍼 (project-labor)** — 신규 = 입사일 > (기준일 − N개월). 컷오프 당일은 기존. 기준일 = 공고일 또는 첫 연차 시작일. 입사일 없으면 미상. 규칙 없으면 판정 안 함.
3. **인력 추가 모달** — 후보마다 신규/기존/미상 라벨, 신규/기존/전체 필터 버튼, "↻ 일괄 재판정"(현재 규칙으로 추가된 인력 전체 newOrExisting 재계산). 규칙 있는 과제에서만 필터 바 노출.
4. **추가 시 기본값** — 추가(단건·일괄)하면 `newOrExisting` 기본값 = 판정 결과(미상/규칙없음 → 기존). 사용자가 구분 드롭다운으로 바꿀 수 있음.
- 데이터: `projects`에 `announceDate`+`newJudgeRule` 추가. 셀/키 변경 0. 미설정 과제는 자동판정 안 함(수동 유지).

### 🧪 테스트 체크리스트 (C4)
- [ ] 과제 수정: 기준일 드롭다운, 공고일 선택 시 공고일 칸 노출, N개월 입력 → 저장 → 재진입 복원
- [ ] 기준일='과제 시작일'이면 공고일 칸 숨김
- [ ] 프로젝트별 인건비 인력 추가 모달: 후보에 신규/기존/미상 라벨이 뜬다(규칙 설정한 과제에서)
- [ ] 신규/기존/전체 필터 버튼이 후보를 거른다
- [ ] 인력 추가 시 구분(신규/기존) 기본값이 판정 결과로 들어간다
- [ ] "↻ 일괄 재판정" → 추가된 인력 전체 신규/기존 재계산, 토스트로 N명 재판정(미상 제외) 표시
- [ ] 규칙 없는 과제: 필터 바 안 보이고, 추가 시 기본 '기존', 라벨 없음
- [ ] 채용 의무(인건비 대시보드) "채용 머릿수"가 이 판정과 맞물려 신뢰도 ↑

### ⚠️ 알아둘 점 (C4)
- 일괄 재판정은 **추가된 인력의 newOrExisting을 덮어씀**(현재 규칙 기준). 수동으로 바꿔둔 값도 재판정으로 바뀌니 주의(버튼 누를 때만 동작).
- 메인 인건비 표 이름 옆 legacy `신규` 배지(인력 단위)는 그대로. 과제별 신규/기존은 구분 드롭다운이 정답.
- 컷오프 월 차감은 일 단위(말일 오버플로 시 근사). 보수적으로 `>` 비교(컷오프 당일=기존).

### ⏳ 다음에 구현
- 채용 의무 디커플링: C4로 **공고일**이 생겼으니, 채용 의무 기준일을 공고일로 바꾸는 것 + 비인건비 과제 모니터링 범위 결정 가능해짐.

## 채용 의무 디커플링 (결정 반영)

**결정:** ① 비인건비 과제 모니터링 = **안 함**(인건비 관리 과제만). 필요 누적 시 추후. ② 유지 기한 기준일 = **과제 시작일**(첫 연차 startDate, 없으면 등록 시작월 폴백).

**건드린 파일:** `labor-dashboard.js`
- 채용 의무 유지 기한 = (과제 시작일) + 유지기간. 기존 "등록 시작월(첫 참여율 입력 월)"은 과제 시작일 없을 때만 폴백.
- 채용 머릿수 카운트는 변경 없음(그 과제 신규 분류 + 재직 인력 전원, 시작일 무관하게 전부 카운트).

### 🧪 테스트 (추가)
- [ ] 채용 의무 표의 "유지 기한"이 과제 시작일 + 유지기간으로 뜬다(D-day)
- [ ] 인력이 중간에 들어와도 채용 머릿수에 다 잡힌다(시작일 기준일과 무관)
- [ ] 과제 시작일이 비어있으면 등록 시작월로 폴백, 그것도 없으면 "미시작"

### ⏳ 다음에 구현
- 비인건비 과제 채용 의무 모니터링 — 사례 누적 시. (그땐 머릿수 소스·기준일 별도 설계 필요)

---

## 묶음 7 — C2 3책5공 관리 (§4.8 C2)

> 사양 2026-06-02 확정 항목. 국가연구개발혁신법 시행령 §64.
> 운영 모델: **책 ≤ 3, 책 + 공 ≤ 5** (책/공 별개 버킷, 한 과제는 한쪽만 +1). 참여율 아닌 과제 "수".

**건드린 파일:** `three-five-rule.js`(신규) / `project-detail.html`·`.js` / `participation-summary.html` / `project-labor.html`·`.js` / `project-budget.html`·`.js`

### 무엇을 개발했나

1. **공유 헬퍼 `three-five-rule.js` (신규)** — 책/공 카운트 순수 모듈(`window.ThreeFiveRule`).
   - `isManagedActive(proj)` = `is3ch5gManaged` && status에 '수행'/'진행' 포함.
   - `bucketForPerson(proj, personId, rosterIds)` = 'chaek'|'gong'|null.
     · 책 = 책임자(`managerPersonId`) AND (단독 || 컨소-주관). · 공 = 그 외 참여(일반 참여자 + 컨소-참여 책임자).
     · 책임자는 명단 여부 무관 인정 / 일반 참여자는 명단(roster)에 있어야 인정.
   - `countForPerson(personId, projects, getRoster)` → `{chaek, gong, total, detail}`.
   - `isOverLimit(chaek, gong)` = 책>3 || 책+공>5. `format(c,g)` = "책/공".
   - **Firestore/DOM 접근 0** — 각 페이지가 데이터 주입. 셀/키 변경 0.

2. **과제 수정 등록 (`project-detail`)**
   - 분류 섹션에 **"3책5공 관리 대상" 체크박스**(`is3ch5gManaged`) + 체크 시 **책임자 personId 드롭다운**(`managerPersonId`, 인력 마스터 구독으로 채움, 이름·회사·퇴직 표기, 저장값이 목록에 없으면 자리표시).
   - 저장: `is3ch5gManaged`(bool) + `managerPersonId`(체크 시만). 복원·토글·구독해제 연결.
   - 참여형태/컨소역할은 아래 "참여 형태" 섹션 값을 그대로 사용(추가 입력 없음).

3. **참여율 (`participation-summary`) — 3책5공 컬럼**
   - 현 `—` placeholder → **현재 연도 조회 시에만** `책/공`(예 `1/3`) 표시. 과거 연도는 `—`(현 시점 의미만).
   - 명단(roster) = `_laborCache[pid].personRoles` 키 또는 `proj.personIds`. 책임자는 명단 무관 인정(헬퍼 처리).
   - **한도 초과 시 빨강 + 툴팁**("책 N / 공 M (책≤3, 책+공≤5) · 한도 초과").

4. **프로젝트별 인건비 (`project-labor`)**
   - **인력 추가 모달 배지**: 현 과제가 3책5공 관리 대상이면 `N개 과제` → **`책/공`** 으로 교체(참여율% 유지, 한도 초과 빨강·툴팁). 비관리 과제는 기존 `N개 과제` 유지.
   - 모달 헤더에 **"3책5공 대상" 칩**(관리 과제일 때만).
   - **메인 표 책임자 행 강조** — 행 배경 살짝(주황) + 이름 볼드·색(뱃지 X). 일반 행·나눔 행 공통.

5. **인건비 예산 (`project-budget`)**
   - **책임자 행 색 강조** — 행의 personId == `managerPersonId`이고 관리 과제이면 성명 볼드·색.
   - (※ 책/공 +1 미리보기는 아래 ⏳ 참조 — roster 미로드로 보류.)

6. **soft 경고(⑤)** — 한도 초과 시 **빨강 + 툴팁**(participation-summary 셀, 모달 배지). 차단·alert 없음(spec "soft만").

### 🧪 테스트 체크리스트 (묶음 7)

**A. 과제 수정 — 3책5공 등록**
- [ ] "3책5공 관리 대상" 체크 → 책임자(인력) 드롭다운이 나타난다
- [ ] 드롭다운에 인력 마스터 전체가 이름순으로 뜬다(회사/퇴직 표기)
- [ ] 책임자 선택 → 저장 → 재진입 시 체크·책임자 복원
- [ ] 체크 해제 후 저장 → 재진입 시 드롭다운 숨김, managerPersonId 비워짐
- [ ] 저장된 책임자가 인력 목록에 없으면 "(이전 책임자 · 목록에 없음)" 자리표시

**B. 참여율 — 3책5공 컬럼**
- [ ] **현재 연도** 조회 시 3책5공 컬럼에 `책/공`(예 1/3)이 뜬다
- [ ] **과거 연도** 조회 시 `—`(미출력)
- [ ] 책 = (단독||컨소-주관) 책임자 과제 수, 공 = 그 외 참여 과제 수와 맞는다
- [ ] 책>3 또는 책+공>5면 빨강 + 마우스 올리면 "한도 초과" 툴팁
- [ ] 책임자로 지정만 됐고 명단엔 없는 사람도 책/공에 잡힌다(책임자 인정)
- [ ] 미배정 행에서도 책임자면 책/공 표시

**C. 프로젝트별 인건비 — 모달/책임자 행**
- [ ] 3책5공 관리 과제에서 인력 추가 모달 헤더에 "3책5공 대상" 칩
- [ ] 후보 배지가 `N개 과제` 대신 `책/공`(참여율% 유지). 한도 초과 빨강
- [ ] 비관리 과제는 칩 없음 + 배지 기존대로 `N개 과제`
- [ ] 메인 표에서 책임자 행 배경(주황)·이름 볼드·색 강조(뱃지 없음)
- [ ] 나눔(split) 인력이 책임자여도 행 강조 적용

**D. 인건비 예산 — 책임자 색**
- [ ] 3책5공 관리 과제 + 책임자(personId 매핑된 행)면 성명 볼드·색

### ⚠️ 알아둘 점 (묶음 7)
- **대상 범위** — 현재 카운트 대상은 `is3ch5gManaged` 켜짐 + status '수행'(/'진행'). 종료·예정·미선정은 자동 제외(라이프사이클 ③).
- **공(共) 카운트의 명단 소스** — 페이지마다 보유한 명단으로 계산: participation-summary=`_laborCache.personRoles`, project-labor 모달=`_modalLaborCache.personRoles`. 두 캐시 모두 **인건비 관리(laborManaged)+수행+해당 연도** 과제만 담으므로, 3책5공 과제가 인건비 관리 대상이 아니면 그 과제의 "공"은 누락될 수 있음(채용 의무 디커플링과 동일 한계 — "인건비 관리 과제만" 전제).
- **책(責) 카운트는 명단 무관** — 책임자(`managerPersonId`)는 어느 페이지서든 `_allProjects`만으로 정확히 셈.
- **soft 경고** — 막지 않음. 빨강+툴팁만. 규정 예외·행정 여지 존중.
- **셀/키 변경 0** — 추가 필드는 `projects.is3ch5gManaged` + `projects.managerPersonId` 둘뿐. funding·결산·labor-dashboard·participation 집계 무영향. 기존 과제 기본 false → 마이그레이션 0.
- **헬퍼 include** — `participation-summary.html`·`project-labor.html`에만 `three-five-rule.js` 추가(실제 호출처). project-detail/budget은 헬퍼 호출 없어 미포함.

### ⏳ 다음에 구현 (이번엔 안 한 것)
- **project-budget 책/공 +1 미리보기** — 정확한 표시는 **다른 과제들의 명단(roster) 로드가 필요**(budget은 현재 자기 과제 labor 문서만 로드). 책 부분은 `_allProjects`만으로 가능하나 공 부분은 타 과제 명단 필요 → roster 로더 추가 후 진행. 잘못된 숫자 방지 위해 이번엔 책임자 색 강조만. 기존 수동 `threeOrFiveGong` 텍스트 칸은 그대로 둠.
- **비관리(비인건비) 3책5공 과제 카운트** — 인건비 관리 안 하는 3책5공 과제의 공 누락 보완(명단 소스·기준일 별도 설계 필요, 채용 의무 디커플링과 동일 보류 사유).
- **3책5공 전용 요약/경고 위치 확대**(예: 한 사람 한도 임박 알림을 인건비 대시보드 "할 일/점검"에 합류) — 필요 시.

### 다음 추천 순서 (이관 메시지 기준)
- C2 완료 → **§4.4 월급 연중 변경**(범위 결정 필요: (a) 과제 오버라이드만 vs (b) +마스터 연봉 시간축·일괄. 사용자 케이스는 (b)쪽) → **C1**(예상 분배 선택 전송, 금액 출처 결정 필요)/**C3**(셀 메모 실제·비교 탭, 권장안 ⓐ=차이행 비허용) → **6묶음 과제별 상세**(사양 미정) → **§4.5 엑셀 입출력**.

### 보강 (묶음 7 — 테스트 피드백 반영)

**건드린 파일:** `project-detail.html`·`.js` / `labor-dashboard.html`·`.js` (+ `three-five-rule.js` 재사용)

1. **[1.1] 공고일 8자리 자동 변환** — 신규 인력 자동 판정 규칙의 공고일을 `type="date"`(피커) → **텍스트 입력**으로 변경. `20260601` 같은 숫자 8자리 입력 후 포커스 아웃 시 `2026-06-01`로 자동 변환(제출일과 동일 `formatDateInput` 패턴, blur 핸들러).

2. **[2.1] 책임자 검색 드롭다운 개편** — 단순 `<select>` → **검색 드롭다운**:
   - 트리거 버튼(현재 책임자 표시) 클릭 → 패널 열림.
   - **이름 검색** 입력 + **재직/퇴직/전체 필터** 버튼.
   - 후보마다 재직/퇴직 태그, 클릭 시 선택·패널 닫힘, 바깥 클릭 닫힘.
   - 선택값은 hidden `#project-managerPersonId`에 저장(저장/복원 로직 그대로). 저장값이 인력 목록에 없으면 "(이전 책임자 · 목록에 없음)" 표시.

3. **[2.2] 대기/예정 과제 책임자 지정 경고** — 진행 여부가 '수행'이 **아닌**(대기·예정 등) 과제에서, 이미 **수행 과제 2건 이상의 책(責)**인 인력을 책임자로 지정하면(그리고 이 과제가 단독/컨소-주관이라 '책'이 될 형태면) 경고:
   - `⚠️ 이 인력은 이미 수행 과제 N건의 책임자입니다. 이 과제를 수행으로 올리면 책 (N+1)건 — 3책 한도 초과/(한도 3).`
   - 수행 과제는 정상 카운트라 경고 없음. 책 카운트는 현재 편집 과제 제외, 다른 과제 기준(`firestoreService.getProjectsData()` + ThreeFiveRule). 진행상태/참여형태/컨소역할 바뀌면 즉시 재계산.

4. **[2.3] 인건비 대시보드 3책5공 점검 섹션** — "할 일/점검" 아래 **📚 3책5공 점검** 섹션 신설.
   - 수행(관리) 과제 기준 인력별 책/공 집계 → **한도 임박/초과만** 노출: 초과(책>3 또는 책+공>5) · 한도(책=3) · 임박(책≥2) · 공 임박(공≥4).
   - 한 줄 = `[이름] 책/공 N/M — 책 3건(3책 도달)` 형태 + "참여율 →" 링크. 회사 칩 필터 반영.
   - **위치 결정**: "인력 대시보드"가 아닌 **인건비 대시보드**(labor-dashboard)에 구현. 이유 = persons-dashboard(인력)는 §3.6에서 persons 전용·모델 변경 0으로 설계돼 과제/명단 데이터가 없음. 인건비 대시보드는 이미 과제·명단을 로드하고 채용 의무·할일/점검 등 규정 모니터링을 담당. → 원하면 인력 대시보드로 이동 가능(데이터 소스 추가 필요).

### 🧪 테스트 체크리스트 (묶음 7 보강)
- [ ] 공고일 칸에 `20260601` 입력 → 포커스 아웃 시 `2026-06-01` 자동 변환
- [ ] 책임자 드롭다운: 트리거 클릭 → 검색창·재직/퇴직/전체 필터·목록 패널
- [ ] 이름 일부 입력 시 후보 필터링, 재직/퇴직 버튼이 목록 거름
- [ ] 후보 클릭 → 선택·패널 닫힘, 저장→재진입 시 복원, 바깥 클릭 시 닫힘
- [ ] 수행 아닌 과제에서 이미 2책 이상인 사람 책임자 지정(단독/컨소-주관) → ⚠️ 경고 문구
- [ ] 진행 여부를 '수행'으로 바꾸면 경고 사라짐(정상 카운트)
- [ ] 인건비 대시보드에 "📚 3책5공 점검" 섹션, 임박/초과 인력만 표시
- [ ] 회사 칩 바꾸면 3책5공 점검도 그 회사로 스코프

### ⚠️ 알아둘 점 (묶음 7 보강)
- **2.2 경고의 책 카운트**도 명단 한계 동일(공은 인건비 관리 과제 명단 기준). 책은 managerPersonId만으로 정확.
- **2.3 "공 임박(공≥4)"** 임계값은 임의 설정(4). 필요 시 조정 가능.
- 책임자 검색 드롭다운은 인력 마스터 구독(`subscribePersons`)으로 채움 — 마스터에 인력이 없으면 "일치하는 인력이 없습니다".

### 수정 (묶음 7 — 책임자 경고 견고화)
- **증상:** 책 2건(2/1)인 인력을 대기 과제 책임자로 지정해도 경고 미표시.
- **원인:** 기존 로직이 ① 진행상태가 '수행'이 아니어야 하고(조기 return) ② 현재 과제가 '책'이 될 형태(단독/컨소-주관)일 때만 경고 → **테스트 과제가 컨소-참여이거나** 게이트에 걸려 아무것도 안 뜸.
- **수정:** **책(責) 2건 이상이면 참여형태·진행상태와 무관하게 안내 노출**. 단독/컨소-주관이면 `⚠️ 책 N+1건 (3책 한도/초과)`, 컨소-참여면 `ℹ️ 이미 책 N건 (이 과제는 공으로 계상, 책 수 불변)`. 책 카운트는 현재 편집 과제 제외·다른 수행 과제 기준.
- **🧪 재테스트:** 책 2건 인력을 책임자로 지정 → (단독/주관) ⚠️ 3책 경고 / (컨소-참여) ℹ️ 안내가 뜬다. 진행여부·참여형태 바꿔도 즉시 갱신.

---

## C3 — 셀 메모 실제·비교 탭 입력 (§4.8 C3)

> 권장안 ⓐ(차이행 비허용) 확정. 데이터 변경 0(기존 `cell.memo` 필드 재사용, `_planned`/`_actual` 둘 다 보유). **건드린 파일: `project-labor.js` 단독** (HTML/CSS 무변경 — 기존 `.pl-cell-has-memo`/`.pl-memo-marker` 재사용).

### 무엇을 개발했나
기존: 셀 메모(v7.4.3 §4.1)가 컨텍스트 메뉴·마커 모두 `state.planned` 하드코딩이라 **예상 탭 전용**. 실제 탭은 읽기전용, 비교 탭은 마커 없음.

1. **메모 팝오버 — 실제 탭 편집 허용** (`openMemoPopover`)
   - `editable = (mode === 'planned' || mode === 'actual')`. `dataMap`은 이미 mode로 `state.actual`/`state.planned` 분기 → 저장도 자동으로 `_actual`/`_planned` 문서. "(읽기 전용)" 라벨·readonly·삭제/저장 버튼이 editable로 켜짐.

2. **실제 탭 우클릭 = 메모 전용 메뉴** (`onCellContextMenu`·`showCellContextMenu`)
   - `onCellContextMenu`: `dataset.mode`가 `planned`/`actual` 둘 다 허용(기존 planned-only 게이트 완화). mode를 `showCellContextMenu`에 전달.
   - `showCellContextMenu(…, mode)`: `mode === 'actual'`이면 **💬 메모 추가/편집 한 항목만** 표시(값 채우기·잠금·버림·글자색은 예상 탭 전용이라 실제 탭엔 미노출). `positionAndBindCtxMenu`도 mode 받아 메모 dispatch가 해당 mode로 팝오버 오픈.

3. **비교 탭 — 예상행/실제행 마커 + 우클릭** (`appendCompareCell` + 비교 렌더)
   - `appendCompareCell(…, memoInfo)` 6번째 인자 추가. 예상행 참여율 셀 → `{mode:'planned', memo:planned.memo}`, 실제행 참여율 셀 → `{mode:'actual', memo:actual.memo}`. **차이행은 memoInfo 미전달 → 메모 비허용**(파생값, 저장처 없음).
   - memoInfo 있으면 셀에 `pl-compare-memocell` 클래스 + `data-personId/ym/mode` 부여(우클릭 훅). 메모 있으면 `pl-cell-has-memo`(분홍 배경·position:relative) + 모서리 마커. 마커 클릭은 기존 `onMemoMarkerClick`이 `dataset.mode`로 자동 분기(코드 그대로).

### 🧪 테스트 체크리스트 (C3)
- [ ] **실제 탭**에서 셀 우클릭 → "💬 메모 추가" 한 항목만 뜨는 메뉴 (값/잠금/색 메뉴 없음)
- [ ] 실제 탭 메모 입력·저장 → 분홍 배경+모서리 마커, 새로고침/재진입 후 유지
- [ ] 실제 탭 마커 클릭 → 편집 팝오버(읽기전용 아님), 삭제도 동작
- [ ] 예상 탭 메모와 실제 탭 메모가 **서로 독립**(같은 인력·월에 둘 다 따로 저장)
- [ ] **비교 탭**: 예상행/실제행 셀에 해당 메모 마커 표시, 차이행엔 마커 없음
- [ ] 비교 탭 예상행 우클릭 → 메모 추가 → `_planned`에 저장(예상 탭에서 확인)
- [ ] 비교 탭 실제행 우클릭 → 메모 추가 → `_actual`에 저장(실제 탭에서 확인)
- [ ] 비교 탭 **차이행** 우클릭 → 메모 메뉴 안 뜸
- [ ] 메모 팝오버 열린 채 Ctrl+Z 입력해도 전역 undo 안 걸림(기존 가드 유지)
- [ ] 예상 탭 기존 메모/채우기/잠금/색 메뉴 **회귀 없음**(전부 그대로)

### ⚠️ 알아둘 점 (C3)
- 데이터 변경 0 — funding·결산·labor-dashboard·participation 무영향. 마이그레이션 0.
- 비교 탭은 셀이 `<input>`이 아닌 `<td>` → 메모 추가는 **우클릭 전용**(셀 직접 편집은 원래 비교 탭에 없음). 마커 클릭으로 기존 메모 편집.
- 비어 있는(데이터 없는) 예상/실제 셀에도 비교 탭에서 우클릭 메모 추가 가능 → 그 셀이 새로 생성됨(예상 탭 동작과 동일).
- 실제 탭 메뉴는 메모만 — 추후 실제 탭 전용 액션이 필요하면 `showCellContextMenu`의 actual 분기에 추가.

### ⏳ 다음에 구현
- 다음 추천: **§4.4 월급 연중 변경**(범위 (a)/(b) 결정 — 사용자 케이스 (b)) / **C1**(예상 분배 선택 전송, 금액 출처 결정) → 6묶음 과제별 상세 → §4.5 엑셀.

---

## C1-a — 이름만 보내기 (§4.8 C1, budget → project-labor)

> C1 전체 중 **결정 불필요 조각만** 구현(금액 출처·split/자부담 정책은 값 전송 시점=C1-b로 보류). **건드린 파일: `project-budget.js`·`project-budget.html`.** 데이터: `_meta.personIds`만 merge, `_planned.cells` 무변경.

### 무엇을 개발했나
기존 분배(`distributeToLabor`)는 인력 매핑 행을 같은 인력·월 셀에 **전부 덮어씀** → 인건비 예상 탭에 **먼저 직접 입력한 값과 충돌**. 운영상 "이름만/일부만" 보내고 싶다는 발단.

1. **"👤 이름만 보내기" 버튼** (`project-budget.html`) — 인건비 예산 툴바, "⚡ 인건비 예상 분배" 바로 옆. 기본 `ui-btn` 스타일(분배는 warning 강조 유지). title로 동작 설명.
2. **`sendNamesToLabor()`** (`project-budget.js`, distributeToLabor 뒤) —
   - 현재 연차 행 중 **personId 있는 행만**(참여율·금액 무관, 중복 personId 제거) 대상.
   - 확인창("참여율·금액 안 보냄 / 기존 셀·메모 보존") 후, `projectLabor/{projectId}_meta` 문서의 `personIds`에만 신규 personId를 **merge**. **`cells`는 절대 손대지 않음**(정적 검증으로 `cells` 미참조 확인).
   - 토스트에 전송 N명·신규 M명 표시.
3. **버튼 바인딩** — `pb-send-names-btn` → `sendNamesToLabor`.

근거: 인건비 예상 탭은 `personIds`만 있으면 셀 없어도 **빈 행으로 정상 표시**(personRoles 비면 로드 시 기본값 기존/현금 자동 채움). → 이름만 보내도 즉시 입력 가능, 직접 입력값 보존.

### 🧪 테스트 체크리스트 (C1-a)
- [ ] 인건비 예산에서 인력 매핑(🔍) 후 "👤 이름만 보내기" → 확인창 문구(참여율·금액 안 보냄)
- [ ] 인건비 예상 탭에 그 인력들이 **빈 행**으로 추가(참여율/금액 0, 바로 입력 가능)
- [ ] **기존에 직접 입력한 다른 인력·월 셀이 그대로 유지**(덮어쓰기 없음) — C1-a 핵심
- [ ] 같은 인력에게 이미 입력해 둔 셀이 있으면 **그 값도 보존**(이름만 보내도 셀 안 건드림)
- [ ] personId 미매핑 행만 있을 때 "인력 매핑된 행이 없습니다" 토스트
- [ ] 신규 0명(이미 다 명단에 있음)일 때도 정상 토스트(신규 0명)
- [ ] 기존 "⚡ 분배"는 그대로 동작(회귀 없음)

### ⚠️ 알아둘 점 (C1-a)
- **비파괴 보장** = `cells` 미접근 + `personIds` merge. funding·결산 무영향(셀/키 변경 0).
- 대상 행 필터가 분배(`personId && rate>0`)와 달리 **`personId`만** — 참여율 0이어도 명단에 넣음(이름만이 목적).
- 자동 트리거 없음(수동 버튼). 분배와 별개 동작.

### ⏳ 다음에 구현
- **C1-b 값 선택 전송** — ① 인력 체크박스 ② 필드(이름만/+참여율/+금액) ③ 월 범위 ④ 덮어쓰기 vs 빈 셀만(비파괴). **결정 필요: 금액 출처**(ⓐ budget값 `actualPay×%` vs ⓑ labor 급여 재계산) + split/자부담현금 전송 정책. → 선택 모달 UI 위주(데이터 장애 0).
- 그 외: **§4.4 월급 연중**(범위 a/b — 사용자 케이스 b) / 6묶음 과제별 상세 / §4.5 엑셀.

---

## C1-b — 선택 전송 모달 (§4.8 C1, budget → project-labor)

> **건드린 파일: `project-budget.js`·`project-budget.html`.** ⚠️ **C1-a("👤 이름만 보내기" 버튼)는 이 모달의 "이름만" 필드 옵션으로 흡수**됨 — 단독 버튼/`sendNamesToLabor` 제거, 대신 "📤 선택 전송" 버튼 하나로 통합. 데이터: `_meta.personIds` merge + (필드가 셀일 때) `_planned.cells` 기록. 금액 출처 ⓐ/ⓑ 모두 모달 라디오로 제공(사용자 결정).

### 무엇을 개발했나
"⚡ 분배"는 전 인력·전월을 덮어쓰는 일괄 동작이라 직접 입력값과 충돌. C1-b는 **고른 인력·필드·월·쓰기 방식**으로 정밀 전송.

1. **버튼 교체** — "👤 이름만 보내기" → **"📤 선택 전송"**(`pb-send-modal-btn`). 분배 버튼은 그대로(일괄 빠른 경로).
2. **모달 5섹션** (`pb-send-modal`):
   - **① 인력** — personId 매핑 행 체크박스(기본 전체), 전체/해제. 이름·참여율·구분·실지급액 표시.
   - **② 보낼 필드** — `이름만` / `+참여율`(금액 0) / `+참여율+금액`(기본).
   - **③ 금액 출처**(+금액일 때만 노출) — **ⓐ 예산 금액**(`실지급액×%`) / **ⓑ 인건비 탭 급여 재계산**(`오버라이드 ?? 마스터 monthlySalary × %`). ⓑ는 `_meta.personRoles[pid].monthlySalaryOverride`>0면 그 값, 아니면 인력 마스터(`_allPersons`) 월급.
   - **④ 월 범위**(셀 쓸 때만) — 연차 월 목록 시작~끝 셀렉트(기본 전체).
   - **⑤ 쓰기 방식** — **빈 셀만**(기본·비파괴: 값 있는 셀 건너뜀) / **덮어쓰기**(교체). 둘 다 **메모 보존**.
3. **구분 버킷** — 항상 예산 행 분류(`현금→cash`/`자부담현금→selfCash`/`현물→inkind`). split(나눔)은 예산 측에 없어 단일 버킷.
4. **`doSendToLabor`** — `_planned`+`_meta` 로드 → personIds merge + 선택 인력×선택 월 셀 기록(batch). 필드 `이름만`이면 cells 무변경.

### 🧪 테스트 체크리스트 (C1-b)
- [ ] "📤 선택 전송" → 모달: 인력 체크리스트(전체 체크)·필드·금액 출처·월 범위·쓰기 방식
- [ ] 필드 `이름만` 선택 시 ③금액출처·④월범위 섹션 숨김, 안내문 갱신
- [ ] 필드 `+참여율` → ③ 숨김, ④ 노출 / `+금액` → ③·④ 모두 노출
- [ ] `+금액` + **ⓐ 예산**: 셀 금액 = 실지급액×참여율%
- [ ] `+금액` + **ⓑ 재계산**: 오버라이드 있으면 오버라이드×%, 없으면 마스터 월급×%
- [ ] `+참여율`: 참여율만 들어가고 금액 0
- [ ] **빈 셀만**: 이미 값 있는 셀은 안 건드림(직접 입력값 보존), 빈/메모만 셀은 기록
- [ ] **덮어쓰기**: 같은 인력·월 셀 교체(메모는 보존)
- [ ] 월 범위 좁히면 그 범위만 기록 / 시작>끝이면 자동 정렬
- [ ] 인력 일부만 체크 → 그 인력만 전송, 0명 체크 시 "선택된 인력이 없습니다"
- [ ] 구분: 자부담현금 행 → selfCash, 현물 행 → inkind 로 들어감
- [ ] 전송 후 인건비 예상 탭에 반영, funding/결산 정상(키 변경 0)

### ⚠️ 알아둘 점 (C1-b)
- **ⓑ 오버라이드 출처**는 `_meta.personRoles[pid].monthlySalaryOverride`(인건비 탭에서 설정한 과제별 월급). 미설정이면 인력 마스터 월급 폴백. (§4.4에서 오버라이드가 객체로 확장되면 ⓑ도 그때 맞춰 갱신 필요.)
- **`+참여율`/덮어쓰기 조합**은 기존 금액을 0으로 교체함(참여율만 보내므로). 의도된 동작 — 금액 유지하려면 `+금액` 또는 `빈 셀만`.
- **빈 셀 판정** = rate/cash/selfCash/inkind 중 하나라도 >0. 메모만 있는 셀은 "빈 셀"로 보고 기록(메모는 보존).
- 데이터 키/스키마 변경 0 → funding·결산 무영향.

### ⏳ 다음에 구현
- C1 완결(이름만·참여율·금액·범위·비파괴 전부 모달에 포함). 다음 추천: **§4.4 월급 연중 변경**(범위 a/b — 케이스 b, `monthlySalaryOverride` 숫자→객체) / 6묶음 과제별 상세 / §4.5 엑셀.
- §4.4에서 오버라이드가 `{YYYY-MM:금액}` 객체가 되면 C1-b ⓑ의 `monthlySalaryOverride` 단일값 가정도 함께 수정.

---

## C6 — 연봉 프로파일(공유 슬롯) · 1단계: 인력 마스터 입력 (A)

> **신규 기능.** 예산 짤 때 불러올 연봉을 골라 쓰기 위한 "용도별 연봉(슬롯)". **확정 설계** + **1단계(인력 마스터 입력)** 구현. 건드린 파일: `persons-master.html`·`.js`. 데이터 추가 = `persons.salarySlots` 하나(맵). 셀/키 변경 0 → funding·결산·labor 무영향.

### 확정 설계 (전 단계 결정)
- **모델**: 공유 슬롯. `실제`(=기존 `annualSalary`, 항상 존재) + 용도 슬롯(`공개용` 등). 인력별 `salarySlots = { 슬롯명: 연봉(원) }`. 월급은 사용처에서 `ceil(연봉/12)`. **빈 슬롯 = 실제로 폴백.**
- **슬롯 목록 저장 안 함(별도 설정 문서 없음)** — 가용 슬롯 = `기본 슬롯(['공개용']) ∪ 전 인력 salarySlots 키 합집합`. "기타 추가" = 편집 중 새 이름 입력→저장되면 다른 사람에게도 빈 슬롯으로 노출. (v1 한계: 전역 일괄 이름변경/삭제 미지원.)
- **입력 UX = A + C**: A) 인력 편집 모달 인라인(이번 1단계) / C) 엑셀 다운·업로드 컬럼(3단계 예정).
- **예산 선택(2단계 예정)**: 상단 기본 슬롯(전체 적용) + 행별 드롭다운 변경. 행엔 숫자만 들어가 기존 분배/C1/내보내기 무영향.

### 무엇을 개발했나 (1단계)
1. **편집 모달 슬롯 섹션** — "연봉(실제)" 바로 아래 "다른 용도 연봉(슬롯)". 가용 슬롯마다 한 줄(슬롯명 · 연봉 입력 · 월급 미리보기). "+ 슬롯 추가" 버튼.
2. **`salarySlots` 저장** — `readForm()`이 `collectSalarySlotsFromForm()`으로 빈 값 제외하고 `{슬롯명:연봉}` 수집해 person에 포함. `updatePerson`은 병합이라 기존 필드 보존.
3. **헬퍼** — `getAvailableSlotNames()`(기본∪합집합, 기본 먼저·그 외 가나다, `실제` 제외) / `renderSalarySlots(person)` / `onAddSlot()`(prompt+`sanitizeSlotName`) / `onSlotInput`(월급 미리보기) / `escHtml`·`escAttr`.
4. **슬롯명 검증** — 공백 trim, `실제` 예약 거부, Firestore 키 위험문자(`. / [ ] * ~ ` # $`) 거부, 20자 컷, 모달 내 중복 거부.
5. 편집 모드: 가용 ∪ 본인 키로 렌더(본인이 가진 슬롯은 합집합에 없어도 노출). 추가 모드: 가용 슬롯 빈 값.

### 🧪 테스트 체크리스트 (C6 1단계)
- [ ] 인력 추가/수정 모달에 "다른 용도 연봉(슬롯)" 섹션, 기본 "공개용" 한 줄
- [ ] 슬롯에 연봉 입력 → 우측 "월 …" 자동 미리보기(ceil/12)
- [ ] "+ 슬롯 추가" → 이름 입력(예: 제안서용) → 새 줄 추가, 기존 입력값 유지
- [ ] "실제"·특수문자(`a.b`)·빈 이름 입력 시 거부 알림, 모달 내 중복 거부
- [ ] 저장 후 재편집 시 슬롯 값 복원
- [ ] 다른 인력 편집 모달에도 방금 만든 슬롯이 빈 칸으로 노출(합집합)
- [ ] 슬롯 비우고 저장 → 그 슬롯 값 사라짐(실제 폴백 대상). 기존 연봉/기타 필드·다른 인력 영향 없음
- [ ] 슬롯 한 번도 안 쓴 기존 인력은 "공개용"만 빈 칸으로 보임(마이그레이션 0)

### ⚠️ 알아둘 점 (C6 1단계)
- `salarySlots`는 **연봉(annual)** 저장. 월급은 쓸 때 `ceil/12`(실제 연봉과 동일 규칙). 예산(2단계)에서도 동일하게 환산.
- 슬롯 목록은 데이터에서 파생(합집합) → 새 슬롯은 **한 명이라도 값 저장 후** 전역 노출. 전역 이름변경/삭제는 v1 미지원.
- **C1-b ⓑ(인건비 급여 재계산)와는 별개 축** — C1-b는 labor의 월급(오버라이드/마스터), C6는 "예산에 무슨 연봉을 불러올지". 2단계에서 예산 행에 슬롯 월급을 채우는 형태로 연결.

### ⏳ 다음에 구현
- **2단계 — 예산 슬롯 선택**: project-budget에 상단 기본 슬롯 셀렉트(전체 적용) + 🔍 인력 선택/행별 드롭다운으로 `salarySlots[슬롯] ?? 실제`의 월급을 행에 채움. (+선택: 「전체 재적용」)
- **3단계 — 엑셀(C)**: 다운로드/업로드에 슬롯 컬럼(`연봉:공개용` 등) 추가, 일괄 입력.
- (병행 검토) 기획서에 C6 정식 등재.

**[C6 1단계 — 문구/기본값 수정]** 라벨 변경: "연봉(원)"→**"실제 연봉(원)"**, "다른 용도 연봉(슬롯)"→**"예산 전용 연봉(원)"**, "+ 슬롯 추가"→**"+ 항목 추가"**. 기본 슬롯명 `공개용`→**`제안서용(공개 가능)`**. 입력 placeholder→"비우면 실제 연봉 사용", 안내문에 "비워두면 그 인력은 실제 연봉 적용" 명시(폴백 의미 명확화). 사용자 알림/프롬프트의 "슬롯"→"항목". (내부 식별자·`salarySlots` 키 구조는 그대로. 괄호 `()`는 Firestore 맵 키 허용 문자.)

**[C6 1단계 — 콤마 표시 + 레이아웃]** ① 실제 연봉·예산 전용 연봉 입력에 **천단위 콤마**(`type=number`→`text inputmode=numeric`, `parseMoney`/`fmtComma`, 입력 시 콤마 자동·캐럿 끝, 읽기/힌트/편집복원 모두 콤마 제거 파싱). ② 슬롯 행을 **스택 레이아웃**으로 변경 — 항목명을 윗줄에 전체 표시(말줄임 제거·줄바꿈 허용), 아랫줄에 입력칸(폭 축소 `flex:0 1 200px`, 우측정렬)+월급 미리보기. 긴 이름("제안서용(공개 가능)") 안 짤림. 데이터/키 무변경.

---

## C6 — 연봉 프로파일(공유 슬롯) · 2단계: 예산에서 슬롯 골라 불러오기 (v8.3 이후)

> **신규 기능.** 예산(project-budget) 행에 인력의 "예산 전용 연봉(슬롯)"을 골라 불러옴. 건드린 파일: `project-budget.html`·`.js`. 데이터 추가 = `row.salarySlot`(문자열, 어느 항목을 불러왔는지 기억). **셀/키 변경 0**, 분배/C1/내보내기는 기존대로 숫자(`actualPay`×%)만 써서 무영향. 마이그레이션 0.

### 결정: 빈 슬롯 폴백 = **하이브리드(차단 + 경고 + 실제로 채우기)** ★ 사용자 확정
- 슬롯을 비운 인력은 불러올 때 **기본은 0으로 비움(실제 연봉 유입 차단 — 제안서 유출 방지)**.
- 그 행을 **경고색(주황)** 으로 표시 + 바에 "⚠️ N명 비워둠" 안내.
- 필요하면 **"빈 항목 실제로 채우기 (N)" 버튼**으로 실제 연봉 일괄 주입. 채운 뒤에도 셀은 자유 수정 가능(불러온 값은 일반 급여 입력칸에 들어감).

### 무엇을 개발했나
1. **연봉 불러오기 바**(제목 박스↔테이블 사이, 연차 로드 시 표시) — `연봉 항목` 셀렉트(실제 연봉 + 가용 슬롯 합집합) + **전체 적용** 버튼 + (폴백 있을 때만) **빈 항목 실제로 채우기 (N)** + 경고 힌트.
2. **전체 적용** — 인력 지정된(`personId` 있는) 모든 행에 선택 항목의 월급(`ceil(연봉/12)`)을 **급여총액·실지급액 둘 다** 채움. 기존 값 덮어씀(confirm). 빈 슬롯은 0으로 비우고 폴백 카운트.
3. **행별 드롭다운** — 급여총액 셀 안(입력칸 위)에 인력 지정 행마다 작은 `연봉 항목` 셀렉트. 변경 시 그 행만 즉시 적용(자잘한 변경이라 confirm 없음).
4. **헬퍼**(persons-master와 동일 로직 이식) — `getAvailableSlotNames()`(기본∪합집합, 가나다) / `getPersonActualMonthly(p)`(monthlySalary 우선, 없으면 ceil(annual/12)) / `getSlotMonthly(p,slot)`({monthly} 또는 {monthly:null,fallback:true}) / `isRowSalaryFallbackPending(r)`(슬롯 비고 급여 0인 행) / `applySlotToRowData`·`applySlotToRow`·`applySlotToAllRows`·`fillFallbackRowsWithActual` / `populateSlotSelect`·`buildRowSlotOptions`·`updateSalaryBarState`.
5. **데이터** — `row.salarySlot` 추가(`''`/`'__actual__'`=실제). 이전 연차 복사 시 함께 복사. `rows: yd.rows`로 통째 저장돼 round-trip 자동(직렬화 코드 무변경).

### 🧪 테스트 체크리스트 (C6 2단계)
- [ ] 과제·연차 선택 후 테이블 위에 "💰 연봉 불러오기" 바 표시(미선택/연차없음 땐 숨김)
- [ ] 셀렉트에 "실제 연봉" + persons에 만든 슬롯들(예: 제안서용(공개 가능))이 합집합으로 보임
- [ ] 인력 지정 행마다 급여 칸 위 작은 드롭다운 노출(인력 없는 청년 신규 행은 없음)
- [ ] "제안서용(공개 가능)" 선택 → **전체 적용** → 그 슬롯 값 있는 인력은 급여총액·실지급액에 월급(연봉/12) 채워짐, 총액/지원금 합계 갱신
- [ ] 슬롯 비운 인력 행은 0으로 비워지고 **주황 경고색** + 바에 "N명 비워둠" 안내 + "빈 항목 실제로 채우기 (N)" 버튼 등장
- [ ] **빈 항목 실제로 채우기** → 경고 행들 실제 연봉으로 채워지고 경고/버튼 사라짐
- [ ] 행별 드롭다운에서 다른 항목 선택 → 그 행만 즉시 갱신
- [ ] 채워진 급여 셀을 직접 수정 가능(불러오기는 초기값일 뿐)
- [ ] 저장 후 재진입 시 행별 선택(`salarySlot`) 복원, 분배(⚡)·선택 전송(📤)은 기존대로 동작
- [ ] "실제 연봉" 선택 시엔 폴백/경고 없이 항상 실제 월급 채움

### ⚠️ 알아둘 점 (C6 2단계)
- 불러오기는 **급여총액·실지급액 둘 다** 세팅(예산 금액 계산은 `실지급액×참여율%`이라 actualPay가 핵심).
- **전체 적용은 기존 급여를 덮어씀**(confirm 1회). 빈 슬롯은 0으로 비워 실제값 차단 — 수동 입력값도 0으로 정리되니 의도된 동작.
- `row.salarySlot`은 **UI 기억용**일 뿐 분배/C1/내보내기 계산엔 안 쓰임(숫자만 사용). 키/셀 무변경.
- 경고색은 `renderTable` 시 갱신 — 폴백 행 급여를 직접 타이핑하면 경고는 다음 렌더에서 해제(자잘한 표시 지연, 기능 영향 없음).
- 노드 시뮬 19개 assertion(합집합·환산·폴백 차단·경고 카운트·실제로 채우기) 통과. node -c·태그/CSS 균형·ID/함수 정합 통과.

### ⏳ 다음에 구현
- **C6 3단계 — 엑셀(C)**: persons 다운로드/업로드(+bulk-upload)에 슬롯 컬럼(`연봉:제안서용(공개 가능)`) 추가, 일괄 입력.
- 이후 §4.4 월급 연중 변경(범위 b·`monthlySalaryOverride` 객체화 → C1-b ⓑ 동기 수정) → 6묶음 과제별 상세 → §4.5 엑셀.

---

## C6 — 연봉 프로파일(공유 슬롯) · 3단계: 엑셀 일괄 입출력 (v8.3 이후)

> **신규 기능.** 인력 일괄 다운로드/업로드(`전체 다운로드`·`일괄 업데이트`)에 예산 전용 연봉(슬롯) 컬럼을 추가해 엑셀로 일괄 입력. 건드린 파일: `persons-master.js`(다운/업/병합) + `persons-master.html`(다운로드 버튼 tooltip). 데이터 모델 변경 0(`salarySlots` 그대로). 마이그레이션 0.

### 무엇을 개발했나
1. **다운로드 슬롯 컬럼** — 헤더 `연봉:` + 슬롯명(예: `연봉:제안서용(공개 가능)`), `연봉(원)`(실제) 바로 뒤에 가용 슬롯 **합집합**(`getAvailableSlotNames`)만큼 동적 삽입. 값 = `salarySlots[슬롯]`(연봉, 빈 슬롯은 빈 칸). 금액 컬럼(실제+슬롯) 천단위 콤마 숫자 서식.
2. **업로드 파싱** — 헤더가 `연봉:` 로 시작하면 슬롯 컬럼으로 인식(`연봉(원)`은 `연봉(`라 충돌 없음), 슬롯명은 `sanitizeSlotName`으로 검증(실제·특수문자·20자). 셀은 `parseSalaryCell`(콤마/공백/원 제거 → 0 이상 정수), **양수만** `salarySlots`에 반영.
3. **병합(비파괴)** — 업데이트 경로: 비어있지 않은 슬롯만 `existing.salarySlots`에 키별 병합(다른 슬롯은 보존, 빈 칸은 변경 없음 = 실제 연봉과 동일 규칙). 신규 추가 경로: `salarySlots` 있으면 세팅.
4. **헬퍼** — `SLOT_COL_PREFIX='연봉:'` / `getSlotColumns()` / `getBulkColumns()`(고정+슬롯) / `parseSalaryCell()`. 다운로드의 `BULK_COLUMNS.map` 직접참조를 `getBulkColumns()`로 교체.
5. **bulk-upload(임시 용역 CSV)는 제외** — 고정 위치 CSV 양식 + 헤더 순서검증이라 슬롯 가변 컬럼과 성격이 다르고 임시 도구라, 이번엔 persons-master만. (필요 시 별도 진행.)

### 🧪 테스트 체크리스트 (C6 3단계)
- [ ] **전체 다운로드** → 엑셀 `연봉(원)` 뒤에 `연봉:제안서용(공개 가능)` 등 가용 슬롯 컬럼이 모두 보임
- [ ] 슬롯 값 있는 인력은 그 칸에 연봉(콤마) 표시, 빈 슬롯 인력은 빈 칸
- [ ] 슬롯 칸을 채워(또는 콤마/원 섞어) 저장 후 **일괄 업데이트** → 해당 인력 `salarySlots`에 반영, 편집 모달에서 확인
- [ ] 한 슬롯만 수정 업로드 → 다른 슬롯·실제 연봉·기타 필드 그대로 유지(비파괴)
- [ ] 슬롯 칸 비워서 업로드 → 기존 슬롯 값 안 지워짐(삭제는 편집 모달에서)
- [ ] 슬롯 없던 인력에 값 채워 업로드 → 새 슬롯 생성, 다운로드 시 그 컬럼 등장(합집합 갱신)
- [ ] 신규 추가 행(id 빈칸)에 슬롯 채우면 신규 인력에 `salarySlots` 들어감
- [ ] 같은 값 재업로드 → "변경 없음(skip)"
- [ ] 다운로드한 슬롯 컬럼 헤더를 그대로 두면 1·2단계(편집 모달·예산 불러오기)와 키 일치

### ⚠️ 알아둘 점 (C6 3단계)
- 업로드 슬롯 반영은 **비파괴**(빈 칸=변경 없음). 슬롯 삭제·이름변경은 엑셀로 안 됨 → 편집 모달 사용.
- 새 `연봉:이름` 컬럼을 사람이 직접 추가해 값 넣으면 **새 슬롯 생성**(오타 주의). 슬롯명은 sanitize(특수문자/실제/20자) 통과분만.
- 다운로드 슬롯 컬럼은 **합집합 기준**이라, 아무도 안 쓰는 슬롯은 컬럼으로 안 나옴(한 명이라도 값 있어야 노출 — 1·2단계와 동일).
- 노드 시뮬 18개 assertion(컬럼 구성·다운값·헤더감지·콤마/원 파싱·비파괴 병합·신규 생성·skip·셀 엣지) 통과. node -c·태그/CSS 균형·심볼 정합 통과.

### ⏳ 다음에 구현
- **§4.4 월급 오버라이드 연중 변경**(범위 b): 마스터 연봉 시간축(상/하반기)+여러 인력 일괄, `monthlySalaryOverride` 숫자→객체화 ⚠️ **C1-b ⓑ 동기 수정 필수**(데이터 모델 결정 필요).
- 이후 6묶음 과제별 상세(사양 미정) → §4.5 엑셀. (C6는 1~3단계로 완료.)

---

## §4.4 월급 오버라이드 연중 변경 · 1단계: 인사 마스터 연봉 변경 시점 입력 (v8.3 이후)

> **신규 기능(단계 1/3).** 인력 편집 모달에 "연봉 변경 시점"(연중 인상 등) 입력 추가 + "그 달 기준 연봉" 해석 함수. 건드린 파일: `persons-master.js`·`persons-master.html`. 데이터 추가 = `person.salaryChanges`(배열) 하나. **변경시점을 안 넣으면 기존 동작과 100% 동일 = 안전.** 마이그레이션 0(저장은 `Object.assign`이라 자동 round-trip).

### 설계 결정 (사용자 확정)
- **범위 = b1 + 마스터 기록**: 연봉 변경을 **인사 마스터에 한 번만** 기록 → 2027 예산·새 계산은 자동으로 새 연봉. (자금·결산·대시보드는 개인 월급을 안 써서 **영향 0** — 소비처는 persons-master·project-budget·project-labor뿐임을 grep으로 확인.)
- **계단식(step)**: `from` 달부터 새 연봉, 그 전까지는 기본 `annualSalary`. (`{from:'2026-07', annualSalary}` → 1~6월 기본, 7월~ 새 값.)
- **자동 재계산 안 함**: 마스터를 바꿔도 이미 입력된 예상 셀은 안 변함. 사용자가 (2단계의) "연봉 변경 반영" 버튼으로 **사람·시점 골라** 적용. 확정 실제 월은 절대 제외.
- 입력 위치 = **인사 마스터 편집 모달**(2단계의 "반영" 버튼은 project-labor 상단).

### 무엇을 개발했나 (1단계)
1. **데이터**: `person.salaryChanges = [{ from:'YYYY-MM', annualSalary:원 }, ...]`. 기본 `annualSalary`는 그대로(첫 변경 전 구간).
2. **해석 함수**: `getAnnualSalaryAt(person, ym)`(from<=ym 중 최근값, 없으면 기본) / `getMonthlySalaryAt`(=ceil/12) / `sanitizeSalaryChanges`(YYYY-MM·양수만, 같은 달 중복=마지막, 오름차순, **콤마 문자열도 파싱**) / `normalizeYm`('2026.7'·'20267' 등 관대 파싱).
3. **UI**: 편집 모달 "예산 전용 연봉" 아래에 **연봉 변경 시점** 섹션 — 행마다 [시작월 `2026-07`] → [새 연봉(콤마)] [월급 미리보기] [✕], "+ 변경 시점 추가". 슬롯 입력과 같은 스택 스타일.
4. **저장/로드**: readForm에 `salaryChanges` 추가, 모달 열 때 `sanitizeSalaryChanges`로 로드. 편집 중 raw 값은 `_modalSalaryChanges`에 보관(빈 행도 유지).

### 🧪 테스트 체크리스트 (§4.4 1단계)
- [ ] 인력 편집 → "연봉 변경 시점" 섹션 보임, "+ 변경 시점 추가"로 행 생성
- [ ] 시작월 `2026-07`, 새 연봉 입력 → 콤마 자동 + "월 OOO원" 미리보기
- [ ] 저장 후 재편집 → 입력한 변경 시점 그대로 복원(정렬됨)
- [ ] 변경 시점 여러 개(상/하반기 등) 추가·✕ 삭제 정상
- [ ] **변경 시점 없는 인력은 예산·인건비 계산이 기존과 동일**(아무 변화 없음 = 안전)
- [ ] 잘못된 월(13월)·빈 연봉·0은 저장 시 자동 제외
- [ ] ⚠️ 이 단계만으론 인건비/예산 화면 숫자는 아직 안 바뀜(2단계에서 연결) — 정상

### ⚠️ 알아둘 점 / 다음 단계
- 해석 함수(`getAnnualSalaryAt` 등)는 현재 `persons-master.js` 안에만 있음. **2단계에서 project-labor가 쓸 때 공용 위치(예: firestore-service `window.salaryResolver`)로 승격**해 중복/드리프트 방지 예정.
- 노드 시뮬 14개 assertion(normalizeYm·콤마 파싱·중복·계단식·내년 유지·변경없음 동일·다중) 통과. **시뮬이 콤마 문자열 NaN 버그를 잡아 수정함**(digit-strip 파싱). node -c·태그/CSS·ID 정합 통과.
- **2단계(project-labor)**: 계산이 `getMonthlySalaryAt(person, ym)`를 쓰도록 연결 + 상단 "📅 연봉 변경 반영" 버튼(변경된 사람 선택 → 해당 월부터 예상 금액 재계산, 확정 실제 월 제외).
- **3단계(project-budget + 선택 전송 ⓑ)**: 그 달 연봉 사용으로 동기. 2027 예산 자동 새 연봉.
- (선택) 엑셀 일괄에 변경시점 컬럼은 추후 검토.

---

## §4.4 2단계-a · 인건비(project-labor) 그 달 연봉 계산 연결 + 표시 (v8.3 이후)

> **단계 2/3의 앞부분.** project-labor 계산이 "그 달 기준 연봉"을 쓰도록 연결 + 연중 변경 인력에 📅 표시. 건드린 파일: `project-labor.js`·`.html`(CSS·인디케이터). 데이터 변경 0. **변경시점 없는 인력은 기존과 100% 동일**(안전 폴백).

### 무엇을 개발했나 (2a)
1. **해석 헬퍼 이식**(persons-master와 동일 알고리즘): `normalizeYm`·`sanitizeSalaryChanges`·`getAnnualSalaryAt`·`getMonthlySalaryAt`·`hasSalaryTimeline`. (공용 위치 승격은 추후 — 변경 시 양쪽 동기 주석 표기.)
2. **`getEffectiveMonthlySalary(person, ym)` 월 인식화** — 우선순위 ① 과제 오버라이드(평탄, >0) ② 마스터 연봉 그 달 기준 ③ `person.monthlySalary`. **ym 없거나 변경시점 없으면 기존 동작과 동일.**
3. **사용자 동작 계산 지점에 ym 전달**(그 달 연봉 사용): 참여율 입력 자동금액, 나눔(splitRate) 2경로, ⚡ 일괄 자동 계산, 가로채우기 폴백(원본월 `fromYm`), undo 참여율 복원 2곳(`entry.ym`).
4. **월급 박스 📅 인디케이터**(예상 탭, 오버라이드 없을 때): 연중 변경 있는 인력에 📅 + 툴팁으로 변경 목록("YYYY-MM부터 OOO원") 표시.
5. **표시값은 base 유지**: 월급 입력칸 숫자는 `person.monthlySalary`(평탄) 그대로 — 한 칸으로 시간축을 못 보여줘서, 실제 월별 값은 계산·툴팁으로.

### 🧪 테스트 체크리스트 (2a)
- [ ] 변경시점 없는 인력: 월급칸·자동계산·⚡ 모두 **이전과 동일**(아무 변화 없음)
- [ ] 인사 마스터에 "2026-07부터 600만" 넣은 인력: 인건비 예상 탭 월급칸 옆 **📅** 표시(마우스 올리면 변경 목록)
- [ ] 그 인력 6월 칸에 참여율 입력 → 금액 = 500만×% / 7월 칸 → 600만×% (그 달 연봉)
- [ ] ⚡ 일괄 자동 계산 → 상반기는 500만, 하반기는 600만 기준으로 한꺼번에 갱신
- [ ] 과제 오버라이드(월급칸 직접 수정)가 있으면 그 평탄값이 우선(📅 숨김)
- [ ] 나눔(분류별 %) 인력도 그 달 연봉으로 cash/selfCash/inkind 계산

### ⚠️ 알아둘 점 / 다음(2b)
- **표시값은 평탄 base** — 시간축 상세는 📅 툴팁·월별 계산으로. (월급칸을 월별로 쪼개는 UI는 과한 변경이라 안 함.)
- 아직 **선택적 "📅 연봉 변경 반영" 버튼은 미구현**(2b). 현재 이미 입력된 셀을 새 연봉으로 바꾸려면 ⚡ 일괄 자동 계산(전체) 사용. 2b에서 "사람 골라 해당 월만" 반영 + 확정 실제 월 제외 + 묶음 undo 추가 예정.
- 표시/렌더용 `getEffectiveMonthlySalary(person)`(L1722 rowSalary·박스 effective·L1982) 3곳은 의도적으로 base(ym 미전달).
- 노드 시뮬 10개(폴백 동일·그 달 연봉·내년 유지·오버라이드 우선·금액) 통과. node -c·태그/CSS 균형 통과.

**[C6 1단계 후속 — 슬롯 행 한 줄]** 예산 전용 연봉(제안서용) 슬롯 행을 스택(2줄)→**한 줄**(이름·입력·미리보기)로. `.pm-slot-row` flex-direction row + 이름 `flex:1 1 110px`·입력 `flex:0 0 150px`·flex-wrap(좁을 때만 줄바꿈). CSS만(persons-master.html).

---

## 대시보드 — 툴바 한 행 정렬 (v8.3 이후)

> 건드린 파일: `dashboard.html`(마크업만). JS·데이터 변경 0, ID 그대로.

- 별도 블록이던 **회사 필터**를 `.dash-toolbar` 안으로 통합 → **기준 연도 [연도] · 회사 [칩]**(왼쪽 정렬) … **📥 PDF 저장**(오른쪽, `margin-left:auto`)이 **한 행**에 배치.
- `#dash-year`·`#dash-company-chips`·`#dash-pdf-btn` ID 유지(JS·company-filter 영향 없음). 태그/CSS 균형 통과.
- 🧪 확인: 한 행에 연도·회사 왼쪽 / PDF 오른쪽, 회사 칩 클릭 필터·연도 변경·PDF 저장 정상, 좁은 화면 줄바꿈(flex-wrap) OK.

---

## 대시보드 — 지원금 현황 카드 재정비 (반응형 + 입금 진행)

> 건드린 파일: `dashboard.html`·`dashboard.js`. 데이터/계산 키 변경 0(`yearActual`·`yearSupport` 등 기존 kpis 재사용).

### 무엇을 개발했나
1. **노트북 줄바꿈 수정** — 윗 행이 `1fr 1fr 구분선 1fr`(4칸: 전체·유형·완료율)이라 좁은 화면에서 "지원사업/기타" 라벨이 줄바꿈됨. **완료율을 아래로 빼고 윗 행을 2칸(`big-stats--duo`)**으로, `.type-mini-label { white-space: nowrap }`로 라벨 줄바꿈 방지.
2. **"지원금 구성(당해)" 제거** — 렌더 코드가 없어 항상 0이던 죽은 섹션(인건비/용역비/기타) 삭제(마크업 제거, JS 무변경).
3. **"입금 진행 (당해)" 신설** — 그 자리에 입금 데이터 표시:
   - 섹션 헤더 우측에 **입금 완료율**(기존 `#hero-payment-rate`·색강조 톤 유지, 배지 `payment-rate-badge`로 이동).
   - 누적막대: **입금 완료**(초록 `--paid`) / **남은 예정**(연회색 `--remain`). 기준 = 입금 예정 지원금(`yearSupport`).
   - 범례: `입금 완료 N억 (xx%)` · `남은 예정 M억 (yy%)`. 완료=`yearActual`, 남은=max(0, 예정−완료).
   - 초과입금 시 막대 100% 캡, 완료율은 실제값(예: 120%) 표시.

### 🧪 테스트 체크리스트 (대시보드 지원금 현황)
- [ ] 노트북 폭에서 "전체 / 과제·지원사업·기타" 라벨 줄바꿈 없이 표시
- [ ] 윗 행에 입금 완료율 없음(아래 입금 진행 헤더로 이동)
- [ ] 입금 진행 막대: 완료(초록)+남은(회색) 비율, 합 100%
- [ ] 완료율 ≥50% 연빨강 톤·≥100% 진빨강 톤(기존 규칙 유지)
- [ ] 범례 금액/퍼센트 = 완료/남은(예정 기준)
- [ ] "지원금 구성(당해)" 섹션 사라짐, 자금 구성(당해)은 그대로
- [ ] 입금 예정 0일 때 막대 0/100·완료율 0 정상

### ⚠️ 알아둘 점
- 입금 '완료'는 총액(`yearActual`)만 있고 과제/지원사업/기타 분리 집계는 없어 입금 진행은 총액 기준.
- 미사용이 된 CSS(`.big-stats--triple`·`.big-stats-divider`·지원금구성 팔레트)는 잔존하나 렌더 영향 없음(추후 정리 가능).

---

## 이월금 — 다음 연차로 이월 (1단계: 시스템 내 추적·표시)

> 건드린 파일: `project-labor.js`·`project-labor.html`. **데이터 추가 = `projectBudget/{id}_year{N}` 문서에 `carryoverCash`/`carryoverSelfCash` 두 필드.** 마이그레이션 0, 입력 안 하면 기존과 100% 동일.

### 개념
- **이월금 = 받았는데 그 해에 안 쓴 돈** → 다음 연차로 넘겨서 씀. 입금·미수와는 **완전 별개**(받은 돈이라 입금 예정/미수 계산 안 건드림).
- 잔액(차액) = (예산 + 이월) − 누계. 이번 연차 잔액을 **다음 연차 이월금**으로 넘김.
- **항목별**: 지원금(cash)·자부담현금(selfCash)만 이월. **현물(inkind)은 이월 불가.**

### 무엇을 개발했나
1. **항목별 이월 읽기/계산** — `getCarryover()`를 숫자 → `{cash, selfCash}` 객체로 교체(현 연차 예산 문서에서 읽음). `getRemainingBreakdown()`이 지원금·자부담엔 이월을 더하고 **현물엔 안 더함**.
2. **예산 로드 확장** — `state.yearBudget`에 `carryoverCash`/`carryoverSelfCash` 로드(`loadLaborData`·`reloadYearBudget` 두 곳).
3. **잔액 박스 "이월금" 칸 재구성** — (껍데기·항상 0) → 전년도→올해 받은 **지원금/자부담 항목별** 표시 + **"📥 N차년도로 이월" 버튼**(마지막 연차면 "이월 대상 없음").
4. **이월 확정 모달** — 지원금 이월(예산−실제 잔액 자동 프리필·수정 가능)+자부담현금 이월(0·직접 입력). 현물 칸 없음. 확정 시 **다음 연차 예산 문서에 merge 저장**(`{merge:true}`, 다른 예산값 보존). 입력 콤마 자동 포맷.
   - 프리필 지원금 잔액 = `max(0, 예산지원금 − 실제탭 지원금 누계)`. 자부담은 셀 집행 집계가 없어 0 프리필·수동.

### 🧪 테스트 체크리스트 (이월금)
- [ ] 다년 과제 1차년도: 잔액 박스 "이월금" 0, "📥 2차년도로 이월" 버튼 보임
- [ ] 실제 탭에 지원금 입력 → 차액(차액 박스) 줄어듦 확인 후, 이월 버튼 → 모달 프리필 = 예산−실제 잔액
- [ ] 모달: 지원금 수정 가능, 자부담 직접 입력, 현물 칸 없음, 콤마 자동
- [ ] "이월 확정" → 토스트. **2차년도 탭**으로 이동 시 이월금 박스에 지원금/자부담 표시, 차액에 반영(현물엔 반영 안 됨)
- [ ] 마지막 연차: 버튼 대신 "마지막 연차 (이월 대상 없음)"
- [ ] 예산 미입력 연차: 모달 프리필 0~음수 → 0으로 클램프, 저장은 됨
- [ ] 입금/미수(자금 관리)는 **변화 없음**(이월금이 입금 예정에 안 더해짐)
- [ ] 이월금 미입력 과제: 기존과 동일(이월금 0)

### ⚠️ 알아둘 점
- **자부담현금 잔액은 자동값이 부정확**(셀에 selfCash 집행이 안 잡혀 누계 0 → 잔액=예산 전액). 그래서 모달에서 **직접 입력**하게 둠. 지원금은 실제 탭에 잡혀서 자동 잔액 정확.
- 저장 위치를 기존 껍데기(`projects.yearBudgets[].carryover` 숫자)에서 **`projectBudget` 연차 문서의 두 필드**로 옮김(예산값과 한 곳, 단일 doc merge). 껍데기엔 값이 없었으므로 마이그레이션 영향 0.
- 이월금은 **현 연차 예산 문서가 받는 쪽**(carryover-into). 1차년도에서 확정하면 2차년도 문서에 기록됨.
- 다음 단계 후보: ① 예산 페이지(project-budget)에도 이월금 표시/입력, ② 자금 관리·과제 관리 표에 "이월 N" 노출, ③ C5 외부 전달의 "계속비(이월금)" 칸 매핑.

---

## 인건비 — 인력 목록 연차별 표시 (예산에서 보낸 그 연차만 보이기)

> 건드린 파일: `project-labor.js` 단독. 데이터 모델 변경 0(명단 `personIds` 구조 그대로). **연차 정렬(§4.10)의 첫 조각** — 창이 '연도'에서 '연차 기간'으로 바뀌면 같은 필터가 그대로 따라감.

### 문제
- 셀(금액)은 월(`YYYY-MM`)별로 분리 저장되는데, **인력 명단(`personIds`)은 과제 전체에 하나만** 공유. 예산에서 보낼 때 명단에 합쳐 넣음.
- → 24년을 보내든 26년을 보내든 한 명단에 다 쌓여, 인건비 화면이 **어느 연도를 봐도 명단 전체**를 그림(다른 연도 사람은 빈 줄로). 사용자 케이스: 26년 입력 상태에서 24년 보내고 24년 조회 → 26년 인력까지 보임.

### 무엇을 개발했나
1. **`personHasDataInView(personId)`** — 현재 보는 연도 1~12월에 그 사람 셀이 planned/actual 중 하나라도 있으면 true(빈 셀=키만 있어도 소속 인정).
2. **`getVisiblePersons()`** = `getPersons()` 중 위 조건 통과만. **표 빌더(`buildTable`)·탭 카운트(`updateTabCounts`)에만** 적용. (재배열·저장·확정·⚡일괄·복사 로직은 명단 전체 `getPersons()` 유지 — 숨은 인력은 그 창에 셀이 없어 자연히 0 기여/skip.)
3. **수동 인력 추가(`addPersonToProject`)** — 추가 시 **현재 보는 연차의 첫 노출 월에 빈 셀(rate 0) 자리**를 만들어, 추가한 그 연차에서만 보이게. 활성 탭(예상/실제) 데이터맵에 생성.

### 🧪 테스트 체크리스트 (연차별 인력 표시)
- [ ] 26년 인력 있는 과제에 24년 예산 보냄 → **24년 조회 시 24년 인력만**(26년 인력 사라짐)
- [ ] 26년 조회 시 26년 인력만
- [ ] 데이터 없는 연도(예: 25년) 조회 → 인력 0명, 빈 표
- [ ] 탭의 "N명"이 그 연차에 보이는 인원수와 일치
- [ ] 2차년도 화면에서 **수동 "인력 추가"** → 그 사람 2차년도에만 보임, 1차년도엔 안 보임
- [ ] 수동 추가 후 바로 참여율/금액 입력 가능(빈 줄로 떠 있음)
- [ ] ⚡ 일괄 계산·이전 분기 복사 시 다른 연도 인력이 끌려오지 않음(rate 0/셀 없음이라 skip)
- [ ] 분기 보기에서도, 같은 해 다른 분기에만 데이터 있는 사람이 보임(연도 기준 판정)

### ⚠️ 알아둘 점
- 필터 기준이 지금은 **달력연도(state.year 1~12월)**. 연차가 달력연도에 걸치는 과제는 **연차 정렬(§4.10)** 적용 후 '연차 실제 기간'으로 정확해짐(이 필터는 창만 바뀌면 그대로 동작).
- 인력 추가 모달의 "이미 추가된 인력" 섹션은 아직 **명단(personIds) 전체 기준**. 다른 연차에 이미 있는 사람을 또 다른 연차에 수동 추가하려는 경우는 연차 정렬에서 모달 재정비 때 다룸(현재는 예산 보내기로 연차별 셀 생성이 정상 경로).
- 수동 추가가 만드는 빈 셀은 합계·환급에 0 기여(무해), ⚡ 일괄은 rate 0이라 건드리지 않음.

---

## 연차 기준 정렬 — 1단계: 전체 보기를 연차 기간으로 (§4.10)

> 건드린 파일: `project-labor.js` 단독. 데이터 모델 변경 0(셀=월 단위·예산/이월금=연차 단위 그대로). **연도(시작연도) 유지 — 그 해 시작하는 연차를 통째로** 표시.

### 문제
- 인건비 화면의 월 축이 "달력 1~12월" 고정 → 연차가 해를 걸치면(예: 24.11~25.4) **24년 화면엔 11·12월만, 나머지(25.1~4)는 25년 화면으로** 쪼개져 헷갈림. 합계도 25년분을 빠뜨림.

### 무엇을 개발했나 (1단계)
1. **연도→연차 매핑을 '시작연도' 기준으로** (`getYearIndexForState`): 범위 겹침→시작연도 일치. "2027이 1차로 오인" 버그도 해소.
2. **`getActiveYearMonths()`** — 활성 연차의 실제 월 목록(시작~종료, 달력연도 걸쳐도 통째로). 연차 없는 과제는 달력 12개월 폴백. 꼬리연도(이 해에 시작 연차 없음)는 `[]`.
3. **표/누계/차액/이월금/인력필터** 모두 이 연차 기간 기준으로(`getVisibleMonths`·`getCumulativeBreakdown`·`personHasDataInView`). 월 헤더는 이미 "YYYY년 M월" 표기라 걸친 달도 명확.
4. **행 합계·그랜드 합계·연봉 재계산·연봉 변경 반영** 6곳을 `getAllMonths(state.year)`→`getActiveYearMonths()`로(연차 걸치면 합계가 틀리던 것 수정). 비연차 과제는 폴백으로 기존 동작.
5. **"12월까지 채우기" → "연차 끝까지 채우기"** (`fillRateToYearEnd`).
6. **꼬리연도 안내**: 연차가 24~25 걸치고 25엔 시작 연차 없으면, 25년 표에 "이 기간은 N차년도(24년 시작)의 일부입니다. 24년에서 보세요" 메시지.
7. **분기 자동 접힘 가드**: 연차 과제는 분기 자동 접힘 미적용(전체 펼침) — 1단계에서는 분기 보기가 사실상 전체 보기로 동작.

### 🧪 테스트 체크리스트 (연차 정렬 1단계)
- [ ] 연차 24.11~25.4 과제: **24년** 조회 → 24.11·24.12·25.01·25.02·25.03·25.04 **6칸 통째로** (헤더에 연도 표기)
- [ ] 그 6개월 누계·차액·이월금이 6개월 전체로 맞게 계산(예산과 정합)
- [ ] 행 합계/맨 아래 합계가 25년분까지 포함
- [ ] **25년** 조회(단일 연차라 25 시작 없음) → "이 기간은 1차년도(24년 시작)의 일부…24년에서 보세요" 안내
- [ ] 다년 과제: 2차년도(25년 시작) 조회 → 2차 연차 통째로
- [ ] 달력연도 정렬 과제(1차=24.1~24.12): 24년 = 1~12월 그대로
- [ ] 연차 정보 없는 과제: 기존 달력연도/분기 동작 그대로(회귀 없음)
- [ ] "연차 끝까지 채우기"가 연차 마지막 달까지만 채움(4월 끝이면 4월까지)
- [ ] 인력 필터(연차별 표시)도 연차 기간 기준으로 동작

### ⚠️ 알아둘 점 / 2단계 예정
- **2단계(바로 다음)**: 분기 보기에서 연차 밖 월 **회색+잠금**. 1단계에선 연차 과제의 분기 보기가 잠시 전체 보기로 동작(분기 자주 쓰면 2단계 우선).
- "이전 분기 복사"는 분기 기반이라 연차 과제에선 2단계에서 같이 정리.
- 월 접기 캐시는 월 번호(1~12) 기반 — 연차 ≤12개월이면 월 번호 안 겹쳐 정상. 13개월↑ 연차는 추후.
- **자금관리 연차 적용**은 이 다음(인건비 안정화 후).

---

## 인건비 — 잠긴/회색 셀 합계 처리 (입사 전·퇴사 후 제외)

> 건드린 파일: `project-labor.js` 단독. 데이터 변경 0(합계 산정 로직만).

### 문제
- 맨 아래 합계·행 합계가 셀의 회색/잠금 여부와 무관하게 **모든 셀을 더함**. 특히 예산 분배가 입사 전/퇴사 후 달까지 금액을 채워두면, 그 회색 칸 값이 합계에 섞여 부풀려짐. (사용자 리포트: footer 17,456,669 = 검은 행 + 회색 행 합)

### 무엇을 개발했나
1. **`isPersonActiveInYm(person, ym)`** — 입사월 이전 또는 퇴사월 이후면 false. (입퇴사 정보 없으면 true=전부 활성, 안전 폴백.)
2. 합계 산정에서 **비활성 월 제외**: `recalcSums`(월별 합계 + 행/그랜드 합계), `getCumulativeBreakdown`(누계 박스), `getActualCashSumForYear`(이월 프리필) 모두 동일 기준.
3. **사용자 직접 잠금(`isYmManuallyLocked`)은 제외하지 않음** — 값을 일부러 남긴 것으로 보고 합계 유지. (입사 전/퇴사 후만 제외.)

### 🧪 테스트 체크리스트
- [ ] 연차 중간 입사자: 입사 전 달의 회색 칸 금액이 **합계에서 빠짐**(맨 아래/행 합계/누계 모두)
- [ ] 퇴사자: 퇴사월 이후 회색 칸이 합계에서 빠짐, 퇴사월까지는 포함
- [ ] 입퇴사 정보 없는 인력: 합계 그대로(회귀 없음)
- [ ] 직접 잠근 칸은 여전히 합계에 포함(의도된 값 유지)

### ⚠️ 알아둘 점
- 비활성 회색 칸은 **값은 그대로 보이되 합계에만 안 잡힘**. "칸엔 숫자가 있는데 합계엔 없다"가 헷갈리면, 비활성 칸 표시값을 비우는 것도 가능(추후).
- 고친 뒤에도 합계에 잡히는 회색 칸이 있으면 = **직접 잠근 칸**. 그것도 빼고 싶으면 별도 처리.

---

## 인건비 — 나눔(분류 split) 개선 + "채우기" 문구

> 건드린 파일: `project-labor.js` 단독. 데이터 모델 변경 0.

### 1) "이 월부터 12월까지 채우기" → "이 월부터 채우기"
- 우클릭 메뉴 문구에서 "12월까지" 제거(월 명시 안 함). 로직은 이미 연차 끝까지 채움.

### 2) 나눔 줄 순서 변경 (지원금 고정 위 → 자유 순서)
- 기존: 줄 구분 드롭다운이 "다른 줄이 쓰는 분류"를 옵션에서 빼서 스왑 불가 → 항상 주 분류(지원금)가 위.
- 변경: 드롭다운에 **모든 분류 노출**. 다른 줄이 쓰는 분류를 고르면 **두 줄을 맞바꿈**(중복 방지 + 순서 변경). 예: 위 줄을 '현물'로 바꾸면 아래 '현물' 줄이 '지원금'으로 내려감.

### 3) 나눔 줄 가로 채우기("옆으로 쭉") 동작 수정 (버튼만 있고 실행 안 되던 것)
- 원인: 나눔 줄 참여율 칸은 `field='rate'` + `split='1'` + `splitField`인데, 우클릭→채우기가 `splitField`를 안 넘겨 `fillRateToYearEnd`가 **주 분류만** 채우고 다른 분류를 0으로 덮어써서 나눔 줄엔 안 먹힘.
- 수정:
  - 우클릭 핸들러가 `splitField` 캡처 → `showCellContextMenu`/`positionAndBindCtxMenu` 통해 전달.
  - `fillRateToYearEnd(personId, fromYm, rate, splitField)` — splitField 있으면 **그 분류 참여율만** 타깃 월에 복사, 금액=급여×%, **다른 분류 보존**, `cell.rate`=세 분류 합.
  - undo는 `__cellSnapshot`(셀 전체 스냅샷)으로 복원 — rates까지 정확히 되돌림(`undoLastCell`에 분기 추가).

### 🧪 테스트 체크리스트
- [ ] 우클릭 메뉴 "이 월부터 채우기"로 표기
- [ ] 나눔 줄 위/아래 순서: 드롭다운으로 현물을 위로 올리면 지원금이 아래로 내려감
- [ ] 나눔 줄에서 우클릭→채우기: **그 분류만** 옆으로 채워지고 다른 분류(같은 사람 다른 줄) 값은 그대로
- [ ] 채운 뒤 Ctrl+Z: 나눔 값(rates 포함) 정확히 복원
- [ ] 단일(나눔 아님) 줄 채우기: 기존대로 동작

---

## 인건비 — 재입사 과거기간 수동 입력 ((B) 절충, 모델 변경 없음)

> 건드린 파일: `project-labor.js` 단독. 데이터 모델 변경 0(personRoles에 `unlockedYms` 배열만 추가 — 기존 personRoles 저장에 같이 보존).

### 배경
- 인력 정보는 입사일·퇴사일 한 쌍만 보유 → 퇴사 후 재입사하면 "현재 입사일" 기준으로 그 이전(첫 재직) 월이 "입사 전"으로 자동 잠겨 입력 불가. 또 직전 합계 수정(입사 전/퇴사 후 제외) 때문에 첫 재직 인건비가 합계에서도 빠짐.
- (A) 재직 구간 다중 지원(모델 변경) 대신 **(B) 수동 입력허용**으로 임시 대응. 디테일은 추후.

### 무엇을 개발했나
1. **`isYmManuallyUnlocked` / `setYmUnlocked`** — `personRoles[pid].unlockedYms = ['2023-06', ...]`. 그 월은 자동 기간잠금(입사 전/퇴사 후)을 무시하고 **편집 가능 + 합계 포함**.
2. 잠금 판정 우선순위 통일(일반행·나눔행·가로채우기): **수동잠금 > 입력허용 > 자동 기간잠금**. (수동으로 잠근 건 그대로 잠김, 입력허용은 기간잠금만 풂.)
3. `isPersonActiveInYm` — 입력허용 월은 활성으로 간주(합계에 포함).
4. 우클릭 메뉴: 입사 전/퇴사 후 자동잠금 셀에서 **"🔓 이 월 입력 허용 (재직 기간 외)"**, 허용된 셀에선 **"🔒 입력 허용 취소"**. → `toggleManualUnlock`.

### 사용법 (재입사자 과거 입력)
1. 과거 월(회색) 칸 우클릭 → "🔓 이 월 입력 허용".
2. 칸이 편집 가능해지면 참여율/금액 입력. 합계에도 자동 반영.
3. 되돌리려면 우클릭 → "🔒 입력 허용 취소".

### ⚠️ 알아둘 점 / 한계 (추후 디테일)
- 과거 기간이 **다른(이전) 연차**라서 그 연차 화면에 그 사람 데이터가 아예 없으면, 인력 표시 필터상 그 연차에 안 나타나 우클릭 자체가 안 됨. 이 경우 그 연차에서 "인력 추가"로 먼저 띄운 뒤 입력해야 함. (같은 연차 안의 과거 월은 바로 가능.)
- 정식 해법은 재직 구간 다중 지원(모델 변경) — 추후 결정.

---

## 인건비 — sticky 패널 용어 통일 (자부담 → 현금)

> 건드린 파일: `project-labor.js` 단독. 표시 라벨만.

- 표는 `fundTypeLabel('자부담현금') = '현금'`을 쓰는데 sticky 박스(예산총액/이월금/누계/차액)만 "자부담"으로 하드코딩돼 어긋남.
- 4곳 모두 **"현금"** 으로 통일 → 지원금 / 현금 / 현물.
- 누계 박스 제목의 "· 12개월"은 연차가 달력연도와 다를 수 있어(6개월 등) 부정확 → 제거("누계 (예상)").

### 이월금 확정 흐름 (참고, 코드 변경 없음)
- 이월금 박스 = **전년도→올해 '받은' 금액(carry-IN)**. 1차년도는 받은 게 없어 0이 정상.
- 확정: 이월금 박스의 **"📥 N차년도로 이월"** → 모달(지원금 잔액 프리필=예산−실제, 자부담현금 0 직접입력, 현물 없음) → 저장 시 `projectBudget/{id}_year{N+1}.carryoverCash/SelfCash`(merge).
- 결과는 **다음 연차(N+1차)** 이월금 박스 + 차액(예산+이월−누계)에 반영. 보낸 연차(N차) 박스는 그대로 0(받은 게 없으니).
- 연차 정렬 후: 다음 연차는 **그 연차가 시작하는 연도**를 골라야 보임(꼬리연도는 안내만).

---

## 인건비 — 이월 버튼을 '차액' 박스로 이동 + 넘길 금액을 차액 기준으로

> 건드린 파일: `project-labor.js` 단독. 데이터/저장 위치 변경 없음.

### 이유
- "📥 N차년도로 이월" 버튼이 **이월금(받은 돈, carry-IN)** 박스에 있어 혼란. 실제로 다음 연차로 넘기는 건 **차액(예산+이월−누계, 남은 돈)** 이라, 버튼은 차액 박스에 있는 게 맞음(사용자 지적).

### 변경
1. **버튼 위치**: 이월금 박스 → **차액 박스**(공식 아래). id(`pl-carryover-btn`) 그대로라 기존 바인딩 유지. 이월금 박스는 '받은 금액 표시'만.
2. **모달 프리필 금액**: 기존 `예산 − 실제` → **`예산 + 이월 − 실제집행`**(=차액). 전년도에서 받은 이월금 잔액까지 다음 연차로 넘어가게 됨(이전엔 받은 이월금이 누락됐음). 모달 안내문도 "예산 + 이월 − 실제 = 잔액"으로.
   - 자부담현금은 여전히 0 프리필·직접 입력(셀에 집행 집계 없음).
   - 잔액 음수면 0.

### 🧪 테스트 체크리스트
- [ ] 차액 박스 아래 "📥 N차년도로 이월" 버튼이 보임 / 이월금 박스엔 버튼 없음
- [ ] 버튼 클릭 → 모달, 지원금 프리필이 차액(지원금)과 일치
- [ ] 전년도 이월 받은 연차(2차+)에서 이월하면, 받은 이월금 잔액까지 포함돼 넘어감
- [ ] 마지막 연차: 버튼 대신 "마지막 연차 (이월 대상 없음)"

---

## 버그픽스 — 이월금이 다음 연차에 반영 안 되던 문제 (예산 저장이 덮어씀)

> 건드린 파일: `project-budget.js` 단독. 데이터 모델 변경 0.

### 증상
- 인건비 페이지에서 1차 → 2차로 이월해도, 2차 이월금이 0으로 보임(반영 안 됨).

### 원인
- 이월금은 `projectBudget/{id}_year{N}` 문서의 `carryoverCash/carryoverSelfCash`에 저장됨(인건비 페이지가 merge로 기록 — 정상).
- 그런데 **예산 페이지 저장(`project-budget.js`)이 같은 문서를 `set(...)` 으로 통째로 덮어씀** — 그 데이터에 carryover 필드가 없어, 예산을 저장하는 순간 이월금이 지워짐.

### 수정
- 예산 저장 `batch.set(..., { merge: true })` 추가 → carryover 필드 보존(예산/rows/period만 갱신, 이월금은 그대로).

### 🧪 테스트 체크리스트
- [ ] 1차에서 "📥 2차년도로 이월" 확정 → 2차 이월금 박스에 금액 표시
- [ ] 그 뒤 **예산 페이지에서 2차 예산을 저장**해도 이월금 유지(0으로 안 사라짐)
- [ ] 2차 차액(예산+이월−누계)에 이월금 반영
- [ ] 예산 입력/수정 정상(merge라 다른 필드 영향 없음)

### ⚠️ 중요 — 기존 데이터
- 이미 "이월 → 예산 저장" 순서로 한 번 지워진 이월금은 복구 안 됨. **이 파일 반영 후 1차에서 이월 버튼을 다시 한 번** 눌러주면 그때부터 유지됨.

---

## 연차 정렬 2단계 — 분기 보기 + 연차 밖 월 회색+잠금

> 건드린 파일: `project-labor.js` 단독. 데이터 모델 변경 0.

### 의도
1단계에서 연차 과제는 분기 토글이 사실상 무력화(연차 전체를 한 화면에)됐었음. 2단계에서 **분기 보기를 되살리되**, 분기에 걸친 연차 밖 월(예: 연차가 4월에 끝나는데 2분기 화면엔 4·5·6월이 보임 → 5·6월)을 **회색+잠금**으로 표시.

### 동작
- **getYearQuarters()** — 연차가 닿는 '달력 분기' 목록. 각 분기는 그 분기의 3개월 전체(연차 밖 패딩 월 포함).
  - 예: 연차 24.11~25.4 → `[24Q4(10·11·12), 25Q1(1·2·3), 25Q2(4·5·6)]` (연차 밖 = 10월, 5월, 6월)
- **isYmInActiveYear(ym)** — 그 월이 연차 실제 기간 안인지. 연차 없는 과제는 항상 true(기존 동작 유지).
- **분기 보기(getVisibleMonths)** — 연차 과제는 `state.quarter`번째 분기의 3개월을 보여줌. 전체 보기는 1단계대로 연차 전체.
- **잠금/회색** — 일반행·나눔행 모두 `연차 밖 월 → 무조건 잠금/회색`(최우선, 수동 입력허용으로도 안 풀림). 그 아래 직접잠금 → 입사전/퇴사후 순.
- **분기 네비** — 연차 과제는 `getYearQuarters()` 목록 안에서만 이동(1~N, 연도/연차 안 바뀜 — 연차는 연도 셀렉터로 전환). 연차 없는 과제는 기존 달력분기 이동(연도 롤오버).
- **분기 라벨** — 연차 과제는 실제 달력 분기 표시: `2025년 2분기 (3/3)`. (n/N = 연차의 몇 번째 분기인지)
- **이전 분기 복사** — 연차 과제는 `getYearQuarters()`의 한 칸 앞 분기에서 복사. 연차의 첫 분기면 "이전 분기 없음" 경고. 연차 밖(패딩) 칸엔 복사 안 함.

### 🧪 테스트 (예: 연차 24.11~25.4 과제, 분기 토글 ON)
- [ ] 1분기 화면 = 24년 4분기: **10월 회색+잠금**, 11·12월 입력 가능
- [ ] 2분기 화면 = 25년 1분기: 1·2·3월 모두 입력 가능(회색 없음)
- [ ] 3분기 화면 = 25년 2분기: 4월 입력 가능, **5·6월 회색+잠금**
- [ ] 분기 ◀▶ 로 1↔2↔3 이동, 양끝에서 멈춤(연도 안 바뀜), 라벨 "25년 2분기 (3/3)" 식
- [ ] 회색 월엔 입력/우클릭 입력허용 불가 (재직기간 외와 달리 못 푼다)
- [ ] 회색 월은 합계(월합/누계/행합)에 안 들어감 (isPersonActiveInYm/isYmInActiveYear와 별개로, 잠금=inactive)
- [ ] "이전 분기 복사": 25Q1→25Q2 복사 시 4월만 들어가고 5·6월(회색)엔 안 들어감
- [ ] **전체 보기 토글** 시엔 회색 없이 연차 전체(24.11~25.4)만 보임(1단계 그대로)
- [ ] 연차 없는 과제: 분기 보기 기존과 동일(회색 없음, 달력분기 이동)

### 주의
- 달력연도에 딱 맞는 연차(예 24.1~24.12)는 분기 4개·회색 0 (정상).
- `state.quarter`가 연차 분기 수보다 크면 표시·네비 모두 clamp 처리(깨지지 않음).

---

## 예산 페이지 급여 — 급여총액=연봉 / 실지급액=월급(ceil) 정합

> 건드린 파일: `project-budget.js` 단독. 데이터 모델(필드명) 변경 0.

### 증상
1. "연봉 불러오기 → 전체 적용" 시 **급여총액에 월급이 들어옴**(연봉이 들어와야 함).
2. **급여총액을 입력해도 실지급액이 자동계산 안 됨**. (자동계산은 마스터 페이지처럼 ceil(연봉/12) → …334/…667 이어야 함)

### 규칙 (마스터 페이지와 동일)
- **급여총액 = 연봉(연 단위)** — 필드명은 기존 `row.monthlySalary` 유지(의미만 연봉).
- **실지급액 = 월급 = `Math.ceil(연봉/12)`** — 필드 `row.actualPay`. 원 단위 올림(예: 100,504,000/12=8,375,333.33 → **8,375,334**).
- 내보내기(`project-budget-export.js`)는 `actualPay`(월급)를 기준으로 급여총액=actualPay×12 계산 → 그대로 정합.

### 수정 (4곳 + 헬퍼)
- `getPersonActualAnnual(p)`, `monthlyFromAnnual(annual)=ceil(/12)` 헬퍼 추가. `getSlotMonthly`가 `annual`도 반환.
- ① 슬롯 전체/행별 적용: `monthlySalary=연봉`, `actualPay=ceil(연봉/12)`
- ② **급여총액 수동 입력 → 실지급액 자동계산** (입력칸도 즉시 갱신)
- ③ 빈 항목 "실제로 채우기": 연봉/월급으로
- ④ 인력(🔍) 지정 시: 연봉/월급으로

### 🧪 테스트
- [ ] "전체 적용" → 급여총액에 **연봉**(예 100,504,000), 실지급액에 **월급**(8,375,334)
- [ ] 급여총액 칸에 연봉 직접 입력 → 실지급액이 즉시 ceil(/12)로 자동 표시(…334/…667)
- [ ] 실지급액 직접 수정은 그대로 유지(급여총액은 안 바뀜 — 한 방향만 자동)
- [ ] 총액(=실지급액×참여율×참여개월) 정상
- [ ] 행별 드롭다운으로 슬롯 바꿔도 동일 규칙

### ⚠️ 기존 데이터
- 이미 저장된 행은 **급여총액에 옛 월급값**이 남아 있음(자동 변환 안 함). 해당 연차에서 **"전체 적용" 한 번** 다시 누르거나 급여총액을 다시 입력하면 연봉 기준으로 정리됨.
- (참고) 내보내기 폴백이 actualPay 없을 때 monthlySalary를 월급으로 오인할 여지 — 정상 흐름(실지급액 항상 채움)에선 발생 안 함. 추후 export 폴백도 정리 가능.

---

## 예산 페이지 — 셀 방향키 이동 + 총액 칸 직접입력

> 건드린 파일: `project-budget.js`. (방향키는 신규 기능, 총액은 totalOverride 필드 추가 — 비파괴/선택적)

### 1) 방향키/엔터 셀 이동 (그동안 예산 페이지엔 아예 없었음)
- 대상: 표 본문 입력칸(`.pb-cell-number`/`.pb-cell-text`). 셀렉트는 제외(드롭다운 보존).
- ↑↓: 같은 열(같은 field)에서 위/아래 행. ←→: 값 전체선택 상태일 때만 좌우 셀. Enter: 아래(Shift+Enter 위). 끝에서 wrap.
- `pbGetCellInputs()`/`pbMoveFocus()` + `keydown` 리스너(인건비 moveFocus와 동일 패턴).
- ※ "방향키 안 먹힘"은 그동안 예산 페이지에 기능이 없어서였음(인건비 페이지에만 있었음).

### 2) 총액 칸 직접입력 (totalOverride)
- 총액 = 기존 읽기전용(실지급액×참여율×참여개월) → **입력칸으로 변경**.
- 직접 입력하면 그 값이 우선(`row.totalOverride`). **비우면(0/빈칸) 다시 자동계산**으로 복귀.
- 현물처럼 월급×율×개월로 안 떨어지는 총액을 직접 넣을 때 용도.
- `calcRowTotal`이 override 우선 반환 → 지원금/현금/현물·합계·요약 모두 그 값 반영.
- 읽기전용 셀이 4개(총액·지원금·현금·현물)→3개(지원금·현금·현물)로 줄어 `updateRowDerivedDisplay` 인덱스 보정함.

### 🧪 테스트 (예산 페이지)
- [ ] 입력칸에서 ↑↓로 같은 열 위아래 이동, ←→(전체선택 시) 옆 칸, Enter 아래로
- [ ] 총액 칸에 숫자 직접 입력 → 그 값이 총액/지원금or현금or현물/합계에 반영
- [ ] 급여총액·실지급액·율·개월 바꿔도 입력한 총액 유지(직접입력 우선)
- [ ] 총액 칸 비우면 자동계산(실지급액×율×개월)으로 복귀
- [ ] 저장/재로드 후 직접입력 총액 유지
- [ ] (배포 시) project-budget.js + firebase.json 같이 올리고 캐시 한 번 비우기

---

## 자부담현금 합산/별도 보기 — 인건비 sticky 박스 (1단계: 인건비 페이지)

> 건드린 파일: `project-labor.js` (sticky 박스 표시만). 데이터/환급 로직 변경 0.

### 개념
- 지원금(정부, 환급대상) vs 자부담현금(기관, 환급제외)은 **데이터에선 항상 분리**. "합치느냐"는 **보기**에서만.
- 과제 플래그 `hasSelfCash`(예산 페이지 체크박스)로 전환:
  - **체크 ON = 별도 보기**: 지원금 / 현금(자부담) 두 줄 (기존).
  - **체크 OFF = 합산 보기**: 현금을 "지원금+자부담" 합계 한 줄로, 그 아래 **지원금 서브라인**(환급 기준)을 작게 표시.
- 환급/정산 계산은 모드 불문 항상 지원금만.

### 적용 (예산총액·누계·차액 세 박스 + 이월금)
- 예산총액: 합산 시 현금 합계 + `└ 환급기준(지원금)`.
- 누계: 합산 시 현금 집행 합계 + `└ 지원금 집행`.
- 차액: 합산 시 현금 차액 합계 + `└ 환급 가능(지원금)` = max(0, 지원금 잔여). (초과집행이면 0 — 자부담이 메움)
- 이월금: 합산 시 현금 한 줄(서브라인 없음).
- `renderStickyBoxes` 안에 `combined`(=!hasSelfCash) + `cashRows()` 로컬 헬퍼로 처리.

### 🧪 테스트
- [ ] 예산 페이지에서 자부담현금 체크 ON → 인건비 박스에 지원금/현금 2줄(기존)
- [ ] 체크 OFF → 현금 1줄(지원금+자부담 합계), 세 박스에 지원금 서브라인
- [ ] 환급 가능(차액) = 지원금 잔여, 음수면 0 표시
- [ ] 현물은 어느 모드든 그대로

### 다음
- 인건비 표 푸터/컬럼도 합산 반영할지 확인 (지금은 박스만).
- 예산 페이지(project-budget) 요약/차액에도 같은 합산+지원금 서브라인 적용 (2단계).

---

## 자부담현금 합산/별도 — 예산 페이지 (2단계: project-budget)

> 건드린 파일: `project-budget.js`. 데이터/환급 로직 변경 0(표시·입력 게이팅만).

### 변경
1. **자부담현금 항상 입력 가능** (체크 OFF에서 막던 것 해제):
   - 행 분류 드롭다운에 '자부담현금' 항상 노출 (`allowSelfCashOption=true`)
   - 행 '현금' 셀 / 푸터 합계 회색 비활성 제거 (`selfCashCellCls=''`, `tfootSelfCashCls=''`)
   - 예산 요약 자부담 입력칸 항상 활성 (`selfCashInput.disabled=false`), 컬럼 is-disabled 제거
   - → 체크박스 의미: "자부담 사용여부"가 아니라 **별도/합산 "표시" 토글**. 자부담은 늘 입력 가능, 데이터 분리 저장.
2. **합산(OFF) 보기 줄** — 예산 요약(`#pb-budget-summary`) 아래 주입(`#pb-combined-note`):
   - 현금 예산 = 지원금+자부담, └ 환급기준(지원금)
   - 현금 차액 = 지원금차액+자부담차액, └ 환급 가능(지원금) = max(0, 지원금 차액)
   - 체크 ON이면 숨김(기존 분리 표시 그대로). 토글 시 renderTable→renderBudgetSummary로 즉시 갱신.

### 🧪 테스트 (예산 페이지)
- [ ] 자부담 체크 OFF에서도 지원금/현금(자부담) 둘 다 입력됨(예산 입력칸·행 분류·행 현금셀 정상)
- [ ] OFF면 요약 아래 "합산 보기" 줄: 현금 예산/차액 합계 + 환급기준·환급가능(지원금)
- [ ] 체크 ON↔OFF 토글 시 합산 줄 즉시 표시/숨김
- [ ] 지원금 초과 집행이면 환급 가능 0
- [ ] 인건비 페이지 sticky 박스(1단계)와 숫자 일치

### 남은 것
- 인건비 표 푸터/행 셀도 합산 표기할지 (지금은 sticky 박스만). 예산 페이지 행별 셀도 분리 표시 유지 중(합산은 요약 줄로).

### 버그픽스 — 합산 보기 줄이 예산 입력 즉시 갱신 안 되던 문제
- 원인: 예산 입력칸 핸들러는 차액 셀만 직접 갱신하고 `renderBudgetSummary`를 안 불러서, 합산 줄(`#pb-combined-note`)이 페이지 재방문 때만 갱신됨.
- 수정: 합산 줄 렌더를 `renderCombinedNote(yd, diffCash, diffSelfCash)`로 분리 → `renderBudgetSummary` + 예산 입력 핸들러 양쪽에서 호출. 이제 입력 즉시 반영.
- (행별 셀 수정은 원래 renderBudgetSummary를 거쳐 반영됨)

---

## 예산→인건비 전송: 명단 순서 유지 + 기존/신규·구분 전달

> 건드린 파일: `project-budget.js` (선택 전송 + 분배 두 경로). 데이터 모델 변경 0.

### 증상
1. 이름을 보내면 인건비에 **입력 순서대로 안 감** (특히 재전송 시).
2. **기존/신규, 현물/지원금(구분)이 안 넘어감.**

### 원인
- 인건비는 `personIds` 순서로 렌더하고 `personRoles[pid].newOrExisting/cashOrInkind`로 기존신규·구분을 읽음.
- 전송 코드가 **personRoles를 안 보냄**(personIds·cells만 저장) → 분류 미전달.
- personIds는 "없으면 append"라 **이미 있던 인력은 옛 순서 유지** → 재전송해도 순서 안 바뀜.
- (추가로 선택 전송 함수에서 personIds append 라인이 outputs본에 누락돼 있던 것도 같이 복구)

### 수정 (선택 전송 + 분배 둘 다)
- 전송 시 `personRoles[pid] = {...기존, newOrExisting: 예산행값, cashOrInkind: 예산행값}` 병합 저장(기존 lockedYms 등 보존).
- 명단 순서: **보낸 인력을 예산 입력 순서대로 앞에** 두고 그 외 기존 인력은 뒤에 유지 → 전체 전송 시 예산 순서와 동일, 재전송해도 예산 순서로 정렬.
- `batch.set(metaRef, { personIds, personRoles, ... }, {merge:true})`.

### 🧪 테스트
- [ ] 예산에서 이름 여러 개를 특정 순서로 입력 → 보내기 → 인건비에 같은 순서로 뜸
- [ ] 순서 바꿔 재전송 → 인건비 순서도 그 순서로 갱신
- [ ] 보낸 인력의 기존/신규, 지원금/현물(구분)이 인건비에 그대로 반영
- [ ] "이름만" 전송도 순서·분류 반영(셀은 무변경)
- [ ] 인건비에서 수동 추가했던 미전송 인력은 뒤에 유지됨

### 추가 — 인건비 월급 칸 방향키
- `.pl-salary-input`은 Enter/Esc만 처리하고 화살표 무시했음 → ↑↓로 위/아래 월급 칸 이동 추가(`moveSalaryFocus`). ←→는 숫자 커서 이동 유지.
- [ ] 월급 칸에서 ↑↓로 위아래 사람 월급 칸으로 이동(끝에서 wrap), 셀 입력칸 방향키와 충돌 없음
