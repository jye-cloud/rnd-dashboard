# 다음 세션 이관 메시지 (v8.4 이후) — 복붙용

인건비 관리 시스템 — 이어서 작업 (v8.4 이후).

## [참조 파일 — 프로젝트에 올려둠]
- `인건비_관리_시스템_기획_v8_4.md`  (최신 기획서. §6 작업 순서 + §4.8 C1~C6 후보 사양 + 변경이력)
- `개발정리_테스트가이드.md`          (지금까지 구현 누적 + 실화면 테스트 체크리스트. 최신 = §4.4 2a)
- 코드(.html/.js): persons-master / project-budget / project-labor / dashboard / firestore-service / 그 외 페이지

## [작업 규칙]
- 코드 원본은 읽기 전용 → **outputs 복사본에 누적 편집(재복사 금지)**. 프로젝트 자동반영 안 됨 = 내가 다운받아 같은 이름으로 덮어씀(교체). **적용 후 화면이 안 바뀌면 강력 새로고침(Ctrl+Shift+R) / 배포 확인** — 이번 세션에 캐시로 "칸이 사라진 것처럼 보인" 사례 있었음(파일은 정상이었음).
- 검증은 정적분석(node -c + 인라인 JS + grep 정합 + div/table·CSS 균형 + 핵심 로직 노드 시뮬)까지. 실화면은 내가 확인.
- 큰 결정은 물어보고, 자잘한 건 바로 진행. **한 파일은 한 번에. 큰 파일(project-labor 5800줄 등)은 단계로 쪼개 화면 확인받으며.** 한국어 진행. 디자인 세부는 나중에 — 기능/숫자 위주.
- 매 작업마다 `개발정리_테스트가이드.md`에 누적 기록(무엇을 개발 + 무엇을 테스트 + 다음 구현).

## [지금까지 한 것]
- v8.1: §3.5 인건비 대시보드+채용 의무 / §3.6 인력 대시보드 / C4 신규 인력 자동 판정 / 채용 의무 디커플링.
- v8.2: C2 3책5공 관리(공유 헬퍼 `three-five-rule.js`).
- v8.3: C3 셀 메모(실제·비교 탭) / C1-b 예산 선택 전송 모달 / **C6 1단계**(인사 마스터 예산 전용 연봉=공유 슬롯 `salarySlots`) / 대시보드 지원금 현황 카드 재정비.
- **v8.4 (이번 세션):**
  - **C6 2단계 — 예산에서 슬롯 골라 불러오기** (`project-budget`): "💰 연봉 불러오기" 바(슬롯 셀렉트+전체 적용+행별 드롭다운). 폴백=**하이브리드**(빈 슬롯 0 차단+주황 경고+"빈 항목 실제로 채우기(N)"). `row.salarySlot` 추가(UI 기억용·계산 무영향).
  - **C6 3단계 — 엑셀 슬롯 컬럼** (`persons-master` 일괄 다운/업로드): `연봉:슬롯명` 컬럼(합집합·콤마). 업로드 비파괴 병합(빈칸=무변경·양수만·콤마/원 파싱·`연봉:새이름`=새 슬롯).
  - **§4.4 1단계 — 인사 마스터 "연봉 변경 시점"** (`persons-master` 편집 모달): `salaryChanges=[{from:'YYYY-MM', annualSalary}]`(계단식). 해석 함수 `getAnnualSalaryAt/getMonthlySalaryAt/sanitizeSalaryChanges/normalizeYm`. **변경시점 없으면 기존과 100% 동일.**
  - **§4.4 2a — 인건비 "그 달 연봉" 계산** (`project-labor`): `getEffectiveMonthlySalary(person, ym)` 월 인식화. 사용자 동작 계산 지점(참여율·나눔2·⚡일괄·가로채우기·undo)에 ym 전달. 월급칸 옆 **📅 인디케이터**(툴팁=변경목록). 표시값은 base 평탄.
  - **대시보드 툴바 한 행 정렬** (`dashboard`): 회사 필터를 `.dash-toolbar` 통합(연도·회사 왼쪽 / PDF 오른쪽).
  - (UI) 예산 전용 연봉(제안서용) 슬롯 행 한 줄 레이아웃.

## [다음 세션 할 일 — 추천 순서]
1. **§4.4 2b — 인건비 "📅 연봉 변경 반영" 선택 버튼** (`project-labor`):
   - 상단 툴바에 버튼 → 모달/팝오버. 현재 과제 인력 중 **연중 변경(`salaryChanges`) 있는 사람** 목록 + 각자 "변경시점 이후 달에 값이 있는 셀 수" 표시.
   - **사람 체크해서 고른 사람만** 해당 월(변경월~)부터 예상 금액 재계산(`getMonthlySalaryAt(person, ym)×참여율`). **확정된 실제 월은 제외**(confirmed). 나눔(split)·분류(cashOrInkind) 처리는 ⚡일괄계산/`onSalaryInputBlur` 패턴 본뜨기. **묶음 undo** 1개(Ctrl+Z 한 번에).
   - 이유: 결정3 = "자동 재계산 안 함, 내가 골라서 반영." (⚡일괄계산은 이미 그 달 연봉을 쓰지만 전체 대상이라, 선택+비파괴 버전이 2b.)
2. **§4.4 3단계 — 예산 동기** (`project-budget`): 예산 행 월급/선택 전송 ⓑ(`override ?? master ×%`)를 **"그 달 연봉"**으로. 단, 예산은 연 단위라 "예산 연도 기준 연봉"으로 해석(2027 예산 자동 새 연봉). ⓑ가 단일 숫자 가정이던 것 동기.
3. **6묶음 — 과제별 상세** (`projects-summary`): 사양 미정 → **화면 기획부터** 물어보고 진행.
4. **§4.5 엑셀 입출력** — 우선순위 낮음(마지막).

## [착수 전 권장 — 해석 함수 공용 승격]
- `getAnnualSalaryAt`/`getMonthlySalaryAt`/`sanitizeSalaryChanges`/`normalizeYm` 가 **persons-master.js·project-labor.js에 동일 중복**(주석으로 "양쪽 동기" 표기해 둠). 2b/3단계에서 또 쓰므로, **`firestore-service.js`에 `window.SalaryUtil`로 한 번만 정의**하고 세 곳이 참조하도록 승격 권장(드리프트 방지). firestore-service는 전 페이지가 로드함.

## [결정 완료 (이번 세션)]
- **C6 2단계 폴백 = 하이브리드** (빈 슬롯 차단+경고+"실제로 채우기").
- **§4.4 범위 = b1 + 마스터 기록(계단식)**. "연봉 변경 시점"을 인사 마스터에 한 번 기록 → 모든 과제·내년 자동. **자금·결산·대시보드는 개인 월급을 안 써서 영향 0**(grep 확인: 소비처 = persons-master·project-budget·project-labor뿐).
- **자동 재계산 안 함** → 사용자가 동작(참여율 재입력/⚡일괄/2b 버튼)해야 기존 셀에 반영. 신규 계산은 그 달 연봉 자동.
- **과제 오버라이드(`monthlySalaryOverride`, 평탄)와 공존**: 오버라이드>0이면 그 평탄값 우선, 없으면 마스터 그 달 연봉.

## [결정 필요 (다음 착수 전 물어볼 것)]
- §4.4 2b: 버튼 위치(인건비 예상 탭 상단 툴바 가정)·모달 형태·"확정 월 제외" 외 추가 제외 조건.
- §4.4 3단계: 예산은 연 단위라 "예산 연도의 어느 달 기준 연봉"을 쓸지(연초/연중 평균/12월?) — 보통 그 연도 1월 또는 최신값.
- 6묶음 과제별 상세 레이아웃·내용 사양.

## [확인 대기 — 실화면 (이월 포함)]
- v8.4: C6 2단계(예산 슬롯 불러오기·하이브리드 폴백) / C6 3단계(엑셀 `연봉:슬롯명` 다운·업로드 비파괴) / §4.4 1단계(편집 모달 연봉 변경 시점) / §4.4 2a(인건비 📅·그 달 연봉 계산·⚡일괄) / 대시보드 툴바. (체크리스트 = 테스트가이드 해당 섹션.)
- (이월) v8.3 C3·C1-b·C6 1단계·대시보드 입금 진행 / v8.2 3책5공 / v8.1 대시보드·채용 의무·C4 / §4.2 스냅샷 / §4.1 메모 / §3.4 3분류.

## [C2 후속·보류 (사례 누적 시)]
- project-budget 책/공 +1 미리보기(타 과제 roster 로드 필요) / 비인건비 3책5공 과제 "공" 카운트 / 청년 군대 예외 보정.
