# 다음 세션 이관 메시지 (복붙용)

```
인건비 관리 시스템 — 이어서 작업.

[참조 파일 — 프로젝트에 올려둠]
- 인건비_관리_시스템_기획_v8_1.md  (최신 기획서. §6 작업 순서 + §4.8 C1~C5 후보 사양)
- 개발정리_테스트가이드.md          (지금까지 구현 누적 + 실화면 테스트 체크리스트)
- 코드: labor-dashboard / persons-dashboard / persons-master / project-detail / project-labor (.html/.js)

[작업 규칙]
- 코드 원본은 읽기 전용 → outputs 복사본에 누적 편집(재복사 금지). 프로젝트 자동반영 안 됨(내가 다운받아 같은 이름으로 덮어씀 = 교체됨).
- project-labor·labor-dashboard·persons-dashboard·project-detail은 .js·.html 둘 다 손볼 수 있음.
- 검증은 정적분석(node -c + grep 정합 + div/table 균형)까지. 실화면은 내가 확인.
- 큰 결정은 물어보고, 자잘한 건 바로 진행. 한 파일은 한 번에. 한국어 진행.
- 디자인 세부는 나중에 — 지금은 기능/숫자 위주.
- 매 작업마다 '개발정리_테스트가이드.md'에 누적 기록(무엇을 개발했는지 + 무엇을 테스트할지 + 다음에 구현할 것).

[지금까지 한 것 — v8.1, 정적검증 통과 / 실화면 일부 확인]
- §3.5 인건비 대시보드 재설계 + 채용 의무: 카드 5개, 채용 의무 표, 할 일/점검(sysReg→confirmed), 추이·진척·명단·활용도 제거. 청년 자동 일원화(getYouthInfo). 과제 수정에 채용 의무 입력(requiredNew/requiredYouth/requiredRetention, 인건비 관리 대상과 분리된 독립 블록). 과제명=키워드 표시. 필수/채용=일반·청년 동등 표기.
- §3.6 인력 대시보드 재설계: 회사 칩 필터, 상단 3박스 한 행(인력 현황·입퇴사+월별막대·시점별), 청년/품질 제거. 순증감 제외. 시점별=드롭다운 즉시 갱신(조회 버튼 제거). 월별 막대 hover 툴팁.
- C4 신규 인력 자동 판정: 과제 수정에 기준일(과제시작일/공고일)+공고일+N개월 규칙(newJudgeRule, announceDate). 인력 추가 모달 신규/기존/미상 라벨·필터·일괄 재판정, 추가 시 newOrExisting 기본값=판정.
- 채용 의무 디커플링 결정: ① 비인건비 과제 모니터링 안 함(인건비 관리 과제만) ② 유지 기한 기준일=과제 시작일(첫 연차 startDate, 없으면 등록 시작월 폴백) — D9 정정. 카운트 무변경.

[이번 세션 할 일 — 다음]
1. C2 (3책5공 관리) — §4.8 C2, 사양 확정. 과제 수정 opt-in 플래그(is3ch5gManaged)+책임자 personId 드롭다운 → 책/공 카운트 헬퍼(기간겹침·역할·컨소) → participation-summary 3책5공 컬럼(현재연도만, 형식 책/공)·인력 추가 모달 배지(N개 과제→책/공)·project-budget 미리보기(저장값+1)·project-labor 책임자 색 → soft 경고. 셀/키 변경 0. **중.**

[결정 필요(아직 미착수) — 착수 전 물어볼 것]
- §4.4 월급 연중 변경: 범위 = (a) 과제 오버라이드만 연중(A안 팝오버) vs (b) +마스터 연봉 시간축(상/하반기)+여러 인력 일괄. 내 실제 케이스는 (b)쪽(연봉 협상 상/하반기, 일괄 업데이트 희망). 입력 UX=A안 확정. 모델: monthlySalaryOverride 숫자→객체{YYYY-MM:금액, step}.
- 6묶음 과제별 상세(projects-summary, §3.2): 레이아웃·내용 사양 미정 → 화면 기획 먼저.
- C1 인건비 예상 분배 선택 전송: 참여율만 보낼 때 금액 출처(budget값 vs labor급여 재계산), split/자부담 정책.
- §4.5 엑셀 입출력: 범위. 우선순위 낮음(트랙B 마지막).

[추천 순서]
C2 → §4.4(범위 결정 후) → C1/C3 → 6묶음 → §4.5

[확인 대기 — 실화면(이전 세션 이월 포함)]
- v8.1: 인건비 대시보드 카드 5·채용 의무 표(과제 시작일 기준 유지기한)·할 일/점검 / 인력 대시보드 3박스·회사칩·월별막대 hover·시점별 즉시갱신 / 과제 수정 채용의무·신규판정 규칙 저장·복원 / 인력 추가 모달 신규/기존/미상 라벨·필터·일괄 재판정.
- (이월) §4.2 스냅샷 / §4.1 메모 / §3.4 3분류 실화면.
```
