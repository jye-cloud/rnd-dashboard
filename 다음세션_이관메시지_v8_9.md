# 다음 세션 이관 메시지 (v8.9 이후) — 복붙용

인건비 관리 시스템 — 이어서 작업 (v8.9 이후).

## [참조 파일 — 프로젝트에 올려둠]
- `인건비_관리_시스템_기획_v8_9.md`  (최신 기획서. 상단 v8.9 블록 = 이번 세션 요약. §4.4 월급 오버라이드·§4.9 이월금·§4.10 연차 정렬, §9 변경이력)
- `개발정리_테스트가이드.md`          (구현 누적 + 실화면 테스트 체크리스트)
- 코드(.html/.js): persons-master / project-budget / project-labor / project-detail / projects / dashboard / funding / firestore-service / three-five-rule / 그 외, `firebase.json`

## [작업 규칙]
- 코드 원본은 읽기 전용 → **outputs 복사본에 누적 편집(재복사 금지)**. 자동반영 안 됨 = 내가 다운받아 같은 이름으로 덮어씀. **적용 후 화면 안 바뀌면 반드시 다시 배포(파일 덮어쓰기) 후 강력 새로고침(Ctrl+Shift+R).** (브라우저 새로고침만으로는 배포 파일이 안 바뀜 — 이번 세션 가장 큰 시간 낭비 원인이었음.)
- 검증 = 정적분석(node -c + 인라인 JS 시뮬 + grep 정합 + 태그/CSS 균형 + 핵심 로직 노드 시뮬). 실화면은 내가 확인.
- 큰 결정은 물어보고 자잘한 건 바로. **한 파일씩.** 한국어. 기능·숫자 우선, 디자인 세부는 나중. **모르겠다 싶으면 김연구원·500만→600만 같은 구체 예시로.**
- 매 작업마다 `개발정리_테스트가이드.md` 누적 기록(무엇을 개발 + 무엇을 테스트 + 다음).

## [지금까지 한 것 — 요약]
- v8.1~v8.5: §3.5/§3.6 대시보드·C4 / C2 3책5공 / C3 셀 메모·C1-b 선택 전송·C6 1~3단계 / §4.4 1·2a·2b 연중 연봉·공용 모듈(SalaryUtil·YearFilterUtil) / 과제 관리 표·당해 환급예정 / 정산 차감·자금 미수 연결.
- v8.6: 이월금 1단계(다음 연차로 이월) + 연차 정렬·정산 반납 용어·§4.4 3단계 기준 결정.
- v8.7: 연차 기준 정렬 1·2단계 + 잠긴 셀 합계 제외 + 나눔 3건 + 재입사 수동 잠금해제 + 이월금 덮어쓰기 픽스 + 예산 급여 정합 + 자부담현금 합산/별도 표시 토글 + 예산→인건비 전송 순서·분류 + 배포 캐시 헤더.
- v8.8: **결정·기록(미구현)** — 이월금 합산모드 산정 규칙(해석 B) + 당해 예상 환급액=지원금 예상+이월금.

- **v8.9 (이번 세션):**
  - **연차별 월급 오버라이드 (`project-labor`)** — `personRoles[pid].monthlySalaryOverride`(단일값) → `monthlySalaryOverrideByYear:{[연차]:금액}` 추가. **인건비에서 월급을 바꾸면 활성 연차에만 기록 → 다른 연차 연동 안 됨.** 해석 우선순위 = 연차별 > 기존 단일값(폴백) > 마스터 연봉(그 달, `SalaryUtil`) > `person.monthlySalary`. 신규 헬퍼 `getYearIndexForYm(ym)`·`getYearOverrideValue(roles, yearIndex)`. **기존 단일값은 폴백으로 유지 → 아직 안 건드린 연차는 기존 화면 그대로**(마이그레이션 0). 월급 변경 시 **그 연차 셀만** 재계산. undo `__salaryOverrideByYear`(yearIndex 포함)·`is-override` 스타일·§4.4 반영엔진 판정 모두 연차별 인식.
  - **예산→인건비 전송: 범위 밖 월 0 클리어 + 폴백 안전화 (`project-budget` `doSendToLabor`)** — ④ 월 범위 밖(안 고른) 연차 월을 **선택 인력 한정 0으로 비움**("고른 범위가 그 인력의 연차를 정의", `cellsCleared` → 토스트 "범위 밖 N개 비움"). 시작/종료 월 못 찾을 때 예전엔 **연차 전체로 폴백해 안 고른 달까지 써버리던 버그** → **안전 중단**(안내 토스트). + 선택 전송 소프트 경고(기록 0 + 건너뜀 시 빨강) + `personIds` 순서 보존(기존 순서 유지·신규만 뒤). distributeToLabor도 순서 보존.
  - **가로 채우기/잠그기 보강 (`project-labor`)** — 현금/현물 셀 "→ 이 값으로 이 월부터 채우기": **셀 전체(참여율 + 금액 3종 + 나눔 `rates`) 그대로 복사**(0이면 0%·0·0 전파), 경로의 **잠긴 월(직접 잠금 `lockedYms` + 재직기간 밖) 자동 해제 후 채움**. 참여율 셀 "→ 이 월부터 채우기"(`fillRateToYearEnd`)도 동일하게 잠금 해제(이전엔 건너뜀). "🔒 이 월부터 잠그기(옆으로)" 신규. 대상 월 = 신규 `getFillMonths()`(분기 보기에서 **연차 밖 회색 월까지** 포함 — 연차 밖 월에 옛 데이터 남은 경우 클리어 가능). 묶음 undo(`__cellSnapshot`).
  - **연차별 인력 순서 (`project-labor`)** — `state.personOrderByYear`로 행 드래그 순서를 연차별 독립 저장(한 연차에서 끌어도 다른 연차 순서 안 흐트러짐). meta 저장·스냅샷 포함.

## [확인 대기 — 실화면 (이번 세션 분, 사용자 미확정)]
- **연차별 월급**: 1차 A·2차 B로 바꿔서 서로 독립인지 / 기존 오버라이드 인력은 모든 연차 그대로인지 / 변경 시 그 연차 셀만 재계산인지 / Ctrl+Z / 연차 전환 시 각 연차 월급 따로 보이는지.
- **예산 전송**: 시작=7월 전송 시 7~12월만 값·1~6월 0인지 / 범위 못 읽으면 전송 중단 안내인지.
- **가로 채우기/잠그기**: 0 채우기 시 잠긴 월(직접+재직기간) 해제되며 0% 0 0 되는지 / 토스트 "잠금 N개 해제" 뜨는지(→ 신버전 확인용).

## [다음 세션 할 일 — 추천 순서]
1. ★ **프로젝트별 인건비 페이지 우측 요약 카드 로직 확인** (사용자 지정) — sticky 박스(예산총액·누계·차액·환급 등) 계산이 연차/합산모드/이월/연차별 월급 변경과 정합하는지 점검. 먼저 무엇이 어떻게 틀려 보이는지 사용자 확인 후 진입.
2. **이월금 합산모드 산정 규칙 구현** (§4.9-2단계, v8.8 결정 — 해석 B): 자부담 별도 OFF 과제에서 현금 한 풀 → 안 쓴 돈 전부 지원금 이월. 지원금 이월=(지원금예산+자부담예산)+(받은이월 cash+selfCash)−실제집행(Σ`cell.cash`), 음수면 0 / 자부담현금 이월=0. 구현 위치 = `project-labor.js` `openCarryoverModal` 프리필+안내. ⚠ 착수 전 `getActualCashSumForYear()`가 '자부담현금' 분류 집행도 `cell.cash`에 잡는지 확인 → 안 잡히면 보정.
3. **당해 예상 환급액 = 지원금 예상 + 이월금** (§4.9-3단계, v8.8 결정): 과제별 당해 예상 환급액 = Σ 예상(planned) 지원금 + 그 연차 `carryoverCash`. 월/분기 칸 현행 유지, 이월금은 **연 합계 칸에만** → 요약 카드=Σ 과제 합계로 자동 포함, 환급률 분모 증가. 적용 = 자금관리 요약 카드 + 과제별 표(묶음). projects '당해 환급예정' 칸 포함 여부 미정. ⚠ `funding.js`가 `projectBudget` 미참조 → `carryoverCash` 로드 경로 신규 추가 필요. (1·2·3은 모두 우측 카드/환급 로직과 얽혀 있어 1 점검과 함께 보는 게 좋음.)
4. **project-budget "보내기" 금액 출처=인건비 탭 → 연차별 월급 반영**: 현재 `roles.monthlySalaryOverride`(단일값)만 봄. 해당 연차의 `monthlySalaryOverrideByYear[연차]` 우선 반영하도록 보정. (v8.9에서 인건비 쪽만 연차별로 바뀌어 생긴 미스매치.)
5. **§4.4 3단계 — 예산 동기**(`project-budget`): 예산 행 월급/선택 전송 ⓑ를 "그 달(차년도=최신) 연봉"으로. `SalaryUtil` 활용.
6. **자금관리 연차 기준**(`funding`): 인건비 연차 정렬 미러(과제×연차 단위).
7. **S2** 인건비 대시보드+청년 자동 → **S3**·**S4** → **C5** 외부 단방향 전달(러버블+스프레드시트) → **과제 상세 페이지**(사양 미정·마지막) → 엑셀 입출력.

## [주의 / 함정]
- **연차 = 시작연도 기준** 매핑(`getYearIndexForState`, 달력연도 아님). `getYearIndexForYm(ym)`은 yearBudgets 기간 포함으로 ym→연차.
- **연차별 월급**: 활성 연차 = `getYearIndexForState()`. 셀 월 단위 재계산은 `getYearIndexForYm(m.ym)` 기준. 기존 단일 `monthlySalaryOverride`는 절대 지우지 말 것(폴백·화면 유지용).
- **가로 채우기 대상** = `getFillMonths()`(분기 보기 = 모든 분기 월 평탄화, 연차 밖 회색 월 포함). `getActiveYearMonths()`(연차만)와 다름 — 연차 밖 월 클리어가 필요해서 분리함.
- **환급은 모드 불문 지원금(`cash`)만.** 자부담현금 carryover는 셀 집행 추적이 없어 자동값 부정확 → 모달 직접 입력. **현물은 이월 불가.**
- 예산 문서 `{projectId}_year{N}`의 N = `yearBudgets` 배열 인덱스+1.
- 인건비는 `personIds` 순서로 렌더(연차 공용), 연차별 표시 순서는 `personOrderByYear`. 기존/신규·구분은 `personRoles[pid].newOrExisting`/`.cashOrInkind`.
- **배포 ≠ 새로고침.** outputs 파일을 실제로 다시 받아 덮어써야 적용됨.
