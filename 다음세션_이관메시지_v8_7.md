# 다음 세션 이관 메시지 (v8.7 이후) — 복붙용

인건비 관리 시스템 — 이어서 작업 (v8.7 이후).

## [참조 파일 — 프로젝트에 올려둠]
- `인건비_관리_시스템_기획_v8_7.md`  (최신 기획서. 상단 v8.7 블록 = 이번 세션 요약. §6 작업 순서·16묶음·다음 세션 시작점, §4.9 이월금·§4.10 연차 정렬)
- `개발정리_테스트가이드.md`          (구현 누적 + 실화면 테스트 체크리스트)
- 코드(.html/.js): persons-master / project-budget / project-labor / project-detail / projects / dashboard / funding / firestore-service / three-five-rule / 그 외, `firebase.json`

## [작업 규칙]
- 코드 원본은 읽기 전용 → **outputs 복사본에 누적 편집(재복사 금지)**. 자동반영 안 됨 = 내가 다운받아 같은 이름으로 덮어씀. **적용 후 화면 안 바뀌면 강력 새로고침(Ctrl+Shift+R).** (이번에 `firebase.json`에 js/css/html `no-cache` 넣어 배포 후 캐싱은 잡힘 — 단 현재 캐시된 건 1회 강력새로고침 필요.)
- 검증 = 정적분석(node -c + 인라인 JS 시뮬 + grep 정합 + 태그/CSS 균형 + 핵심 로직 노드 시뮬). 실화면은 내가 확인.
- 큰 결정은 물어보고 자잘한 건 바로. **한 파일씩.** 한국어. 기능·숫자 우선, 디자인 세부는 나중. **모르겠다 싶으면 김연구원·500만→600만 같은 구체 예시로.**
- 매 작업마다 `개발정리_테스트가이드.md` 누적 기록(무엇을 개발 + 무엇을 테스트 + 다음).

## [지금까지 한 것 — 요약]
- v8.1~v8.4: §3.5/§3.6 대시보드·C4 / C2 3책5공 / C3 셀 메모·C1-b 선택 전송·C6 1~3단계 / §4.4 1·2a 연중 연봉.
- v8.5: §4.4 2b(선택 연봉 반영) / 공용 모듈 `SalaryUtil`·`YearFilterUtil` / 과제 관리 표 재구성·당해 환급예정 / 과제 상세 정산 차감(이자 반납)·자금 미수 연결 / 비중 VAT 정합.
- v8.6: 이월금 1단계(다음 연차로 이월) + 연차 정렬·정산 반납 용어·§4.4 3단계 기준 결정.
- **v8.7 (이번 세션):**
  - **연차 기준 정렬 1단계** (`project-labor`): 연차 있는 과제는 표·누계·차액·이월금을 **그 연차 실제 기간(시작연도 기준) 전체**로. 시작연도 매핑(2027→1차 오인 픽스), 12월채우기→연차끝, 꼬리연도 안내. **데이터 0.**
  - **잠긴 셀 합계 제외**: 입사 전/퇴사 후 셀은 합계 제외(`isPersonActiveInYm`). 분배가 기간 밖 월 채워 합계 부풀던 것 해결.
  - **나눔(split) 3건** + **재입사 수동 잠금해제(B안)**(`unlockedYms`, 우클릭 "🔓 이 월 입력 허용"). 잠금 우선순위: 연차밖>직접잠금>입력허용>자동.
  - **연차 정렬 2단계** (`project-labor`): 분기 보기에서 **연차 밖 월 회색+잠금**(`getYearQuarters`/`isYmInActiveYear`), 분기 ◀▶는 연차 분기 내에서만, "이전 분기 복사"도 연차 기준.
  - **이월금 덮어쓰기 픽스** (`project-budget`): 예산 저장이 연차 문서 통째 set → carryover 지움 → `{merge:true}`. ⚠️ 이미 지워진 이월값은 인건비에서 "📥 이월" 다시 눌러 복구.
  - **예산 급여 정합** (`project-budget`): 급여총액=**연봉**, 실지급액=`ceil(연봉/12)`(마스터와 동일). 기존 행은 다시 "불러오기/적용" 해야 새 값.
  - **예산 총액 직접입력**(`totalOverride`, 비우면 자동) + **예산 셀 방향키**(없던 것 신규) + **인건비 월급 칸 ↑↓**.
  - **자부담현금 합산/별도 "표시" 토글**: `hasSelfCash` 체크 = 데이터 아닌 **표시 방식**(별도 ON/합산 OFF). 데이터는 항상 지원금·자부담 분리, **환급은 항상 지원금만**. 합산이면 인건비 박스(예산총액·누계·차액) 현금=지원금+자부담 + 작은 "지원금" 서브라인, 예산 페이지 요약 아래 합산 줄. 예산 자부담 입력 항상 허용.
  - **예산→인건비 전송 순서·분류** (`project-budget`): `personRoles`(기존/신규·구분)도 저장 → 분류 반영. 명단 순서 = **보낸(예산 입력) 순서 우선** + 기존 미전송 인력 뒤. 선택 전송·분배 둘 다.
  - **배포 캐시 헤더** (`firebase.json`): js/css/html `no-cache`.

## [다음 세션 할 일 — 추천 순서]
1. **정산 반납 용어 통일 + 사유 툴팁** (결정 완료·미구현): '차감'→'반납'. 자금 "정산 반납 N 반영"(hover 툴팁=사유)·과제상세 "➖ 정산 반납(이자 반납 등)". 색 `#f97316` 유지. 가장 작은 건이라 먼저.
2. **§4.4 3단계 — 예산 동기** (`project-budget`): 예산 행 월급/선택 전송 ⓑ를 **"그 달(차년도=최신) 연봉"**으로. `window.SalaryUtil`(`getAnnualSalaryAt`/`getMonthlySalaryAt`) 활용 — 중복 해석 함수 승격은 v8.5에 이미 됨.
3. **자금관리 연차 기준** (`funding`): 인건비 연차 정렬을 미러(과제×연차 단위). 데이터 0.
4. (선택) 인건비 표 푸터·예산 행별 셀도 합산(지원금+자부담) 표기 — 지금은 합산이 sticky 박스/요약 줄만, 행 셀은 분리 유지.
5. **S2** 인건비 대시보드+청년 자동 → **S3**(과제상세 채용의무+C4) → **S4**(채용의무 판정) → **과제 상세 페이지**(사양 미정·마지막, 화면 기획부터).

## [주의 / 함정]
- **연차 = 시작연도 기준** 매핑(달력연도 아님). 연차 정렬이 이월금 잔액 자동값 정확도의 선행 조건.
- **환급은 모드 불문 지원금(`cash`)만.** 자부담현금 carryover는 셀 집행 추적이 없어 자동값 부정확 → 모달에서 직접 입력.
- **현물은 이월 불가.**
- 예산 문서 `{projectId}_year{N}`의 N = `yearBudgets` 배열 인덱스+1 (project-budget·project-labor 둘 다 동일 방식 — 일치 확인됨).
- 인건비는 `personIds` 순서로 렌더, 기존/신규·구분은 `personRoles[pid].newOrExisting`/`.cashOrInkind`에서 읽음.
- 재입사 정식 처리(재직 구간 복수)는 데이터 모델 변경이라 보류 — 현재 B안(수동 잠금해제) 임시.
