# 인건비 관리 시스템 기획 v2

> 60hz R&D Dashboard 인건비 관리 기능 기획서 (현재 스냅샷 + 남은 작업 로드맵)
> 작성일: 2026-05-26
> 작성자: 정지혜 + Claude
> 이전 버전: `인건비_관리_시스템_기획.md` (2026-05-25 초안)

---

## 0. v2 작성 의도

v1(초안)은 실제 작업 전 머리속 기획이었음. 실제로 만들면서 **구조·데이터 모델·기능이 상당히 진화**했고, **회사 구분이라는 새 축**이 들어왔고, **참여연구원 표 내보내기** 같은 큰 모듈이 추가됨.

v2는 다음을 동시에 담음:
- **현재까지 무엇이 만들어졌는가** (스냅샷)
- **무엇이 v1과 달라졌는가** (실제 구현 기준)
- **남은 작업이 무엇이고 어떤 순서로 갈 것인가** (로드맵)
- **결정 보류 중인 미해결 이슈** (의식적으로 남긴 빈칸)

v1의 뼈대(워크플로우 시나리오, 환급 계산 공식, 디자인 시스템)는 유효하므로 보존하되, 세부는 실제 구현 기준으로 다시 씀.

---

## 0.5 공통 원칙 / 이 프로젝트의 기본값

페이지를 새로 만들거나 기존 페이지를 수정할 때 반드시 따라야 하는 공통 룰. 인건비 모듈에 한정되지 않고 **전체 R&D 대시보드 어디서든 적용**됨. 향후 다른 공통 룰도 이 섹션에 추가.

### 0.5.1 레이아웃: 좁은 화면 대응의 통일된 방식

**3가지 원칙으로 어떤 모니터·노트북에서 봐도 일관된 레이아웃을 보장.**

#### 원칙 1. 본문은 모니터 폭에 관계없이 꽉 찬다

사이드바를 뺀 본문 영역(`.main-area`)을 모니터 크기와 무관하게 꽉 채운다. 14인치 노트북에서든 27인치 모니터에서든 동일한 비율로 콘텐츠가 채워져야 함.

#### 원칙 2. 좁아져도 콘텐츠는 압축하지 않고 가로 스크롤로 도망간다

`.container { min-width: 1200px }`로 본문이 1200px 미만으로 줄어드는 걸 막고, 그 이하 화면에선 `.main-area`에 가로 스크롤이 생긴다. **콘텐츠 자체는 그대로**, 좁아져도 카드·표가 깨지지 않음.

#### 원칙 3. 1400px 미만이면 사이드바 자동 collapse

`sidebar.js`의 `AUTO_COLLAPSE_BREAKPOINT = 1400`. 1400px 미만 창에선 사이드바가 자동으로 접혀서 본문이 충분한 공간을 확보. **단, 사용자가 수동으로 토글한 상태는 존중**(localStorage `rnd-sidebar-collapsed`).

#### breakpoint 산식 (왜 1200·1400인가)

- 사이드바 펼침 폭: 220px
- 사이드바 접힘 폭: 60px
- 본문 최소 폭: 1200px

→ 스크롤 없이 들어가는 창폭:
- 펼침 상태: 220 + 1200 = **1420px**
- 접힘 상태: 60 + 1200 = **1260px**

→ 1400px 미만 = 사이드바를 접어야 본문이 가로 스크롤 없이 들어감
→ 1260px 미만 = 사이드바를 접어도 본문에 가로 스크롤 발생 (콘텐츠는 1200px 폭으로 유지)

#### 구현 (`style.css` 전역 `.container` 룰)

```css
.container {
  width: 100% !important;
  max-width: none !important;
  min-width: 1200px;
  margin: 0 !important;
  padding: 20px 30px 2rem 80px;
  box-sizing: border-box;
}
```

`.main-area`는 이미 `overflow: auto`라 가로 스크롤이 자동 동작함 (`style.css` 200줄).

#### 페이지에서 절대 하지 말 것 (Anti-patterns)

```css
/* ❌ 금지 1: 인라인 .container 재정의 — 모니터에서 좁아 보이게 만듦 */
.container {
  width: 1400px;
  margin: 0 auto;
}

/* ❌ 금지 2: 1100~1400px 구간에서 그리드 무너뜨리기 */
/* 이 구간은 이미 .container min-width로 가로 스크롤이 생기는 영역이라 */
/* 그리드를 1열로 무너뜨리면 동작이 이중으로 일어나 일관성이 깨짐 */
@media (max-width: 1280px) {
  .my-grid { grid-template-columns: 1fr; }
}

/* ❌ 금지 3: 페이지 메인 컨테이너에 min-width: 0 */
/* (단, flex/grid 자식 셀에 박는 min-width: 0은 OK — 텍스트 오버플로우 방지용) */
```

**과거 사례 (2026-05-26 일괄 정리됨):**
- 11개 페이지에 `.container { width:1400px; margin:0 auto }` 인라인 박혀있어 모니터에서 본문이 좁아 보임 → 모두 제거
- 4개 페이지(lab, persons-detail, project-budget, projects-summary)에 `@media (max-width: 1100~1280px)` 그리드 무너뜨림 → 모두 제거

#### 허용되는 미디어 쿼리

- **`@media print`** — 인쇄/PDF 출력 시 레이아웃 조정 (사이드바 숨김, 컨테이너 폭 조정 등). 일반 화면 영향 없음.
- **`@media (max-width: 700px)` 이하** — 우리 정책상 1200px에서 이미 가로 스크롤이라 사실상 발동 안 함. 미래 모바일 대응 여지로 남기고 싶다면 보존 가능.
- **모달·드롭다운 같은 작은 영역의 자체 반응형** — 페이지 메인 그리드가 아닌 곳.

### 0.5.2 컨테이너 구조 (HTML 뼈대)

모든 페이지는 다음 wrapper 구조를 따름:

```html
<div class="dashboard">       <!-- display:flex (사이드바 + 본문) -->
  <div id="sidebar-mount"></div>   <!-- sidebar.js가 자동 렌더링 -->
  <main class="main-area">    <!-- flex:1, height:100vh, overflow:auto -->
    <div class="container">   <!-- width:100%, min-width:1400px -->
      <header class="header">
        <h1 class="header-title">...</h1>
      </header>
      <!-- 페이지 콘텐츠 -->
    </div>
  </main>
</div>
```

이 구조를 벗어나면 사이드바·스크롤·반응형이 정상 동작 안 함.

### 0.5.3 사이드바

- `sidebar.js`가 자동 렌더링. HTML에는 `<div id="sidebar-mount"></div>`만 두면 됨. 메뉴 수정은 `sidebar.js`의 `MENU_ITEMS` 한 곳에서만.
- 창 너비 < 1200px이면 자동 collapse. 사용자 수동 토글 상태는 localStorage(`rnd-sidebar-collapsed`)로 보존.

### 0.5.4 회사 필터

- 회사 구분이 있는 페이지(인력·프로젝트·인건비 관련)에는 상단에 `.company-chips` 배치
- localStorage 키 `rnd-company-filter`로 전 페이지 공유
- 칩 형식: `.company-chip[data-company="식스티|굿뉴스|패리티|all"]`

### 0.5.5 향후 추가될 공통 룰 / 점검 항목

이 섹션은 새로운 공통 패턴이 발견될 때마다 확장.

**룰 후보:**
- 토스트/알림 표시 룰
- 모달 z-index 규칙
- 빈 상태(empty state) 표현 일관성
- 로딩 인디케이터 패턴

**언젠가 점검할 것 (지금은 안 건드림):**
- **`min-width: 0` 사용처 점검** — 현재 여러 파일에 `min-width: 0`이 박혀있음. 둘 중 어떤 용도인지 케이스별 구분 필요:
  - ✅ 좋은 용도: flex/grid 자식 셀에서 텍스트 오버플로우 방지 (보존)
  - ❌ 나쁜 용도: 페이지 메인 컨테이너나 카드에서 "마음껏 줄어들기" 허용 (§0.5.1과 충돌, 제거 대상)
  - 결정 보류: 모든 파일 일괄 검사하면 가짜 양성이 많아서, 새 페이지 만들거나 기존 페이지 수정할 때 케이스별로 결정.

---

## 1. 현재 시스템 스냅샷

### 1.1 페이지 구조 (5개로 분화)

v1은 4페이지였으나 실제는 5페이지로 분리됨:

| 페이지 | 파일 | 상태 |
|---|---|---|
| 인력 마스터 | `persons-master.html/.js` | ✅ 완료 |
| 인력 상세 | `persons-detail.html/.js` | ✅ 완료 (v1에는 모달로 계획) |
| 연차별 인건비 예산 | `project-budget.html/.js` | ✅ 본체 완료, 참여연구원 표 RTF 마무리 중 |
| 월별 인건비 | `project-labor.html/.js` | 🔨 미구현 (다음 작업) |
| 인건비 대시보드 | `labor-dashboard.html/.js` | 🔨 미구현 |

**사이드바 메뉴 구조:**
```
💰 인건비 관리
   ├ 👥 인력 마스터
   ├ 💵 연차별 인건비 예산   ← project-budget (NEW)
   ├ 📋 월별 인건비          ← project-labor (메인 입력)
   ├ 📈 인건비 대시보드      ← labor-dashboard
```

### 1.2 회사 구분 시스템 (v1에 없던 신규)

식스티/굿뉴스/패리티 3개 회사 구분이 인력·프로젝트 양쪽에 들어옴.

- **인력**: `persons.company` 필드 (`'식스티'|'굿뉴스'|'패리티'`)
- **프로젝트**: `projects.company` 필드 (동일)
- **색상 토큰** (style.css 전역):
  - 식스티: `#e0e7ff` / `#1e3a8a`
  - 굿뉴스: `#ffedd5` / `#9a3412`
  - 패리티: `#d1fae5` / `#065f46`
- **UI 컴포넌트**:
  - `.company-badge` + 회사별 modifier (`--sixty`/`--goodnews`/`--parity`/`--unset`)
  - `.company-chip[data-company="..."]` — 회사 필터 칩
- **필터 상태**: localStorage 키 `rnd-company-filter`로 전 페이지 공유
- **제약 룰**: 프로젝트 인력 추가 모달은 `proj.company === person.company`인 인력만 노출

### 1.3 데이터 모델 (실제 구조)

#### 1.3.1 `persons` 컬렉션

v1보다 필드가 훨씬 풍부해짐 (참여연구원 표 출력 때문에 확장됨):

```js
{
  id, name,
  birthDate, hireDate, exitDate,
  isYouth,           // 청년 인력
  isNew,             // 신규/기존
  company,           // '식스티'|'굿뉴스'|'패리티'
  gender, ageBucket,
  annualSalary,      // 연봉
  monthlySalary,     // 월급 (현재 기준)
  status,            // 'active'|'exited'
  memo,
  // 인력 상세에서 관리:
  position,          // 직위 (대표/책임/선임/연구원 등)
  finalDegree,       // 최종 학위
  major,             // 전공
  degreeDate,        // 학위 취득일
  // ... 추가 필드 점진 확장 가능
}
```

> v1에서 계획했던 `salaryHistory` (급여 변경 이력 배열)는 아직 미구현. 추후 필요 시 추가.

#### 1.3.2 `projects` 컬렉션 (인건비 관련 필드)

```js
{
  id, projectName, keywords, status,
  company,                  // 식스티/굿뉴스/패리티
  laborManaged,             // 인건비 관리 대상 여부
  laborRefund,              // 인건비 환급 대상 여부
  requiredNew,              // 신규 채용 필수 인원
  requiredYouth,            // 청년 필수 인원
  division1,                // 사업 구분
  yearBudgets,              // 연차별 예산 정보
  // ...
}
```

#### 1.3.3 `projectLabor` 컬렉션 — 월별 인건비 (v1과 크게 다름)

**v1 설계**: 셀 1개당 문서 1개 (`{projectId}_{ym}_{personId}`)
**실제 구조**: 프로젝트당 3개 문서로 통합

```js
// 1. 예상 계획
projectLabor/{projectId}_planned = {
  cells: {
    '{pid}_{ym}_{personId}': { rate, cash, inkind, memo }
  }
}

// 2. 실제 지급
projectLabor/{projectId}_actual = {
  cells: { /* 동일 구조 */ }
}

// 3. 메타 (월별 상태, 인력 목록)
projectLabor/{projectId}_meta = {
  meta: {
    [ym]: { sysReg, amtConf }   // 시스템 등록, 금액 확인
  },
  personIds: [...]              // 이 프로젝트에 추가된 인력 ID 목록
}
```

**셀 스키마 미정 사항** (이슈로 남김 — §5 참조):
- `selfCash` (자부담 현금): v1 기획엔 있었으나 현재 미정. 추후 결정.
- `memo` vs 별도 인력 이동 메모: 한 셀의 `memo`로 통합할지, 인력별/월별 메모를 따로 둘지 미정.

#### 1.3.4 `projectBudget` 컬렉션 — 연차별 예산 (v1에 없던 신규)

```js
projectBudget/{projectId}_year{N} = {
  rows: [
    {
      personId, personName,
      position, actualPay, monthlySalary,
      rate,              // 인건비계상률 (%)
      participMonths,    // 참여기간 (개월)
      cashOrInkind,      // 현금/현물
      role,              // 담당역할
      newOrExisting,     // 기존/신규
      // ...
    }
  ],
  period,         // 연차 기간
  budgetCash,     // 현금 예산
  budgetInkind    // 현물 예산
}
```

### 1.4 참여연구원 표 내보내기 (v1에 없던 신규 모듈)

`project-budget` 페이지 하단 모듈. `project-budget-export.js` 별도 파일.

**표 양식 2종:**
- **표1 (정부 R&D 표준)** — 15컬럼, 2단 헤더, "학위 및 전공" 그룹 헤더 가로 병합, 단독 컬럼 세로 병합
- **표2 (인건비 계상 합계)** — 11컬럼, 합계 행 자동, 금액 컬럼 우측 정렬+천단위 콤마

**다운로드 옵션 (우선순위):**
1. 🌟 **한글용 RTF** — 메인. cp949 인코딩, 함초롬바탕(charset 129), 한컴 한워드가 표로 변환
2. 📊 **엑셀(.xlsx)** — SheetJS 사용
3. 📋 **표 복사** — 클립보드 HTML+plain. ※ 한컴 한글 2022는 HTML clipboard를 무시하므로 평문만 들어감. RTF가 정답.

**의존성:**
- xlsx@0.18.5 (CDN)
- JSZip은 제거됨

**핵심 RTF 스펙:**
```
{\rtf1\ansi\ansicpg949\deff0\uc1
\fonttbl {\f0\fcharset129 함초롬바탕}
\colortbl: 1=검정, 2=헤더배경(#E5E7EB), 3=합계배경(#FEF3C7), 4=그룹헤더배경(#dde4ee)
한글: \uNNNN? signed 16-bit
표: \trowd\trgaph60\trleft0\trqc + \cellx{X} + \intbl + \cell + \row
병합: \clmgf/\clmrg (가로), \clvmgf/\clvmrg (세로)
```

**알려진 한계 (표 출력 시 임시 처리):**
- 국적 → "한국" 고정
- 타 사업 인건비계상률(B) → 0 고정
- 내·외부 구분 → "내부" 고정
- 참여기간 인력별 오버라이드: localStorage 키 `rnd-pb-export-period` 준비만 됨, UI 미구현

### 1.5 디자인 토큰

#### 컨테이너 / 사이드바
- `.container` 최소 너비 1400px
- 창 < 1200px이면 sidebar.js가 자동 collapse

#### 색상 (v1 그대로 유효)
- 초록 `#3B6D11` — 안전/완료
- 주황 `#BA7517` — 주의 (90%↑)
- 빨강 `#A32D2D` — 위험 (100%↑)
- 파랑 `#185FA5` — 정보
- 노랑 `#854F0B` — 비교/차이
- 회색 `#5F5E5A` — 퇴사/미참여

#### 뱃지
- 청년 = 보라 `#3C3489`
- 기존 = 회색 `#5F5E5A`
- 신규·청년 = 파랑 `#0C447C`
- 활성 = 초록 `#3B6D11`
- 퇴사 = 빨강 `#791F1F`

#### 카드 배경
- 환급/긍정 = 연한 초록 `#EAF3DE`
- 인원/정보 = 연한 파랑 `#E6F1FB`
- 주의 = 연한 노랑 `#FAEEDA`
- 위험 = 연한 빨강 `#FCEBEB`

### 1.6 스크립트 로드 순서 (인건비 페이지 공통)

```
firebase-* → firebase-config → firebase → auth-guard
  → firestore-service → sidebar → {page}.js → floating-buttons
```

---

## 2. v1에서 살아남은 핵심 결정 사항

이 결정들은 변경 없이 v2에서도 유효함:

| 항목 | 결정 | 비고 |
|---|---|---|
| 인력 관리 방식 | 자유 입력 + 자동 매칭 | persons-master 구현됨 |
| 예상/실제 화면 | 탭 구분 (예상/실제/비교) | project-labor에서 구현 예정 |
| 월별 레이아웃 | 분기 단위 이동 (3개월씩) | project-labor |
| 환급액 계산 공식 | 월급 × 참여율 | 유효 |
| 참여율 100% 초과 | 입력 차단 | project-labor에서 구현 예정 |
| 환급 구분 | 현금(정부)=환급 / 현금(자부담)=환급X / 현물=환급X | selfCash 자체가 미정이므로 보류 |
| 회사 전체 인원 | 인력 마스터에서 자동 집계 | persons-master 구현됨 |
| 퇴사 처리 | 흔적 남김. 지급된 달은 회색 처리 안 함 | project-labor에서 적용 예정 |
| 시스템 등록/금액 확인 | 월별 컬럼 헤더 체크박스 | meta.{ym}.sysReg/amtConf로 데이터 모델 준비됨 |

---

## 3. 환급액 계산 로직 (v1 유효)

### 3.1 환급 대상 구분

| 구분 | 환급 | 비고 |
|---|---|---|
| 현금 (정부지원금) | ✅ | 가장 흔한 케이스, `cells.cash` |
| 현금 (자부담) | ❌ | 드물게 발생, `selfCash` (미정) |
| 현물 (회사부담) | ❌ | `cells.inkind` |

### 3.2 계산 공식

```
[월별·인력별]
환급 예정액 = planned.cells.{key}.cash
환급 확정액 = actual.cells.{key}.cash  (단, meta.{ym}.sysReg=true 일 때)

[월별·프로젝트별]
월 환급 예정 = Σ 모든 셀의 planned.cash (해당 ym)
월 환급 확정 = Σ 모든 셀의 actual.cash  (해당 ym, sysReg=true)

[연간·전 프로젝트]
연 환급 예정 = Σ 모든 프로젝트의 월별 환급 예정
연 환급 확정 = Σ 모든 프로젝트의 월별 환급 확정
```

### 3.3 자동 계산

```
참여율 입력 → 현금 자동 계산
예: monthlySalary 4,500,000 × rate 30% = 1,350,000
```

역계산은 미구현. 추후 검토.

---

## 4. 남은 작업 로드맵

### Phase A. 월별 인건비 페이지 (`project-labor`) ← **다음 작업**

**목적**: 사진 양식을 디지털화한 핵심 입력 페이지. v1 §6.2 내용을 실제 구현.

**상단 컨트롤바**
- 회사 필터 칩 (`.company-chips`, `rnd-company-filter` 공유)
- 프로젝트 선택 드롭다운 (회사 필터 적용된 목록)
- 연도 선택 / 분기 선택 (`[◀ 2분기 ▶]`)
- 이전 분기 복사 / 되돌리기
- 신규 채용 진척 표시

**탭 (3개)**: 📝 예상 / ✅ 실제 / ⚖️ 비교

**테이블 (예상/실제)**
```
              | 4월              | 5월              | 6월
인력      월급 | 참여% | 현금 | 현물 | 참여% | 현금 | 현물 | ...
김지혜  4.5M  | 30%   |1,350K|  0  | 30%   |1,350K|  0  |
김홍석  6.0M  | 20%   |1,200K|  0  | 20%   |1,200K|  0  | (5월 100%)
박정수  4.0M  | 50%   |2,000K|  0  | 퇴사  |  -   |  -  | (회색)
─────────────┼─────────────────┼─────────────────┼─────
월별 합계     | 환급 예정 4,550K | 환급 예정 2,550K | ...
```

**비교 탭**: 인력당 3행 (예상/실제/차이). "차이만 보기" 토글.

**컬럼 헤더 메타**: ☑ 시스템 등록 / ☑ 금액 확인 (각 월별)

**액션 버튼**: `+ 인력 추가` (회사 제한 필터 적용) / `⚡ 일괄 자동 계산` / `💰 자부담 추가` (selfCash 결정 시)

**키보드 조작** (v1 §7 우선순위 2):
- Tab/Enter: 다음 셀
- 화살표: 이동
- Ctrl+C/V/Z
- Esc: 편집 취소

**100% 초과 차단**: 입력 시점에 다른 프로젝트 참여율 합산 확인 → 빨간 셀 + 저장 차단

**저장 정책**:
- 셀 수정 시 해당 문서(`{projectId}_planned` 또는 `_actual`) cells 맵 부분 업데이트
- 디바운스 권장 (입력 직후 즉시 저장보다는 1초 지연 등)

**0건일 때 안내**: "이 과제는 ○○ 명의라 ○○ 소속만 추가할 수 있습니다" (회사 제한 룰 안내)

### Phase B. 인력 마스터 합산 참여율 (월 기준)

**전제**: Phase A 완료 후 진행 (월별 인건비 데이터가 있어야 정확한 월 기준 합산 가능)

**기능**:
- 인력 마스터 상단에 "기준 월" 셀렉터
- 각 인력 행에 "합산 참여율" 컬럼 추가 (해당 월의 모든 프로젝트 참여율 합)
- 색깔 코딩: 초록(<80%) / 주황(≥90%) / 빨강(=100%)
- 요약 카드: "참여율 ≥ 90%", "참여율 = 100%" 자동 집계

**임시 구현 (Phase A 전이라도 가능한 것)**:
- `project-budget`의 `rate`(연 기준)만으로 임시 합산 표시 가능. 단 "연 기준"임을 명시.
- v2에서는 정식 구현은 Phase B로 미루고, 임시 구현 여부는 작업 시점에 다시 결정.

### Phase C. 인건비 대시보드 (`labor-dashboard`)

**상단 요약 카드 (4개)** — v1 §6.3 그대로
1. 회사 전체 인력: `N명 (활성 X · 퇴사 Y)`
2. 과제 등록 인원: `X / N (P%)`
3. 여유 가용 참여율: `XXX%` ← 핵심
4. 100% 참여 인원: `N명`

**섹션 (기본 6개 + 7번째 후보 명시)**

1. **월별 환급 추이** (그래프) — 연한 파랑=예상 / 진한 파랑=실제
2. **프로젝트별 환급 진척** — 막대 (받은 금액 / 목표 환급액)
3. **인력 참여율 (현재 월)** — 정렬+색깔 코딩 (회사 뱃지 인라인 표시 ⭐)
4. **신규 채용 필수 인력 진척** — 표 (회사 칩 카운트 뱃지 활용 가능)
5. **회사 인력 활용도** — 좌: 숫자 / 우: 월별 추이
6. **액션 아이템** — 긴급/주의/알림 4단계

**7번째 섹션 후보 (작업 시점에 결정):**
- 후보 A: **회사별 분해** (식스티/굿뉴스/패리티 비교) — 가장 자연스러움
- 후보 B: **연차별 예산 vs 실제** — `projectBudget` 데이터 활용
- 후보 C: **참여연구원 표 일괄 출력** — `project-budget-export.js` 확장

**전 페이지 공통: 회사 필터 적용**

### Phase D. v2 로드맵 후속 작업 (확정)

작업 시점은 Phase A-C 진행하면서 자연스럽게 결정. 컨텍스트에서 합의된 항목:

1. **타 사업 인건비계상률(B) 자동화** — 통합 대시보드(Phase C) 기반. 인력별 모든 프로젝트 참여율 합산해서 표 출력 시 자동 주입.
2. **참여기간 인력별 오버라이드 UI** — `rnd-pb-export-period` localStorage 키는 준비됨. UI 추가 (각 인력 행마다 개월 수 override 가능).
3. **회사 칩 카운트 뱃지** — `.company-chip-count` CSS는 준비됨. 칩 옆에 해당 회사 인력/프로젝트 수 표시.
4. **대시보드 참여율 진척표 회사 뱃지 인라인** — Phase C 섹션 3·4에 통합.

### Phase E. v1에서 이월된 보완 작업

1. **채우기/반복** (드래그, Ctrl+D, 우클릭 "이전 달과 같음")
2. **엑셀 입출력** (project-labor에서. project-budget은 이미 있음)
3. **PDF 출력**
4. **기존 데이터 마이그레이션** (구글 스프레드시트 → 새 시스템)

---

## 5. 미해결 이슈 (의식적으로 남긴 빈칸)

### 5.1 셀 스키마 — 자부담 현금 (selfCash) **상태: 미정**

- v1 기획: `plannedSelfCash` / `actualSelfCash` 필드 + 자부담 토글 UI
- 현재 cells 맵: `{rate, cash, inkind, memo}` — selfCash 없음
- **결정 보류**: project-labor 구현 시점에 자부담 케이스가 실제로 얼마나 발생하는지 보고 결정. 필요해지면 cells 맵에 필드 추가.
- 추가될 시 영향 범위: project-labor UI(자부담 토글), 환급액 계산 공식, 환급 대상 구분 표

### 5.2 셀 메모 vs 인력 이동 메모 **상태: v2 작성 시점에는 정리 보류**

- v1 기획: "인력 이동 메모(빨간 메모)" — 예: "굿뉴스 이동", "25.04.30 퇴사"
- 현재: 셀의 `memo` 필드 하나
- **결정 보류**: project-labor 만들면서 실제 케이스를 보고 결정.
  - 한 셀 메모로 충분한가? (한 셀 = 한 인력 × 한 월의 노트)
  - 인력별/월별 메모를 따로 둘 필요가 있나?
  - 또는 인력별 글로벌 메모(`personIds`에 메모 추가)가 필요한가?

### 5.3 인건비 대시보드 7번째 섹션 **상태: 후보 명시 + 작업 시 결정**

§4 Phase C 참조. 후보 A/B/C 중 작업 시 선택.

### 5.4 v1 §10에서 이월된 결정 사항

- [ ] 권한 관리: 지혜님만 입력? 다른 사람도?
- [ ] 모바일 대응 범위
- [ ] 데이터 마이그레이션 방식
- [ ] "회사 인력" 정의: 인력 마스터 등록자 = 회사 인력? 사무직/대표는?
- [ ] 인건비 환급 신청 일정 → 일정 관리 페이지 연동 여부
- [ ] 수행 현황 페이지에 "인건비" 컬럼/탭 추가 여부
- [ ] 프로젝트별 인건비 예산 한도 설정 / 초과 시 처리

### 5.5 v1에서 언급된 확장 가능성 (장기)

- 인력별 상세 페이지의 월별 급여 이력 그래프 (`salaryHistory` 필드 필요)
- 예측 시뮬레이션 (다음 분기 환급액 예측)
- 자동 알림 (이메일/슬랙: 100% 도달, 미입력 등)
- 청년 인력 비율 자동 검증 (정부 규정)
- 국적 필드 추가 (현재 "한국" 고정)
- isExternal 필드 추가 (현재 "내부" 고정)

---

## 6. 작업 순서 가이드 (다음 세션 진입 시)

```
1. project-labor.html 골격 + project-labor.js 스캐폴딩
   - 상단 컨트롤바(회사 필터, 프로젝트/연도/분기)
   - 빈 테이블 + 인력 추가 모달
   - planned 탭 우선

2. 셀 입력 + 자동 계산
   - 참여율 입력 → 현금 계산
   - 디바운스 저장
   - 100% 초과 차단 (다른 프로젝트 합산)

3. 시스템 등록/금액 확인 헤더 체크박스
   - meta.{ym}.sysReg/amtConf 토글

4. 실제 탭 + 비교 탭
   - 비교 탭: 3행(예상/실제/차이), 차이만 보기 토글

5. 키보드 조작 (Tab/화살표/Ctrl+C/V/Z/Esc)

6. 이전 분기 복사 / 되돌리기

7. (이슈 5.1·5.2 결정 시) 자부담 토글 / 메모 UI

→ 여기까지 Phase A. 그 다음 Phase B (합산 참여율 월 기준).
→ 그 다음 Phase C (인건비 대시보드).
```

---

## 7. 변경 이력

| 일자 | 내용 | 작성자 |
|---|---|---|
| 2026-05-25 | v1 초안 (기획 세션) | 정지혜 + Claude |
| 2026-05-26 | v2 작성. 페이지 4→5, 회사 구분 시스템, 참여연구원 표 모듈, projectLabor 구조 변경, projectBudget 신규 반영. 미해결 이슈 명시. | 정지혜 + Claude |
| 2026-05-26 | §0.5 공통 원칙 신설. 11개 페이지에 박혀있던 `.container { width:1400px; margin:0 auto }` 일괄 제거 + style.css의 `.container`에 `!important` 추가. 모니터 폭에 관계없이 본문이 꽉 차도록 통일. | 정지혜 + Claude |
| 2026-05-26 | §0.5.1 확장: 좁은 화면 대응 정책 3원칙 확립. `.container` min-width 1400→1200, `sidebar.js` `AUTO_COLLAPSE_BREAKPOINT` 1200→1400. 4개 페이지(lab·persons-detail·project-budget·projects-summary)의 1100~1280px 그리드 무너뜨림 미디어 쿼리 제거. `min-width:0` 점검은 §0.5.5에 미래 항목으로 보류. | 정지혜 + Claude |

---

*문서 끝*
