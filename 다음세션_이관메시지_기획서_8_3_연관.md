# 다음 세션 이관 메시지 (복붙용) — v8.3 연관

```
인건비 관리 시스템 — 이어서 작업 (v8.3 이후).

[참조 파일 — 프로젝트에 올려둠]
- 인건비_관리_시스템_기획_v8_2.md (+ "기획서_v8_3_업데이트패치.md" 반영)  ← 최신 기획. §4.8 C1/C3/C6 후보·§6 작업 순서
- 개발정리_테스트가이드.md  (구현 누적 + 실화면 체크리스트. 묶음 7=C2, 이후 C3·C1-b·C6 1단계·대시보드)
- 코드: project-labor / project-budget / persons-master / dashboard (.html/.js) / three-five-rule.js(공유 헬퍼)

[작업 규칙]
- 코드 원본은 읽기 전용 → outputs 복사본에 누적 편집(재복사 금지). 프로젝트 자동반영 안 됨(내가 다운받아 같은 이름으로 덮어씀 = 교체됨).
- 검증은 정적분석(node -c + 인라인 JS + grep 정합 + div/table·CSS 균형)까지. 실화면은 내가 확인.
- 큰 결정은 물어보고, 자잘한 건 바로 진행. 한 파일은 한 번에. 한국어 진행. 디자인 세부는 나중에 — 지금은 기능/숫자 위주.
- 매 작업마다 '개발정리_테스트가이드.md'에 누적 기록(무엇을 개발 + 무엇을 테스트 + 다음 구현).

[지금까지 한 것]
- v8.1: §3.5 인건비 대시보드+채용 의무 / §3.6 인력 대시보드 / C4 신규 인력 자동 판정 / 채용 의무 디커플링.
- v8.2: C2 3책5공 관리(공유 헬퍼 three-five-rule.js, 책≤3·책+공≤5) + 테스트 피드백.
- v8.3(이번 세션):
  · C3 셀 메모 실제·비교 탭 입력 완료(project-labor.js). 예상·실제 탭 편집(실제는 메모 전용 우클릭), 비교 탭 예상행→planned·실제행→actual 마커+우클릭, 차이행 비허용.
  · C1-b 선택 전송 모달 완료(project-budget). "📤 선택 전송": 인력 체크·필드(이름만/+참여율/+금액)·금액 출처 ⓐ예산(실지급액×%)/ⓑ인건비 급여 재계산(override??master×%)·월 범위·빈셀만/덮어쓰기. (C1-a "이름만" 버튼은 이 모달 옵션으로 흡수.)
  · C6 1단계 완료(persons-master) — "연봉 프로파일(공유 슬롯)". persons.salarySlots={슬롯명:연봉}, 실제=annualSalary, 빈슬롯=실제 폴백. 인력 편집 모달 인라인 입력(A). 라벨: 실제 연봉/예산 전용 연봉, 기본 슬롯="제안서용(공개 가능)". 실제·예산전용 연봉 콤마 표시, 슬롯 행 스택 레이아웃(이름 전체 표시). 가용슬롯=기본∪전인력 키 합집합(별도 설정문서 없음).
  · 대시보드 지원금 현황 카드 정비(dashboard) — 윗행 2칸화(big-stats--duo, 노트북 라벨 줄바꿈 해소), 죽은 "지원금 구성(당해)" 제거, "입금 진행(당해)" 신설(완료/남은 막대 + 입금 완료율 헤더 배지, seg-paid/seg-remain, yearActual/yearSupport), 입금 진행↔자금 구성 순서 = 입금 진행 먼저.

[이번 세션 할 일 — 다음 추천 순서]
1. C6 2단계 — 예산에서 슬롯 골라 불러오기(project-budget). 상단 기본 슬롯 셀렉트(전체 적용) + 행별 드롭다운으로 salarySlots[슬롯] ?? 실제 의 월급(ceil(연봉/12))을 행에 채움. 🔍 인력 선택·분배·C1-b 연계. ★폴백 안전장치 결정 먼저: ⓐ 슬롯 비운 사람은 실제 연봉으로 불러오되 화면에 "실제 연봉 사용" 표시 / ⓑ 빈칸·0으로 둬 실제값 유입 차단(유출 방지). 가용 슬롯은 persons 합집합(getAvailableSlotNames 동일 로직).
2. C6 3단계 — 엑셀(C). 다운로드/업로드에 슬롯 컬럼(예: "연봉:제안서용(공개 가능)") 추가, 일괄 입력. (persons-master 다운/업 + bulk-upload.)
3. §4.4 월급 오버라이드 연중 변경 — 범위 (b): 마스터 연봉 시간축(상/하반기)+여러 인력 일괄, 입력 UX=A안 팝오버. monthlySalaryOverride 숫자→객체 {YYYY-MM:금액, step}. ⚠️ C1-b ⓑ가 personRoles[pid].monthlySalaryOverride 단일값(숫자)을 가정 → 객체화 시 C1-b ⓑ 계산도 동기 수정.
4. 6묶음 과제별 상세(projects-summary) — 사양 미정 → 화면 기획 먼저.
5. §4.5 엑셀 입출력 — 우선순위 낮음.

[결정 필요(착수 전 물어볼 것)]
- C6 2단계 폴백 처리: ⓐ 실제 연봉 적용 + 표시 / ⓑ 빈칸·0 차단.
- §4.4 진행 시 C1-b ⓑ 동기화(오버라이드 객체화).
- 6묶음 레이아웃·내용 사양.

[확인 대기 — 실화면(이월 포함)]
- v8.3: C3 메모(예상·실제·비교 탭) / C1-b 선택 전송 모달(필드·금액출처·월범위·빈셀만/덮어쓰기) / C6 1단계 슬롯 입력·콤마·스택 레이아웃·합집합 노출·저장복원 / 대시보드 지원금 현황(노트북 줄바꿈·입금 진행 막대·완료율 배지·순서).
- (이월) v8.2 3책5공 / v8.1 인건비·인력 대시보드·채용 의무·C4 / §4.2 스냅샷 / §4.1 메모 / §3.4 3분류.

[데이터 모델 메모(이번 세션 추가)]
- persons.salarySlots = { 슬롯명: 연봉(원) }  (월급은 사용처에서 ceil/12). 셀/키 변경 0 → funding·결산·labor 무영향. 마이그레이션 0.
- C1-b: _planned.cells + _meta.personIds merge. 금액출처 ⓑ = personRoles[pid].monthlySalaryOverride>0 ? 그 값 : 인력 마스터 monthlySalary, × 참여율%. 빈셀만=값(rate/cash/selfCash/inkind>0) 있는 셀 건너뜀(메모 보존), 덮어쓰기=교체(메모 보존).
- 대시보드 입금 진행: paid=yearActual, remaining=max(0, yearSupport-yearActual), 완료율=actual/support. 초과입금 시 막대 100% 캡·완료율 실제값.
```
